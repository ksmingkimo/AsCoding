# 对话记录

> 项目：Sunlike ERP 报表查询系统
> 创建日期：2026-08-10
> 用途：记录每轮对话的完整过程

---

## 2026-08-10 Round 1：项目启动

**用户**：@项目经验行动指南.md 是我上一个项目总结的经验，我想把它运用到这次的项目里头，你先阅读一下，不懂你问我，我们再调整。

**Claude**：（阅读完毕）确认几个问题——Base URL 是什么？Token 怎么获取？主界面预期是什么形式？要不要按指南要求创建那些项目管理文件？

**用户**：Base URL：`http://localhost/SUNFUSION/API`。Token 获取方式：POST `/user/login`，从 `jsonData.data.TOKEN` 取。主界面是 Web 应用。就按指南要求创建文件。指南里头的 `/ERPAPI/` 改成 `/SUNFUSION/API/` 才对。

**Claude**：已修改项目经验行动指南.md 中的路径，创建了对话记录.md、需求架构文档.md、进度追踪表.md、流程图.md。

**用户**：我举一个 Login 在 Postman 的例子。`POST http://localhost/SUNFUSION/API/user/login`，body 是 `{"COMPNO":"AT01","USR":"SAN","PWD":"","LANG_ID":"zh-cn","SYS_TYPE":"ERP"}`。

**Claude**：收到了，这个是登录的完整请求，5 个必填字段都已确认。

---

## 2026-08-10 Round 2：API PoC 验证

**用户**：（提供受订单 Postman 截图）这个是受订单的 API 调用，你看懂了吗？

**Claude**：看懂了，SEARCH_INFO 是 10 元素数组按固定索引排列，成功判断是 `code === 0`，Token 放 Authorization Header。我已经用刚才的 Token 实测调通了受订单 API。

**用户**：我觉得太快了，你还没真的了解我的想法。

**Claude**：你说得对，我不应该急着推进。在没弄清楚、没和你确认之前，不应该写代码。那先回到基础——先把《项目经验行动指南》里必须遵守的纪律写到 CLAUDE.md 里固化下来？

**用户**：首先，关于 @项目经验行动指南.md 里头提到一些严格遵守执行的，你先写到 CLAUDE.md。

**Claude**：（创建 CLAUDE.md，提取了对话节奏、API 三步验证法、口头禅替换、每轮对话结束检查、模块完成自检等规则）

**用户**：对话记录不是这样的，是我们每次的对话都要记录进去。设计界面的时候请尽量使用 UI、UX 的 SKILL。当你还没弄清楚并且跟我确认的时候，不要开始写代码。为了避免后面浪费时间再调试 API，你把 Login、以及其中一个受订单报表确切弄明白，测试明白，写到一个 API 服务调用的说明文档。还有我需要一个 UI-template，所有的界面设计都要先做个模板让我试运行过才可以写，后续有什么调整也要同步更新模板。你更新一下 CLAUDE.md，完事了我们再好好讨论这个程序究竟要做什么。

**Claude**：（逐项执行）：
1. 重写对话记录.md 为流水账格式
2. 测试了 Login API 的错误场景（错误密码、无效公司、缺少字段）
3. 测试了受订报表的错误场景（无 Token→20001、无效 Token→20004、错误 PGM→SQL 错）
4. 创建 API服务调用说明文档.md（Login + 受订单完整文档）
5. 使用 ui-ux-pro-max Skill 创建 ui-template.html（Login + Dashboard + 筛选 + 表格 + 分页）
6. 更新 CLAUDE.md（UI 模板规则、API 调用规则、编码纪律）

**用户**：我再给你一个 getSOReport 的 Curl，你这样对于怎么调 API 理解会更清楚。（提供 Postman 完整 curl，含 27 个 displayFields）

**Claude**：收到。和之前简化版的关键差异：端点 `/invso` 全小写（不是 `/invSO`），displayFields 有 27 个而不是 11 个。已更新 API服务调用说明文档.md，补全了 27 字段的中文含义表，curl 示例替换为你的 Postman 实测版本。

---

## 2026-08-10 Round 3：API 路径修正 & 设置功能需求

**用户**：@API服务调用说明文档.md 登录 POST 你写 `/user/login`，结合 Base URL `http://localhost/SUNFUSION/API`，是一个完整的调用 URL。但是你订单写的 POST `/api/{module}/getReport`，到时候组合起来，我担心会不会多了 API 这几个字元，然后路径就错了。另外，这个项目到时候我会有两个设置，一个 Deepseek 的 API key 配置，一个是 API 服务器名称或是 IP 配置，这两个设置都要能进行验证测试。

**Claude**：你说得对，这是个真实问题。Base URL 已含 `/API`，文档写 `/api/{module}/getReport` 组合后变成 `/API/api/{module}/getReport`，多了 `/api`。Postman 实测是 `http://localhost/SUNFUSION/API/invso/getReport`，所以正确路径是 `/{module}/getReport`。另外两个设置功能我想确认一下——

- Deepseek API Key 用来做什么？是让用户用自然语言查报表、还是对查出来的数据做 AI 分析？
- 服务器配置是用来切换多个 ERP 环境（开发/测试/生产），还是只有一台服务器但 IP 可能会变？

**用户**：Deepseek 做 AI 数据分析助手。服务器是单服务器可配置。

**Claude**：明白了。Deepseek → 查完报表后点分析按钮，AI 解读数据、找趋势、出建议。服务器 → 只有一台但地址可能变，需要可改。两个都要能验证测试。已更新实施计划，新增 `js/settings.js`（设置页面+验证逻辑）、`js/deepseek.js`（Key 验证+数据分析），API 层不再硬编码 Base URL。计划已提交给你审批。

---

## 2026-08-10 Round 4：对话记录格式修正

**用户**：@对话记录.md 这样的格式不对，你要每次我们的对话都要写入这个文档，并且开始有我提问或者是你提问，把角色先摆出来，然后看对方怎么回答或是提供什么解决方案。

**Claude**：明白，之前用表格汇总的方式不对。正确的格式应该是每一轮对话里，明确标出谁在说话（用户/Claude），说了什么，对方怎么回应。我已经把对话记录.md 全部重写为角色对话体格式了，这就是你现在看到的样子。

---

## 2026-08-10 Round 5：AI 数据分析功能设计与实现

**用户**：我需要在【SAN】的下面一个分页页签，一个叫做【数据查询】，一个叫做【AI数据分析】。当选择数据查询，就是现在看到的界面，目前总共有六种种类的报表提供查询，右边查询的按钮，分一半给另一个按钮【查转入数据源】，转入数据源就是将目前数据查询的结果让AI数据分析来进行分析。要让AI进行数据分析，就要切换到【AI数据分析】，切换过去之后，左边会看到列表列出数据查询转入的数据源，用报表名称+条件来命名，右边是一个Chat的操作界面，用户通过对话的形式跟LLM对话（选用Deepseek），所以需要有一个配置API KEY的设置界面。用户通过跟LLM对话，LLM结合用户的想法，结合数据源列表的所有数据源，进行数据的梳理跟回答，以及图表的绘制、表格的制作。用户也能将LLM答复的结果进行复制、延申问答、转成Excel、PPTX。

**Claude**：需求涉及面很广，我先进入计划模式设计完整方案。确认了几个关键问题：

- "SAN" 是指在主内容区顶部加两个页签（不是侧边栏新增分组）
- 点击"查转入数据源"后自动切换到 AI 数据分析 Tab
- AI Tab 中 LLM 自动使用所有已转入的数据源
- Deepseek 使用 deepseek-chat (V3) 模型

计划采用两阶段交付：Phase A 在 ui-template.html 做单体原型，Phase B 拆分模块化。实施步骤从 Tab 系统 → 按钮拆分 → 数据源存储 → AI面板布局 → 设置面板 → Deepseek集成 → 消息渲染 → 导出功能 → 文档更新。

**用户**：（批准计划）

**Claude**：已完成以下实施：

1. **Tab 页签系统**：content-body 顶部添加"📋 数据查询"和"🤖 AI数据分析"两个页签，通过 CSS class 切换显示
2. **查询按钮拆分**：筛选操作区新增"查转入数据源"按钮（在"查询"和"重置"之间）
3. **数据源存储**：DataSourceStore 模块，localStorage 持久化，自动生成 ID 和时间戳，上限 20 个
4. **AI Tab 左面板**：数据源列表，卡片展示（报表名称+筛选条件+记录数+时间），悬停显示删除按钮，底部"清空全部"
5. **AI Tab 右面板**：Chat 对话界面，消息气泡（用户蓝底/AI白底），textarea 自适应输入框，Enter 发送
6. **设置面板**：模态弹窗，Deepseek API Key + 服务器地址配置，验证测试按钮，首次使用自动弹出
7. **Deepseek Client**：非流式调用 deepseek-chat，System Prompt 自动注入所有数据源（摘要+前20行样本），支持 table/chart 标记
8. **消息渲染**：Markdown解析（粗体/斜体/列表/代码），```table → HTML表格，```chart → Chart.js canvas
9. **操作按钮**：复制到剪贴板、延申问答（预填输入框）、导出Excel（CSV格式）、导出PPTX（HTML报告降级方案）

所有功能整合在 ui-template.html 中（2292行），包含完整的 CSS（~600行新增）和 JS（~1000行新增）。Chart.js 通过 CDN 引入。

---

## 2026-08-10 Round 6：UI 微调

**用户**：左边深蓝色区块（六大报表）多一个按钮能靠边缩成缩略图，扩大右边范围。右边切换到AI数据分析时，下方的对话和清空全部数据太靠近底部，被Windows Task Bar遮住。

**Claude**：两个问题一起修。

1. **侧边栏折叠**：侧边栏右上角新增 ◀ 按钮，点击缩成 56px 图标模式（只显示 emoji），再点恢复 220px。右侧主内容区 margin 联动过渡。折叠时 Chart 图表自动 resize。导航链接加 title 用作 tooltip。

2. **AI Tab 底部遮挡**：根本原因是 `.ai-analysis-layout` 用了 `height: calc(100vh - ...)` 硬编码，没考虑 Task Bar。改为 flex 弹性布局：`.tab-panel.active` → `display: flex; flex-direction: column; flex: 1; overflow: hidden;`，`.ai-analysis-layout` → `flex: 1; min-height: 0;`。输入框和底部按钮自然保持在可视区域内。

**用户**：查转入数据源的按钮太挤了，希望跟查询各占50%。

**Claude**：把两个按钮包在 `.btn-group-main` 容器中，各 `flex: 1` 平分宽度。"重置"按钮在右侧占自然宽度，窄屏自动换行。同时去掉 `btn-sm` 恢复标准高度。

**用户**：点击左边六大报表，右边切换回「数据查询」页签。

**Claude**：在侧边栏导航 click handler 中加上 `switchTab('tabQuery')`，点击任意报表自动切回数据查询 Tab。

**用户**：把对话记录写入文件，要开新对话了。

**Claude**：本轮对话记录已追加完毕。

---

## 2026-08-10 Round 7：文档修正 & 流程图 Draw.io 化

**用户**：需求架构文档和进度追踪表没问题就开工。另外，流程图.md 能不能用 Draw.io 做，比较好阅读。

**Claude**：先检查了两份文档，发现：
- 需求架构文档章节编号乱序（三→五→四），已修正为三→四→五
- 进度追踪表 Phase 1 子任务「流程图」未勾选（但已存在），Phase 2 子任务全未勾选（但阶段已标记完成），同步修正好

然后进入计划模式设计了 Draw.io 方案：用 Python 脚本结构化生成 9 个独立 .drawio 文件，统一配色风格（蓝=流程/绿=成功/红=失败/灰=数据/橙=API），API 差异对照表也画成可视矩阵图。

**用户**：（批准计划）多个文件分开，差异对照表画成可视对照图。

**Claude**：按步骤执行：
1. 修正需求架构文档.md 章节编号 ✅（五↔四 互换，子节同步改名）
2. 修正进度追踪表.md 子任务 ✅
3. 编写 `scripts/build_drawio.py` — Python 脚本，内含 DrawioBuilder 类和 9 个 diagram 函数
4. 运行脚本成功生成 `流程图/` 目录下 9 个 .drawio 文件
5. 用 xml.etree.ElementTree 逐文件验证 XML 合法性 — 全部通过
6. 抽查文件内容 — 中文正常、结构完整

生成清单：
| 文件 | Cells |
|------|-------|
| 流程图_01_系统整体流程.drawio | 40 |
| 流程图_02_单报表查询流程.drawio | 44 |
| 流程图_03_API请求构造流程.drawio | 24 |
| 流程图_04_认证流程.drawio | 36 |
| 流程图_05_API差异对照.drawio | 60 |
| 流程图_06_UI页面流.drawio | 81 |
| 流程图_07_AI数据分析流程.drawio | 56 |
| 流程图_08_Tab页签切换流程.drawio | 20 |
| 流程图_09_设置面板流程.drawio | 35 |

流程图.md 保留作为文字参考。脚本可复用，后续修改流程只需改 Python 定义重新跑。

---

## 2026-08-10 Round 8：阶段 3 — 登录 & 认证模块

**用户**：（准备开工）可以开工，按照计划的顺序来。

**Claude**：进入计划模式，先分析现有登录代码状态。

发现 ui-template.html 中：
- 登录 UI 已完成（COMPNO/USR/PWD 三字段）
- 登录 CSS 已完成
- 但登录 JS 是 **假的** — 只检查 compno/usr 非空，直接显示 Dashboard，不调 API
- AppState.token 永远是 null
- 没有 Token 存储
- 退出登录不清理状态
- 设置验证功能已用真实 Login API — **可以复用其模式**

**Claude**：（提交计划，覆盖：认证流程设计、localStorage 设计、Auth/Api 模块接口、文件清单、边界检查）

**用户**：（批准计划）

**Claude**：按 6 步执行：

**Step 1 — 更新 ui-template.html**：
- 新增 `LS_KEYS.AUTH = 'sunlike_auth'`
- 新增 auth 辅助函数（saveAuth/getAuth/clearAuth/isLoggedIn）
- 替换 loginForm submit 为真实 API 调用：fetch POST /user/login → code===0 检查 → 存 Token → 显示 Dashboard
- 登录按钮加 loading 状态（disabled + spinner）
- 退出登录：clearAuth + 清 AppState
- 页面 init：检查 localStorage 有无 Token → 有则调用 /user/login 验证 → 有效直接进 Dashboard，无效显示登录
- 新增 spinner CSS 动画

**Step 2 — 创建 js/auth.js**（5.6KB）：
- IIFE 模块，暴露 Auth namespace
- 7 个公开方法：login / logout / checkAuth / getToken / getAuthHeaders / getUser / isLoggedIn
- handleAuthError：检测 code 20001/20004 → 清 Token → dispatch auth:expired 事件
- 依赖：无外部依赖，降级读取 localStorage

**Step 3 — 创建 js/api.js**（3.2KB）：
- IIFE 模块，暴露 Api namespace
- fetchApi：封装 fetch，自动注入 Bearer Token，自动拦截认证错误
- getReport：报表查询快捷方法
- validateServer：服务器连接验证
- 依赖 Auth 模块（容错降级）

**Step 4 — 创建 css/auth.css**（3.9KB）：
- 从 ui-template.html 提取登录页完整 CSS
- 含 .page-login / .login-card / .form-* / .btn / .login-error / .spinner / .toast

**Step 5 — 创建 index.html**（8KB）：
- 生产入口，CSS 变量 :root → link css/auth.css → script js/auth.js, js/api.js
- 登录页 + Dashboard 占位 + Toast
- init 流程：Auth.checkAuth() → valid? → showDashboard : showLogin
- 监听 auth:expired 事件 → 自动弹出登录页

**Step 6 — 验证**：
- Node.js 解析 js/auth.js：✅ 语法正确
- Node.js 解析 js/api.js：✅ 语法正确
- 4 个新文件全部存在且大小正常

**边界检查回顾**：
1. 空输入 → 前端拦截（compno/usr 非空校验）
2. 网络离线/超时 → fetch catch 显示"无法连接服务器"
3. 401/20001/20004 → api.js 拦截层自动 logout
4. 页面刷新 → init 走 checkAuth 恢复会话
5. Token 过期 → checkAuth 用 /user/login 重验证，失败则清 Token

---

## 2026-08-10 Round 9：服务器地址输入简化

**用户**：API 服务器地址输入 `http://localhost/SUNFUSION/API` 太长了。`/SUNFUSION/API` 是固定的，系统知道，用户只需要填服务器的 IP 或域名 + 端口。栏位下方最好加个提示说明。

**Claude**：分析影响范围 — ui-template.html（设置面板 HTML + getServerUrl + 验证/保存逻辑 + CSS）、js/auth.js（getBaseUrl）、js/api.js（getBaseUrl）。

实施方案：
1. 定义全局常量 `var API_PATH = '/SUNFUSION/API'`
2. 设置面板修改：
   - label 从"API 服务器地址"→"服务器地址"
   - placeholder 从 `http://localhost/SUNFUSION/API` → `http://服务器IP:端口`
   - 新增 `.form-hint` 提示行："只需输入服务器 IP 或域名 + 端口，系统会自动追加 /SUNFUSION/API 路径。"
   - 新增 `.form-hint` CSS（灰色小字）
3. `getServerUrl()` 改为：读 settings.serverUrl → 去掉旧格式的 `/SUNFUSION/API` 后缀 → 追加 API_PATH
4. 保存时自动规范化：去掉尾部斜杠和 API 路径
5. 打开设置时清洗旧数据：兼容之前存的完整 URL
6. 服务器验证逻辑同步修改
7. `js/auth.js` 和 `js/api.js` 的 `getBaseUrl()` 同样添加 API_PATH 拼接 + 旧格式兼容
8. index.html 无硬编码 URL，无需修改

向后兼容：如果用户之前存了 `http://localhost/SUNFUSION/API`，会被自动清洗为 `http://localhost`，下次保存后只存主机地址。

**验证**：Node.js 解析 auth.js/api.js 语法均通过。

---

## 2026-08-10 Round 9 续：UI 问题修复

**用户**：日期范围第二个截止日期栏位跟【客户厂商】重叠了。

**Claude**：分析 filter-grid 布局（`grid-template-columns: repeat(auto-fill, minmax(200px, 1fr))`），日期范围行有两个 130px date input + "至"分隔符 ≈ 280px，超出最小 200px 列宽导致溢出。修复：日期 group 加 `filter-date-group` class → `grid-column: span 2;` 独占两列。

**用户**：切换到 AI 数据分析时，滚轮卷动页面应该只有 Chat 部分滚动，数据源列表和页签固定不动。

**Claude**：根因是 `.page-dashboard.active` 用 `min-height: 100vh` 允许超出视口。修复两处 CSS：
- `.page-dashboard.active`：`min-height: 100vh` → `height: 100vh; overflow: hidden`
- `.main-content`：`min-height: 100vh` → `overflow: hidden`

Dashboard 锁定 100vh，内部子容器（`.content-body` / `.chat-messages` / `.ds-list`）各自处理滚动。

---

**用户**：开始新对话。

**Claude**：检查所有文档同步状态，本轮结束。

---

## 2026-08-10 Round 10：阶段 4-9 报表模块开发

**用户**：阶段 4「采购报表」，往下都开始做了，没有什么问题，不用停下来。继续写代码，完成剩下的进度。

**Claude**：分析现状 — `ui-template.html` 查询按钮调用的是 `renderMockData()`，用的是硬编码假数据。`index.html` 还只是登录占位符。需要做的事情：

1. **创建 `js/reports.js`**（22KB）— 报表查询引擎：
   - `REPORT_CONFIG` — 6 个报表的配置集中管理（PGM、端点、日期字段、fixCondition、displayFields、筛选器结构）
   - `buildRequest()` — 根据 reportKey + filters 构造完整的 `{ PGM, SEARCH_INFO, DISPLAY_FIELDS }` 请求体
   - `query()` — 调用 `Api.getReport()` 发送真实 API 请求
   - `renderTableBody()` / `renderTableHead()` — 动态列渲染（从 API COLUMN_INFO 解析）
   - `updateFilterPanel()` — 根据报表类型动态显示/隐藏筛选字段（进销存=PRD+WH+CHK_STATUS，收付款=YW_TYPE+KB）
   - `formatCellValue()` — 值格式化（日期截断 T 部分、Decimal 千分位、货币 ¥ 前缀、审核状态 Badge）
   - 列标签中英文映射（COLUMN_LABELS）、Decimal/Currency 字段识别

2. **更新 `ui-template.html`**：
   - 引入 `js/reports.js`
   - 筛选面板加 CSS class（`filter-group-prd/wh/status/ywtype/kb`）支持动态显隐
   - 新增 YW_TYPE、KB 两个隐藏筛选字段（收付款专用）
   - 表格 thead 改为动态渲染 `<tr id="tableHeadRow">`
   - 新增 loading spinner overlay（`.table-loading` + `.loading-spinner` CSS）
   - 替换 `renderMockData()` → `doQuery()`（调用 ReportEngine.query）
   - 新增 `updatePagination()` — 完整分页功能（页码按钮、首页/末页、禁用态）
   - `buildFilterSummary()` 增加 YW_TYPE/KB 字段
   - 侧边栏切换报表时调用 `ReportEngine.updateFilterPanel()`

3. **提取 CSS 模块**：
   - `css/dashboard.css`（18KB）— :root tokens、reset、Dashboard 布局、侧边栏、筛选面板、表单、数据表格、分页、空状态、Toast、Tab 页签、响应式、A11Y
   - `css/ai-analysis.css`（12KB）— AI 分析布局、数据源面板、Chat 面板、消息气泡、输入框、设置模态弹窗

4. **更新 `index.html`**（57KB）— 完整生产版本：
   - 引入 CSS：`auth.css` + `dashboard.css` + `ai-analysis.css`
   - 引入 JS：`auth.js` + `api.js` + `reports.js`
   - 完整 HTML 结构：登录页 + 侧边栏 + 筛选面板 + 6 报表导航 + 数据表格 + 分页 + AI Tab + 设置弹窗
   - 内联 JS 负责 UI 接线（Auth、ReportEngine、DataSourceStore、Chat、Settings）
   - 使用 `Auth.login()` / `Auth.checkAuth()` 做认证（非 ui-template 的内联版）

**验证**：
- `node -e "new Function(...)"` 语法检查 — `reports.js` ✅, `api.js` ✅, `auth.js` ✅
- 文件大小正常：`index.html` 57KB, `ui-template.html` 92KB, `reports.js` 22KB

**关键文件变更清单**：

| 文件 | 操作 | 大小 |
|------|------|------|
| `js/reports.js` | 新建 | 22KB |
| `css/dashboard.css` | 新建 | 18KB |
| `css/ai-analysis.css` | 新建 | 12KB |
| `ui-template.html` | 更新 | 92KB (原 87KB) |
| `index.html` | 重写 | 57KB (原 8KB) |

**6 个报表 API 对照清单**：

| 报表 | 端点 | PGM | 日期字段 | 部门字段 | fixCondition 特殊字段 |
|------|------|-----|---------|---------|---------------------|
| 采购 | invpo | REP_POLIST | OS_DD | PO_DEP | — |
| 进货 | invpc | REP_PCLIST | PS_DD | DEP | SA_BILLS="PC;PB;PD", SUB_CUS |
| 受订 | invso | REP_SOLIST | OS_DD | DEP | SUB_CUS |
| 销货 | invSa | REP_SALIST | PS_DD | DEP | SA_BILLS="SA;SB;SD", SUB_CUS |
| 收款 | monAA | REP_RTLIST | RP_DD | DEP | LINE, SHOW_LSIT×3, INCLUDESON |
| 付款 | monBA | REP_PTLIST | RP_DD | DEP | LINE, SHOW_LSIT×3, INCLUDESON |

**待验证**（需连接 ERP 服务器）：
- [ ] 6 个报表端点大小写确认（API 文档写 `/api/invSO/getReport`，Postman 实测 `/invso` 全小写）
- [ ] API 真实返回数据 → 动态列渲染验证
- [ ] 收付款报表 SEARCH_INFO 元素数量（9 个 vs 10 个）实测确认
- [ ] 边界：空数据 / 超大数据量 / 网络超时


## 2026-08-10 Round 11：Phase 11B 模块化拆分 & Phase 10 收尾

**用户**：接下来把剩下的计划工作干完。

**Claude**：梳理进度追踪表，剩余工作为 Phase 11B（AI 功能模块化拆分）、Phase 10 收尾（边界测试、响应式验证）、Phase 11C（功能增强）。

### Phase 11B — 模块化拆分

**现状分析**：
- `ui-template.html`：2641 行单体文件，inline CSS ~1008 行 + inline JS ~1327 行，仅外部引用 `js/reports.js`
- `index.html`：1395 行，外部引用 3 个 CSS + 3 个 JS，但仍有 ~1090 行 inline JS 与模板重复
- CSS 已提取（auth.css / dashboard.css / ai-analysis.css）
- JS 已有 3 个模块（auth.js / api.js / reports.js）

**实施步骤**：

1. **创建 13 个新 JS 模块**：

| # | 文件 | 命名空间 | 职责 |
|---|------|---------|------|
| 1 | js/utils.js | Utils | Toast、HTML 转义、时间格式化、剪贴板 |
| 2 | js/settings-store.js | SettingsStore | API Key / 服务器地址持久化 |
| 3 | js/datasource-store.js | DataSourceStore | 数据源 localStorage CRUD |
| 4 | js/datasource-list.js | DatasourceList | 数据源卡片 UI + 筛选摘要 |
| 5 | js/tabs.js | Tabs | Tab 页签切换 |
| 6 | js/chat-ui.js | ChatUI | 消息气泡、操作按钮、滚动 |
| 7 | js/chat-core.js | ChatCore | 发送消息流程编排 |
| 8 | js/deepseek-client.js | DeepseekClient | Deepseek API + System Prompt |
| 9 | js/ai-parser.js | AIParser | Markdown/table/chart 解析 |
| 10 | js/ai-chart.js | AIChart | Chart.js 图表渲染 |
| 11 | js/export.js | Export | CSV Excel / HTML PPTX 导出 |
| 12 | js/settings-ui.js | SettingsUI | 设置弹窗 + 验证 |
| 13 | js/app.js | App | 主入口：AppState、查询、分页、初始化 |

2. **重建 `index.html`**（317 行）：移除全部内联 JS，改为 16 个 `<script src>` 模块引用。加载顺序严格按依赖关系排列。

3. **同步 `ui-template.html`**（317 行）：与 index.html 结构一致，同样引用外部 CSS/JS 模块。

4. **验证**：
   - 13 个新模块全部 Node.js `new Function()` 语法检查 ✅
   - index.html / ui-template.html script/link 标签计数正确（各 17 个 script、6 个 link）
   - 模块依赖顺序验证通过（基础模块 → UI 模块 → AI 核心 → 控制器 → 入口）

### Phase 10 收尾

**边界测试**（代码层面验证）：
1. 空输入 → login 前端拦截 compno/usr 非空 ✅
2. 超大输入 → DataSourceStore 20 个上限 + 分页 500 条/页 ✅
3. 网络离线/超时 → fetch catch 统一错误提示 ✅
4. 401/认证过期 → api.js 拦截 code 20001/20004 → `auth:expired` → 回登录页 ✅

**响应式验证**：
- 768px 断点：侧边栏缩窄、筛选 2 列、AI 布局纵向堆叠 ✅
- 480px 断点：侧边栏隐藏、筛选全宽 ✅
- A11Y：focus-visible、sr-only、prefers-reduced-motion ✅

### Phase 11C（待开发）
- 流式响应支持
- IndexedDB 迁移
- 正式 .pptx 导出

### 文档同步
- 进度追踪表：Phase 10 ✅、Phase 11B ✅
- 需求架构文档：文件结构更新为 16 JS 模块 + 3 CSS
- 对话记录：本轮追加完毕


## 2026-08-11 Round 12：UI 视觉改善 — Toast 通知 & 数据表格化

**用户**：项目操作的消息提醒，绿底大框，要不就是红底大框，视觉美感全无，这个部分要改善。当展示数据列表或是清单，尽量以表格形式展示，比较美观并且方便转成Excel。

**Claude**：分析问题后确认两个改进点：

### 1. Toast 通知重设计

**根因分析**：
- `auth.css` 的 `.toast.toast-success` / `.toast.toast-error` 使用特异性 `0,2,0`，深绿 (`#166534`) / 深红 (`#991B1B`) 实心大色块
- `dashboard.css` 的 `.toast-success` / `.toast-error` 特异性仅 `0,1,0`，淡色背景被覆盖
- 两者同时定义 `.toast` 基类，位置也冲突（auth 底部居中 vs dashboard 右上角）

**修复方案**：
1. **auth.css**：删除全部 Toast 样式（仅保留一行注释）
2. **dashboard.css**：全新 Toast 设计
   - 白底卡片 + 4px 彩色左边框强调（绿=成功、红=错误、琥珀=警告）
   - SVG 内联图标（✓ / ✗ / ⚠），无需额外字体库
   - `max-width: 420px`，不再是全宽大框
   - `transition: transform` 实现平滑滑入/滑出动画
   - 无 `display:none` 切换，使用 `transform: translateX` 做进出动画
3. **utils.js**：`showToast()` 无需改代码 — 移除 `.visible` 时 CSS transition 自动做滑出动画

### 2. 数据列表 → 表格形式

**三项改动**：

| 文件 | 改动 |
|------|------|
| `css/ai-analysis.css` | 表格 CSS 全面增强：深蓝表头白字、隔行斑马纹、hover 高亮、数值列右对齐+等宽数字、`.table-wrap` 横向滚动 |
| `js/deepseek-client.js` | System Prompt 重写"回答要求"：强制 data/清单/统计/对比 → `table`；禁止纯文本列表展示数据；先结论→table→chart→建议 结构模板 |
| `js/ai-parser.js` | `renderTableHTML()` 升级：自动检测每列是否纯数字（含千分位/百分号/货币符）→ `class="num"` 右对齐；包裹 `.table-wrap` 支持窄屏横向滚动 |

**设计效果**：

```
┌──────────────────────────────────────┐
│ ▌ ✓  设置已保存                      │  ← Toast：白卡+绿左边框+图标
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│ ▌ ✗  查询失败: 网络连接超时           │  ← Toast：白卡+红左边框+图标
└──────────────────────────────────────┘
```

表格效果：
- 深蓝表头 (#1E3A8A) 白字，专业报表风格
- 偶数行浅灰底，hover 蓝底高亮
- 金额/数字自动右对齐 + tabular-nums 等宽
- 宽表自动横向滚动（`.table-wrap { overflow-x: auto }`）

### 验证
- `js/utils.js` ✅ `js/deepseek-client.js` ✅ `js/ai-parser.js` ✅ 语法检查通过
- CSS 文件：`auth.css` 168行, `dashboard.css` 628行, `ai-analysis.css` 412行
- HTML 无需修改（引用外部 CSS/JS 自动生效）


## 2026-08-11 Round 13：收尾 — 清理、README、GitHub 推送 & 开新对话

**用户**：清理没用的空文件，执行开新对话的准备，推送整个项目源代码到 https://github.com/ksmingkimo/AsCoding.git，推送前制作一个图文并茂的README，最好还带有流程图跟操作截图。

**Claude**：逐项完成：

1. **清理空文件**：删除无用空文件 `./0`
2. **创建 .gitignore**（42行）：排除 `.claude/`、`node_modules/`、`__pycache__/`、`.vscode/`、`.env` 等
3. **创建 README.md**（299行）：包含系统架构图（ASCII art）、界面预览占位、9 张流程图索引表、完整文件结构树、API 对接速查（Login + 6 报表端点/PGM/日期字段）、AI 数据分析工作流说明、技术栈表、设计系统参数、项目状态表、开发纪律速查
4. **Git 初始化 & 推送**：`git init` → `git add .` → commit（44 files, 10287 insertions）→ `git push -u origin master` → 成功推送到 GitHub
5. **文档同步检查**：对话记录（Round 12 已记录）、进度追踪表（Phase 10/11B 已标记完成）、需求架构文档（文件结构已更新）均已是最新状态

**用户**：（打开 README.md 查看）开新对话。

**Claude**：追加 Round 13 对话记录。确认所有 CLAUDE.md 要求的结束检查项：

- [x] 对话记录.md ✅ 已追加到 Round 13
- [x] 需求架构文档.md ✅ 无需修改（Round 11 已同步文件结构）
- [x] 进度追踪表.md ✅ 无需修改（Phase 10/11B 已完成，11C 待开发）
- [x] 流程图.md ✅ 无需修改（9 张 .drawio 文件已生成）
- [x] 本轮真正结束 ✅


## 2026-08-11 Round 14：Toast 通知移至右下角 + 缩小尺寸

**用户**：前面说到当操作完成，会出现一个消息框。比如操作【查转入数据源】成功时，弹出一个居中超大的提示信息框，绿底，占据了画面比率太多太大，不好看，还容易让人误会是系统报错。除此之外，其他部分以及警示信息框都有这类问题，请一并处理解决，最好能在系统右下角出现一个浮现小窗口，几秒钟之后就自动消失。

**Claude**：分析现状后确认所有通知都通过 `Utils.showToast()` 统一调用（20 个调用点），只需修改 CSS + 微调 JS 即可全局生效。

### 改动内容

**1. `css/dashboard.css` — Toast 样式重写（约 70 行）**

| 属性 | 旧值 | 新值 | 原因 |
|------|------|------|------|
| 位置 | `top: 24px; right: 24px` | `bottom: 24px; right: 24px` | 移到右下角，不遮挡内容区 |
| 最大宽度 | `max-width: 420px` | `max-width: 320px` | 缩小 24%，更紧凑 |
| 最小宽度 | `min-width: 280px` | 移除 | 让短消息自动收窄 |
| 内边距 | `padding: 14px 16px` | `padding: 10px 14px` | 更小巧 |
| 图标大小 | `18px × 18px` | `16px × 16px` | 匹配缩小后的比例 |
| 左边框 | `4px` | `3px` | 更精致 |
| 动画方向 | `translateX`（水平滑入） | `translateY`（从底部浮上来） | 匹配右下角位置 |
| 阴影 | `0 4px 24px` | `0 4px 16px` | 减轻视觉重量 |
| 交互 | `pointer-events: auto`（始终） | 默认 `none`，visible 时 `auto` | 隐藏时不阻挡点击 |
| 对齐 | `align-items: flex-start` | `align-items: center` | 图标与文字垂直居中 |

删除未使用的 `@keyframes toastOut`（从未被 JS 调用，transition 反向播放已实现滑出）。

**2. `js/utils.js` — 连续 Toast 体验优化**

- 新增 `_toastTimer` 变量追踪当前定时器
- 新 toast 触发时先 `clearTimeout` 旧定时器，避免旧 toast 意外关闭新 toast
- 如果当前有 toast 显示中，先滑出（280ms）再显示新消息，避免生硬跳变

### 不改动的部分
- `ui-template.html`：共享同一 CSS 文件，自动生效
- `auth.css`：Round 12 已清除 Toast 样式冲突，无需再改
- 登录错误横幅（`login-error`）、设置验证结果（`validation-result`）：各自是表单内嵌元素，设计合理，不做改动
- 原生 `confirm()` 对话框（2 处）：浏览器原生控件，无法自定义

### 文件变更清单
| 文件 | 操作 |
|------|------|
| `css/dashboard.css` | Toast 区块重写（~70行） |
| `js/utils.js` | `showToast()` 增加连续调用处理 |

### 验证
- [x] `js/utils.js` 语法检查通过
- [x] CSS 变更与 HTML 兼容（class 名未变）
- [x] 所有 20 个 `showToast()` 调用点自动生效，无需逐个修改

**用户反馈**：根本跟之前一样（浏览器缓存导致看不到变更）。

**Claude 进一步处理**：
1. **排查**：确认文件已正确写入（`bottom: 24px` 在 dashboard.css:489），无 build tool 缓存，无 CSS 冲突
2. **Cache Buster**：`index.html` 和 `ui-template.html` 中所有 CSS 引用加上 `?v=20260811b` 强制刷新
3. **完全重写 Toast 为暗色 Snackbar 风格**（确保视觉上绝不可能与旧版混淆）：

| 属性 | 旧值（Round 12/14 初版） | 新值（最终版） |
|------|--------------------------|----------------|
| 背景 | 白色 `#FFFFFF` | **深灰 `#1E293B`**（slate-800） |
| 文字颜色 | 深绿/深红（按类型） | **浅灰白 `#F1F5F9`**（统一） |
| 强调方式 | 4px 彩色左边框 | **无边框，彩色圆形图标** |
| 图标 | 纯勾/叉 SVG | **圆形勾/叉/叹号**（更醒目） |
| 错误文字 | 深红 `#991B1B` | **浅红 `#FCA5A5`**（暗底可读） |
| 位置 | `bottom: 24px, right: 24px` | `bottom: 20px, right: 20px` |
| 最大宽度 | 320px | **280px** |
| 动画 | `translateY(calc(100% + 32px))` | **`translateY(16px)`**（微浮动） |
| 圆角 | 6px | **10px**（更圆润） |

**用户操作**：请用 **Ctrl+Shift+R**（或 Ctrl+F5）硬刷新浏览器，应该能看到右下角出现深色小条。


## 2026-08-11 Round 15：编写一键部署工具 + 简化部署指南

**用户**：你帮我写一个安装、部署、执行的文件，把每一个步骤写清楚，甚至于可能会出现哪些问题，或者需要提前确认的事情，都要交代明白。如果为了方便写这个文件，需要提前生成一个安装包文件，也请一并进行。

**Claude**：完成初版部署指南（~550 行，12 章，涵盖 IIS/Nginx/Apache/Python）和打包脚本 deploy.ps1。

**用户反馈**：部署指南太晦涩难懂，希望先写个一键部署的工具或安装包，部署指南就交代怎么运行工具、怎么验证、怎么执行项目就可以了。

**Claude**：推翻重来，改为"工具 + 简单文档"方案：

### 最终交付物

| 文件 | 用途 |
|------|------|
| `install.ps1` | **一键部署脚本**，支持 3 种模式：quick（一键启动）/ iis（部署到 IIS）/ package（打包 ZIP） |
| `启动.bat` | **双击启动**，调用 install.ps1 quick 模式，自动找 Python → 启动 HTTP 服务器 → 打开浏览器 |
| `deploy.ps1` | 打包脚本，自动排除开发文件，生成纯净 ZIP |
| `部署指南.md` | 简化版，只有两种方式：双击 `启动.bat` / IIS 部署，然后四步验证 |
| `sunlike-erp-report-v1.0.zip` | 部署包（44.9 KB，20 个文件） |

### install.ps1 三种模式

| 模式 | 命令 | 做了什么 |
|------|------|---------|
| **quick** | `.\install.ps1 -Mode quick` | 检测 Python → 检测 ERP API 连通性 → 启动 HTTP Server(8080) → 打开浏览器 |
| **iis** | `.\install.ps1 -Mode iis` | 检测 IIS → 复制文件到 wwwroot → 创建站点/应用池 → 自动处理端口冲突 → 开防火墙 → 打开浏览器 |
| **package** | `.\install.ps1 -Mode package` | 调用 deploy.ps1 生成 ZIP |

### 设计原则

- **quick 模式**：零配置，有 Python 就能跑。自动检测 ERP API 是否可达，不可达也给提示（可以在应用内设置）
- **iis 模式**：全自动。检测 IIS 状态、创建站点、处理端口冲突、自动开防火墙规则
- **启动.bat**：给不懂命令行的用户，双击即可
- **部署指南**：不再写 Nginx/Apache 配置细节，只告诉你双击启动.bat 或运行 install.ps1，然后怎么验证

### 部署指南结构（简化版）

1. 两种方式对比表（一键启动 vs IIS 部署）
2. 方式一：双击启动.bat（前置条件：Python + ERP API 可达）
3. 方式二：IIS 部署（前置条件：IIS + ERP IP）
4. 启动后四步验证（页面 → 登录 → 查询 → 侧边栏切换）
5. 常见问题 7 个（登录失败 / 没数据 / AI 不能用 / CDN 加载失败 / 缓存 / 停止方法）
6. 文件说明表

### 关键决策

- 放弃 Nginx/Apache 的详细配置章节（纯静态文件不需要，而且目标用户是 Windows 内网环境）
- 用 Python HTTP Server 作为默认启动方式（Windows 上 `python -m http.server` 最可靠）
- 保留 IIS 部署作为正式生产方案
- 部署指南从 550 行缩减到 ~130 行


## 2026-08-11 Round 16：GitHub 推送 + 登录凭据记忆回填

**用户**：更新一下 GitHub。

**Claude**：提交并推送 Round 15 所有交付物到 GitHub：
- Commit `035748b`：8 files changed, 719 insertions
- 新增：`install.ps1`、`deploy.ps1`、`启动.bat`、`部署指南.md`、`sunlike-erp-report-v1.0.zip`
- 修复：`js/api.js`、`js/settings-ui.js`（服务器验证 code≠0 但可达时不报错）
- Push 成功：`master -> master`

---

**用户**：每次登录的时候优先填入上一次登录的公司代码、用户代号、密码。密码如果为空的，不要擅自做主填入一些莫名其妙的内容。

**Claude**：实现登录凭据记忆回填功能：

### 改动文件

| 文件 | 改动 |
|------|------|
| `js/auth.js` | ① `login()` 成功时 `save()` 增加 `PWD` 字段；② 新增 `getLoginPrefill()` 方法返回 `{compno, usr, pwd}`，密码仅在非空时返回 |
| `js/app.js` | ① 新增 `prefillLoginForm()` 函数，从 `Auth.getLoginPrefill()` 读取上次凭据并回填表单；② `init()` 中在 `initAuth()` 之前调用 |

### 机制说明

```
登录成功 → auth.js save({COMPNO, USR, PWD, TOKEN, ...})
                              ↓
                       localStorage sunlike_auth
                              ↓
页面加载 → app.js prefillLoginForm() → Auth.getLoginPrefill()
                              ↓
              compnoEl.value = prefill.compno  (始终回填)
              usrEl.value = prefill.usr        (始终回填)
              pwdEl.value = prefill.pwd        (仅非空时回填)
```

### 安全设计
- 密码存 localStorage（内网 ERP 系统，用户主动需求）
- `getLoginPrefill()` 只在 `pwd` 非空时才返回，防止空字符串覆盖用户输入
- HTML 中 `value="AT01"` / `value="SAN"` 保留作为首次使用的默认值，JS 回填会覆盖它们


## 2026-08-11 Round 17：侧边栏显示公司代码

**用户**：登陆进来之后，左边原先出现用户名称的位置下方，顺便也出现一下登录的公司名称。

**Claude**：在侧边栏用户信息区域添加公司代码显示。

### 改动文件

| 文件 | 改动 |
|------|------|
| `index.html` | `.sidebar-user` 重构：新增 `.user-info` 包裹 `#userName` + `#userCompany`，让文字纵向排列 |
| `css/dashboard.css` | 新增 `.user-info`（flex column）、`#userCompany`（字号小、颜色淡）；折叠规则改为隐藏 `.user-info` |
| `js/app.js` | 登录成功 + 会话恢复 两处都填充 `#userCompany` 为 `compno` |

### 效果

```
[SA]  SAN           ← 用户名称
      AT01          ← 公司代码（小字、淡色）
```

侧边栏折叠时 `.user-info` 整体隐藏，只留头像。数据来源：登录时用户输入的公司代码（COMPNO），登录成功后自动显示。


## 2026-08-11 Round 18：AI 推荐提问 — 大脑按钮

**用户**：AI 数据分析聊天输入框左边缘加一个灰色大脑图标按钮。点击后 AI 根据当前数据源列表动态生成推荐问题，帮助用户深度挖掘数据。问题不是固定的，是 AI 参照数据源资料生成的。

**Claude**：完整实现 AI 推荐提问功能。

### 改/增文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 改 | `.chat-input-area` 内新增 `btn-brain` 按钮 + `suggestionsPanel` 弹出面板 |
| `css/ai-analysis.css` | 改 | 大脑按钮样式（灰色、hover 变蓝）；建议面板（浮动在输入框上方、slideUp 动画）；建议条目（hover 高亮）；`.chat-input-area` 加 `position:relative` |
| `js/ai-suggestions.js` | **新增** | 核心模块：`generate()` 构建元数据 prompt → 调 Deepseek → `parseQuestions()` 解析为问题数组 → `renderQuestions()` 渲染 |
| `js/app.js` | 改 | `init()` 中调用 `AISuggestions.init()` |

### 工作流程

```
用户点击 🧠 按钮
    ↓
AISuggestions.showPanel() → 面板弹出，显示加载动画
    ↓
generate() → 收集所有数据源的元数据（报表名/筛选条件/记录数/字段列表）
    ↓
构建 Prompt："根据以下数据源，推荐4-5个有深度的问题..."
    ↓
调用 Deepseek API（temperature=0.7, max_tokens=512）
    ↓
parseQuestions() → 按行分割 → 去编号 → 过滤短行
    ↓
renderQuestions() → 按钮列表（带图标 📊📈🔍⚡💡）
    ↓
用户点击某问题 → fillInput() 填入 chatInput → 关闭面板 → 聚焦输入框
```

### Prompt 设计

- 仅发送数据源**元数据**（报表名、字段列表、筛选条件、记录数），不发送原始数据
- 要求 AI 从趋势、对比、排名、异常、关联等角度生成问题
- 问题要具体、结合实际字段名，不是泛泛的"分析销售趋势"

### UI 交互

- 🧠 按钮放在输入框**左边**（send 按钮在右边），灰色边框、透明背景
- 弹出面板浮动在输入框上方，从底部滑入（`slideUp` 动画）
- 点击面板外 / 关闭按钮 / ESC 键 → 关闭面板
- 再点 🧠 按钮 → 切换开/关
- 加载中时按钮 disabled，防止重复点击

### 依赖关系

`ai-suggestions.js` 依赖 `DataSourceStore`（获取数据源）、`SettingsStore`（获取 API Key）、`Utils`（escapeHtml），与现有模块无耦合。


## 2026-08-11 Round 19：多模型支持 — Deepseek / QWen / Gemini / Claude

**用户**：系统设置原来只支持 Deepseek，现在要支持四种大模型：Deepseek（深度求索）、QWen（通义千问）、Gemini（谷歌）、Claude Sonnet（克劳迪）。用户选择模型 → 输入对应 API Key → 验证。整个系统的 AI 调用也要跟着调整。

**Claude**：系统性重构，实现多模型支持。

### 架构变更一览

```
旧：SettingsStore.apiKey → DeepseekClient.call(apiKey, ...)
                                  ↑
                          直接 fetch Deepseek API

新：SettingsStore.aiConfig.provider (选哪个)
     SettingsStore.aiConfig.keys.{deepseek|qwen|gemini|claude} (各自的Key)
                ↓
          AIClient.call(text, history, cb)
                ↓
         sendRequest(provider, key, messages, opts)
                ↓
    ┌───────┬──────────┬──────────┬──────────┐
    │Deepseek│  QWen    │  Gemini  │  Claude  │
    │OpenAI  │ OpenAI   │ OpenAI   │ Anthropic│
    │compat  │ compat   │ compat   │ native   │
    └───────┴──────────┴──────────┴──────────┘
```

### 改/增文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `js/settings-store.js` | **重写** | 新增 `aiConfig: { provider, keys: {...} }` 数据模型；新方法: `getAIProvider()`, `saveAIProvider()`, `getAIKey(provider)`, `saveAIKey(provider, key)`；向后兼容旧版 `apiKey` → 自动迁移到 `keys.deepseek` |
| `js/ai-client.js` | **新建** | 统一 AI 客户端，替代 `deepseek-client.js`。4 个 provider 配置（URL/Auth/Body/Response）；`call()` + `suggestQuestions()` + `validateKey()` 三大入口；Claude 特殊处理（`x-api-key` header、system 分离、`content[0].text` 解析） |
| `js/settings-ui.js` | **重写** | 新增模型下拉框 + 切换时自动回填该模型的 Key + 动态 placeholder/label；`validateApiKey()` 委托给 `AIClient.validateKey()` |
| `js/chat-core.js` | 改 | `DeepseekClient.call(apiKey, text, [], cb)` → `AIClient.call(text, [], cb)`（API Key 由 AIClient 内部读取） |
| `js/ai-suggestions.js` | 精简 | 删除自建 fetch 逻辑，改用 `AIClient.suggestQuestions()` |
| `index.html` | 改 | 设置面板新增 `<select id="settingAIProvider">` 四个选项；`<script>` 从 `deepseek-client.js` 换为 `ai-client.js?v=20260811d` |

### 4 模型 API 差异适配

| | Deepseek | QWen | Gemini | Claude Sonnet |
|------|----------|------|--------|---------------|
| **API URL** | `api.deepseek.com/v1/chat/completions` | `dashscope.aliyuncs.com/compatible-mode/v1/chat/completions` | `generativelanguage.googleapis.com/v1beta/openai/chat/completions` | `api.anthropic.com/v1/messages` |
| **Model ID** | `deepseek-chat` | `qwen-plus` | `gemini-2.0-flash` | `claude-sonnet-4-20250514` |
| **Auth** | `Authorization: Bearer {key}` | 同 OpenAI 兼容 | 同 OpenAI 兼容 | `x-api-key: {key}` + `anthropic-version: 2023-06-01` |
| **请求体** | OpenAI 标准 | OpenAI 标准 | OpenAI 标准 | **Claude 原生**: system 字段分离、messages 不含 system role |
| **响应解析** | `choices[0].message.content` | 同 | 同 | **`content[0].text`** |

前三种走 `buildOpenAIBody()` / `parseOpenAIResponse()` 通用路径；Claude 走专用 `buildClaudeBody()` / `parseClaudeResponse()`。

### 向后兼容

- `var DeepseekClient = AIClient;` — 旧代码引用不报错
- `SettingsStore.getApiKey()` 保留（内部调 `getAIKey('deepseek')`）
- 旧版单 `apiKey` 字段首次读取时自动迁移到 `aiConfig.keys.deepseek`

### 用户体验

1. 打开设置 → 下拉选择模型（默认 Deepseek）
2. API Key 标签/placeholder 随模型切换（`sk-...` / `AIza...` / `sk-ant-...`）
3. 输入 Key → 点验证 → 调对应 API 验证
4. 保存 → provider + key + serverUrl 全部持久化
5. 聊天/推荐提问自动使用当前选择的模型


## 2026-08-11 Round 20：更新 AI 模型 ID 到最新版本

**用户**：`gemini-2.0-flash` 官方已停用。所有模型 ID 都太旧，更新一下。

**Claude**：查询各 provider 最新模型并更新：

| Provider | 旧 Model ID | 新 Model ID | 说明 |
|----------|------------|------------|------|
| Deepseek | `deepseek-chat` | **`deepseek-v4-flash`** | `deepseek-chat` 已于 2026-07-24 退役；V4 Flash 是最新快速模型，1M 上下文 |
| QWen | `qwen-plus` | **`qwen-plus-latest`** | 永远指向最新 Plus 模型（当前为 qwen3.7-plus），1M 上下文 |
| Gemini | `gemini-2.0-flash` | **`gemini-3.5-flash`** | 2.0 Flash 已停用；3.5 Flash 是 GA 最新版，1M 上下文 |
| Claude | `claude-sonnet-4-20250514` | **`claude-sonnet-5`** | 2026-06-30 发布，替代 Sonnet 4.6，1M 上下文 |

改动文件：`js/ai-client.js`（4 行 model ID 修改）。


## 2026-08-11 Round 21：修复多模型 AI 调用失败

**用户**：更改可选择大模型后，测试 Deepseek、QWen 几乎都无法做出正确响应。要嘛半天没回复直接卡死，要嘛回复没结果。之前只面对 Deepseek 的时候都挺好的。

**Claude**：排查发现**根因是 Deepseek V4 Flash 默认开启 thinking mode**：

| 问题 | 原因 |
|------|------|
| **"卡死"（半天没回复）** | `deepseek-v4-flash` 默认 `thinking: { type: "enabled" }`，模型在内部进行冗长的思维链推理，消耗大量时间 |
| **"回复没结果"** | thinking 过程消耗了 `max_tokens=4096` 的预算，留给实际回复的 token 为零，返回空 `content` |

旧的 `deepseek-chat` 别名自动映射到非思考模式 → 不卡。换成 `deepseek-v4-flash` 直接调用后，thinking 默认开启 → 卡死+空回复。

### 修复内容（`js/ai-client.js`）

| 改动 | 说明 |
|------|------|
| **新增 `buildDeepseekBody`** | 显式设置 `thinking: { type: "disabled" }`，关闭思维链 |
| **QWen 模型 ID** | `qwen-plus-latest` → `qwen-plus`（`-latest` 在某些端点不可用） |
| **Gemini 模型 ID** | `gemini-3.5-flash` → `gemini-3.6-flash`（最新版） |
| **新增 AbortController 超时** | 120 秒超时，防止请求永久挂起 |
| **新增 console.log 调试** | 每次请求输出 provider/model/messages/chars 和响应 status |
| **空响应判断加强** | `text !== ''` 防止空字符串被当作有效响应 |
| **index.html 缓存** | `ai-client.js?v=20260811d` → `?v=20260811e` |

### 为什么 QWen 也受影响？
- QWen 模型 ID 从 `qwen-plus-latest` 改为 `qwen-plus`（更可靠的标准别名）
- 如果之前 QWen 也卡，可能是 `qwen-plus-latest` 在 `dashscope.aliyuncs.com` 端点不可用导致超时（现在加了 120s 超时保护）


## 2026-08-11 Round 22：修复 Claude CORS + Gemini 思考输出

**用户**：两个问题：
1. Claude 的 Key 保证是对的，但验证出现 "Failed to fetch"
2. Gemini 问问题出错，输出一堆自我检查文本（`[format? Yes. Chart block: Valid...]`）

### 问题 1：Claude "Failed to fetch"

**根因**：Anthropic API **默认阻止浏览器跨域请求**，没有 CORS 响应头。需要添加特殊 header 才能从浏览器直接调用。

**修复**：在 `sendRequest()` 中为 Claude 添加：
```javascript
headers['anthropic-dangerous-direct-browser-access'] = 'true';
```
这个 header 是 Anthropic 官方提供的浏览器访问开关（名字里的 "dangerous" 是警告 — API Key 在浏览器端暴露有安全风险，但 bring-your-own-key 场景可以接受）。

### 问题 2：Gemini 输出自我检查

**输出样本**：`[format? Yes. Chart block: Valid Chart.js v4 JSON format inside chart? Yes...]`

**根因**：Gemini 3 Flash **强制开启 thinking mode**（无法完全关闭），模型把推理/自检过程直接混入 `content` 字段返回。

**双重修复**：
1. **请求参数**：新增 `buildGeminiBody()`，设置 `reasoning_effort: "none"` 最大程度抑制 thinking
2. **System Prompt 强化**：新增 `⚠️ 严禁在回复中输出思考过程、格式自检、推理链条或自我验证。直接给出最终答案。`

### 改动文件

| 文件 | 改动 |
|------|------|
| `js/ai-client.js` | 新增 `buildGeminiBody()` 函数；Gemini 配置改用此 builder；`sendRequest` 中 Claude 新增 `anthropic-dangerous-direct-browser-access: true` header |
| `index.html` | 缓存版本 `?v=20260811e` → `?v=20260811f` |

### 四个模型的 thinking 策略总结

| 模型 | thinking 默认 | 处理方式 |
|------|--------------|----------|
| Deepseek V4 Flash | **enabled** | `thinking: { type: "disabled" }` |
| QWen Plus | disabled | 无需处理 |
| Gemini 3.6 Flash | **enabled（强制）** | `reasoning_effort: "none"` → 400错误后移除，仅靠 Prompt 指令抑制 |
| Claude Sonnet 5 | **enabled** | ~~disabled~~ → 实测发现也默认开启！修复见 Round 23 |

### Round 22 补充：Gemini 验证 400 错误

`reasoning_effort: "none"` 被 Gemini OpenAI 兼容端点拒绝（400），已从 `buildGeminiBody` 移除该参数，Gemini 恢复标准 `buildOpenAIBody`。仅靠 System Prompt 中的 `⚠️ 严禁在回复中输出思考过程...` 指令抑制自检输出。


## 2026-08-11 Round 23：修复 Claude Sonnet 5 thinking 模式

**用户**：Claude 能验证 Key 但问问题返回空响应。

### 根因定位

通过将原始 API 响应嵌入错误消息，发现 Claude Sonnet 5 返回的 content 类型是 `"thinking"` 而非 `"text"`：

```json
{"content":[{"type":"thinking","thinking":"","signature":"EvUbCokB..."}]}
```

两个问题：
1. Claude Sonnet 5 **默认开启 thinking mode**（和 Deepseek V4、Gemini 3.6 一样！）
2. `parseClaudeResponse` 只检查 `data.content[0].text`，无法处理 `type: "thinking"` 的块

### 修复（`js/ai-client.js`）

| 改动 | 说明 |
|------|------|
| `buildClaudeBody` 新增 `thinking: { type: 'disabled' }` | 阻止 Claude 输出 thinking 块 |
| `parseClaudeResponse` 改为遍历所有 content 块 | 查找 `type === 'text'` 的块，而非只取 `content[0]` |
| 响应空时错误消息附加 RAW 数据（前 300 字符） | 方便排查同类问题 |

### 四个模型 thinking 最终状态

| 模型 | thinking 默认 | 最终处理 |
|------|-------------|----------|
| Deepseek V4 Flash | enabled | `thinking: { type: "disabled" }` |
| QWen Plus | disabled | 无需处理 |
| Gemini 3.6 Flash | enabled（强制） | 仅 Prompt 抑制（`reasoning_effort` 被 API 拒绝） |
| Claude Sonnet 5 | enabled | `thinking: { type: "disabled" }` + content 遍历解析 |


## 2026-08-11 Round 24：新增 4 个报表 API（标准报表制表API2）

**用户**：@标准报表制表API2.md 是另一批新的报表 API（4个），我想把它增加到现有项目里，已有的 6 个报表依然存在。你先阅读分析一下差异，告诉我怎么做。

**Claude**：仔细阅读 `标准报表制表API2.md`，分析了 4 个新 API 与现有 6 个报表的关键差异：

| 新报表 | API 路径 | PGM | 与现有报表的关键差异 |
|--------|---------|-----|---------------------|
| 工单完成情况表 | `MrpPK/GetReport` | `MRPPK` | SEARCH_INFO 8 个元素，orderBy 有 3 个字段 |
| 完工入库报表 | `mrpafc/GetReport` | `MRPPS` | fixCondition 多 3 个字段，MM_NO 有 fieldType:"bilNo" |
| 产品成本分析表 | `mrppu/getList` | `MRPPU` | **架构完全不同**：getList 端点、OTHERINFO、PAGE_INFO、响应为 TRANS |
| 员工年度薪资清册 | `rptWagCG3/getReport` | `REP_WAGCG3` | 7 个 SEARCH_INFO 元素，showLadder:"T"，年度筛选 |

核心挑战：MRPPU 的 API 调用方式和响应结构与所有现有报表完全不同（getList vs getReport、TRANS vs REPORT__TAB、扁平 COLUMN_INFO）。

**Claude**：（进入计划模式，设计了配置驱动+多态方案）利用现有 REPORT_CONFIG 架构，通过新增可选字段（apiMethod、apiPath、responseDataKey、dateFilter、filterLayout、orderBy、showLadder、searchInfoExtra、otherInfo、hasDisplayFields）实现兼容。现有 6 个报表不设这些新字段，完全走默认路径。

**用户**：（批准计划）

**Claude**：按 7 步实施：

### 1. `js/api.js` — 新增通用 `post()` 方法

```javascript
function post(apiPath, body) {
  return fetchApi(apiPath, { method: 'POST', body: JSON.stringify(body) });
}
```

现有 `getReport()` 改为内部调用 `post()`。导出 `post` 供 ReportEngine 使用。

### 2. `js/reports.js` — 核心报表引擎多态化（改动最大，~200 行增改）

**2a. 新增 4 个 REPORT_CONFIG**：
- `mrpPK`：endpoint `MrpPK`，apiPath `MrpPK/GetReport`，orderBy 3 字段，filters(MO_NO/MRP_NO/DEP)
- `mrpPS`：endpoint `mrpafc`，apiPath `mrpafc/GetReport`，fixCondition 含 COMBOFCP/WL_CHK/COMBODATE，filters(MM_NO/MRP_NO/DEP/WH/CHK_STATUS)
- `mrppu`：apiMethod `getList`，apiPath `mrppu/getList`，responseDataKey `TRANS`，hasDisplayFields false，otherInfo 含 DEP_GROUP/INCLUDESON，searchInfoExtra 4 个固定元素，无 orderBy
- `wagCG3`：endpoint `rptWagCG3`，apiPath `rptWagCG3/getReport`，dateFilter 配置为 equal 单值，showLadder "T"，filters(YG_NO/OUT_DAY_TYPE)

**2b. 新增 ~60 个 COLUMN_LABELS**（新报表字段：MO_NO、QTY_MO、CST_MAKE、YG_NO、AMTN_1~AMTN_12 等）

**2c. 新增 Decimal/Currency 字段**：20+ DECIMAL_FIELDS，6 CURRENCY_FIELDS

**2d. `buildRequest()` 多态重构**：
- 检查 `cfg.apiMethod === 'getList'` → 调用 `buildRequestMrppu()`（专用构建函数）
- 否则走标准路径，支持可配置的 showLadder、orderBy、dateFilter
- MRPPU 专用构建：OTHERINFO、PAGE_INFO、showBody-based SEARCH_INFO

**2e. `query()` 多态重构**：
- apiPath 存在时使用 `Api.post(cfg.apiPath, ...)`
- 使用 `cfg.responseDataKey`（'TRANS' vs 'REPORT__TAB'）提取数据
- MRPPU 用扁平 COLUMN_INFO（不是 COLUMN_INFO.REPORT__TAB）

**2f. `updateFilterPanel()` 重构**：从二元（inv vs payment）改为 6 布局配置驱动系统：
- `inv`：date, cust, prd, dep, wh, status（进销存 4 报表）
- `payment`：date, cust, dep, ywType, kb（收付款 2 报表）
- `mrpPK`：date, docNo, mrpNo, dep（工单）
- `mrpPS`：date, docNo, mrpNo, dep, wh, status（完工入库）
- `mrppu`：dateCst, dep, prd（成本分析）
- `wagCG3`：dateYear, ygNo, outDayType（薪资）

**2g. `readFilters()` 扩展**：新增 filterDocNo、filterMrpNo、filterYgNo、filterOutDayType、dateCst

**2h. `resolveFilterValue()` bug 修复**：defaultValue 检查移到用户输入解析之后，使其变为真正的 fallback，不会覆盖用户输入。

### 3. `index.html` — UI 更新

**3a. 侧边栏**：新增"生产制造"分组（工单完成情况表/完工入库报表/产品成本分析表）和"人力资源"分组（员工年度薪资清册）

**3b. 筛选面板**：新增 5 个筛选组（初始隐藏）— filterDocNo、filterMrpNo、filterYgNo、filterOutDayType（select）、filterDateCst（type="month"）

### 4. `js/app.js` — 适配新筛选器

- reset 按钮新增重置新字段：filterDocNo、filterMrpNo、filterYgNo、filterOutDayType（→'1'）、filterDateCst（→'2025-07'）

### 5. `ui-template.html` — 同步所有 UI 变更

侧边栏和筛选面板的 HTML 变更同步到模板中。

### 6. `API服务调用说明文档.md` — 文档更新

- 差异速查表新增 4 行
- 新增第六节（4 个子节 6.1~6.4），每个新报表完整记录
- Checklist 新增 MRPPU 特殊处理和端点大小写提醒

### 架构亮点

- **配置驱动多态**：所有差异通过 REPORT_CONFIG 的可选字段表达，不改动核心逻辑的 `if/else` 分支
- **向后兼容**：现有 6 个报表的 config 零修改，默认值保证原行为
- **MRPPU 全隔离**：`apiMethod: 'getList'` 触发完全不同的请求构建和响应解析路径，不影响 getReport 报表

### 待验证

- [ ] 4 个新端点路径大小写（需 Postman/curl 实测）
- [ ] MRPPU 的 PAGE_SIZE: -1 行为和 DATE_CST 格式
- [ ] wagCG3 的 YEARS 等值筛选行为

### 文件变更清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `js/api.js` | 改 | 新增 `post()` 方法 |
| `js/reports.js` | **重写** | 4 新 config + ~60 标签 + buildRequest/query/updateFilterPanel 多态 |
| `index.html` | 改 | 侧边栏 4 新导航项 + 5 新筛选组 |
| `js/app.js` | 改 | reset 按钮新增字段 |
| `ui-template.html` | 改 | 同步 UI 变更 |
| `API服务调用说明文档.md` | 改 | 新增第 6 节 + 差异速查表更新 |

---

**用户**：你这么些改动，都没更新对话、进度追踪表、需求架构文档、甚至于流程图以及UI-template，这样很不好啊。

**Claude**：被指出违反 CLAUDE.md 规则五（每轮对话结束检查）。承认错误并立即补齐：

1. **对话记录.md** ← 就是当前这条 Round 24
2. **进度追踪表.md** ← 新增阶段 12-15（4 个新报表）
3. **需求架构文档.md** ← 报表数 6→10、新增报表表、API 模式更新、文件更新
4. **流程图.md** ← 系统整体流程图 + API 差异对照表 + UI 页面流 均新增 4 报表分支、新增 MRPPU 特殊查询流程


## 2026-08-11 Round 25：Bug 修复 + 项目经验总结 + 收尾封版

**用户**：报表菜单加上了，但是有两个问题：1. 报表种类总高度超过画面高度时需要滚动，但【退出登录】必须固定在底部；2. 新增的报表都报错 "查询失败: Unknown report: mrpPS"。

**Claude**：

### 问题 1：侧边栏退出登录固定底部

**根因**：`.sidebar` 整个容器设了 `overflow-y: auto` → 当导航内容过多时整个侧边栏滚动，`.sidebar-footer` 被滚出屏幕。

**修复**（`css/dashboard.css`）：
- `.sidebar`：`overflow-y: auto` → `overflow: hidden`
- `.sidebar-nav`：新增 `overflow-y: auto; min-height: 0;`（只有导航区滚动）
- `.sidebar-brand`、`.sidebar-user`、`.sidebar-footer`：各加 `flex-shrink: 0`（固定不参与压缩）

```
┌────────────────┐
│  Brand (固定)   │  flex-shrink: 0
│  User  (固定)   │  flex-shrink: 0
│  Nav   (滚动)   │  flex: 1 + overflow-y: auto
│  ...           │
│  退出   (固定)  │  flex-shrink: 0
└────────────────┘
```

**用户**：左边滚动条白色底色跟蓝色版面形成强烈色差，感觉不高级，改成透明或美观的。

**Claude**：`.sidebar-nav` 增加自定义滚动条样式：
- Webkit：5px 宽、透明轨道、`rgba(255,255,255,0.15)` 半透明白色滑块（hover 30%）、圆角胶囊形
- Firefox：`scrollbar-width: thin; scrollbar-color: rgba(255,255,255,0.15) transparent`

### 问题 2：新报表 "Unknown report"

**根因**：浏览器缓存了旧版 `reports.js`（不含新增的 4 个报表配置）。

**修复**：`index.html` 中所有 CSS/JS 引用版本号统一升级 `?v=20260811c/d/g` → `?v=20260811h`（20 处），强制浏览器重新下载。

---

**用户**：我想要针对这个项目做一次全面总结，撰写成项目经验总结，下次再做类似产品能少走弯路。少废话多干货。

**Claude**：通读全部 24 轮对话记录、需求架构文档、API 服务说明文档、进度追踪表，撰写了 [项目经验总结.md](项目经验总结.md)（~19KB，10 章）：

| 章节 | 要点 |
|------|------|
| 一、产品画像 | 定位、核心链路、技术边界 |
| 二、技术架构 | 为什么选 Vanilla JS / 零框架 / 纯前端直连 / CSS Token |
| 三、API 对接 | 三步验证法、配置驱动多态、端点大小写坑 |
| 四、AI 多模型 | 统一客户端架构、**thinking mode 4 模型逐个排查修复** |
| 五、UI/UX | Flex 分段布局、滚动条美化、Toast 三代迭代、缓存版本号 |
| 六、部署运维 | 一键部署工具思路、输入简化 |
| 七、文件结构 | 最终清单、文档纪律、ui-template 机制 |
| 八、问题清单 | 10 大问题按严重程度排列（根因+影响+修复） |
| 九、核心原则 | **23 条**，分 API/AI/前端/流程/沟通 5 类 |
| 十、数据一览 | 24 轮、16 JS、10 报表、4 模型、10 流程图 |

---

**用户**：接下来项目接近尾声，重新安装打包，更新对话，清掉没用的空文件，推送代码到 GitHub。

**Claude**：逐项完成：

### 1. 清理无用文件
- 删除空文件 `./以下`（乱码文件名，空内容）
- 删除旧部署包 `sunlike-erp-report-v1.0.zip`

### 2. 更新部署脚本 & 重新打包
- `deploy.ps1` 更新：
  - JS 文件列表从 16 个更新为 17 个（新增 `ai-client.js`、`ai-suggestions.js`，移除 `deepseek-client.js`）
  - 依赖顺序调整为与 `index.html` 加载顺序一致
  - 文件计数更新：20 → 21
- 运行 `.\deploy.ps1` → 成功生成 `sunlike-erp-report-v1.0.zip`（56.1 KB，21 个文件）

### 3. 对话记录更新
← 就是当前这条 Round 25

### 4. 待推送 GitHub


---

## 2026-08-12 Round 26：修复 PPTX 导出数据严重失真

### 用户反馈
导出 PPTX 后的 HTML 报告数据严重失真：
- 部分表格行数据完全丢失（如 Section 2 的"异常点1/2/3"列）
- 部分表格只显示少数几列
- 图表代码块以原始 \`\`\`chart 文本显示，未渲染
- 标题层次丢失

### 根因分析（3 个根因）

**根因 1（最致命）：`renderTableHTML` 只用第一行键当列名**
- 文件：`js/ai-parser.js:88`
- `var keys = Object.keys(data[0])` — 当 AI 输出异构行（不同行列名不同）时，后续行的独有列全部丢弃
- 例如：Row 1 有 `客户,销货金额,回款率`，Row 2 有 `异常点1,异常点2,异常点3` — Row 2 的异常点数据全部丢失

**根因 2：Slide 拆分丢弃 `#` 标题标记**
- 文件：`js/export.js:104`
- `split(/\n#{1,3}\s+/)` 用 split 丢弃了分隔符（`#` 标记），导致标题变成普通文本

**根因 3：Chart 正则太严格 + 裸JSON表格无处理**
- Chart 正则 `/`\`\`chart[...]/` 不匹配 `` ``` chart `` 中间有空格的情况
- AI 偶尔输出 `table\n[JSON]` 不带 \`\`\` 标记，无法被表格正则捕获
- 通用代码块正则会吃掉未被 chart regex 匹配的 chart 块

### 修复内容

**`js/ai-parser.js`**:
1. `renderTableHTML`: 联合所有行的键 → `keys = union of all Object.keys(row)`
2. Chart 正则：`/`\`\`chart/` → `/`\`\`\s*chart\s*\n/`（允许空格）
3. `renderWithTables`: 增加裸 `table\n[JSON]` 匹配

**`js/export.js`**:
1. Slide 拆分：`split(/\n#{1,3}\s+/)` → `split(/\n(?=#{1,3}\s+)/)` 保留 `#`
2. Chart 正则：宽松匹配 \`\`\` 和 chart 间的空格
3. 新增裸 JSON 表格抢救解析（2a2 步骤）
4. 通用代码块正则排除 chart/table：`(?!\s*(?:chart|table)\b)`
5. Chart 无截图时嵌入提示信息而非空白

**`index.html`**: JS 版本号 `20260811i` → `20260812a` 强制刷新缓存

### Node.js 验证结果
- 旧代码：Row 2 的 6 列中 5 列 ❌ LOST
- 新代码：Row 2 的异常点1/2/3 全部 ✅ 正确渲染

---

## 2026-08-12 Round 27：PPTX 按钮改名 HTML + 新增真正的 PPTX 导出

**用户**：你那 PPTX 的按钮，名词改成 HTML，新增一个 PPTX 生成真正的 PPTX，你懂我的意思吗？

**Claude**：懂。当前 `📊 PPTX` 按钮实际调用 `Export.toPPTX()` 导出的是 HTML 文件（PPTX 降级方案），名不副实。改为：
1. 现有按钮改叫 `📄 HTML`，调用 `Export.toHTML()`
2. 新增 `📊 PPTX` 按钮，调用 `Export.toPPTX()`，使用 PptxGenJS (CDN) 生成 Office 可打开的 .pptx 文件

### 改动内容

**index.html / ui-template.html**:
- 新增 `<script src="pptxgenjs@3.12.0/dist/pptxgen.bundle.js">` CDN 引入

**js/export.js** (完全重写):
- `toPPTX()` → `toHTML()` — 功能不变，仅改名
- 新增 `toPPTX()` — 真正生成 .pptx：
  - 按 `# / ## / ###` 标题拆分为幻灯片
  - 段落文本用 Markdown 渲染（支持 **粗体**、列表项）
  - 表格用 PptxGenJS `addTable()` 原生 PowerPoint 表格
  - 图表用 canvas 截图 `addImage()` 嵌入
  - 代码块用 Consolas 等宽字体文本框
  - 标题带蓝色下划线装饰条、底部 footer
  - 内容溢出自动分页
- 新增辅助函数：`splitSlides()`, `parseSegments()`, `renderTextSegment()`, `renderTableSegment()`, `renderChartSegment()`, `renderCodeSegment()`, `parseBoldRuns()`, `splitBareTables()`
- Public API: `{ toExcel, toHTML, toPPTX, downloadFile }`

**js/chat-ui.js**:
- 按钮文案：`📊 PPTX` → `📄 HTML` (data-action="html")
- 新增：`📊 PPTX` (data-action="pptx")，title="导出为 PPTX（真正的 PowerPoint）"
- 事件绑定：html → Export.toHTML / pptx → Export.toPPTX

**index.html 版本号**: `export.js?v=20260812a` → `20260812b`, `chat-ui.js?v=20260811i` → `20260812b`

### 关键设计决策
- PptxGenJS 3.12.0 CDN 浏览器 bundle（含 JSZip），无需服务端依赖
- Hex 颜色不加 `#` 前缀（pptxgenjs 要求）
- 表格列宽均分，小于等于 4 列时自适应更宽
- 图表截图缩放为 16:9 比例 contain 模式
- 代码块超过 800 字符截断防溢出

---

## 2026-08-12 Round 28：PPTX 导出品质修复系列

### 问题 1：HTML 导出 Slide 高度不自然

**用户**：生成 HTML，不要坚持那个 Slide 的高度，不然每个段落中间会出现长短不一的空白。

**Claude**：移除 HTML 导出中 slide 的 `min-height: 400px`，让每个 slide 高度由内容自然决定，消除不均匀空白。

### 问题 2：PPTX 内容溢出 Slide 边界

**用户**：PPTX 生成的品质很不好，很多 Slide 的内容都满溢出去了。

**Claude**：根因是初版用 `estLines * 0.28` 手动估算高度，不可靠。彻底重构为 smart content organization 方案：
- 不再堆叠多个内容块估算高度
- 使用 PptxGenJS `autoFit: true` + `fitTo: 'shrinkText'` 作为安全网
- 布局常量：`BODY_Y=1.3, BODY_H=3.8, FOOTER_Y=5.2, MARGIN_L=0.7, CONTENT_W=8.6`

### 问题 3：内容被拆得七零八落

**用户**：你这版本内容不溢出了，但是内容被拆的七零八落，有些 Slide 就一句话，根本不能让人直接使用。

**Claude**：初版 "one block per slide" 过度修正。重构为 3 步智能流水线：

```
原始内容 → Step 1: flatten (扁平化) → Step 2: smart merge (智能合并) → Step 3: split (拆分长内容) → 最终 Slide
```

合并规则：
- 短文本(≤10行) + 小表格(≤7行) → 35/65 高度分配，合并为一页
- 短文本(≤6行) + 图表 → 22/78 高度分配，合并为一页
- 不满足条件的内容单独成页

拆分规则：
- 文本 >20 行 → 按 `\n\n+` 段落边界拆分为 ~15 行 chunk
- 大表格 → MAX_ROWS=14，超出拆分为多页，续页标题加 "（续2/5）" 后缀，每页重复表头

新增核心函数：`buildSlideUnits()`, `countTextLines()`, `countTableRows()`, `renderMainText()`, `renderMainTable()`, `renderMainChart()`, `renderMainCode()`, `renderCombo()`, `renderMixed()`

---

## 2026-08-12 Round 29：Markdown 管道表格渲染 + PDF 导出按钮

### Markdown 管道表格 → 美观 HTML 表格

**用户**：Chat 里 `| A | B |` 这样的管道表格不要用纯文本展示，要用美观的表格。

**Claude**：在 `js/ai-parser.js` 中新增 `parseMarkdownTable()` 函数，在 `renderMarkdown()` 中用占位符保护模式处理：

```
文本 → 正则提取管道表格 → 替换为 %%MDTBL{N}%% → escapeHtml + Markdown 处理 → 还原表格 HTML
```

- 正则：`/(\|[^\n]+\|\s*\n\|[-:\s|]+\|\s*\n(?:\|[^\n]*\|\s*\n?)+)/g`
- 自动检测数值列 → `class="num"` 右对齐
- 包裹 `<div class="table-wrap">` 支持窄屏横向滚动

### PDF 导出按钮

**用户**：Chat 转出的部分我要新增一个 PDF 按钮，内容就跟 HTML 一模一样，按钮位置放在 HTML 按钮后面。

**Claude**：提取共享函数 `buildHTMLReport()` → `toHTML()` 和 `toPDF()` 共用：
- `toHTML()`：调用 buildHTMLReport + downloadFile
- `toPDF()`：调用 buildHTMLReport + `window.open()` 新窗口 + `window.print()`（800ms setTimeout fallback）

按钮布局最终为：`📋 复制 | 💬 延申问答 | 📥 Excel | 📄 HTML | 📑 PDF | 📊 PPTX`

### 改动文件

| 文件 | 改动 |
|------|------|
| `js/ai-parser.js` | 新增 `parseMarkdownTable()` + `renderMarkdown()` 占位符保护 |
| `js/export.js` | 提取 `buildHTMLReport()`；新增 `toHTML()` / `toPDF()` / `toPPTX()`；版本号 → 20260812f |
| `js/chat-ui.js` | 按钮加 `📑 PDF`；版本号 → 20260812b |
| `index.html` | 引入 `pptxgenjs@3.12.0` CDN；版本号更新 |
| `ui-template.html` | 同步 CDN 引入 |

---

## 2026-08-12 Round 30：README 更新 + 对话记录补齐 + 代码推送

**用户**：把新功能也补充到 README，更新一下对话记录，最终 commit、push 源代码。

**Claude**：逐项完成：

### 1. README.md 更新
- 文件结构：`export.js` 描述从 `CSV Excel / HTML PPTX` → `Excel / HTML / PDF / PPTX`
- AI 工作流：导出选项从 `Excel / PPTX` → `Excel / HTML / PDF / PPTX`，新增"含管道表格自动渲染"
- 技术栈表：导出行从 `CSV + HTML` → `CSV + HTML + PDF + PPTX`，补充 PDF（浏览器打印）和 PPTX（PptxGenJS 原生 .pptx）
- 项目状态：`正式 .pptx 导出` 从 ⬜ 待开发 → ✅ 已完成

### 2. 对话记录.md 更新
- Round 27 标题修正（Round N → Round 27）
- 新增 Round 28（PPTX 品质修复：Slide 高度、溢出、碎片化、大表格分页）
- 新增 Round 29（Markdown 管道表格渲染 + PDF 导出按钮）
- 新增 Round 30（当前轮 — README 更新 + commit + push）

### 3. Git 提交 & 推送
- 6 个修改文件：`index.html`, `ui-template.html`, `js/export.js`, `js/chat-ui.js`, `js/ai-parser.js`, `README.md`, `对话记录.md`
- Commit message: 总结 PPTX/HTML/PDF 导出、Markdown 表格渲染等新功能
- Push to GitHub

---

## 2026-08-12 Round 31：分页器Bug修复 — API无视offset start值

**用户**：发现分页器有很多页，但切换页次显示的还是第一页的数据。

**Claude**：添加 console.log 调试 → 发现关键证据：

| 请求 offset | 返回条数 | firstRow |
|-------------|---------|----------|
| `[0, 20]` | 20 | `SO2601200001` |
| `[20, 40]` | **40** | `SO2601200001`（一样！） |

**根因**：API 的 `offset[start, end]` 参数中 **`start` 值被服务端无视**，始终从第 0 条开始返回。`end` 值直接当成了"返回前 N 条"。所以 offset=[20,40] 实际返回了 0-39 共 40 条数据。

### 修复方案：客户端分页

| 操作 | 行为 |
|------|------|
| **查询/重置/切换报表** | API 取全量 `[0, 5000]` → 存 `_allData` 缓存 → 切片渲染当前页 |
| **翻页** | 从 `_allData` 切片直接渲染，**无 API 调用，即时响应** |
| **改每页条数** | 从缓存重切片，不调 API |
| **刷新** | 重新调 API 取全量 |

新增函数：
- `renderPageSlice(viewPage, pageSize)` — 从 `_allData` 切片 + 边界保护
- `goToPage(targetPage)` — 翻页优先走缓存，缓存失效时调 API

`updatePagination()` 改为基于 `totalCount` 计算总页数，显示 "第 21-40 条，共 106 条"，末页按钮 `>>` 跳到最后一页。

### 附带修复

- **异步转入**：点击"转入数据源"→ 立即创建占位卡片+切换 AI Tab+Tab badge spinner → 后台异步拉全量 → 完成更新卡片
- **DataSourceStore**：新增 `addPending()` 占位 + `update()` 更新方法
- **DatasourceList**：渲染 loading/error 两种卡片状态
- **`fieldDisabled` 默认值修复**：`reports.js` 中 `dFilter.fieldDisabled !== false` → `dFilter.fieldDisabled === true`（之前日期筛选条件被忽略）

### 重要发现：API 文档索引不完整

**用户**指出：`标准报表制表API.md` 只包含 6 个报表（采购/进货/受订/销货/收款/付款），不包含 `标准报表制表API2.md` 的另外 4 个报表（工单完成情况/MRPPK、完工入库/MRPPS、产品成本分析/MRPPU、员工年度薪资/REP_WAGCG3）。CLAUDE.md 的文件索引没有提到 API2，这是一个重要的遗漏。

### API 行为确认
- `offset[start, end]` — **start 被忽略**，只取前 `end` 条
- 文档所有示例都是 `offset: [0, 5000]` — 没有任何非零 start 的例子（现在知道原因了）
- 服务端分页不可用，必须客户端分页

### 改动文件

| 文件 | 改动 |
|------|------|
| `js/app.js` | 新增 `_allData` 缓存 + `renderPageSlice()` + `goToPage()`；重写 `doQuery()` 全量取数；重写 `updatePagination()` 基于 totalCount |
| `js/reports.js` | `fieldDisabled` 默认值修正；移除调试日志 |
| `js/datasource-store.js` | 新增 `addPending()` + `update()` |
| `js/datasource-list.js` | 渲染 loading/error 卡片状态 |
| `css/dashboard.css` | `.tab-badge` spinner、`.ds-loading`、`.ds-error` 样式 |
| `index.html` | cache busters 更新

---

## 2026-08-12 Round 32：API3 — 21 个新报表接入

**主题**：集成 `标准报表制表API3.md` 中的 21 个新报表 API，系统从 10 报表扩展到 31 报表。

### 完成事项

**代码改动（4 文件）**：

| 文件 | 改动 |
|------|------|
| `js/reports.js` | 新增 21 个 REPORT_CONFIG 条目（accabgt ~ fixaa）；扩展 COLUMN_LABELS（~150+）、DECIMAL_FIELDS、CURRENCY_FIELDS；buildRequest() 支持 showBody/hasPagination/dateOperator/searchInfoExtra/hideDateUI/contain；updateFilterPanel() 新增 layoutMap 7 种 + hideDateUI + 3 新 filter 组（batNo/prdMark/fxKnd）；readFilters()/resolveFilterValue() 扩展 20+ 新字段映射 |
| `index.html` | 侧边栏重构为 7 分组 31 条目（进销存4/财务管理8/库存管理5/采购与价格5/生产制造5/人力资源3/固定资产1）；新增 filter-group-batno/prdmark/fxknd；所有 CSS/JS 版本号升级至 ?v=20260813a |
| `js/app.js` | reset 按钮 fields 数组扩展 3 个新字段（filterBatNo/filterPrdMark/filterFxKnd）；版本号升级 |
| `ui-template.html` | 同步：侧边栏 7 分组 31 条目 + 筛选面板 3 新 group + 版本号升级 |

**新架构模式（参数化，无 if/else 分支）**：

| 模式 | 说明 | 使用报表数 |
|------|------|-----------|
| `showBody: "T"` | SEARCH_INFO[1] 显示 body 字段 | 2（rptwagyg0, monjk） |
| `hasPagination: false` | 跳过 offset 分页参数 | 1（rptsarp） |
| `dateOperator` | 预设日期操作符（last_year/this_year/today/last_week/this_month/equal） | 12 |
| `searchInfoExtra` | fixCondition 后插入隐藏条件 | 4（rptinvdo, fixaa, invhp, invhs） |
| `hideDateUI: true` | SYS_DATE 报表隐藏日期选择器 | 3（rptwagyg, invhp, invhs） |
| `contain` operator | 文本包含搜索 | 1（scmdrpti） |
| 非 date `range` | 非日期字段范围筛选 [from, to] | 2（invic, invij） |
| `apiPath` 大写端点 | 精确指定端点路径（大小写） | 8 |

### 问题/决策

- **无新问题**：API2 接入时的 9 个坑全部在本次设计中预先规避（坑1缓存→版本号先升级；坑2大小写→全部 apiPath 精确指定；坑3滚动→布局已修复无需改；坑7 fieldDisabled→代码已用 === true）
- 21 个报表全部使用 getReport 端点，无需新增 getList 专用路径（风险低于 API2）
- 文档同步（7 文件）在代码改完后立即执行

### 待验证

- [ ] 浏览器逐组测试 21 个报表
- [ ] 部署环境端点大小写验证（文档中的大小写以实测为准）
- [ ] 数据超 5000 条的报表需调整 offset end 值

---

## 2026-08-12 Round 33：清理 & 打包 & 推送

**主题**：清理乱码空文件，重建部署包，推送代码到 GitHub。

### 完成事项

- [x] 清理 4 个乱码空文件（PowerShell 编码问题产生的 0 字节文件）
- [x] 部署包重新构建（deploy.ps1 → sunlike-erp-report-v1.1.zip）
- [x] Git add + commit + push 到 GitHub（master 分支）

### 本次提交文件清单

| 类型 | 文件 |
|------|------|
| 核心代码 | `index.html`, `ui-template.html`, `js/reports.js`, `js/app.js` |
| 部署工具 | `deploy.ps1`, `install.ps1` |
| 新 API 文档 | `标准报表制表API3.md` |
| 文档同步 | `CLAUDE.md`, `对话记录.md`, `进度追踪表.md`, `需求架构文档.md`, `流程图.md`, `README.md`, `API服务调用说明文档.md`, `部署指南.md` |
| 部署包 | `sunlike-erp-report-v1.1.zip` |

### 决策

- 部署包内置到仓库便于直接下载部署，不走 release 流程

---

## 2026-08-12 Round 34：记事本功能实现

**主题**：在 AI 数据分析旁边新增第三个平行页签【📓 记事本】，支持保存/恢复数据源+对话。

### 需求回顾

- 三个平行页签：📋数据查询 / 🤖AI数据分析 / 📓记事本
- ds-footer 双按钮并列：📓存到记事本 + 🗑清空全部数据源
- 保存时弹出 prompt 输入事件名称
- 点击事件卡片 → confirm → 覆盖数据源+对话 → 跳转 AI 页签
- 存储：localStorage key `sunlike_notepad`，上限 50 个

### 修改文件清单

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `js/notepad-store.js` | localStorage CRUD（IIFE，参考 DataSourceStore 模式） |
| 新建 | `js/notepad-ui.js` | 记事本 UI：render/saveCurrent/loadEvent/deleteEvent/clearAll/init |
| 新建 | `css/notepad.css` | 记事本 + ds-footer 双按钮覆写独立样式（184 行，缓存版本 v=20260814a） |
| 修改 | `js/utils.js` | 新增 `deepClone()` 工具函数 |
| 修改 | `js/chat-ui.js` | 新增 `clearAllMessages()` + `renderHistory(chatHistory)` |
| 修改 | `js/tabs.js` | `switchTab()` 增加 `tabNotepad` 分支自动刷新 |
| 修改 | `js/datasource-list.js` | 最终只保留 `btnClearAllDS` 绑定（`btnSaveToNotepad` 绑定移入 NotepadUI.init） |
| 修改 | `js/app.js` | init 序列添加 `NotepadUI.init()` |
| 修改 | `index.html` | 添加第三 tab 按钮 + `#tabNotepad` 面板 + ds-footer 双按钮 + 脚本/CSS 引用 |
| 修改 | `css/ai-analysis.css` | 先追加后移除（恢复原状，记事本样式独立成文件） |
| 修改 | `ui-template.html` | 移除 inline `<style>`，改用共享 `css/notepad.css` |
| 更新 | `对话记录.md` | 本轮对话记录 |
| 更新 | 方案文件 | 从侧边栏架构改为页签架构 |

### 架构决策

- `tabs.js` 本身不需要核心改动（data-tab/id 匹配机制天然支持任意数量 tab），只需加一个 `tabNotepad` 分支刷新列表
- `chat-ui.js` 的 `renderHistory()` 对 assistant 消息重新调用 `AIParser.parse()` 恢复表格/图表渲染
- 保存时对整个 dataSource 和 chatHistory 执行 `Utils.deepClone()`（JSON 序列化），避免引用污染
- 加载时 `DataSourceStore.add()` 会生成新 ID 和 createdAt，避免 ID 冲突
- **记事本 CSS 独立成文件**：保证在 `ai-analysis.css` 之后加载，同特异性规则后加载者胜，避免缓存和优先级问题

### 边界条件覆盖

- 无活跃数据源 → toast "请先选择数据源"
- 无对话内容 → toast "暂无对话内容"
- prompt 取消/空输入 → 中止保存
- 加载 → confirm 确认后才替换
- 超 50 个事件 → 自动裁剪最旧
- localStorage 读取失败 → try/catch 容错返回 []

### Bug 修复记录

1. **生产样式与模板不一致**：notepad CSS 原本追加在 `ai-analysis.css` 末尾，但版本号未变导致浏览器缓存旧 CSS；模板用 inline `<style>` 不受影响。修复：抽成独立 `css/notepad.css`（新版本号），`index.html` 和 `ui-template.html` 共用
2. **保存后记事本无记录**：`DatasourceList.init()` 和 `NotepadUI.init()` 双重绑定 `btnSaveToNotepad`，每次点击触发两次 `saveCurrent()`（两次 prompt、两次保存）。修复：移除 `datasource-list.js` 中的重复绑定，保留 `NotepadUI.init()` 唯一绑定
3. **多数据源未选中时无法保存**：原来只处理"恰好 1 个数据源"的自动选择。修复：过滤 ready 状态后，1 个或多个都自动选第一个
4. **静默失败无提示**：`saveCurrent()` 无 try/catch。修复：包裹 try/catch，异常时 toast 报错

### 待验证

- [ ] 浏览器端到端测试：保存 → 清空 → 加载 → 删除 → 清空全部
- [ ] 三个页签切换正常
- [ ] 保存后记事本页签正确显示事件卡片
- [ ] 加载后聊天格式完整（markdown 表格/图表重新解析）
- [ ] 刷新页面后记事本数据保留


## 2026-08-13 Round 35：报表菜单改进 — 搜索/折叠分组/收藏置顶（原型阶段）

### 需求回顾

- 报表已有 31 种，静态平铺侧边栏找菜单吃力，且未来会继续增加
- 用户确认范围：① 搜索框（中文模糊 + 拼音首字母，Ctrl+K 聚焦 / Esc 清空）② 7 个分组可折叠（保持现有分组结构）③ ⭐ 收藏置顶；**不做**"最近使用"
- 按项目纪律：先改 ui-template.html 原型，用户浏览器确认通过后再动 index.html（本轮 index.html 未动）

### 完成事项（原型阶段）

- **js/reports.js**：31 条 REPORT_CONFIG 各加 `group`/`icon`/`pinyin` 字段（单一数据源，之后新增第 32 个报表只需加一条配置即自动出现在菜单）
- **新建 js/report-menu-store.js**：`ReportMenuStore`，localStorage key `sunlike_report_menu`，结构 `{favorites:[], collapsedGroups:[]}`；读取时 try/catch 容错 + 按 getReportKeys 剪枝；存"已折叠分组名单"（新分组默认展开，向前兼容）
- **新建 js/report-menu.js**：`ReportMenu` 模块，动态渲染侧边栏。一条 render() 代码路径覆盖 搜索×折叠×收藏×active 四种状态；MENU_ORDER/MENU_GROUPS 常量保持现有菜单顺序；搜索匹配：中文名/拼音首字母两路 contains + key 兜底（仅名称拼音全无命中时生效，防 "cg" 误中 wagCG3）；容器级事件委托处理星标与分组折叠；Ctrl+K 聚焦（折叠态自动展开）、Esc 清空（仅搜索框焦点内生效）
- **js/app.js**：`initSidebarNav` 改为容器级事件委托（兼容静态与动态渲染的 .nav-item），提取 `openReport(reportKey)`（报表名改从 config 读取，菜单高亮按 data-report 全量更新，收藏区+分组两份条目同亮）；`App.init()` 加 `ReportMenu.init()`（typeof 守卫，index.html 未加载该模块时跳过）
- **css/dashboard.css**：新增 `.sidebar-search` / `.nav-star` / `.nav-section-toggleable` / `.nav-caret` / `.nav-count` / `.nav-fav-header` / `.nav-empty` 样式；56px 折叠态隐藏搜索框/星标/caret/count；`prefers-reduced-motion` 支持
- **ui-template.html**：静态 31 条侧边栏替换为搜索框 + 空 `<nav id="sidebarNav">`；script 区在 reports.js 之后加载 report-menu-store.js → report-menu.js
- **设计系统**：已跑 ui-ux-pro-max（Enterprise ERP + Dashboard, density 8），采用 focus ring / cursor-pointer / 150-300ms 过渡 / 键盘可达性规格

### 验证记录

- `node --check` 语法检查 4 个 JS 全部通过
- node + DOM 桩冒烟测试 30 项断言全部通过：31 条渲染、7 分组、active 唯一、搜索"采购"/"cg"/key 兜底/无结果空态、收藏区置顶+两份条目+实心★+新收藏排最前、折叠后条目数正确（收藏2+分组26=28）、localStorage 持久化、损坏 JSON 回退、星标点击不触发跳转

### 问题/决策

1. 冒烟测试首轮 2 个"失败"是测试脚本预期写错（折叠后总数应为收藏 2 + 分组 26 = 28；剪枝在**读取时**生效），代码本身正确
2. 星标默认 45% 透明度而非仅 hover 显示：触屏设备无 hover，收藏入口必须可见可点（ui-ux 规范：不依赖 hover 交互）
3. 星标用 ★/☆ 文本字形而非 emoji（新控件图标不用 emoji，颜色可控）

### 待验证（用户浏览器打开 ui-template.html）— 用户已确认通过 ✅

- [x] 中文搜索"采购"、拼音搜索"cg"/"CG"、无结果空态
- [x] Ctrl+K 聚焦（含 56px 折叠态自动展开）、Esc 清空
- [x] 分组**默认全折叠**（登录/刷新即折叠）；会话内展开/折叠正常；刷新后复位折叠、收藏保持
- [x] 收藏/取消收藏，刷新后保持，收藏区置顶
- [x] 56px 折叠模式无破版
- [x] 用户确认 → Phase 2 落地 index.html + Phase 3 文档同步

### 用户反馈修订（分组默认折叠）

- 用户反馈：登录/刷新后分组标题**预设折叠**，侧边栏更简洁利落
- 变更：ReportMenuStore 折叠状态由 localStorage 持久化改为**会话内内存态**（存"已展开名单" `_expandedGroups`，默认全折叠）；刷新/重新登录即复位；收藏仍走 localStorage 持久化，数据结构简化为 `{favorites:[...]}`，不再写入 collapsedGroups
- report-menu.js 无需改动（`isGroupCollapsed`/`toggleGroup` 接口不变，语义翻转由 store 内部消化）；ui-template.html 的 report-menu-store.js 引用版本号升为 `20260813b`
- 验证：`node --check` 通过；node + DOM 桩冒烟测试 25 项断言通过（默认 7 组全折叠 / 会话内切换 / 模拟刷新复位折叠+收藏保持 / 孤儿剪枝 / 损坏 JSON 回退 / 持久化不含 collapsedGroups / 折叠态渲染 / 收藏区在分组折叠时仍可见 / 搜索与空态不受折叠影响）
- 边界评估：第 32 个新报表（新分组）同样默认折叠——存"展开名单"的模型天然覆盖；登录页无侧边栏不受影响
- 测试记录：两处"失败"均为测试桩预期问题（桩元素缺 addEventListener；展开 1 组后其余 6 个空组仍折叠），代码本身正确

### Phase 2 落地 + Phase 3 文档同步（用户浏览器确认通过）

- 用户确认原型效果 → 落地 index.html
- **index.html**：静态 31 条侧边栏删除 → 搜索框 + 空 `<nav id="sidebarNav">`；script 区在 reports.js 之后加载 report-menu-store.js → report-menu.js（顺序与模板一致）；改动文件版本号统一升 `20260813b`（dashboard.css / reports.js / app.js + 两个新模块）
- **ui-template.html**：同步升版本（dashboard.css / reports.js / app.js / report-menu.js），两个 HTML 的菜单区与引用保持一致
- **js/report-menu.js**：focusSearch 增加登录页守卫——loginPage 未隐藏时不响应 Ctrl+K（避免焦点落进隐藏面板）；模板无 loginPage 时天然放行
- **回归验证**：
  - `node --check` 4 个 JS 全部通过
  - 冒烟测试 27 项断言通过（新增登录页 Ctrl+K 守卫 2 项：登录页可见不聚焦 / 登录后正常聚焦）
  - 静态一致性：两个 HTML `class="nav-item"` 静态条目 0 个；openReport 路径复查（cfg.name 取名 / 双份高亮 / switchTab / updateFilterPanel / renderTableHead / doQuery）；App.init 中 ReportMenu.init 在 initSidebarNav 之前
- **文档同步**：需求架构文档.md 新增 4.8 报表菜单模块 + 侧边栏导航条目更新；进度追踪表.md 新增任务 18；流程图.md 新增第十三节（渲染主流程 / 交互流程 / 状态持久化 3 张 Mermaid 图）
- **待验证（浏览器回归 index.html）**：登录 → 点报表切换（openReport 全链路：筛选面板/表头/查询/分页）→ Ctrl+K/Esc → 分组默认折叠/展开/刷新复位 → 收藏/刷新保持 → 转移数据源 → 登录页 Ctrl+K 无反应 → 56px 折叠无破版

---

## 2026-08-13 Round 36：折叠侧边栏分组标题处理 — 全量 emoji 平铺

**主题**：折叠态（56px）下分组标题文字被旧 9px 样式硬挤换行/截断（[dashboard.css:97](css/dashboard.css#L97) 旧规则），部分展开部分折叠时更乱；且折叠态分组标题点击已被守卫禁用，纯视觉噪音。

### 问题/决策

- 用户从 3 个方案中选定：**A 全量 emoji 平铺**（放弃 B 只显示已展开分组——默认全折叠时侧边栏几乎空白；C 序号标记——56px 仍拥挤）
- 折叠态渲染规则：忽略分组折叠状态，31 个报表图标全部平铺（回到无分组时旧版效果）；收藏去重置顶（折叠态不重复显示两份）；组间 1px 细分割线；分组标题不渲染

### 修改

- `js/report-menu.js`：新增 `_renderCollapsed()`（收藏去重 + 按组输出 + 组间分割线，整组已收藏时跳过防孤儿分割线）；`_renderNormal()` 增加折叠态分支
- `js/app.js`：initSidebarCollapse 切换后调用 `ReportMenu.render()`（折叠/展开都即时重渲染）
- `css/dashboard.css`：`.sidebar.collapsed .nav-section` 由 9px 居中文字改为 `display:none`；新增 `.nav-divider` 分割线样式
- 版本号：report-menu.js / app.js / dashboard.css → `20260813c`（index.html + ui-template.html 同步）

### 验证

- `node --check` 通过；冒烟测试 32 项断言通过（折叠态无分组标题 / 全量平铺不受折叠状态影响 / 收藏去重 / 分割线 / 展开恢复分组视图）
- 待浏览器验证：折叠按钮切换即时生效、56px 下 emoji 列表可滚动可点击、展开后分组折叠状态不丢失

### 打包与提交

- deploy.ps1 → v1.2：CSS 清单加 notepad.css；JS 清单按 index.html 依赖顺序插入 report-menu-store/report-menu（reports.js 后）、notepad-store/notepad-ui（export.js 后）；总文件数 22 → 27（1 html + 4 css + 22 js）
- install.ps1 横幅 v1.1 → v1.2；部署指南.md 版本行同步
- 生成 sunlike-erp-report-v1.2.zip（88 KB，27 文件，已验证含 4 个新模块）；仓库保留 v1.0/v1.1 旧包
- git commit + push：feat: 报表菜单(搜索/折叠/收藏) + 记事本 + 部署包 v1.2

## 2026-08-13 Round 37：多语言 i18n Phase A — 模板原型（简/繁/英）

**主题**：按已批准的三语系计划执行 Phase A——在 ui-template.html 完成三语原型，待浏览器确认后进 Phase B。

### 完成事项

- 新建 `js/i18n.js`（核心：boot/getLang/setLang/t/applyStatic/detectOSLang/audit，ES5 IIFE）；`js/i18n-data.js`（静态字典 zh-tw/en 各 201 条，分节组织，末尾同步 I18n.boot() 保证零闪变）；`js/dialog.js`（应用内 confirm/prompt，替换原生弹窗以保持全英文承诺）
- 语言值 = Sunlike 服务器 LangTag（zh-cn/zh-tw/en）；持久化键 `sunlike_lang`，仅用户显式点击时写入；解析链：已存偏好(校验) → OS 检测(zh-TW/zh-HK/zh-Hant→zh-tw) → zh-cn
- ui-template.html：登录卡三按钮 #langSwitch 分段控件（简/繁/英）；全文 ~103 个 data-i18n 属性；DEMO ONLY 演示面板（报表名/分组/栏位/分页/toast/Dialog 实时联动）；#tableCount 重构为 #tableInfoText
- report-menu.js 提前接 t()：渲染名/分组名/星标 title/空状态/收藏头；搜索四路匹配（译名+中文名+拼音+key 兜底）
- css/auth.css 追加 .lang-switch；css/ai-analysis.css 追加 .dialog-card/.btn-danger
- 静态审计（.claude/audit-i18n.js）：103 属性 × 2 语言零缺失、无真重复、无僵尸 key（4 个"僵尸"实为 Phase B 预置 key，已逐一对上调用点）；node --check 全过

### 决策

- 用户需先 DEMO 旧版 → Phase A 成果存 GitHub 分支 `feat/i18n-phase-a`（commit b1bf809，7 文件 +1236/-120，已推送），本地 master 还原到 v1.2（4921a48）供 DEMO
- 生产文案统一：`清空全部数据源`（原 title 与正文不一致），Phase B 同步 index.html

### 遗留

- A6 浏览器确认（Phase B 门禁，CLAUDE.md 规则八）顺延至 DEMO 后
- Phase B：生产代码移植 B1-B17（见计划）；栏位标签 ~400 条代表集已译 12 条，Phase B2 补全

## 2026-08-13 Round 38：DEMO 完成 — i18n 分支合并回 master

**主题**：用户完成旧版 DEMO 后，将 feat/i18n-phase-a 分支合并回 master，继续未完成任务。

### 完成事项

- `git merge feat/i18n-phase-a` 快进合并成功：master 4921a48 → b1bf809，7 文件（i18n.js/i18n-data.js/dialog.js 新建 + 模板/CSS/菜单修改），工作树干净
- 本地与远程 feat/i18n-phase-a 分支保留，随时可查

## 2026-08-13 Round 39：i18n Phase B（生产代码移植）+ Phase C（验证与文档）

**主题**：用户 DEMO 完成后浏览器确认模板效果（A6 门禁通过），批准进入 Phase B——把三语系支持从模板移植到生产代码。

### B0（规则二 API 三步验证，B5 前置）

- curl PoC 实测：`LANG_ID` 传 LangTag（zh-cn / zh-tw / en）三语登录全部 `code: 0`，错误消息随语系返回——决策 1（LANG_ID 跟随所选语言）落地验证通过

### B3-B17 完成事项

| 项 | 内容 |
|----|------|
| B3 | index.html：104 个 data-i18n 属性（登录页/侧边栏/页签/筛选面板含 option value 不动/空状态/设置弹窗）；`#tableInfoText` 由 JS 填充 `t('共 {0} 条记录', n)`；脚本首位插 i18n.js/i18n-data.js、utils.js 后插 dialog.js；改动文件 `?v=` 统一 20260814a |
| B5 | LANG_ID 动态化 4 处（auth.js×2/api.js/settings-ui.js）：`I18n.getLang()` + typeof 守卫回退 zh-cn |
| B6-B13 | 15 个文件渲染点包 t()：reports.js（getColumnLabel 一行通杀全部表头 + 徽章 + 错误兜底）、app.js（toast/分页/标题，currentReportName 保持简体规范值）、report-menu.js（四路搜索：译名+中文名+拼音+key 兜底）、datasource-list.js、notepad-ui.js（6 处原生弹窗换 Dialog）、utils/chat-ui/chat-core/ai-suggestions/ai-chart/ai-parser/notepad-store/settings-ui/auth/api |
| B14 | ai-client.js：提示词三语版（PROMPTS 按 I18n.getLang() 分发，内嵌 table/chart 示例表头与末尾语言指令随语言）；provider 显示名入字典 |
| B15 | export.js 23 处 t()：标题/文件名（AI数据分析报告）/toast/占位文案 |
| B16 | deploy.ps1：$jsFiles 插 i18n.js/i18n-data.js/dialog.js；版本 v1.3 |
| B17 | ui-template.html 脚本列表与生产对齐（deepseek-client.js→ai-client.js，补 notepad-store/notepad-ui/ai-suggestions，版本号 20260814a） |

### 字典（i18n-data.js）

- 601 个 key × 2 语言 = zh-tw/en 各 628 条；对账工具 `.claude/extract-i18n-keys.js`（提取 I18n.t() 静态调用 + data-i18n 属性 + REPORT_CONFIG.name + COLUMN_LABELS 值 + MENU_GROUPS；排除 i18n.js/i18n-data.js/dialog.js）
- **最终对账：缺失 0；27 僵尸全部可解释**（模板 DEMO 面板 ~20 条 / dialog.js '确定''删除' / '取消收藏' 为条件表达式提取盲区——实际在 report-menu.js 使用）
- 提取盲区已知：`I18n.t(isFav ? '取消收藏' : '收藏')` 这类条件字面量静态正则抓不到，属非真僵尸

### 顺手修复的问题

1. **ai-client.js 原 bug**：`:196` 用 `e.message.indexOf('API 错误') === 0` 判断内部 API 错误永远为 false（消息前缀是 cfg.name）——改为 throw 时打 `err._isApiError = true` 标记，跨语言可靠
2. **index.html utils.js 版本号遗漏**：B6-B13 改了 utils.js 但 `?v=` 未升——已升 20260814a（B17 时发现）
3. **deploy.ps1 清单与生产不一致**：deepseek-client.js 已被 ai-client.js 取代且 index.html 不加载——从打包清单移除，实际为 **29 文件（1 html + 4 css + 24 js）**，与 index.html 加载清单逐项一致

### Phase C 验证结果

- ✅ `node --check` 全部 JS 语法通过
- ✅ 字典对账：601 key / 缺失 0
- ✅ deploy.ps1 → sunlike-erp-report-v1.3.zip（111.7 KB，29 文件）；解压清单与 index.html 28 个本地引用逐项比对：缺失 0、冗余 0

### 遗留（浏览器三语走查，需用户执行）

- [ ] 登录页三语即时切换/刷新记住/清存储回 OS 预判；`<html lang>`/`<title>` 正确
- [ ] 三语各登录一次全量走查：菜单 31 报表名/7 分组/表头/筛选/分页/空状态/toast；英文搜 "sales" 命中受订报表、拼音仍可用
- [ ] 6 处弹窗为应用内样式且随语言；Enter/Esc 正常
- [ ] AI 聊天：en 会话英文作答+英文表头，zh-tw 繁体作答；推荐问题随语言
- [ ] 导出：HTML/PPTX/CSV 标题文件名随语言
- [ ] zh-cn 回归：界面与改造前一致，收藏/折叠持久化完好，格式与 ¥ 未动

## 2026-08-13 Round 40：表头语言渲染时机修复 — 切语系登录后表头不跟随

**用户报告（问题现象）**

- 资料查询表头栏位名：登录页选繁体，登录进来仍是简体；英文也不起作用；只有登出再进才生效

**根因调查（先不改代码，弄清根源，确认后动手）**

1. 表头来源：纯本地 `getColumnLabel` → `I18n.t(COLUMN_LABELS[字段名] || 字段名)`（reports.js:1210），与服务器 COLUMN_INFO 无关
2. 全工程表头渲染仅两处：App.init（DOMContentLoaded、登录前、boot 语言）与 openReport（切换报表时）
3. 登录成功 / checkAuth 恢复只 doQuery（仅表体+分页），不渲染表头
4. 登录页切语言 bindLangSwitch 只 applyStatic 静态文字 + ReportMenu.render；表头 `<th>` 是 JS 动态渲染的 HTML、无 data-i18n 属性 → 默认报表表头停留在页面加载时的语言
5. "登出再进才生效"：重开页面 boot 读 sunlike_lang → init 用新语言渲染表头；英文同根因

**修复（js/app.js）**

- 新增 `renderCurrentTableHead()`：表头渲染**唯一入口**，四时机共用（init / 登录成功 / checkAuth 恢复 / openReport）
- 新增 `refreshDynamicI18n()`：重刷表头 + 报表标题 + 当前页表体切片（审核徽章等动态文字）+ 分页控件 title（数据未加载时只刷表头与标题）
- init() 注册 `window 'i18n:changed'` 监听（setLang 内部 dispatch）→ 语言一换，已渲染的动态内容立即跟随
- 登录成功（doQuery 前）与 checkAuth 恢复（doQuery 前）补 `renderCurrentTableHead()`
- `node --check` 通过

**决策**

- 采用「最小修复（登录/恢复补渲染）+ 根治（i18n:changed 事件）」组合方案，经用户确认后实施
- 筛选面板不重渲染：updateFilterPanel 只切组可见性、不毁输入值；面板标签是静态 HTML 由 applyStatic 覆盖
- ui-template.html 无 UI 变化，不动（纯行为逻辑修复）

**待验证（浏览器）**

- [ ] 登录页切繁/英 → 默认报表表头即时变化；登录后表头/表体徽章/分页 title 随语言；切报表路径回归正常
- [ ] 英文同场景走查；zh-cn 回归

## 2026-08-13 Round 41：记事本多数据源快照 — 回存只剩一个数据源修复

**用户报告（问题现象）**

- AI 资料分析中来源列表有 2+ 个数据源时「存到记事本」，回存后来源列表只剩 1 个数据源

**根因调查（先诊断，经确认后动手）**

- **设计问题**：既不是保存漏了、也不是回存掉了。Round 34 记事本事件模型就是"单数据源快照"：
  1. `saveCurrent` 只取 `AppState.activeDSId` 一个源（notepad-ui.js）
  2. 事件结构是单数 `dataSource`（notepad-store.js `add()`）
  3. `loadEventConfirmed` clearAll 后只 add 一个源
- 两端都忠实执行了单源设计，是"记事本事件只含一个数据源"的模型撑不起多数据源场景

**决策（用户拍板）**

- 保存范围：**全部数据源**快照（含 loading/error 状态；loading 快照回存后仍显示 loading 卡片属预期行为）
- 回存选中：**保存时选中的那个**（activeDSId 随事件持久化）

**修复（6 文件）**

1. `js/notepad-store.js`：add() 事件结构 `dataSource` → `dataSources` 数组 + `activeDSId`
2. `js/notepad-ui.js`：
   - 新增 `getEventSources()` — 旧事件单数 dataSource 兼容迁移为数组
   - `saveCurrent`：列表空 → 新 toast「请先添加数据源」；快照在保存瞬间重新读取 `getAll()`（避免 prompt 期间状态漂移）；自动选中优先 ready 否则第一个
   - `loadEventConfirmed`：`saveAll()` 全量恢复（保留原始 id/status/createdAt；**不用 add()**——add 会强制 status=ready 且重新生成 id，破坏快照保真）；activeDSId 恢复保存时选中源（校验存在性，缺失回退第一个）
   - `render` 卡片：单源保持原描述；多源显示「报表名（共 N 个数据源）」
3. `js/i18n-data.js`：+3 key（「保存全部数据源和对话到记事本」「{0}（共 {1} 个数据源）」「请先添加数据源」×2 语言）、-2 僵尸（旧按钮 title key + 旧 toast key）；对账 602 key / 缺失 0 / 僵尸 27（全部已知可解释）
4. `index.html` + `ui-template.html`：按钮 title 同步「保存全部数据源和对话到记事本」
5. `js/datasource-list.js` **零改动**：activeDSId 高亮机制天然支持恢复指定选中源

**边界确认**

- 旧事件（修复前保存的单 dataSource）回存/卡片均兼容迁移 ✓
- 50 事件 × 多源快照的 localStorage 5MB 容量风险由现有 quota toast 兜底（记录在案）
- node --check 全部通过

**待验证（浏览器）**

- [ ] 2+ 数据源 → 存到记事本 → 清空数据源 → 回存 → 列表恢复全部 + 选中保存时选中的那个
- [ ] 旧事件（修复前保存的）回存正常
- [ ] 多源卡片描述显示「（共 N 个数据源）」；三语走查

**用户验证反馈**：回存后仍只显示 1 个数据源 → **根因：缓存版本号没升**。Round 40/41 改了 app.js / i18n-data.js / notepad-store.js / notepad-ui.js 共 4 个文件，但 index.html 里 `?v=` 仍是 20260814a，浏览器一直跑缓存里的旧代码。修复：4 个文件升 `?v=20260814b`（index.html + ui-template.html 同步）+ 重建部署包 v1.3（112.9 KB）。**教训：改动任何 JS/CSS 必须同步升 `?v=` 并重建部署包**（第二次犯同类错误——B17 时 utils.js 漏升过一次）。
- 另外注意：**修复前保存的旧事件**本身只有 1 个源（旧代码只存了单源），回存显示 1 个属正常；验证必须**新保存**一个事件。
- 若浏览器从部署目录（zip 解压）加载，需重新解压部署包；若从项目目录加载，Ctrl+F5 硬刷新即可。

## 2026-08-13 Round 42：README 新功能补充 + 部署包重建 + commit/push

**用户**：对话记录及相关文件更新一下，把新功能也补充到 README，安装包也重新制作一下，最终 commit、push 源代码。

**完成事项**：

1. **README.md**：
   - 架构图模块引擎 18 → 24；badge 加 i18n
   - 项目结构全量对齐生产：24 个 JS 清单（补 i18n.js / i18n-data.js / dialog.js / notepad-store / notepad-ui / report-menu-store / report-menu / chat-core / ai-suggestions；deepseek-client.js 标注兼容别名未加载）、css 补 notepad.css、docs/ 移到根目录实际位置、补 deploy.ps1 与部署包
   - 新增三个功能章节：🌐 多语言支持（简/繁/英）/ 🗂 报表菜单（搜索/折叠/收藏）/ 📓 记事本（多数据源快照）
   - 项目状态表 +3 行（菜单 ✅ / 记事本 🟡 待 E2E / i18n 🟡 待三语走查）；首次配置加语言选择步骤
2. **对话记录**：本条 Round 42；四文档此前已在 Round 40/41 同步完毕
3. **部署包**：sunlike-erp-report-v1.3.zip 重建（29 文件，含 ?v=20260814b 修复后 JS）
4. **commit + push**：Phase B/C + Round 40/41 全部改动随本条提交推送 origin/master

## 2026-08-18 Round 43：API4 总分类账长连接（SSE）模式理解 + 文档更新

**用户**：@标准报表制表API4.md 是新 API 文件，跟前三个不一样：① 长连接模式；② 总账系统报表，【账簿】BOOK_NO 必填否则不能搜寻结果；③ 返回是流的形式，"PERCENT:" 后是百分比进度，希望返回过程有进度条正确反映进度；④ 后续提到【长连接】都按此处理。要求先思考，懂了就更新 API服务调用说明文档.md 和 项目经验总结.md 并汇报理解，不懂就问。

**Claude**：（理解 + 文档更新完成）

1. API4 = 总分类账查询制表 `POST /accGeneralLedger/GetReportStream`，PGM `ACCRPTGL`，SSE 流式响应（data: 行按行解析），与现有 getReport/getList 一次性 JSON 完全不同
2. SEARCH_INFO 仅 3 元素（无 offset 分页）：[0] 展示配置 / [1] fixCondition（BOOK_NO 必填 + ACC_IPERIOD_B/E 会计期间）/ [2] orderBy
3. 流中 `PERCENT: xx.x` = 进度 → 前端进度条实时更新；REPORT__TAB 数据随流到达（示例在 PERCENT: 90.0 消息）；流结束 → 置 100%
4. 技术要点：EventSource 不支持 POST → fetch + ReadableStream + 行缓冲解析 + AbortController 120s 超时；BOOK_NO 空值前端拦截
5. 配置驱动扩展：REPORT_CONFIG 加 apiMethod 'getReportStream' 专用分支，后续【长连接】报表复用

**完成事项**：

- API服务调用说明文档.md：新增第九节「长连接（SSE）流式报表」（概述 / 请求体差异 / 前端处理模式含代码骨架 / 待实测清单）+ 速查表加总分类账行 + 前端 Checklist 加流式/进度条/BOOK_NO 5 项 + 请求结构标题同步 32 报表 3 模式
- 项目经验总结.md：新增 3.5「长连接（SSE）流式报表」+ 数据一览更新（报表 32、API 模式 3 种）+ 9.1 原则加流式条目
- 进度追踪表.md：新增阶段 20「API4 总分类账（长连接）」待办
- 本轮只写理解与文档，**未写任何代码**；进度条实现待后续【长连接】实施轮

**待实测/待确认**：

- [ ] 原始流抓包：PERCENT 行与数据行确切原始格式（当前解析按文档推断）
- [ ] 流结束标记（PERCENT: 100.0 / END 标记？）
- [ ] 多页行为（PAGE_COUNT > 1 时流怎么发）
- [ ] 错误行为（BOOK_NO 空 / PGM 错 / Token 过期的流返回）
- [ ] 端点大小写 GetReportStream（历史教训：以 Postman 实测为准）
- [ ] BOOK_NO 账簿清单来源 API

**Round 43 续：curl 实测结果（用户提供 Postman 请求后本机验证）**

用户 Postman 请求要点：URL `http://localhost/SUNFUSION/api/accGeneralLedger/GetReportStream`（**/api/ 小写**）、Authorization **不带 Bearer 前缀**、BOOK_NO=00000000、YEARS=2026、IPERIOD=1。curl 实测结论：

- **流格式**：SSE `text/event-stream`，每条消息 = 单行 `data: {JSON}` + 空行分隔；消息固定 6 字段 `{CODE, PERCENT, TITLE, ERR, DATA, IS_RES_ID}`
- **消息序列**（9 条）：0.0(CON_ID:SYNC_xxx) → 10.0 → 30.0 → 40.0(查询本期数据) → 50.0(查询期初数据) → 70.0 → 90.0(空) → 90.0(REPORT__TAB + PAGE_COUNT=1) → **100.0**(PAGE_COUNT=0) 流结束
- **关键修正**：原推断 `data: PERCENT: 90.0` 前缀行**错了**——PERCENT 是消息 JSON 里的字段。幸好按铁律实测（文档只给提取后 JSON 示例的教训）
- **BOOK_NO 空** → HTTP **406** + 空 body（Content-Type 仍是 event-stream）→ 前端必须非空拦截
- **PGM 写错** → **该端点不校验 PGM**，照样正常返回（与 getReport 报 SQL 错误不同！）
- **Token 无效** → HTTP 200 + `application/json` `{"code":20004,...}`（与 getReport 一致）；**无 Authorization** → HTTP 200 + 空 body 永久挂起（靠 120s 超时兜底）
- **判别方式**：res.ok + Content-Type 头（application/json = 错误 JSON / event-stream = 流），不要 JSON.parse 流首行去猜
- **大小写**：`/api/`、`/API/`、`GetReportStream`、`getReportStream` 四组合全通（IIS 大小写不敏感）
- **Bearer 前缀**：带不带都通 → 前端沿用现有 Bearer 格式即可
- **CODE 语义**：全程 =1（含 100.0 完成消息）→ 前端不依赖；ERR 空串 = 正常，非空即提示；TITLE 混合 i18n key（服务器端拼写 perncent）与中文直文 → 原样显示不翻译
- 文档已同步：API服务调用说明文档.md 第九节改为实测版（消息序列表/三步判别/实测代码）/ 项目经验总结.md 3.5 实测修正 / 进度追踪表阶段 20 更新。账簿清单来源 API 仍待确认

## 2026-08-18 Round 44：账簿列表 API（AccBook/GetList）实测 + 账簿下拉设计规则

**用户**：@账簿列表查询.md 可以把账簿清单 List 出来。建议登录一开始先把 BOOK_NO 拿到，在【总分类账】条件提供下拉框选择账簿；LIST > 1 预选第一个；LIST = 0 时打开总分类账不进入 GetReportStream 调用，弹警告「你的账套没有启用总账，所以你不能操作这个查询」；**且警告之后该报表的【查询】【转入】按钮都置禁用**。明白就先把相关要改的文件更新。（范围确认：本轮只更新文档）

**Claude**：（curl 实测 + 文档更新）

**实测结论**：
- `POST /AccBook/GetList`：code===0 标准信封；数据路径 `data.ACC_BOOK_BS`（⚠️ 不是 REPORT__TAB）+ COLUMN_INFO 34 字段 + PAGE_INFO（CURRENT_PAGE 从 1 开始）+ HEADER_TABLE=ACC_BOOK_BS
- 端点 `/api/`、`/API/`、`GetList`、`getList` 变体全 200；Bearer 带不带都通
- 本机账套仅 1 条账簿：BOOK_NO=00000000 / NAME=First Department / BOOK_XZ=1（与 GetReportStream 请求 BOOK_NO 一致）
- 请求体：DEP=00000000 + SEARCH_INFO 4 元素（展示配置/fixCondition {}/BOOK_NO in/NAME contain，value 均 ""）+ PAGE_INFO {200, 1}

**设计规则（入档，实施轮照此执行）**：
1. 登录成功（app.js:501）与会话恢复（app.js:647）两处拉取账簿清单，不阻塞登录
2. 新建 BookStore（IIFE + LS_KEY sunlike_books + 内存缓存，按公司隔离）
3. 总分类账账簿下拉：显示 BOOK_NO + NAME；>1 预选第一个
4. 0 账簿 → openReport/doQuery 拦截：弹警告「你的账套没有启用总账，所以你不能操作这个查询」→ **【查询】【转入】按钮置禁用**，不调流、不切换报表；切换其他报表恢复可用。⚠️ Dialog 无 alert 方法，实施时需新增 Dialog.alert()
5. 拉取失败 ≠ 0 账簿：重试一次，仍失败 toast 报错；只有 code===0 且列表为空才算「未启用总账」
6. 动态下拉实施锚点：index.html + ui-template.html 新增 select#filterBookNo；reports.js updateFilterPanel allGroups/layoutMap、readFilters、filterId（BOOK_NO 已被 fieldToKey 映射到 filterMrpNo，须用 filterId 避开）；参考 accabgt 配置；i18n 补三语 + extract-i18n-keys.js 审计

**完成事项**：
- API服务调用说明文档.md：新增第十节「账簿列表查询 API」（概述/请求体/前端设计规则含实施锚点）+ 9.2/9.4 账簿来源补全 + 第八节 Checklist +3 项 + 头部日期
- 项目经验总结.md：3.5 BOOK_NO 条补来源与下拉/0账簿设计规则
- 进度追踪表.md：阶段 20 已完成/待实施更新
- 本轮无代码改动；需求架构文档/流程图留到实施轮

## 2026-08-18 Round 45：【长连接】总分类账实施轮 — 流式查询 + 账簿下拉 + 0 账簿拦截

**用户**：「那就开始写代码，写完我看看对不对」→ 中途纠正「涉及界面要先改 ui-template 我看效果」→ 原型确认后补充规则：侧边栏**新增独立分组【总账报表】排在最上面**，总分类账挂其下 → 「开工吧」写正式代码。

**流程**：① 先只改 ui-template.html 原型（账簿下拉 + 流式进度条 + 演示按钮）供浏览器确认 → ② 用户「开工吧」后才动正式代码 → ③ 全链路接线 + i18n 三语 + 部署包 v1.4 + 文档四件套。

**实施清单（全部完成）**：
- **js/book-store.js（新建）**：BookStore IIFE，LS_KEY `sunlike_books`，内存缓存 + localStorage 按 compno 隔离；`fetchBooks()` 调 `AccBook/GetList`（DEP 00000000 + 4 元素 SEARCH_INFO + PAGE_INFO 200/1，读 `data.ACC_BOOK_BS`）；`ensureLoaded()` 缓存命中直返 / 失败重试一次；`prefetch()` 登录与会话恢复两处后台静默预热
- **js/api.js**：新增 `fetchStreamReport(path, body, callbacks)` — POST + fetch + ReadableStream + TextDecoder 行缓冲（跨 chunk `buffer.split('\n')` + `lines.pop()`），AbortController 120s 超时；三步判别：res.ok（406）→ Content-Type application/json（错误信封 + Auth.handleAuthError）→ text/event-stream 逐行 `data: {JSON}`；PERCENT 用 `msg.PERCENT || 0`（0.0 首消息不能吞）；100.0 结束消息实测存在；超时文案走 I18n.t
- **js/dialog.js**：新增 `Dialog.alert(message) → Promise<void>`（仿 confirm，单确定按钮 + Enter/Esc/backdrop 关闭）
- **js/reports.js**：新配置 `accgl`（group 总账报表 / apiMethod getReportStream / apiPath api/accGeneralLedger/GetReportStream / pgm ACCRPTGL / needsBook / filterId filterBookNo 避开 fieldToKey BOOK_NO→filterMrpNo 冲突）；`buildRequestStream` 由会计期间 YYYY-MM 派生 ACC_IPERIOD_B/E、YEARS/IPERIOD、START_DD、DATE_B/E 等 25 字段 fixCondition；query() 流分支优先（onProgress 回传）；金额列 AMTN_*/AMT_*/UP_* 计入 DECIMAL/CURRENCY_FIELDS
- **js/report-menu.js**：MENU_GROUPS 首位插入 `总账报表`
- **css/dashboard.css + index.html + ui-template.html**：账簿下拉 select#filterBookNo（加载中...占位）+ 表格 loading 区流式进度条（.stream-progress fill/text，min(340px, 86%)）；模板清掉 DEMO 进度 CSS（迁入正式样式表）；版本号 ?v=20260818a；引入 book-store.js（datasource-store.js 之后）
- **js/app.js**：`_ledgerBlocked` 标志 + `setLedgerButtonsDisabled`（#queryBtn/#transferBtn）；`populateBookSelect`（BOOK_NO · NAME，保持已选值否则预选第一个）；openReport 拆 `_openReportCore`——needsBook 先 `ensureLoaded`：失败 → toast「账簿清单加载失败: {0}」不切换；0 账簿 → `blockLedgerReport()` + `Dialog.alert(「你的账套没有启用总账，所以你不能操作这个查询」)` 不调流不切换；切换其他报表自动 `unblockLedgerReport()`；doQuery 顶部 `_ledgerBlocked` 兜底 + BOOK_NO 空 toast「请选择账簿」；`resetStreamProgress`/`onStreamProgress` 驱动进度条；登录成功（~501）与会话恢复（~647）两处 `BookStore.prefetch()`；重置按钮：账簿回第一项、会计期间回当前月（仅 accgl）
- **js/i18n-data.js**：zh-tw/en 各 +15 key（总账报表/总分类账/账簿/会计期间/请选择账簿/0账簿警告/账簿清单加载失败/查询超时/借贷方向/摘要类型/借方金额/贷方金额/加载中.../2 个模板预览 key）；extract-i18n-keys.js 审计 **0 缺失**（29 僵尸含 2 个模板专属 key，工具只扫 index.html 属正常）
- **deploy.ps1**：v1.4，jsFiles 加 book-store.js，Contents 30 文件（1 html + 4 css + 25 js）；产出 sunlike-erp-report-v1.4.zip（120.2 KB）
- 7 个改动 JS 全部 `node --check` 通过

**决策记录**：
- 原型先行：用户中途纠正后确认 CLAUDE.md 第八条「UI 先模板后代码」必须覆盖生产 UI 的所有变更（含新控件）
- 0 账簿拦截点放 openReport（菜单唯一收口）+ doQuery 顶部兜底双保险；按钮禁用/恢复随切换自动处理
- 转入按钮走 ReportEngine.query 无 onProgress——用户转 AI 时进度条在查询 Tab 的 tableLoading 区，接受现状
- 失败 ≠ 0 账簿：仅 code===0 且 ACC_BOOK_BS 空才弹「未启用总账」
- 会计期间默认当前月：index.html 硬编码 2025-07 是 mrppu 旧演示值，总分类账不能用——updateFilterPanel accgl 分支打开即置当前月（模板 showLedgerPreview 同步此规则）；此改动在首版打包后补入，已重打 v1.4

**浏览器实测 Bug 修复（同轮）**：
- **用户实测**：点总分类账查询 → 「查询失败: HTTP 404」
- **根因**：config `apiPath: 'api/accGeneralLedger/GetReportStream'` —— fetchStreamReport 拼 `getBaseUrl()`（已含 `/SUNFUSION/API`）→ 实际请求 `/SUNFUSION/API/api/accGeneralLedger/GetReportStream` 多了一段 → 404。文档 9.1 端点行写得清楚：相对路径 `accGeneralLedger/GetReportStream`，URL 里的 `api` 段就是 `/SUNFUSION/API` 本身
- **教训（第二次同类错误）**：对话记录 Round 早期（对话记录.md:59）getReport 也踩过「文档写 `/api/{module}`，Base 已含 `/API`，拼成 `/API/api/{module}` 多一段」——长连接端点再次按原文档 URL 字面复制前缀。**根治法：所有新端点只认文档里「相对 /SUNFUSION/API 的路径」列，原文档 URL 前缀一律与 Base 比对后再落代码；跨轮经验已在 9.1 端点行 + api.js JSDoc 双重加警告**
- **修复**：apiPath → `accGeneralLedger/GetReportStream`；api.js JSDoc 示例 + 需求架构 4.10 同步；API服务调用说明文档 9.1 加警告行；index.html / ui-template.html reports.js 版本号 bump → ?v=20260818b（浏览器缓存旧文件必须换版本）；重打 sunlike-erp-report-v1.4.zip（120.6 KB）

**浏览器实测 Bug 修复②（同轮）：退出重登后账簿下拉残留旧账套账簿**
- **用户实测**：退出登录 → 再登录，账簿下拉可选的还是上一次登录抓到的账簿，没有更新成这次登录应有的账簿
- **根因（两层）**：① 退出登录不清 DOM 与状态——login success 只跑裸 doQuery()，不重跑 openReport → 下拉 select 里的旧选项原样保留（哪怕 BookStore 已抓到新公司的账簿也没人填进下拉）；② BookStore 内存缓存跨登出存活（同 compno 重登命中旧缓存），且登出时进行中的拉取可能晚到回写
- **修复**：
  - book-store.js：新增 `reset()`（世代计数 `_gen`+1 作废进行中拉取、清内存缓存、清 _pending）；fetchBooks 捕获世代，旧响应不回写缓存；ensureLoaded 的 pending 清理改为「只清自己的句柄」（reset 后新 pending 不受影响）；prefetch 改走 ensureLoaded（与 openReport 并发去重共享同一拉取）
  - app.js：新增 `resetLedgerBooksState()`（BookStore.reset + 下拉回「加载中...」占位 + unblockLedgerReport），挂到退出登录与 auth:expired 两处；登录成功由「renderCurrentTableHead + doQuery」改为 `openReport(AppState.currentReport)` 全链路（needsBook → ensureLoaded → populateBookSelect → _openReportCore）
  - 版本号 bump：book-store.js / app.js ?v=20260818b（index.html 与 ui-template.html 同步）；重打 v1.4 包（121.1 KB）
- **设计规则入档**：API服务调用说明文档 10.3 新增第 8 条「登出重置」（登出清缓存/下拉占位/解除阻断 + 登录走 openReport 全链路）

**遗留**：用户浏览器测试生产版未做（用户称「写完我看看对不对」）；根目录 2 个 0 字节杂散文件（`1`、乱码名）未删（待用户确认）

**浏览器实测 Bug 修复③（同轮）：流式进度条位置太下面、缩放比太大看不到**
- **用户实测**：进度条显示位置太靠下（在加载层底部、340px 大块），缩放比太大，根本看不到
- **第一次尝试（细条贴表格区顶部）**：改成表格区顶部 4px 单行细条 + 右侧小字——用户看后否决：「感觉上就像卡住了，还是之前那个好，只要解决显示位置就行了，找到 Windows center 不就好吗」
- **最终方案（用户确认）**：恢复原来有厚度的进度条样式（8px 条 + 下方阶段文字），只改位置——`position: fixed; top:50%; left:50%; transform: translate(-50%,-50%); z-index:1000`，白色卡片（surface 底 + border + shadow-lg + radius-md）悬浮在**视口正中央**，不随表格滚动、不看滚动位置，查询中永远在屏幕中间可见
- **同步**：ui-template.html 模板先行（用户浏览器确认「可以」）→ index.html 同结构（streamProgress 移出 .table-loading 成 .table-wrapper 首个子元素，兄弟化后收起加载层不再连带隐藏进度条）；dashboard.css 版本号 → ?v=20260818c（两处 HTML 同步）；demo 脚本 ids 不变无需改；重打 sunlike-erp-report-v1.4.zip（121.4 KB）

## 2026-08-18 Round 46：REM_TYPE 摘要类型语义映射（表格 badge + 转入数据源）

**用户提供语义**：GetReportStream 返回的 REM_TYPE 数值语义——`1` = 期初余额，`2` = 本期合计，`3` = 本年合计。要求：① 写进 API服务调用说明文档；② 调整报表表格展示效果；③ 调整转入数据源摘要类型栏位的资料。

**完成事项**：
- **API服务调用说明文档.md**：9.1 字段语义新增 REM_TYPE 行（1/2/3 语义 + 表格与转入数据源均映射、非 1/2/3 原样显示）；第八节 Checklist 新增 REM_TYPE 映射条目；头部最后更新备注
- **模板先行（用户确认「效果我很满意」）**：ui-template.html【预览：总分类账】新增演示数据表渲染（10 列含摘要类型），摘要类型列 1/2/3 → 彩色徽章——1 蓝色 badge-info（期初余额）/ 2 黄色 badge-warning（本期合计）/ 3 绿色 badge-success（本年合计）；dashboard.css 新增 .badge-info（蓝，与 success/warning 同族）；i18n-data 三语 key 期初余额/本期合计/本年合计（zh-tw：期初餘額/本期合計/本年合計；en：Opening Balance / Current Period Total / Year-to-Date Total）
- **生产代码**：
  - reports.js：`REM_TYPE_LABELS` 映射表 + `getRemTypeBadge()`（仿 getStatusBadge）+ renderTableBody REM_TYPE badge 分支 + formatCellValue REM_TYPE 映射（非 1/2/3 原样显示）
  - app.js 转入链路：query 结果行 REM_TYPE 映射成语义文字后再存 DataSourceStore（AI 系统提示里读到的是「期初余额」而不是「1」）；行复制为浅拷贝不污染原数据
- **版本号 bump**：index.html / ui-template.html 两处同步——dashboard.css → ?v=20260818d、i18n-data.js → ?v=20260818b、reports.js → ?v=20260818c、app.js → ?v=20260818c
- **校验**：reports.js / app.js node --check 通过；重打 sunlike-erp-report-v1.4.zip（121.9 KB，30 文件）
- **文档同步**：需求架构文档 4.10 新增「摘要类型映射」条目 + 进度条 UI 条目更新为最终 fixed 视口中央形态；进度追踪表阶段 20 追加

**决策记录**：
- 映射点双落：表格走 formatCellValue/badge（渲染层），转入走存储层（数据本身替换文字）——AI 拿到的就是语义文字，不依赖 AI 理解 1/2/3 编码
- 转入映射为语言快照（转入时的语言），与 filterSummary 快照策略一致，不随语言切换变化
- badge 三色沿用系统既有 badge 族（success/warning 复用，新增 info），不引入新组件

**收尾（用户指示：更新文档 + README + 安装包 + commit/push）**：
- README.md 全面更新至 v1.4：32 报表（8 分组含总账报表置顶）、25 模块化 JS（+book-store.js）、架构图端点表 + /accGeneralLedger/GetReportStream、新增「长连接报表（总分类账）」功能小节、报表速查表加总账报表行（含 REM_TYPE 语义）、deploy.ps1 v1.4 30 文件、i18n 620+ key、项目状态表加 API4 行、项目结构索引加 API4/账簿列表查询两份原始文档、流程图.md 15 节
- 安装包 sunlike-erp-report-v1.4.zip（121.9 KB，30 文件）已制作并入库（v1.0~v1.4 zip 均 git 跟踪）
- git commit 6b05bf3 → push origin master 成功（21 files changed, +2110/-52；新增 book-store.js / v1.4.zip / 标准报表制表API4.md / 账簿列表查询.md）
- 根目录 2 个 0 字节杂散文件（`1`、乱码名）仍保持未跟踪未删除（待用户确认）

## 2026-08-19 Round 47：垃圾文件直接清理纪律入档

**用户指示**：此前遗留汇报的根目录 2 个 0 字节杂散文件（`1`、乱码名）就是垃圾——以后这种不用遗留、不用犹豫，直接处理；纪律写入 CLAUDE.md。

**完成事项**：
- 当场删除 2 个 0 字节杂散文件（`1`、乱码名文件），工作区已干净
- CLAUDE.md 九、编码纪律新增第 4 条：**垃圾文件直接清理**——0 字节杂散文件/乱码名文件/临时文件等垃圾，当场删除，不留待办、不汇报"遗留待确认"、不等用户批准（2026-08-19 用户指定）

## 2026-08-19 Round 48：转入数据源 0 笔拒绝

**用户指示**：查询结果是 0 笔时，【查转入数据源】不应该能转数据——转过去 0 笔的数据源没有意义。此情况应拒绝执行并告知「查无数据，所以无法为您进行转入数据源」。

**完成事项**：
- **app.js initTransferBtn 双层拦截**：
  - 前置快查：`AppState.lastQueryData` 最近一次查询结果为空，且 reportKey 与筛选条件（JSON 快照比对）都没变 → 直接拒绝——不发请求、不切 Tab、不建占位数据源、即时 toast
  - 查询后兜底：重新拉取后仍是 0 笔 → `DataSourceStore.remove` 撤销占位数据源 + 列表重渲染 + toast 告知 + `Tabs.switchTab('tabQuery')` 切回查询页
- **lastQueryData 快照扩充**：doQuery 写入时新增 `reportKey` + `filters`（readFilters 固定 19 key 扁平结构，JSON.stringify 比对安全）
- **i18n 三语**：新 key「查无数据，所以无法为您进行转入数据源」——zh-tw：查無資料，所以無法為您進行轉入資料來源；en：No data found, so it cannot be transferred to the data source.；toast 用 warning 类型
- **版本号 bump**：index.html / ui-template.html 同步——i18n-data.js、app.js → ?v=20260819a
- **边界自检**（模块完成三问）：从未查询过（lastQueryData null）→ 跳过前置走兜底；改筛选未重查 → 快照不一致走兜底；切报表 → reportKey 不一致走兜底；查询失败 → 原 catch 占位 error 状态不变；总分类账 0 账簿 → 按钮已禁用、点击事件不触发
- **文档同步**：需求架构文档 4.3 新增「0 笔拒绝」条目；流程图 七/八节加 0 笔判断分支；进度表阶段 20 追加 + 待验证项；README 工作流程第 1 步补充
- **校验**：app.js / i18n-data.js node --check 通过

## 2026-08-19 Round 49：【推送代码】命令惯例入档

**用户指示**：CLAUDE.md 记录一笔——当用户说【推送代码】，就依次执行：① 更新对话记录.md；② 同步进度追踪表、流程图、需求架构文档、README 等相关文件；③ 重新打包部署安装包；④ commit + push 源代码到 GitHub。

**完成事项**：CLAUDE.md 头部命令惯例区（【要开新对话】旁边）新增【推送代码】一条，四步流程原文入档。

## 2026-08-19 Round 50：【推送代码】首次执行（Round 48/49 变更入库）

**完成事项**：
- 文档核对：Round 48 已同步的四件套 + Round 49 CLAUDE.md 惯例，均已在工作区，无遗漏
- 新文件入库：根目录发现 `标准报表制表API5.md`（100 KB，用户新提供）——8 个报表原始文档（科目余额表 accBalanceTable / 资产负债表·利润表·现金流量表 accRptPreview / 物料分析明细 mrpcu / 在制成本明细 mrpct / 在制原料明细 mrpcx / 直接原料明细 mrpce，全部 GetReportStream 长连接）；CLAUDE.md 十、README 项目结构索引同步补上（CLAUDE.md 索引此前缺 API4/账簿列表查询，一并补齐）
- 安装包：v1.4 已在 Round 48 重建（122.4 KB），本轮无代码再变更
- commit + push origin master 成功

## 2026-08-19 Round 51：API5 八只长连接报表接入（分析 + PoC 实测）

**背景**：用户提供标准报表制表API5.md（8 只新报表：总账 4 只依赖账簿 + 生产制造 4 只，全部 SSE 长连接）。计划六阶段（PoC → 基础设施 → config → 渲染 → 转入 → 文档），用户已批。

**完成事项**：
- **逐一分析 8 只报表**：端点/PGM/SEARCH_INFO 形状/fixCondition/响应行/PERCENT 落点已逐报表提取（API5 文档 2684 行全文分析 + 探索代理核实代码链路）
- **设计决策两轮对齐**：STAT_GROUP 原样传；财务报表层级缩进+加粗+动态列（前导列 ITEM_NO+ITEM_NAME）；科目余额表仅账簿+期间；MRPCE 汇总行加粗且转入剔除；五阶段中文列名用户提供（QC 期初在制/TR 本期投入/WG 本期完工/DQ 本期对冲/QM 期末在制/HY 本期耗用）；生产 3 只日期可编辑+预填
- **PoC 实测 14 项**（curl 流式实测）：
  - 上午 6 端点全 404 → 用户部署 ERP 后端后全 200
  - BOOK_NO=00000000 真实账簿可用（科目余额表出真数据：库存现金/银行存款…）
  - ⚠️ BOOK_NO 空 = 200 + SSE ERR「账簿不能为空」，**不是 API4 的 406**
  - ⚠️ 本服务器不返回 REM_TYPE（显式请求也没有）→ 扁平行
  - ❌ 财务报表 3 样式 STD001/2/3 在 ERP 不存在 →「报表样式[STD001]不存在或无访问权限」，需 ERP 侧建立/授权（已告知用户）
  - ✅ 制造 4 只端点正常；本服务器无制造数据（MRPCT 空结束 CODE=12；MRPCU 空条件 0.3s）
  - ✅ STAT_GROUP 缺省不报错（按决策仍原样传）；YYMM range 任意值不报错
  - ⚠️ COLUMN_INFO 制造报表空数据时是 {} 对象 → 前端须 Array.isArray 防护
  - ❌ PAGE_COUNT 不可靠（有数据为 0 / 无数据为 1）→ 一律客户端计算
  - ⚠️ 登录端点当日 code 10002 REGCHECK_ERR（旧 token 仍可用，环境性现象备查）
- **API服务调用说明文档.md 第十一节入库**（用户最重要要求）：11.1 概述（8 端点速查 + CODE 语义 + 实测 PERCENT 序列）/11.2 八报表对照速查表/11.3 实测 14 项/11.4 与 getReport·API4 差异/11.5 坑清单 13 条

**决策**：财务报表 3 只前端按文档实现（样式就绪即可用）；制造 4 只服务器无数据，行格式以文档响应样例为准，待数据后回归。

**代码实施（同轮完成）**：
- **基础设施**：`buildRequestStream` 配置驱动化（`cfg.stream` fixCondition @占位符模板/elements/topFields/statGroup/showLadder/orderBy/datePreset + `buildStreamCtx` 派生 @PERIOD/@YEARS/@IPERIOD/@DATE_B-E/@START_DD/@MONTH_FIRST/@MONTH_LAST/@YEAR_FIRST/@TODAY，期间非法兜底当前月）；accgl 29 键 fixCondition 与旧硬编码逐字一致（零行为变化）；query() 流分支收集 COLUMN_INFO（Array.isArray 防护）；doQuery needsBook 收窄（生产 4 只不再拦截账簿）+ 成功路径先重渲染表头再分页
- **8 config 接入**：accBalTable（25 字段 fixCondition 含 REL_CLS_B/E Int32）/accZcfzb/accLrb（DISPLAY_FIELDS 前导逗号逐字保留 + requestFields _1 后缀）/accXjllb（topFields RPT_NO/TYPE_NO）/mrpcu（工单号+母件代号筛选，QTY=0 照传）/mrpct（YYMM 两端同值 + statGroup 3 组）/mrpcx（operator this_month + statGroup 3 组）/mrpce（showLadder=T + statGroup 2 组 + 年初~今天预填）；筛选面板 4 新布局 + 标签分支（工单号/母件代号/生产货品/材料代号）+ 日期预填；菜单 MENU_ORDER 排序
- **渲染特例**：财务报表动态列（renderColumns:'columnInfo' → 前导列 ITEM_NO/ITEM_NAME + COLUMN_INFO 映射）；SPACES 层级缩进 16px/级 + .row-level1；_SKIP_STAT='T' → .row-total；DECIMAL/CURRENCY_FIELDS + COLUMN_LABELS 扩充（科目余额 8 + 财务 7 + MRPCU 11 + 五阶段 46 + MRPCE 5）；转入数据源 _SKIP_STAT 剔除 + 筛选摘要账簿/期间
- **模板先行**：ui-template.html 新增【预览：财务报表层级】【预览：MRPCE 汇总行】演示块（动态列表头/缩进/加粗/汇总行剔除说明）
- **i18n**：zh-tw + en 各 ~90 key（8 报表名/筛选标签/动态列标题/五阶段/演示文案）；全部改动 JS node --check 通过
- **部署**：?v= bump 20260819b（i18n-data/reports/report-menu/datasource-list/app/dashboard.css 两处入口）+ deploy.ps1 v1.5
- **文档四件套**：需求架构文档（40 报表清单 + 两种模式表加 getReportStream + 4.11 API5 模块）/进度追踪表（阶段 21）/流程图（K32~K40 + 十五节推广 + 15.3 渲染特例）/README（40 报表 + 长连接章节推广 + 里程碑）

**遗留待办**：① 财务报表 STD001/2/3 样式需 ERP 侧建立/授权（已告知用户）；② 制造 4 只待 ERP 数据后回归（五阶段/成本构成列语义、MRPCU 列标签校核）；③ 浏览器实测 checklist（进度表阶段 21 待验证清单）。

## 2026-08-19 Round 52：三财务报表【报表样式】下拉（RPT_NO 动态化 + TYPE_NO 取自科目表）

**背景**：Round 51 实测三财务报表报「报表样式[STD001]不存在或无访问权限」；用户 ERP 已在科目表下建立 4 个样式（STD001 资产负债表月结/STD002 利润表/STD003 现金流量表/STD004 资产负债表年结）。用户要求：三报表查询条件增加【报表样式】下拉（传 RPT_NO），先通过 报表样式 列表查询.md 获得样式清单；调用样式清单又需先通过 科目表 列表查询.md 获得 TYPE_NO——两者都不能写死。

**两轮问答澄清（用户拍板的设计）**：
- 下拉选的是【报表样式】（accRptStyle/getlist 清单），不是报表名称；TYPE_NO 必须经 AccType/getlist 查出，不能写死
- 按报表类型过滤（RPT_TYPE：2=资产负债表/3=利润表/4=现金流量表）；科目表静默获取（无 UI）；预选匹配类型的第一个样式

**PoC 实测（三步验证法）**：
- AccType/getlist → code=0，科目表 TYPE_NO 真实值 "1"（⚠️ 文档示例 "01"）
- accRptStyle/getlist(TYPE_NO=1) → 4 个样式（RPT_NO/RPT_TYPE/NAME/TYPE_NO/TYPE_NAME）
- 三报表用写死 TYPE_NO="3" → 立即「报表样式[STD001]不存在或无访问权限」；改用 "1" + STD001 + BOOK_NO 00000000 → ✅ 67 行完整数据——实证「不能写死」
- PowerShell 5.1 传 JSON 给 curl.exe 吞引号 → 请求体写文件 + `--data-binary "@file"` 解决

**模板先行**：ui-template.html 新增【预览：财务报表查询条件】块（账簿 + 样式下拉 + 会计期间；样式下拉按 RPT_TYPE==='2' 过滤、选项 "RPT_NO · NAME"、预选第一项）——用户浏览器确认「看到了，可以！」

**代码实施**：
- **新模块 js/rpt-style-store.js**（照抄 book-store.js 模式）：fetchAccTypes（AccType/getlist → data.ACC_TYPE_BS）→ fetchStyles(typeNo)（accRptStyle/getlist，顶层 PGM:"ACCRPTSTYLE" + TYPE_NO → data.MF_RPTSTYLE_BS）→ fetchAll 两段式（逐科目表查样式，按 RPT_NO 去重排序）→ ensureLoaded（内存 + localStorage `sunlike_rpt_styles` 按 compno 缓存/并发去重/失败重试一次/世代作废）+ getStyles/getStyle/getCachedStyles/prefetch/reset
- **index.html**：filter-group-rptstyle 下拉（「加载中...」占位）+ rpt-style-store.js 引入 + ?v=20260819c（i18n-data/reports/datasource-list/app）
- **reports.js**：新布局 `accglStyle`（bookNo+rptStyle+dateCst，不动共用 accgl）；三 config filterLayout 改 accglStyle + needsRptStyle:true + rptTypeFilter '2'/'3'/'4'；fixCondition 与 topFields 的 RPT_NO/TYPE_NO 改 @RPT_NO/@TYPE_NO；**topFields 注入改走 ph()**（原实现不替换的坑）；buildStreamCtx 加 RPT_NO/TYPE_NO；readFilters 加 filterRptNo；dateCst「会计期间」标签分支扩为含 accglStyle
- **app.js**：doQuery 加 RPT_NO 拦截（未选 → toast 请选择报表样式）+ styleTypeNo 注入（getStyle(filterRptNo).TYPE_NO，多科目表也正确）；populateRptStyleSelect（按 rptTypeFilter 过滤、选项 "RPT_NO · NAME"、预选第一个；0 匹配 → 「无可用样式」占位）；openReport needsBook 成功后 ensureLoaded 样式 → populate → _openReportCore（失败 toast 报表样式加载失败 不切换）；重置按钮样式回第一项 + 会计期间按 filterLayout（accgl/accglStyle）回当前月（旁路修复：原只认 reportKey==='accgl'）；resetLedgerBooksState 加 RptStyleStore.reset + 下拉回占位；登录成功/会话恢复双点 RptStyleStore.prefetch()
- **datasource-list.js**：筛选摘要加「报表样式=选中 option 文本（RPT_NO · NAME）」
- **i18n-data.js**：新 key ×4 三语（报表样式/请选择报表样式/报表样式加载失败: {0}/无可用样式）
- **deploy.ps1** 文件清单加 rpt-style-store.js；**垃圾文件** cfg.needsBook、1（均 0 字节）当场清理
- **校验**：5 个改动 JS node --check 全通过；交叉 grep 确认 STD00 残留均为合法列名映射、accgl 布局残留为总分类账/科目余额表（不动）

**边界自检（模块完成三问）**：0 科目表/0 样式/该类型无样式 → 下拉空占位 + doQuery 拦截；样式接口失败重试一次 → toast 不切换报表；离线内存缓存可用；登出世代作废防串账套；重置/三报表互切（重过滤下拉）/登出重登全路径覆盖

**待验证（浏览器实测）**：登录 → 资产负债表（下拉 2 个类型 2 样式、预选 STD001）→ 查询出数据（无样式错误）→ 利润表 STD002/现金流量表 STD003 预选 → 换样式查询 RPT_NO 生效 → 转入数据源摘要含报表样式 → 登出重登缓存正常

### Round 52 链路修正（同轮，用户纠正）

**用户纠正**：TYPE_NO 的来源我说错了。正确链路是 **先通过账簿查询 /api/AccBook/GetList 获得 TYPE_NO**，拿 TYPE_NO 去 /api/accRptStyle/getlist 获得报表样式清单；**账簿一旦改变，报表样式清单就得重新获取**；至于 /api/AccType/getlist 变得不重要，只有需要拿 TYPE_NAME 时才需要去查。

**实测验证**（三步验证法）：curl 实测 AccBook/GetList 响应行直接带 `TYPE_NO: "1"`、`TYPE_NAME: "企业会计准则-First Department"`——确认账簿行自带 TYPE_NO，无需 AccType/getlist。请求体 displayFields 本就含 TYPE_NO/TYPE_NAME（book-store.js 原样请求），前端零请求变更。

**返工实施**：
- **rpt-style-store.js 重写**：删除 fetchAccTypes 与两段式 fetchAll；缓存键改为 compno+BOOK_NO（`{ [compno]: { [bookNo]: {styles,...} } }`），ensureLoaded(bookNo, typeNo) 按账簿加载；_pending 按账簿去重；getStyles(bookNo)/getStyle(bookNo, rptNo)/getCachedStyles(bookNo)；删除 prefetch（样式依赖账簿选中值，登录时无从预取）
- **app.js**：populateBookSelect 的 option 写 `data-type-no`（账簿行 TYPE_NO）；新增 loadRptStylesForBook(bookNo, onOk, onFail)（读选中 option 的 TYPE_NO → ensureLoaded → populateRptStyleSelect；TYPE_NO 空 → toast「账簿无科目表代号」）；openReport 改为 loadRptStylesForBook 就绪后才 _openReportCore；新增账簿下拉 change 监听（样式下拉先回「加载中...」→ 按新账簿重取，失败回「无可用样式」）；doQuery 注入改 getStyle(filterBookNo, filterRptNo)；重置流程改为样式就绪/失败后再 doQuery；删除登录/会话恢复两处 RptStyleStore.prefetch
- **i18n**：新 key「账簿无科目表代号」三语；模板演示文案 key 补入字典（上轮遗漏）
- **ui-template.html**：演示块注释/文案改为「TYPE_NO 取自账簿行」链路
- **校验**：node --check 全过；grep 确认 app.js 无旧 API 调用残留
- **文档同步**：需求架构文档 4.12 改写为账簿行链路版；流程图 16.1 换新 mermaid（账簿行 TYPE_NO → ensureLoaded(bookNo,typeNo) → populate → doQuery + 账簿切换重取虚线分支）；API服务调用说明文档 第十二节改为 AccBook/GetList + accRptStyle/getlist 两端点（实测账簿行 TYPE_NO="1" + 坑清单「账簿改变样式必须重取」）；进度追踪表 22 概述/待验证改账簿版；README 报表样式下拉 bullet 链路改正；CLAUDE.md 索引「科目表 列表查询.md」描述改为「需要 TYPE_NAME 时才查」

### Round 52 时序修正（同轮，用户实测反馈）

**用户实测反馈**：① 第一次打开资产负债表，样式下拉一直显示【加载中...】不结束；② 切到利润表，利润表的报表样式下拉显示的却是本应在资产负债表看到的样式。用户判断「调用报表样式的 API 那个点不对」。

**定位（两个症状同一病根）**：原实现「先拉样式就绪后，才 `_openReportCore` 打开报表」，且 populate 读 `AppState.currentReport`：
- 症状①：populate 执行时 currentReport 还是旧报表（无 rptTypeFilter → populate 直接 return，什么都没填）；随后 `_openReportCore` 里 updateFilterPanel 切显隐，下拉停在初始「加载中...」占位 → 再无人填充 → 卡死
- 症状②：切到利润表时账簿缓存已命中，populate 先于 `_openReportCore('accLrb')` 执行，读到的 currentReport 仍是 accZcfzb → 用资产负债表过滤（RPT_TYPE=2）填了下拉；随后打开利润表布局相同不重建 DOM → 类型 2 样式留在利润表下拉

**修复（时序颠倒 + reportKey 显式传递）**：
- `openReport` needsRptStyle 分支改为：下拉置「加载中...」占位 → **先 `_openReportCore(reportKey, skipAutoQuery=true)` 渲染面板** → 再异步 `loadRptStylesForBook(reportKey, bookNo)` → 样式就绪后自动 doQuery；失败回调把下拉置「无可用样式」（报表已打开，不再「不切换报表」）
- `_openReportCore` 加 `skipAutoQuery` 参数（样式未就绪前 doQuery 会被「请选择报表样式」拦截，所以先跳过、就绪后补查）
- `loadRptStylesForBook` 加 reportKey 首参：populate 用传入的 reportKey 做类型过滤，**不再读 AppState.currentReport**（异步完成时用户可能已切报表——正是症状②根因）
- 账簿 change 监听、重置流程两处调用点同步改传当前报表 key
- 版本号 bump 20260819d；node --check 通过

**待浏览器实测**：打开资产负债表（下拉短暂「加载中」后填 STD001/STD004、自动查询出数据）→ 切利润表（STD002/STD003，过滤正确）→ 切现金流量表 → 三表互切不再串样式；账簿切换重取；重置按钮流程。

**用户检查：科目表 列表查询.md 还用得到吗？** 全库 grep AccType/ACC_TYPE 确认：js/ 已无任何 AccType/getlist **调用**（Round 52 返工已删 fetchAccTypes，仅剩 2 处注释）。顺手修掉 3 处过时文字：reports.js:473 注释（「TYPE_NO 来自该样式所属科目表（AccType/getlist）」改为账簿行→样式行链路）、README 结构树 rpt-style-store.js 行（旧两段式描述改账簿缓存版）、README「科目表 列表查询.md」行（删「报表样式的 TYPE_NO 来源」改为「仅需 TYPE_NAME 时才查」）。结论：该文档与 API 都不在链路内，保留文档备查（将来需要 TYPE_NAME 时用），当前代码零依赖。

### Round 52 收尾（【推送代码】执行）

- **本地服务器**：浏览器 ERR_CONNECTION_REFUSED → 诊断：ERP 正常（登录 API HTTP 200，80 端口在听），是应用 8080 端口 python http.server 停了；后台重启并验证 http://localhost:8080/index.html 200
- **deploy.ps1 v1.6**：$Version 1.5→1.6；jsFiles 已含 rpt-style-store.js（26 js）；Contents 行 30→31 文件（1 html + 4 css + 26 js）；synopsis 输出行同步
- **删除 sunlike-erp-report-v1.5.zip**：该包是 Round 52 时序修正**前**打的，内含「先拉样式再开报表」的串样式 bug，且从未 commit——删除，重打 v1.6 干净包
- **README**：deploy.ps1（v1.5，31 文件）→（v1.6，31 文件）；v1.5.zip → v1.6.zip
- **进度追踪表**：总体进度加「22. 三财务报表报表样式下拉 ✅ 代码完成（Round 52），待浏览器验证」
- **提交**：commit + push（含 rpt-style-store.js、科目表/报表样式两份 API 原始文档、v1.6 部署包；说明：浏览器实测清单待用户确认后勾选）

### Round 52 转入数据源报错修复（用户实测反馈）

**用户实测反馈**：资产负债表查询出数据后点【转入数据源】，数据源卡片多了一句「❌ 科目表代号不能为空」。

**定位**：是报错。转入数据源不走 doQuery（app.js:534 直接 `ReportEngine.query(reportKey, readFilters(), 1, 5000)` 独立全量请求），而 TYPE_NO 注入只在 doQuery 里做（filters.styleTypeNo = 样式行 TYPE_NO）→ 转入请求的 buildStreamCtx 拿到 `filters.styleTypeNo || ''` = 空字符串 → 顶层与 fixCondition 的 @TYPE_NO 都替换成空 → ERP 拒绝并返回「科目表代号不能为空」→ query reject → .catch 把 err.message 写进数据源卡片的 errorMsg（datasource-list 以 ❌ 前缀展示）。

**修复（TYPE_NO 注入下沉到 readFilters，两条路径都拿到）**：
- populateRptStyleSelect 的样式选项写入 `data-type-no`（样式行 TYPE_NO，与账簿选项 data-type-no 同模式）
- reports.js readFilters 新增 `styleTypeNo` 字段：从样式下拉选中 option 的 data-type-no 读取（新增 _selData 辅助函数）
- doQuery 的注入保留作兜底校验（样式不存在 → toast），值一致无冲突；0 笔前置判断两侧 filters 字段一致
- 版本号 bump 20260819e（app.js / reports.js 两入口 HTML）；node --check 全过

**待浏览器实测**：资产负债表查询 → 转入数据源 → 卡片无「科目表代号不能为空」，数据条数正确；旧报错卡片需手动删除。

## 2026-08-19 Round 53：AI 聊天裸 JSON 直接渲染表格（已确认方案，待实施）

**用户问题**：AI 数据分析对话中经常出现裸 JSON（如 `[{"序号":1,"项目":"流动资产合计",...}]`），乍看之下不懂；点「生成 HTML」导出的报告里却是清楚好看的表格。问是技术原因还是效率原因。

**定位（已读代码确认）**：两者都不是——是解析规则的覆盖缺口。
- 聊天渲染链路：AI 回复 → AIParser.parse()（js/ai-parser.js）→ 只识别两种形态转表格：① ```` ```table ```` 围栏内 JSON；② 裸 `table\n[JSON]` 模式。其余全走 renderMarkdown 转义成纯文本。
- 提示词（js/ai-client.js:472 三语版）要求模型「必须使用 ```table 格式」，但模型经常不遵守，直接吐不带标记的裸 JSON 数组 → 不在识别规则内 → 原样显示。
- 导出端与聊天端共用同一个表格渲染函数 `AIParser.renderTableHTML`（export.js 引用）——聊天端完全具备渲染能力，只是识别规则窄。

**已确认方案（用户批准：「是的」）**：扩展 AIParser 识别规则——
1. 裸 JSON 数组：行首 `[` 开头（含缩进）→ 括号平衡扫描（字符串感知）找出完整 JSON → JSON.parse 验证 → **非空对象数组**才渲染成表格
2. ```` ```json ```` 围栏内的对象数组同样转表格
3. 防误伤：非 json 代码围栏内不转换、parse 失败不动、非对象数组（如 `["a","b"]`）不动、单对象不动

**实施要点（下次开工照此执行）**：
- js/ai-parser.js 新增：`findJsonEnd(s, start)`（括号平衡+字符串/转义感知）、`isPlainObject(v)`、`bareJsonScan(text)`（逐字符扫描，``` 行切换围栏态）、`renderJsonTables(text)`（先处理 ```json 围栏→tryTableOrCode，围栏外跑 bareJsonScan）；`renderWithTables()` 末尾 `renderMarkdown(...)` 改为 `renderJsonTables(...)`
- 导出端顺带受益：export.js renderMixed 的 fallback 仍用 renderMarkdown——可后续把 bareJsonScan 也接上（可选，用户未要求，注意 buildHTMLReport 2c 已把围栏全转成 %%CODE%% 占位，围栏保护天然成立）
- 完成后：node --check、版本号 bump（ai-parser.js ?v=20260819f 起，index.html + ui-template.html 同步）、对话记录/需求架构/进度表同步
- 聊天表格已有 .table-wrap 样式（dashboard.css），无需新 CSS

**进度**：方案已确认，代码未动（用户要求先 compact 上下文再开工）。

### Round 53 实施完成（继续上轮已确认方案）

**开工前置**：读 ai-parser.js 现状 + grep 调用点确认覆盖范围——聊天端 chat-ui.js:171 / chat-core.js:77 两条路径都走 `AIParser.parse`，一次改动全覆盖；export.js:820/827 用 renderMarkdown 兜底（本轮不动，export.js:119 围栏已转 %%CODE%% 占位保护天然成立）。

**代码实施（照已确认要点逐条执行）**：

- **js/ai-parser.js** 新增 5 函数：
  - `isPlainObject(v)` — 非 null/非数组的 object 判定
  - `isObjectArray(v)` — 非空对象数组判定（可渲染成表格的最小形态）
  - `findJsonEnd(s, start)` — 括号平衡扫描，字符串/转义感知（字符串内 `]`、`\\"` 不误判），失衡/未闭合返回 -1
  - `tryTableOrCode(jsonText)` — ```json 围栏内容：对象数组 → renderTableHTML；解析失败/非对象数组/单对象 → `<pre><code>` 代码块原样
  - `bareJsonScan(text)` — 逐字符扫描，行首 ``` （任意语言）切换围栏态；围栏外行首 `[`（含缩进）→ findJsonEnd 提取 → JSON.parse 验证 → isObjectArray 才渲染，否则原样走 Markdown
  - `renderJsonTables(text)` — 先正则处理 ```json 围栏（→ tryTableOrCode），围栏外跑 bareJsonScan
- `renderWithTables()` 末尾 `renderMarkdown(...)` → `renderJsonTables(...)`（唯一改动点）
- 版本号 bump：ai-parser.js `?v=20260814a` → `?v=20260819f`（index.html:407 + ui-template.html:537）
- 防误伤四规则落地：代码围栏内不转换 / 解析失败不动 / 非对象数组·单对象不动 / 仅行首识别

**Node 实测 24 项全过（临时脚本 test-round53-tmp.js 跑完即删，垃圾文件纪律）**：

- 裸 JSON → 表格 ✅ 表头正确 ✅ 数字列右对齐 ✅ 无残留转义文本
- 缩进行首识别 ✅ 字符串内 `]` 不误判结束 ✅ 转义引号不破坏解析
- ```json 五形态：对象数组→表格 / 非对象数组→代码块 / 单对象→代码块 / 解析失败→代码块 / 空数组→代码块 全过
- 防误伤 5 项：python 围栏内不转换、非对象裸数组不动、单对象不动、未闭合不动、仅行首识别
- 回归 4 项：```table 围栏、table\n[JSON] 旧模式、Markdown 标题/粗体/管道表格、```chart 提取
- 边界 3 项：空串、普通文字、中文/空值/数字混合
- 过程中的 3 个 FAIL 全是测试脚本断言自身错误（漏了 class="num" 的数字列断言、行首测试文本没换行），代码输出均正确——修正断言后全过

**文档同步**：需求架构文档（模块索引 ai-parser.js 行 + 4.5 节表格识别规则四形态与防误伤）、流程图 七（节点 P 标注裸 JSON/```json 围栏 → 表格 + 防误伤）、进度追踪表（23 行 ✅ + 第 23 节详情 + 待验证清单）、本记录

**导出端未动**（方案明确为可选后续）：export.js renderMixed fallback 仍 renderMarkdown；因 buildHTMLReport 已把围栏转 %%CODE%% 占位，无误伤风险

**待浏览器实测**：让 AI 吐裸 JSON 数组 → 聊天直接显示表格；```json 围栏各形态；代码围栏不误转；```table/table\n 旧模式回归；导出 HTML 报告回归

### Round 53 浏览器实测修复①（用户反馈：新回复仍显示裸 JSON）

**用户反馈**：刷新后重新问，新回复还是裸 JSON。第一轮我给的验证命令 `typeof AIParser.bareJsonScan` 返回 undefined 引发"你确定改了吗"的质疑——**该命令是我给的错**：bareJsonScan 是模块内部私有函数，Public API 未导出，新旧代码都返回 undefined。改用行为级验证 `AIParser.parse('[{"a":1}]').html` → 用户浏览器确认出表格（新代码已加载）。服务器侧三重核实：8080 index.html 引用 ?v=20260819f、serve 的 ai-parser.js 含 bareJsonScan、80 端口只有 ERP API、仓库无旧副本。

**破案（字节级证据）**：让用户在控制台 dump `AppState.chatHistory` 最新 assistant 消息前后 300 字节 → 真实结构是 `**加粗**` + **闭合的 ```table 围栏** + **闭合的 ```chart 围栏** + 洞察文字。病灶：`parse()` Step 1 chart 循环里，**chart 块之前的文本走的是 renderMarkdown 而不是 renderWithTables** —— 位于 chart 之前的 ```table 围栏被降级成普通代码块原样甩出。这正是"另一条路径"自检漏掉的路径（Round 53 实施时只改了 renderWithTables 末尾，没覆盖 chart 循环的段落）。

**修复（两处）**：
- parse() chart 循环：chart 块之前文本 renderMarkdown → **renderWithTables**（```table/裸 JSON/```json 在 chart 前段落全部生效）
- bareJsonScan() 围栏分支新增**未闭合围栏兜底**：行首 ```table/```json 且后面无闭合 ``` → 不进入跳过态、围栏行丢弃，下方 JSON 由裸 JSON 规则转换（闭合围栏早在前面被消费，到不了这里）；非 table/json 语言围栏跳过态不变（python 围栏防误伤保持）

**Node 实测 22 项全过**（用户真实消息结构 9 项：表格/列/行/chart 提取/配置/加粗/洞察/无残留×2；未闭合 table/json 兜底 5 项；防误伤回归 4 项；原有路径回归 4 项），临时脚本当场清理。版本号 bump ai-parser.js ?v=20260819f → 20260819g（index.html + ui-template.html）。

**待浏览器实测**：Ctrl+F5 → 重新问 → 带图表回复里的表格正常显示；无图表回复回归；未闭合围栏情况兜底。

### Round 53 收尾（【推送代码】执行）

- **deploy.ps1 v1.7**：$Version 1.6→1.7（.DESCRIPTION 输出行同步）；打包 31 文件 = 1 html + 4 css + 26 js，140.2 KB；解包抽查：index.html 引用 ai-parser.js ?v=20260819g、包内 ai-parser.js 含 fenceLang 修复代码
- **删除 sunlike-erp-report-v1.6.zip**：内含 Round 53 修复前的 ai-parser（聊天表格 JSON 不渲染 bug），沿用 Round 52 先例删除有缺陷的旧包，重打 v1.7 干净包
- **README**：deploy.ps1（v1.6，31 文件）→（v1.7，31 文件）；v1.6.zip → v1.7.zip；结构树 ai-parser.js 行补 Round 53 能力描述
- **文档四件套**：对话记录（Round 53 实施 + 修复① + 本收尾）/ 进度追踪表（23 节含修复与教训入档）/ 需求架构（4.5 识别规则 + chart 前段落修复）/ 流程图（七 P 节点）均已同步
- **提交**：commit + push（含 ai-parser.js 修复、版本号 bump、v1.7 部署包、README/deploy.ps1 同步、v1.6 旧包删除；浏览器实测清单待用户确认后勾选）

### Round 54：生产制造 9 只报表逐一实测验证（AT04 2022 数据）+ 4 处修复

**任务来源**：用户找到账套 AT04（用户 SAN，空密码，2022 年生产数据），与 ERP 客户端核对发现生产制造类别报表"ERP 能查、本项目没动静"——该类别此前因无合适账套数据从未验证。要求逐一验证。

**实测结论（16 轮 Node 脚本逐只验证，脚本当场清理）**：

| 报表 | 结论 |
|------|------|
| 工单完成情况表 mrpPK | ✅ 请求格式正确，2022 全年 30 行 |
| 完工入库报表 mrpPS | ✅ 2022 全年 5000 行（注：文档示例用 this_year + 2022 值会 0 行，代码 range 正确） |
| 产品成本分析表 mrppu | ✅ 成本年月 2022-07 → 1078 行（2022-01→436、2022-12→4462；空值全量 154385 行） |
| 直接原料明细表 mrpce | ✅ 2022 全年 4699 行（早前全量口径 15077 行） |
| 领退补料报表 mrpag | ❌→✅ 配置锁死 last_year（2025）用户日期被忽略；改 range 后 2022 → 5000 行 |
| 单位成本分析表 mrpcf | ❌→✅ 配置锁死 this_year（2026）；改 range 后 2022 → 5000 行 |
| 在制原料明细表 mrpcx | ❌→✅ 元素 operator=this_month 服务端忽略传入日期、自取服务器当月；改 range 后 2022 → 26996 行 |
| 物料分析明细表 mrpcu | ⚠️ 全空筛选 → SQL Server 8623「查询处理器用尽了内部资源」（COMBOEXP/BOM_ITEM/NOBOM 等 fixCondition 变体全复现）；母件代号非空 → 正常（1.01.01.092 → 3 行）；仅工单号 → 0 行无数据 |
| 在制成本明细表 mrpct | 🔍 端点与请求格式正确（AT02 同格式 → 119 行，MO 40 + TW 79）；AT04 全日期范围（2022 各月/2023/2020-2030 超宽）0 行——在制原料成本明细（mrpcx）有 1223/1232 行非零成本，但在制成本汇总表本身无记录，判定为账套数据侧空表，非代码 bug；用户可在 ERP 客户端核对该报表是否同样为空 |

**关键证据链**：mrpct 从 0 行到「格式正确」的转折点是跨账套对照——AT01（文档示例账套）已无任何工单数据（mrpPK 2020-2030 宽范围 0 行），AT02 有数据且同请求 119 行，证明请求体逐字段与文档一致、端点工作正常；AT04 的 0 行是数据特性（其 2022 生产的成本计算发生在 2023-01，在制成本结转未产生汇总记录）。

**代码修复（4 处）**：
- `js/reports.js` mrpag：dateFilter `last_year` → `range`（用户日期生效）
- `js/reports.js` mrpcf：dateFilter `this_year` → `range`
- `js/reports.js` mrpcx：BIL_DD 元素 operator `this_month` → `range`（服务端不再忽略传入日期）
- `js/app.js` doQuery：新增 MRPCU 母件代号铁律（空值 → toast「请输入母件代号」，不发请求；沿用 BOOK_NO/RPT_NO 铁律模式）
- `js/i18n-data.js`：新增键「请输入母件代号」简/繁/英
- `js/app.js` 重置按钮 + init：写死的 2026-01-01/2026-12-31/2025-07 → 动态当前年/月（老账套重置后不再必查空）

**回归验证（vm 加载真实 reports.js 构造器 + 真实 API，非手工拼请求体）**：
- mrpag 2022 → 5000 行；2026 → 0 行（日期真实生效的对照组）
- mrpcf 2022 → 5000 行；2026 → 0 行
- mrpcx 2022 → 26996 行；mrpct app 请求体（YYMM range+this_month+sameEndsFrom、fixCondition 8 字段、STAT_GROUP 3 字段）与 AT02 验证格式逐项一致
- mrpcu 走 app query → buildRequestStream 正确路径复测：仅母件代号 3 行、全空 8623、仅工单号 0 行
- 过程中一次误判修正：Round 14/15 我误用 getReport 风格 buildRequest 测 mrpcu（空壳 fixCondition），其 8623 是我测试口径错，app 真实流式路径无此问题——已用正确路径复测澄清

**教训入档**：
1. `this_month`/`last_year` 等快捷操作符在服务端的行为按报表而异：getReport 系列 computeDateValue 客户端算死日期、流式报表元素 operator=this_month 服务端自取当月——"锁死"配置对老账套必空，日期条件一律用 range 走用户输入
2. 验证"服务端无数据"必须跨账套对照，不能单账套 0 行就下结论（AT02 119 行是 mrpct 格式正确性的关键证据）
3. 无数据诊断顺序：先证明请求格式正确（跨账套/对照组），再归因数据侧

---

## 2026-08-20 Round 55：单位成本分析表（mrpcf）仍无数据 —— 根因是 index.html 版本号未 bump，浏览器缓存跑了修复前旧代码

**用户**：单位成本分析表，没有查到数据，但是 ERP 却有（Round 54 修复后复报）。
**用户确认**：跑的是项目源码（非 v1.7 安装包）。

**排查过程（先实测、不猜）**：
1. 源码 `js/reports.js` mrpcf 已是 Round 54 修复后的 `dateFilter: { operator: 'range' }`（reports.js:1103）
2. v1.7 部署包内 mrpcf 仍是旧 `this_year` 锁死配置（2026-08-19 打包，早于修复）——但用户跑的是源码，排除
3. vm 加载当前源码构造器 + 真实 API（AT04）三组对照：
   - 2022-01-01 ~ 2022-12-31 → **5000 行** ✅（修复有效）
   - 2026-08-01 ~ 2026-08-20（默认当前月）→ **0 行**（AT04 无 2026 数据，空是正确结果）
   - 日期留空 → 5000 行（值 [null,null]，服务端返回 2018 年起全部数据）
4. **根因找到**：`index.html` 引用 `reports.js?v=20260819e`、`app.js?v=20260819e`、`i18n-data.js?v=20260819c` —— 三个文件都在 2026-08-20 早间被 Round 54 修改，但版本号未 bump。浏览器按 URL?v= 缓存了 8/19 的旧 JS，页面实际跑的是修复前代码（mrpcf 锁死 2026）→ 无论选什么日期都 0 行。

**修复**：
- `index.html` 三处版本号 bump：reports.js / app.js → `?v=20260820a`，i18n-data.js → `?v=20260820a`
- `index.html` 日期输入框移除硬编码 `value="2026-01-01"` / `"2026-12-31"` → 清空，由 app.js init 动态填当前年（双保险，HTML 不再藏写死日期）

**回归**：node --check 三个 JS 全部通过；vm+真实 API 三组对照如上；临时脚本当场删除。

**教训入档**：
1. **改 js 必 bump index.html 版本号**（项目 cache-busting 惯例 `?v=`）——Round 54 漏做，导致"代码已修、页面照旧"的假象，白排查一轮。以后任何 js/css 修改，版本号同步是收尾动作的一部分，与文档同步同级
2. 默认当前月日期查老账套 → 0 行是正确结果而非 bug；判断"没数据"前先确认日期区间覆盖了账套数据年份（AT04 是 2022）

**待浏览器验证**：Ctrl+F5 强刷后，单位成本分析表选 2022-01-01 ~ 2022-12-31 → 应出 5000 行。

---

## 2026-08-20 Round 56：转入数据源 localStorage 配额溢出 —— 超大拆分多卡片 + 透明压缩

**用户**：在制原料明细表（mrpcx）转入数据源报错：Failed to execute 'setItem' on 'Storage': Setting the value of 'sunlike_data_sources' exceeded the quota。方案选定：**沿用 5000 行上限，超限自动拆分，卡片名称加后缀 _1/_2/_3…**（用户原话方向，两轮 AskUserQuestion 确认）。

**根因实测**：mrpcx 是流式报表，转入走 `ReportEngine.query(mrpcx, …, 1, 5000)` → 流式路径忽略分页返回**全量 26996 行**；JSON 实测 **10.19 MB**，超过 localStorage ~5MB 配额必爆。且配额按整个站点计，**单纯拆分不解决**（6×1.9MB 仍超），必须压缩。

**压缩实测（真实 26996 行数据）**：
- LZString compressToUTF16：10.19MB → 1.10MB（9.3x）；输出字符范围 0x20–0x7FFF（无控制符/代理区），JSON 安全无转义膨胀
- 5000 行/块 × 6 块压缩后容器实测 **683k 单元（0.65MB）**；记事本全量快照 0.55MB
- Node 压缩 2.2s / 解压 0.18s（浏览器转入时一次性 ~2.5s 可接受）

**实现（5 文件）**：
- `js/utils.js`：新增 `compressText/decompressText`（LZString 缺失/失败 → 原样返回，降级安全）
- `js/datasource-store.js`：存储层透明压缩——ready 源 data JSON > 50KB → 压缩存串 + `_z:1` 标志；getAll 解压还原，调用方无感知；旧格式（无 _z）完全兼容；内存缓存（同会话零解析开销，'storage' 事件跨页签失效）；setItem try/catch → 友好中文报错「本地存储空间不足，数据源保存失败，请清理旧数据源」
- `js/notepad-store.js`：同款全容器压缩信封 `{_z:1,d}`（记事本快照含数据源全量数据，同一条配额路径，自检纪律覆盖）
- `js/app.js` 转入 .then()：rows > 5000 → 移除占位卡，一次性 saveAll 构造 _1/_2/… 卡片（升序 _1 在前；id 加序号保唯一；20 卡上限同 add()），toast「数据共 N 行，已拆分为 X 张卡片转入」
- `js/i18n-data.js`：新键三语（拆分 toast + 配额友好报错）

**配套**：index.html + ui-template.html 增加 LZString 1.5.0 CDN（utils.js 之后、两个 store 之前）+ 五处版本号 bump（Round 55 纪律：改 js 必 bump）。

**回归（vm 加载真实 utils/datasource-store/notepad-store + 真实 26996 行，17/17 通过）**：6 卡拆分名称/顺序/recordCount（5000×5+1996）✅；往返 26996 行首末行一致 ✅；旧格式兼容 + 小数据源不压缩 ✅；人为 200k 小配额 → 友好中文报错 ✅；记事本压缩信封往返 ✅；Utils 往返 ✅。临时脚本/11MB 数据文件/临时 node_modules 全部删除。

**教训入档**：
1. localStorage 配额按站点计，拆分不能解决配额，压缩才能（数据源 10.19MB → 0.65MB）
2. LZString compressToUTF16 输出无控制符/代理区字符，直接嵌 JSON 无转义膨胀；compressToBase64 反而大 2.5 倍
3. 存储层透明压缩 + `_z` 标志 = 调用方零改动 + 旧数据零迁移
4. 流式报表转入拿全量数据，getReport 转入受 pageSize 5000 封顶——拆分只对前者的超大场景触发

**待浏览器验证**（Ctrl+F5，AT04）：mrpcx 选 2022 转入 → 6 张卡片 _1…_6；卡片计数 5000×5 + 1996；AI 提问正常（每卡采样 20 行）；删卡/记事本保存恢复正常。

---

## 2026-08-20 Round 57：登录页右上角系统设置齿轮 + 登录前置检查（未配置不强行登录）

**用户**：登录页面右上角应有个【系统设置】的齿轮图标（跟主界面的那个齿轮一样），目的是别的机器通过浏览器登录进来时 localStorage 是空的，需要先做系统设置；并且按下登录时如果系统设置未正确配置保存，不要直接强行登录然后报错，而要提醒用户进行系统设置。

**实现（模板先行 + 生产同步，7 文件）**：
- `ui-template.html`：登录页右上角加 `#loginSettingsBtn`（与主界面齿轮同款 SVG/按钮样式）；新增演示按钮「演示：未配置系统设置时点登录」——临时移除 `sunlike_settings` → dispatch 登录表单 submit → 拦截演示 → 恢复设置（app.js 的配置检查是同步的，演示期间绝不会发出真实登录请求）
- `index.html`：同款齿轮正式实现；主界面齿轮一并补 `data-i18n-aria-label`（icon-only 按钮无障碍，Skill 规范）
- `css/auth.css`：`.page-login` 加 `position:relative`；`.login-settings-btn` 绝对定位右上角（随 `.page-login.hidden` 一起隐藏，不进主界面）
- `js/settings-store.js`：新增 `isConfigured()`——设置已保存 且 serverUrl 非空、`http(s)://` 开头才算配置正确
- `js/settings-ui.js`：登录页齿轮绑定 openSettingsModal（与主界面齿轮共用同一面板）
- `js/app.js` 登录提交：在 compno/usr 空校验之后、Auth.login 之前加配置检查——未通过 → 不发请求，loginError 提示「请先进行系统设置，配置服务器地址后再登录」+ 自动打开设置弹窗引导配置
- `js/i18n-data.js`：新提示文案三语

**设计决策**（ui-ux-pro-max Skill 规范）：
1. 拦截判定只看服务器地址（登录只依赖它）：已保存 + 非空 + http(s):// 合法格式；API Key 不拦登录（登录后 checkFirstRun 照旧引导）
2. 提醒交互 = 原因说明（loginError）+ 恢复路径（自动弹设置面板），配置保存后重新点登录即通
3. 检查是纯本地判定（localStorage），离线也能完成拦截，不发必然失败的网络请求

**边界自检（CLAUDE.md 六）**：
- localStorage 空/乱码 serverUrl/空字符串 → 全拦截 ✅；正常 `http://IP:端口` → 放行 ✅
- 离线：拦截照常工作（无网络依赖）✅
- 另一条路径：登录入口只有 loginForm submit 一处；会话恢复（checkAuth）在 localStorage 空时天然回登录页 ✅；登出/过期回登录页齿轮在 ✅

**回归**：node --check 4 个 JS（settings-store/settings-ui/app/i18n-data）全过；版本号 bump 五处 → `?v=20260820c`（Round 55 教训：改 js 必 bump）。

**垃圾清理（纪律当场执行）**：根目录 4 个 0 字节杂散文件（`50KB`、`s.FIELD).join('`、`璇存槑锛歅ERCENT`、`锛圧ound`）直接删除。

**待浏览器验证**：① 登录页右上角齿轮出现、点击开弹窗；② 清空 localStorage 点登录 → 拦截提示 + 自动弹设置，配置后重登成功；③ 已配置机器登录零回归。

**Round 57 追加（同日，用户浏览器反馈修正）**：齿轮位置摆错了——用户要的是白色登录窗体的右上角，不是浏览器页面右上角。修正：按钮标记移入 `.login-card` 内，CSS 锚点从 `.page-login` 改到 `.login-card`（`position:relative` + 卡片右上角绝对定位，top/right 用 `--space-md`）；auth.css 版本号二 bump → `?v=20260820d`（Round 55 纪律）。流程图/需求架构/进度表同步把「登录页右上角」改为「登录卡片右上角」。拦截逻辑与演示按钮不受影响（按钮 id 未变）。

**Round 57 收尾（README/安装包/commit-push 记录）**：
- README.md 同步 7 处（认证流程加齿轮+登录前置检查、utils.js LZString 压缩、settings-store.js isConfigured、datasource-store.js 拆分+压缩、notepad-store.js 压缩信封、settings-ui.js 登录卡片齿轮入口、app.js 登录前置检查）；deploy.ps1/zip 行 v1.7→v1.8
- deploy.ps1 bump v1.8，`.\deploy.ps1` 打包成功：sunlike-erp-report-v1.8.zip（145.8 KB，31 文件全 [OK]）
- commit `f7d15b2`「feat: 登录卡片系统设置齿轮 + 登录前置检查（Round 57）+ Round 54-56 日期筛选修复/数据源压缩拆分入库 + 部署包 v1.8」→ push 到 GitHub master 成功（75d6227..f7d15b2）
- 新增报表明细清单.md 入库（CLAUDE.md 文件索引由用户补充确认）

---

## 2026-08-28 Round 58：新增【总账报表调用方法.md】——总账 5 只报表即拿即用 API 速查

**用户需求**：把「总账报表」按报表名称、端点、调用方法及参数整理成一份「拿了就能直接用」的 API 文件，存为【总账报表调用方法.md】。

**内容构成（逐项对照原始文档 + 实测代码，非凭记忆）**：
- 总账分组 5 只一览：总分类账（accGeneralLedger/GetReportStream）、科目余额表（accBalanceTable/GetReportStream）、三财务报表（accRptPreview/GetReportStream ×3，PGM 各异）——来源报表明细清单.md 分组五
- 公共约定：Base URL 拼接铁律（文档 URL 的 `/api/` 段就是 `/SUNFUSION/API`，勿重复带 api/ 前缀）、登录 curl、会计期间派生规则（PERIOD → YEARS/IPERIOD/DATE_B/DATE_E/START_DD，照 js/reports.js buildStreamCtx 实测）、SSE 解析规范（data: 行、{CODE,PERCENT,TITLE,ERR,DATA}、REPORT__TAB 累积、COLUMN_INFO 空数据为 {}、错误三形态、PAGE_COUNT 不可靠）
- 依赖清单接口：AccBook/GetList（账簿下拉 BOOK_NO 来源）+ accRptStyle/getlist（三财务报表 RPT_NO 来源，RPT_TYPE 2/3/4 过滤，链路=账簿行 TYPE_NO → 顶层 TYPE_NO）
- 5 只报表逐只：完整可照抄请求 JSON（期间 2026-01 示例）+ fixCondition 全字段表 + 响应要点（REM_TYPE 1=期初/2=本期/3=本年；科目余额表 10 金额列 _D/_C 标题取自 reports.js COLUMN_LABELS；利润表 _1 后缀 + DISPLAY_FIELDS 前导逗号 + CHK_NO_SYTZ_VOH="T" 两陷阱）
- 完整调用顺序 + 11 条实测陷阱（BOOK_NO 空 = 200+ERR 非 406、STD001/2/3 样式不存在报错、code 10001 通用错误码等）

**文档同步**：CLAUDE.md 项目文件索引新增本文件一行；需求架构文档/进度追踪表/流程图本次无改动（纯文档新增，不涉代码与流程）。

### Round 58 追加（同日）：与 Online总账报表-查询制表.md 对比——确认是两套不同 API

**用户**：要求比较 Online总账报表-查询制表.md（新下载的原始文档，IDE 中打开）与 总账报表调用方法.md 的相似度与差异。

**逐项核对结论**：同 5 张报表名称、同 POST+GetReportStream SSE 调用方式（相似度：业务主题 100%、调用方式 ~90%），但 API 契约接近 0% 相同——
- **端点 0/5 相同**：Online 版 RPTACCBlank/RPTACCDetail/RPTZFListA/RPTSYListA/RPTCshFroList vs 标准制表版 accGeneralLedger/accBalanceTable/accRptPreview
- **请求体**：Online 版扁平顶层参数（dateB/dateE/AccNoB/ACCLEVEL，无 PGM/SEARCH_INFO）；无账簿依赖（仅可选账别 ACCA/ZHANGID）；日期是自由 YYYY-MM-DD 区间
- **响应**：数据表键 RPT/MyTable/RPT_CSHFROLIST（非 REPORT__TAB）；COLUMN_INFO 是嵌套对象 {表名:[...]} + 另有 COLUMN_PROP
- **报表形态**：Online 版是「空白纸打印」系列（总账 REM 直接给「承上期/凭证总金额」、ACC_NOTE 给「小计」、SHOWDC 给「借」；资产负债表账户式左右栏 + REPNO1/2/3 公式代号；利润表单部门损益；现金流量直接法）

**现状**：项目 40 只报表接的是标准制表版端点（reports.js）；Online 版 5 端点未进 REPORT_CONFIG、未进任何索引，且该文件 git 未跟踪。是否并入调用方法文件/报表系统待用户决策（已向用户提问）。

**文档同步**：CLAUDE.md 项目文件索引补 Online总账报表-查询制表.md 原始文档行。

### Round 58 再追加（同日，用户指正）：账簿依赖是两套 API 的最大结构性差异

**用户指正**：上一轮对比把「账簿依赖」压成一行，分量给轻了——标准制表版 5 只全部有【账簿】，Online 版是不是全都没有。逐只重新核对全部请求参数，确认：

- **标准制表版**：账簿（BOOK_NO）是 5 只全依赖的第一等公民——fixCondition.BOOK_NO 必填、来源账簿清单 AccBook/GetList、BOOK_NO 空实测报错（200+ERR）；三财务报表还叠加报表样式 RPT_NO/TYPE_NO 链路（链路起点仍是账簿行 TYPE_NO）。
- **Online 版**：5 只没有一只需要账簿——无 BOOK_NO、无 AccBook/GetList 前置；账别概念仅以可选 ACCA（总分类账/科目余额表）、ZHANGID（资产负债/利润表）出现且示例全空，现金流量表 11 参数里完全没有账簿/账别概念。
- **维度体系相反**：Online 版 5 只普遍带部门参数（DEPT/DEPNO/txtDep/DEPT_SEL/DEPTSHOW），标准制表版这 5 只无部门概念——一套是「部门/账别」维度，一套是「账簿/样式」维度。

**附带发现（逐字段核对）**：
1. Online 版资产负债表参数表字段名 REPO1/REPO2/REPO3 与请求示例 REPNO1/REPNO2/REPNO3 不一致——原文档笔误，用哪个以实测为准。
2. 「账别」ACCA/ZHANGID 与「账簿」BOOK_NO 是否同一概念、空值取什么账——两份文档均未说明，无实测证据，不猜；若接 Online 版，第一件事是 ACCA 空/非空对照实测。

**待用户决策**：Online 版 5 端点是否并入调用方法文件/报表系统。

## 2026-08-31 Round 59：总账未启用 → 5 只报表静默降级 Online 空白纸打印端点（规划 + PoC 实测）

**用户需求**：把 Round 44「账簿清单 LIST=0 → 弹『你的账套没有启用总账』并禁查」改为：LIST=0 时总分类账/科目余额表/资产负债表/利润表/现金流量表 5 只全部静默降级调用 Online总账报表-查询制表.md 的 5 端点（RPTACCBlank/RPTACCDetail/RPTZFListA/RPTSYListA/RPTCshFroList 的 GetReportStream）。

**用户 4 决策（问询拍板）**：
1. 公式代号（资产负债表 REPNO1/2/3、利润表 REPNO）→ 条件面板加输入框，预填文档示例值 10/20/30/40
2. 切换完全静默：不弹窗、不 toast
3. 日期沿用现有「会计期间(月)」选择器，映射 dateB=月初/dateE=月末（资产负债表 dtE=月末）
4. 5 只 Online 报表全部按响应 COLUMN_INFO 动态列渲染

**PoC 实测结论（三步验证法，PowerShell + curl.exe --data-binary @文件）**：
- 5 端点路径/登录验证通过；SSE 消息 `{CODE:1,PERCENT,TITLE,ERR,DATA}`，数据行只出现在 PERCENT:100 消息（fetchStreamReport 忽略 CODE 只看 ERR 的逻辑复用成立）
- **REPNO1/REPNO2/REPNO3 定论**：传 REPO1/2/3 变体报「请先设置报表公式」→ 原文档参数表是笔误，以请求示例 JSON 为准
- 公式值 10/20/30/40 有效：资产负债表 5 行公式壳、利润表 23 行公式壳（行数由公式决定，非凭证数）
- 数据表键：总分类账/科目余额表 = `RPT`；资产负债/利润表 = `MyTable`；现金流量表 = `RPT_CSHFROLIST`。COLUMN_INFO 是嵌套对象 `{表键:[{NAME,TYPE,SIZE}]}`（无 TITLE 字段），空数据时 `COLUMN_INFO:{}`
- **AT02（books=5）五端点全部出真实数据**：accblank 9 行、accdetail 4 行、zcfzb 5 行、lrb 23 行、xjll 3 行，金额列大量非空 —— 端点/参数/公式/数据流全链路验证通过
- **列语义锁定（AT02 实测行值对照）**：accblank：AMTND=本期借方发生额、AMTNC=贷方、AMTNYE=期末余额、REM=摘要（承上期/凭证总金额/小计）、SHOWDC=借/贷；accdetail：AMTND/AMTNC=本期借/贷、AMTNYE_D/AMTNYE_C=期末借/贷余额、AMTDCCE=期末余额（贷方为负）、AMTNQC=期初；xjll：AMT=原币、AMTN=本位币；zcfzb：ACC_NAMEL/R=左右科目名称、AMTN_SUML/R=左右金额；lrb：AMTN=本期金额、AMTN_SUM=合计
- **AT01 空数据已解释**：标准版总分类账 2026-01 有 12 行（账簿 00000000），但 Online 版同期间 0 行 —— AT01 凭证挂在账簿模型下，Online 空白纸打印读不到该作用域（ACCA/科目范围/ACCLEVEL 变体均试过）；而 AT01 的 Online 利润表 23 行金额非空（公式引擎能取到）。结论：非参数错误，属数据作用域差异
- **COMPNO 遍历**：AT01 books=1、AT02 books=5、**AT03/AT04 books=0（第 8 步降级验证用）**；AT03/AT04 是极简演示账套：无报表公式（「制表公式不存在」/ACC.ACCN.NORPTFUN）、缺 DB 函数（FN_GET_ACCN_USR 无效）→ 空/ERR 属账套数据层面正常，端点契约行为正确
- **两个环境陷阱（入经验库）**：
  1. PowerShell 5.1 直接给 curl.exe 传无空格 JSON 参数会剥掉双引号 → 伪造「服务端拒绝字符串日期/空串必填 400」假象。PoC 一律 JSON 写文件 + `--data-binary @文件`
  2. 服务端 token 单会话互踢：后续任何登录（含失败账套）会使先前 token 立即失效（20004 credential expired）。每账套批次测试前重新登录

**已批准 9 步实施计划**：①PoC 定论 → ②ui-template.html 公式输入组原型（用户浏览器确认）→ ③api.js dataKey 参数化 → ④reports.js online 块 + 动态列 → ⑤app.js 静默降级 + 死代码清理 → ⑥index.html → ⑦i18n 三语 → ⑧验证（含 AT03 LIST=0 真实验证）→ ⑨文档同步。

**用户两次反馈 → ui-template 重构为「自动判断公式栏位」**（2026-08-31）：
- 反馈 1：不是每张表都 3 个公式框，必须按每个 API 传入参数区分（总分类账/科目余额表/现金流量表无公式参数、资产负债表 REPNO1/2/3、利润表 REPNO）
- 反馈 2（打断后重申）：模板要【自动判断并显示该有的公式输入栏位】，不能让用户去选「一个、三个、无公式」场景
- 最终模板架构：`ONLINE_DEMO` 配置（reportKey → endpoint + `formulaInputs` 数组）驱动自动推导 `formulaInputs.length===3 → onlinerepno3 组；===1 → onlinerepno1 组；===0 → 仅会计期间`；5 个演示按钮（demoOnlineGl/Abt/Zcfzb/Lrb/Xjll）各自触发，信息行打印自动判断结论。用户浏览器确认通过，说「继续往下做」

**第 3~7 步实施完成**（2026-08-31）：
- **api.js**：fetchStreamReport 表键参数化 `var dataKey = callbacks.dataKey || 'REPORT__TAB'`，累积与 onData 改 `msg.DATA[dataKey]`
- **reports.js**：
  - 5 只总账 REPORT_CONFIG 各加 `online` 块（apiPath/responseDataKey/filterLayout='accglOnline'/inputs/body 扁平模板照抄调用方法 6.2；zcfzb inputs=filterRepno1/2/3 默认 10/20/30、lrb inputs=filterRepno 默认 40、其余 inputs=[]）
  - 新增 buildOnlineBody：buildStreamCtx + inputs 注入 ctx（未填用 defaultValue）+ @占位符解析 → 扁平 body
  - query() 流式分支：`filters.onlineMode && cfg.online` 判别 → buildOnlineBody + `cfg.online.apiPath` + `dataKey` + 嵌套 COLUMN_INFO 解包（Array.isArray 防护）
  - 模块状态 `_ledgerOnline` + getter/setter；readFilters 追加 onlineMode/filterRepno1/2/3/filterRepno
  - getVisibleColumns：`_ledgerOnline && cfg.online` 强制动态列、丢弃 leadingColumns；标签链 `ONLINE_COLUMN_LABELS[key] || COLUMN_LABELS || TITLE || NAME`
  - 新增 ONLINE_COLUMN_LABELS（5 报表分表，只收实测高/中置信度字段，与全局冲突的 NAME=科目名称/REM=摘要 在此覆盖）
  - updateFilterPanel：开头清 _currentColumnInfo（修切报表脏列头）；allGroups 加 onlinerepno3/onlinerepno1；online 模式用 cfg.online.filterLayout；layoutMap 加 'accglOnline'=['dateCst']；**公式栏位自动推导**（inputs.length 3→onlinerepno3、1→onlinerepno1、0→无，与模板演示同源）；dateCst label 条件改用 layout 变量（覆盖 accglOnline）
  - DECIMAL_FIELDS/CURRENCY_FIELDS 追加 Online 数值/金额列（AMTND/AMTNC/AMTNYE/AMTNQC/AMTNYE_D/AMTN_SUML/RATE1 系等；比率 RATE*/笔数 D_NUM 不加 ¥）
- **app.js**：openReport 顶部复位 ledgerOnline；0 账簿分支改「静默降级」（`ReportEngine.ledgerOnline = true; _openReportCore(reportKey); return;`，删 blockLedgerReport + Dialog.alert）；doQuery 的 BOOK_NO/RPT_NO/TYPE_NO 拦截全部加 `!isOnline` 守卫；删 `_ledgerBlocked` 死代码块（block/unblock/setLedgerButtonsDisabled 全清，grep 确认 0 残留）；resetBtn 公式框重置回默认值 10/20/30/40 + needsRptStyle 分支加 `!ReportEngine.ledgerOnline` 守卫；resetLedgerBooksState 改复位 ledgerOnline + 公式框回默认
- **index.html**：datecst 组后插入与模板完全一致的两组公式输入框（filter-group-onlinerepno3 三框预填 10/20/30、filter-group-onlinerepno1 一框预填 40）；?v bump：api/reports/app/i18n-data → 20260831a、dashboard.css → 20260831a（此前模板已 bump，index.html 还停在 20260819b，一并修正）
- **i18n-data.js**：zh-tw/en 两段各追加 40 个 key（公式栏位 5 个 + Online 列名 35 个，如 科目说明/本期借方/期末借方/科目代号(左)/本位币金额）；删失效 key「你的账套没有启用总账…」两行
- **node --check**：api/reports/app/i18n-data 四文件全过

**第 8 步验证（进行中）**：浏览器回归待用户执行 —— ①AT01（books=1）标准版路径回归；②AT03（books=0）静默降级：5 只面板（accgl/accBalTable/accXjllb=仅会计期间、accZcfzb=期间+3 公式框、accLrb=期间+1 公式框）、查询走 Online 端点（注意 AT03 极简账套无公式 → zcfzb/lrb 会透出 ERR「制表公式不存在」，属账套数据层面）；③转入/重置/切报表/登出。

**总账报表调用方法.md 重写 + Online 文档整并（2026-08-31，用户要求）**：
- 用户：「结合这次的讨论，重新修改 总账报表调用方法.md，最好能把 Online总账报表-查询制表.md 内容整并进去」
- 重写后结构：〇 一览表改「5 只 × 2 套 API」双路径分流表（加 Online 端点/公式参数列）；一/二 公共约定与依赖清单注明「仅标准版需要」；三 改题「标准版五只报表逐个」（内容不变）；四 调用顺序加 0 账簿分流分支；五 陷阱不变；七 原始文档索引保留
- **第六章 Online 版大幅扩充为完整可照抄文档**：6.1 端点一览；6.2 五端点逐个 = 原始文档参数表（字段/类型/必填/说明）整并 + 实测修正（REPNO1/2/3 定论、REPO 笔误标注）+ 响应全列清单（GL 19 列 / ABT 34 列 / ZCFZB 26 列 / LRB 21 列 / XJLL 28 列）；6.3 响应差异表；6.4 金额列语义（AT02 锁定）；6.5 实测陷阱；**6.6 新增前端实现映射**（分流判定/公式框自动判断/请求构造/动态列/列名映射/拦截守卫 → 对应 js 位置）
- 原始文档 Online总账报表-查询制表.md **保留**（溯源 + 完整行数据示例），CLAUDE.md 索引两行同步更新（调用方法标注整并、Online 标注已整并保留溯源）

**第 8 步验证结果①（2026-08-31，用户回报）**：执行科目余额表 → 「查询失败: SqlException 对象名 'DBO.FN_GET_ACCN_USR' 无效（Error 208）」。核对：堆栈显示 `RPTACCDetailController.MakeReportData` = **Online 端点已被正确命中**（标准版会走 accBalanceTable 控制器）→ 浏览器端降级链路（0 账簿判定 → 静默 → Online 请求）验证通过；该 SQL 错与 PoC 定论一致（AT03/AT04 极简演示账套缺 DB 函数，属账套数据层），已补记 总账报表调用方法.md 6.5 陷阱第 6 条。待用户确认账套是否为 AT03（若为 AT01/AT02 则属真 bug 需排查）。

**第 8 步验证结果②（2026-08-31，用户回报）**：
- 确认账套 = **AT03** → 科目余额表 SQL 错属账套数据层（PoC 定论），非 bug
- AT03 科目余额表面板 = 「会计期间 + 部门」、**无公式框** → 公式栏位自动判断正确（inputs=[] → 不推公式组）；「部门」是 reports.js 2728 既有注释的**全报表常显组**（DEP is always visible），标准版面板同样有，非 Round 59 引入；Online 请求体 DEPT 固定 "" 与 PoC 一致（标准版总账报表同样未使用部门过滤，两路径一致）
- **AT01 标准版回归通过**：抽查 1-2 只，请求体与旧版一致
- 剩余待验：AT03 其余 4 只（accZcfzb=3 公式框、accLrb=1 公式框、accgl/accXjllb=无公式框，各按数据层预期报错）+ 转入/重置/切报表/登出

**第 8 步验证结果③（2026-08-31，用户回报）**：AT03 科目余额表再执行仍报同一 SQL 错（FN_GET_ACCN_USR）。结论：AT03 数据库缺该函数，**每次必报、客户端无法修复**，属账套终点；AT03 的验证价值 = 静默降级/端点路由/面板/错误透出（已全过）。给出「AT02 + DevTools 拦截 AccBook/GetList 返回空 + BookStore.reset()」临时 mock 法（手段 B，仅当前页面会话，F5 恢复），让用户在浏览器里用 AT02 真实数据完整验证 Online 链路（面板公式框 + Online 端点 + 动态列出数据）。

**第 8 步验证结果④（2026-08-31，用户质疑 + 参数矩阵复测，推翻两条旧结论）**：
- 用户：Postman CLI 在 AT03「简单就能抓出数」，质疑「必然报错」结论
- **矩阵复测过程**：①第一次矩阵 10 组 + ②URL 大小写/Bearer 前缀/token 归属 4 组 + ③AT04 五端点摸底 + ④用户 token 账簿归属比对。中途发现并修正解析器 bug：SSE 消息是**带空格 pretty JSON**（`"ERR": "..."`），旧正则不留空格导致 ERR/行数全部漏报（已入 调用方法 6.6 陷阱第 6 条）
- **定论 A：用户 Postman token 是 AT01 的，不是 AT03**。证据：用户 token 查 AccBook/GetList = 1 本账簿「First Department」，与 AT01 登录实测（BOOK_NO=00000000，同名）完全一致；AT03 登录查 = 0 本。用户的 Postman 跑在 AT01 数据库上（有 FN_GET_ACCN_USR → 无 SQL 错）；且**用用户 token+其 CLI 原样重放 = 无错误但 0 数据行**（AT01 凭证挂账簿模型，Online 作用域读不到，与旧 PoC 定论一致）
- **定论 B：AT03 科目余额表 SQL 错与参数无关**。5 组矩阵（AccNo 1001/空 × 2024/2026-08）全报 FN_GET_ACCN_USR（Error 208）→ AT03 数据库确实缺该函数（仅此账套此端点；AT04 同端点干净空表不报错）
- **推翻旧结论 1**：「AT03 zcfzb/lrb 报制表公式不存在」→ 错。实测 AT03：zcfzb 6 行公式壳（负债合计/库存股/本期损益等）、lrb 23 行公式壳（科目 6001/6011/6021…），公式 10/20/30/40 有效、正常返回，金额空因 AT03 无凭证数据
- **推翻旧结论 2**：「AT03/AT04 都缺 DB 函数/无公式」→ 错。AT04 科目余额表干净空表、zcfzb 0 行无错误、lrb 报的是另一种错（##CSH_FRO_TEMP 临时表冲突）；账套间端点行为逐套而异
- **AT01~AT04 Online 五端点实测地图已固化进 总账报表调用方法.md 6.5**（表格：每账套每端点 行数/错误形态）；6.6 陷阱重写（公式无效行、token 互踢范围修正为「同账套互踢、跨账套不互踢」、新增 SSE 空格陷阱、FN_GET_ACCN_USR 收敛到「仅 AT03 科目余额表」）
- **对浏览器验证预期的影响**：AT03 上 zcfzb/lrb 应能出表（空金额）、accgl/accXjllb 空表、仅科目余额表报 SQL 错——用户可继续第 8 步剩余项（面板形态/转入/重置/切报表/登出）

**AT03 科目余额表 SQL 错定案搁置（2026-08-31，用户拍板）**：用户在 Postman 重新跑 AT03，确认「的确是我的 API 就有错」→ 该问题先忽略，**明天拿到新 API 后再试**；降级链路其余验证不受影响（静默/端点路由/面板/错误透出已过）。

**第 9 步文档同步完成（2026-08-31）**：
- 报表明细清单.md：总账 5 只表加「0 账簿降级端点（Online）」列（端点/dataKey/公式参数），备注 3 改写为静默降级说明
- 流程图.md：15.1 节点 J 的 LIST=0 分支改「静默降级」（ledgerOnline=true、不弹窗不 toast）→ doQuery 跳过 BOOK_NO/RPT_NO/styleTypeNo 拦截；15.2 说明补「BOOK_NO 铁律仅标准版路径」；新增 **15.4 Online 降级：请求构造 + 动态列**（buildOnlineBody → 日期派生 → 公式注入 → 扁平请求 → dataKey → 嵌套 COLUMN_INFO 动态列 → 列名链）
- 需求架构文档.md：新增 **4.13 Online 降级模块**（动机/触发/端点映射/请求构造/公式框自动判定/渲染/响应差异/账套实测地图/验证状态）
- 进度追踪表.md：总体进度 +24 行、风险表 +AT03 行（API 侧问题搁置）、模块详情新增 **27. 总账 0 账簿静默降级 Online**（已完成清单/已验/待验，含手段 B mock 法）

**第 8 步剩余浏览器验证（用户侧，不受 AT03 影响）**：AT03 其余 4 只面板形态 + 转入/重置/切报表/登出；可选 AT02 mock 法看真实数据。待新 API 到后补 AT03 科目余额表。

**阶段化工作 SKILL 创建 + 首次执行（2026-08-31，用户要求）**：
- 用户要求：「写一个 SKILL 就叫做阶段化工作——对话记录补上去、同步文件检查、空白文件删一删、README 重新修改、新安装包及说明制作、commit+push GitHub；生成后第一次执行看看效果；以后听到关键字或智能判断必要时自动调用」
- **SKILL 落地**：`.claude/skills/staged-work/SKILL.md`（name: staged-work，描述含触发词 推送代码/阶段化工作/收尾/打包/commit/push——靠描述驱动自动触发）；六步流程 = ①补写对话记录 ②同步四件套+索引检查 ③垃圾文件当场删（区分历史资产不删）④README/部署指南更新 ⑤deploy.ps1 bump+打包+与 index.html 清单核对 ⑥commit+push（项目惯例信息格式 + master 直推）。注：.claude/ 在 .gitignore 内，SKILL 仅本机生效（与既有 skills 一致）；系统已注册生效（skill 列表可见）
- **首次执行结果（六步全过）**：
  1. 对话记录 → 本条目即第 1 步产物
  2. 文档同步检查 → 四件套+调用方法+报表明细清单本会话已同步，无新增过时项
  3. 垃圾清理 → 删 2 个 0 字节杂散文件（`.form-group').forEach(function(g)`、`璇存槑锛歅ERCENT`），根目录复核 0 字节文件清零
  4. README 更新 → js 25→26 个（架构图+项目结构两处）、reports.js 描述加 Online 降级、deploy.ps1/zip 引用 v1.8→v1.9、总账报表速查行补静默降级+样式 STD001~STD004、长连接节「0 账簿弹警告禁用」改为「0 账簿静默降级 Online」新条目、项目状态表补 Round 54-59 四行；部署指南.md 版本行 v1.2→v1.9
  5. 打包 → deploy.ps1 $Version 1.8→1.9，运行生成 sunlike-erp-report-v1.9.zip（150.1 KB，31 文件 = 1 html + 4 css + 26 js，无 MISSING；与 index.html 实际加载清单逐项核对一致）
  6. commit + push → 见 git log（feat: 总账 0 账簿静默降级 Online + 文档整并 + 部署包 v1.9）
- **以后自动触发**：用户说【推送代码】/【阶段化工作】/【收尾】/【打包】，或一轮开发告一段落时，系统按 SKILL 描述自动调用 staged-work
