# Sunlike ERP 报表查询系统 — 部署指南

> 版本：v1.9 | 一句话：**运行 install.ps1，选模式，搞定。**

---

## 有两种部署方式，选一个

| 方式 | 适合场景 | 操作 |
|------|---------|------|
| **一键启动**（推荐） | 快速试用、内网小团队、个人使用 | 双击 `启动.bat` |
| **部署到 IIS** | 正式生产、全公司使用、需要外网访问 | 右键 `install.ps1` → 用 PowerShell 运行 → 选 2 |

---

## 方式一：一键启动（双击就行）

### 前置条件

启动前确认一件事：**这台电脑能访问 Sunlike ERP 服务器吗？**

```powershell
# 在 PowerShell 里跑一下这个，把 <ERP服务器IP> 换成你的 ERP 服务器地址
Invoke-WebRequest -Uri "http://<ERP服务器IP>/SUNFUSION/API/user/login" `
  -Method POST -Body '{"COMPNO":"AT01","USR":"SAN","PWD":"","LANG_ID":"zh-cn","SYS_TYPE":"ERP"}' `
  -ContentType "application/json"
```

看到 `"code":0` 就说明没问题。如果跑不通，**先把 ERP 服务器的问题解决了再往下走**。

### 步骤

1. **装 Python**（如果还没有）
   - 打开 Microsoft Store → 搜 `Python 3.13` → 安装
   - 或者去 https://www.python.org/downloads/ 下载
   - ⚠️ 安装时勾上 "Add Python to PATH"

2. **双击 `启动.bat`**

   脚本会自动：
   - 检测 Python
   - 启动 HTTP 服务器（端口 8080）
   - 打开浏览器

3. **浏览器里看到登录页面 → 完成**

### 如果启动失败，看这里

| 现象 | 怎么解决 |
|------|---------|
| 提示"Python not found" | Python 没装或没加 PATH。重新安装，一定勾上 "Add Python to PATH" |
| 提示端口被占用 | 关掉占用 8080 的程序，或改 `install.ps1` 里的 `$Port = 8080` 为其他值 |
| 浏览器打开但页面空白 | 按 `Ctrl+Shift+R` 硬刷新 |
| 登录失败 | 点右上角齿轮 → 设置 ERP 服务器地址 → 验证测试 |

---

## 方式二：部署到 IIS（正式环境）

### 前置条件

1. IIS 已安装（没装的话：控制面板 → 启用或关闭 Windows 功能 → 勾选 Internet Information Services）
2. 知道 ERP 服务器的 IP 地址

### 步骤

1. **右键 `install.ps1` → 使用 PowerShell 运行**

2. **输入 `2` 选择 IIS 模式**

   脚本会自动：
   - 检测 IIS 是否安装并运行
   - 把文件复制到 `C:\inetpub\wwwroot\SunlikeReport\`
   - 创建 IIS 站点（端口 8080，如果被占用会自动换 8081/8088）
   - 启动站点
   - 打开浏览器
   - 自动添加防火墙规则（允许其他电脑访问）

3. **浏览器里看到登录页面 → 完成**

### 让同事也能访问

脚本已自动开放防火墙端口。同事在浏览器输入 `http://<你这台电脑的IP>:8080/` 即可。

如果同事访问不了：
- 检查防火墙：`Get-NetFirewallRule -DisplayName "SunlikeReport*"`
- 如果 ERP API 在不同服务器上，同事的电脑也需要能访问那个 ERP 服务器

---

## 启动后怎么验证？

按顺序检查这 4 步：

### 1. 页面能打开 ✅

浏览器看到 Sunlike ERP 登录页 → 过关。

### 2. 能登录 ✅

输入公司代码、用户代号、密码 → 点登录 → 看到主界面 + 右下角浮现"登录成功" → 过关。

如果登录失败：点右上角齿轮图标 → 检查"ERP 服务器地址"是否正确 → 点验证测试。

### 3. 能查报表 ✅

登录后默认显示"受订报表" → 点查询按钮 → 表格出现数据 → 过关。

### 4. 侧边栏能切换 ✅

点左侧其他报表（采购、进货、销货…）→ 表格内容跟着变 → 过关。

---

## 常见问题

### 登录报错

> 先确认 ERP 服务器地址配对了没有。点右上角齿轮，服务器地址只填 IP（如 `http://192.168.1.100`），不要带 `/SUNFUSION/API` 后缀。

用这个命令直接测试 ERP 服务器：

```powershell
Invoke-WebRequest -Uri "http://<ERP服务器IP>/SUNFUSION/API/user/login" `
  -Method POST `
  -Body '{"COMPNO":"AT01","USR":"SAN","PWD":"","LANG_ID":"zh-cn","SYS_TYPE":"ERP"}' `
  -ContentType "application/json"
```

返回 `"code":0` 说明 API 没问题，检查前端设置。返回其他说明 API 端有问题。

### 查询报表没数据

> 检查日期范围是不是太窄，或者筛选条件太严格。尝试把日期范围扩大、清空所有筛选框再查一次。

### AI 分析不能用

> 本系统支持 4 个 AI 模型：Deepseek / QWen（通义千问）/ Gemini（谷歌）/ Claude（Anthropic）。需要至少配置一个模型的 API Key。
>
> - **Deepseek**：去 https://platform.deepseek.com 注册获取 Key
> - **QWen**：去 https://dashscope.aliyun.com 注册获取 Key
> - **Gemini**：去 https://aistudio.google.com 注册获取 Key
> - **Claude**：去 https://console.anthropic.com 注册获取 Key
>
> 然后在设置面板（右上角齿轮）选择模型并填入对应 Key，点验证测试。

### 页面样式乱了 / 图表不显示

> 内网可能访问不了 CDN（jsDelivr、Google Fonts）。这种情况需要把 Chart.js 和 Inter 字体下载到本地，比较麻烦，联系管理员处理。

### 改了文件但浏览器没变化

> 按 `Ctrl + Shift + R` 硬刷新，清掉浏览器缓存。

### 其他问题

> 打开 F12 → Console 选项卡，截图红色报错信息，发给管理员。

---

## 怎么停止？

- **一键启动模式**：关掉 PowerShell 窗口就行
- **IIS 模式**：系统服务，开机自启。要停的话：
  ```powershell
  Stop-IISSite -Name SunlikeReport
  ```

---

## 文件说明

| 文件 | 干嘛的 |
|------|--------|
| `启动.bat` | 双击启动 |
| `install.ps1` | 部署脚本（一键启动 / IIS 部署 / 打包） |
| `deploy.ps1` | 打包脚本（生成部署 ZIP） |
| `index.html` | 系统入口页面 |
| `css/auth.css` | 登录页样式 |
| `css/dashboard.css` | 主界面布局、侧边栏、表格、分页 |
| `css/ai-analysis.css` | AI Chat、数据源面板、设置弹窗 |
| `js/utils.js` | 通用工具（Toast / 转义 / 格式化） |
| `js/settings-store.js` | 设置持久化（API Key / 服务器地址） |
| `js/datasource-store.js` | 数据源 CRUD |
| `js/auth.js` | 认证模块 |
| `js/api.js` | API 客户端 |
| `js/reports.js` | 10 大报表查询引擎 |
| `js/datasource-list.js` | 数据源列表 UI |
| `js/tabs.js` | Tab 页签切换 |
| `js/chat-ui.js` | Chat 消息气泡 / 操作按钮 |
| `js/chat-core.js` | Chat 发送流程编排 |
| `js/deepseek-client.js` | Deepseek 兼容别名 |
| `js/ai-client.js` | 多模型 AI 统一客户端 |
| `js/ai-suggestions.js` | AI 推荐提问 |
| `js/ai-parser.js` | Markdown / 表格 / 图表标记解析 |
| `js/ai-chart.js` | Chart.js 图表渲染 |
| `js/export.js` | Excel / HTML / PDF / PPTX 导出 |
| `js/settings-ui.js` | 设置面板弹窗 / 验证 |
| `js/app.js` | 主入口 |

其他文件（需求文档、API 文档、流程图、开发文档等）是给开发者看的，不参与部署。
