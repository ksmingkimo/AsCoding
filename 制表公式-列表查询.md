# 制表公式 - 列表查询

## 接口信息

- **接口名称**：制表公式 - 列表查询
- **接口地址**：`POST /api/billcommon/GetAccRepNoList`

------

## 请求头 (Headers)

| 参数名        | 类型   | 必填 | 说明                      |
| :------------ | :----- | :--- | :------------------------ |
| Content-Type  | string | 是   | 固定为 `application/json` |
| Authorization | string | 是   | 用户认证令牌              |

------

## 请求体 (Body)

该接口请求体为空对象 `{}`，无额外参数。

### 请求示例（JSON）

json

```
{}
```



## 成功响应 (200 OK)

json

```json
{
    "code": 0,
    "message": "Success",
    "data": {
        "Table": [
            {
                "REP_NO": "10",
                "NAME": "资产(一般企业)"
            },
            {
                "REP_NO": "20",
                "NAME": "负债(一般企业)"
            },
            {
                "REP_NO": "30",
                "NAME": "股东权益(一般企业)"
            },
            {
                "REP_NO": "40",
                "NAME": "损益表(一般企业)"
            }
        ]
    }
}
```

------

## 实测备注（Round 60 PoC，2026-09-01）

| 项 | 实测结果 |
|----|---------|
| 实际清单条数 | **14 条**（文档示例只节选前 4 条）：10 资产(一般企业) / 11 资产 / 20 负债(一般企业) / 21 负债 / 30 股东权益(一般企业) / 31 股东权益 / 40 损益表(一般企业) / 41 损益表 / 42 金融业损益表 / 43 保险业损益表 / 44 证券业损益表 / 50 费用明细表 / 51 营业成本表 / 61 管销费用明细表 |
| 端点路径 | `POST /SUNFUSION/API/billcommon/GetAccRepNoList`（**不带 `api/` 前缀**，带前缀 404——与全项目端点规则一致） |
| 路径大小写 | 全小写 `getaccrepnolist` 也可通（路由不区分大小写），实现按文档大小写 |
| 请求体 | `{}` 与 `""` 均可，实现按文档用 `{}` |
| 账套相关性 | AT02/AT03 清单完全一致（与账套无关） |
| 前端使用 | 打开 Online 资产负债表/利润表前拉取；成功后公式框换装下拉（全量 14 条、预选默认值 10/20/30/40）；**拉取失败 = Dialog.alert「没有报表公式，无法进行查询」并中止打开**（不渲染、不查询） |