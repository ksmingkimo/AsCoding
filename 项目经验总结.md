# 项目经验总结：Sunlike ERP 报表查询系统

> 从零构建一个 Web 报表查询 + AI 数据分析平台，63 轮对话的完整经验提炼。
> 目标读者：下次要做类似产品的自己。少废话，多干货。
> 2026-08-11 首版（24 轮）；2026-09-23 更新至 63 轮（新增：SSE 长连接推广、三语 i18n、API 站点迁移、40 报表、部署运维教训）。

---

## 一、产品画像

**一句话**：给 Sunlike ERP 做一个 Web 查询面板，40 只报表 + AI 对话分析数据。

**用户**：内网 ERP 操作员。不需要花里胡哨，需要快、准、能导出。

**核心链路**：
```
登录 → 选报表 → 设筛选条件 → 查数据（流式报表有实时进度条）→ （可选）转入AI分析 → 导出Excel/HTML/PDF/PPTX
```

**技术边界**：
- 纯前端，无后端中间层（直接调 ERP API + AI API）
- 浏览器即客户端，localStorage 做持久化（数据源压缩后入库）
- Windows 内网环境，IIS 或 Python HTTP Server 部署

---

## 二、技术架构 — 做了什么选择 & 为什么

### 2.1 零框架（Vanilla JS + IIFE 模块化）

**选型理由**：
- 不需要 React/Vue 的响应式系统（页面是表单+表格，不是富交互应用）
- 避免构建工具链（Webpack/Vite）→ 部署就是一个文件夹
- 目标浏览器是现代 Chrome/Edge，ES5 语法足够兼容

**模块化方式**：IIFE + 命名空间 + `<script>` 标签加载顺序

```javascript
// 每个 JS 文件一个 IIFE，返回公开 API
var ReportEngine = (function() {
  'use strict';
  // ... 私有变量 & 函数 ...
  return { query: query, buildRequest: buildRequest, ... };
})();
```

**27 个 JS 模块，严格按依赖顺序加载**：
```
Foundation:  utils → settings-store → datasource-store → book-store → rpt-style-store → acc-repno-store
I18n & Auth: i18n → i18n-data → dialog → auth → api → reports
Menu & UI:   report-menu-store → report-menu → datasource-list → tabs → chat-ui
AI Core:     ai-client → ai-parser → ai-chart
Export:      export
Persistence: notepad-store → notepad-ui
Controllers: chat-core → ai-suggestions → settings-ui
Entry:       app
```

**经验**：
- ✅ 依赖关系通过加载顺序保证，不需要模块打包器 → 零构建成本
- ✅ 每个文件职责单一，好定位问题；模块从 16 个涨到 27 个后依然可控
- ⚠️ 全局命名空间污染是代价，但 27 个命名空间可控
- ⚠️ 没有 TypeScript 类型检查 → 字段名拼写错误要到运行时才发现（全靠 API 铁律兜底）
- ⚠️ 模块多了之后，**改 js 忘记 bump index.html 版本号**成为最高频事故（见 5.4）

### 2.2 纯前端直连 API（无 BFF 层）

**为什么不加中间层**：
- 加一层 Node/Go 服务 = 多一个要部署维护的东西
- ERP API 和 AI API 都支持 CORS（或可通过浏览器直接调）
- 内网环境，安全风险可控

**代价**：
- API Key 暴露在浏览器端（Claude 需要 `anthropic-dangerous-direct-browser-access` header 才能调）
- 不能做服务端缓存/限流/日志
- 跨域问题需要逐个处理（Gemini/Claude）

### 2.3 CSS 设计系统（Design Tokens）

```css
:root {
  --color-primary: #1E40AF;
  --space-sm: 8px; --space-md: 12px; --space-lg: 16px;
  --text-sm: 13px; --text-base: 14px;
  --shadow-sm: 0 1px 2px rgba(0,0,0,.05);
  --radius-md: 6px;
}
```

**经验**：变量体系让 4 个 CSS 文件保持视觉一致，改主题色只需改一处。后加的功能（记事本）样式独立成文件——放在依赖 CSS 之后加载，靠加载顺序解决优先级，而不是靠 `!important`。

### 2.4 配置驱动贯穿全项目（本项目的骨架思想）

`REPORT_CONFIG` 大对象是**全项目唯一数据源**，一份配置同时驱动：
- 侧边栏菜单（名称/分组/图标/拼音搜索——加第 41 只报表只需加一条配置）
- 筛选面板（layoutMap 布局切换、字段映射、日期预填）
- 请求构造（getReport / getList / getReportStream / Online 四种模式多态）
- 列标签翻译（COLUMN_LABELS + I18n.t 一行通杀）
- 金额列格式化（DECIMAL/CURRENCY_FIELDS 白名单）

**关键经验**：当你发现自己在写 `if (reportKey === 'xxx')` 超过 2 次时，停下来，把差异抽成配置字段。不要等到 10 个 if 才重构。Round 32 一次接入 21 只报表零 if/else 分支，就是靠 8 个参数化配置项（showBody/hasPagination/dateOperator/searchInfoExtra/hideDateUI/contain/非日期range/apiPath）。

---

## 三、API 对接 — 这个项目最大的坑 & 最大的收获

### 3.1 三步验证法（来自血的教训）

对接任何 API，严格按这个顺序：

```
Step 1: 打开原始 API 文档阅读（不是用户的口头描述）
Step 2: 拿到可工作的 curl/Postman 示例
Step 3: 逐字段对照文档定义与代码实现
```

**为什么必须这样**：初版登录 API 连续 5 次 `code: 10001` 失败。原因：
- 字段名凭"常识"猜：PSW→ 实际是 `PWD`，CPY→ 实际是 `COMPNO`
- 漏了必填字段：`LANG_ID` 和 `SYS_TYPE` 文档写了但我们没传
- Token 路径猜错：开始取了 `response.TOKEN`，实际是 `response.data.TOKEN`

**后来的两个例证（Rounds 52/59，说明这条铁律终身有效）**：
- 文档参数表写 REPO1/REPO2/REPO3，实测传这些变体报「请先设置报表公式」——**请求示例 JSON 里的 REPNO1/2/3 才是真字段**，参数表是笔误。哪个为准？实测。
- 「TYPE_NO 写死 "3"」→ 报表样式不存在；实测账簿行自带 TYPE_NO="1" 才通。**链路中任何一环都不能凭文档示例值写死**。

**核心原则**：看到 API 响应先别高兴。确认 `response.code === 0` 才算成功（HTTP 200 不代表业务成功）。

### 3.2 配置驱动模式 — 40 只相似但不同的报表怎么管

**问题**：40 只报表 API 结构类似，但每个都有小差异：
- 端点不同（`/invpo/getReport` vs `/mrppu/getList` vs `/accGeneralLedger/GetReportStream`）
- 日期字段不同（`OS_DD` / `PS_DD` / `RP_DD` / `MO_DD` / `MM_DD` / `DATE_CST` / `YEARS` / `ACC_IPERIOD`...）
- fixCondition 不同（有的只有 `REPORT_DD_FIELD`，有的加 `SA_BILLS`、`SUB_CUS`、`COMBOFCP`、`DEP_ORG_PAY_TYPE`...）
- MRPPU 干脆换了一套 API（getList 端点、OTHERINFO、PAGE_INFO、TRANS 响应）
- 流式报表换 SSE 长连接（GetReportStream），Online 版又换扁平请求体

**方案**：一个 `REPORT_CONFIG` 大对象，差异全在配置里，核心逻辑按 `apiMethod` 多态：

```javascript
var REPORT_CONFIG = {
  invpo: { name: '采购报表', endpoint: 'invpo', pgm: 'REP_POLIST', dateField: 'OS_DD', ... },
  mrppu: { name: '产品成本分析表', apiMethod: 'getList', apiPath: 'mrppu/getList',
           responseDataKey: 'TRANS', hasDisplayFields: false, otherInfo: {...}, ... },
  accgl: { name: '总分类账', apiMethod: 'getReportStream', apiPath: 'accGeneralLedger/GetReportStream',
           needsBook: true, stream: {...}, online: {...}, ... }
};
```

**四种调用模式**：
| 模式 | 端点 | 报表数 | 差异点 |
|------|------|--------|--------|
| getReport | 各报表 getReport | 27 | 标准 SEARCH_INFO |
| getList | mrppu/getList | 1 | OTHERINFO + PAGE_INFO + TRANS |
| getReportStream（标准版） | 总账/财务/制造 | 12 | SSE + fixCondition 模板 + @占位符 |
| getReportStream（Online 版） | 5 只总账降级端点 | 5 | 扁平 body + 嵌套 COLUMN_INFO + dataKey |

**筛选面板布局**（配置驱动动态切换）：
```javascript
var layoutMap = {
  'inv':     ['cust','prd','wh','status','dateRange'],   // 进销存
  'payment': ['cust','ywType','kb','dateRange'],          // 收付款
  'mrpPK':   ['docNo','mrpNo','dateRange'],               // 工单
  'mrpPS':   ['docNo','mrpNo','wh','status','dateRange'], // 完工入库
  'mrppu':   ['prd','dateCst'],                           // 成本分析
  'wagCG3':  ['ygNo','outDay'],                           // 薪资
  'accglStyle': ['bookNo','rptStyle','dateCst'],          // 三财务报表
  'accglOnline': ['dateCst']                              // Online 降级
};
```

### 3.3 API 端点大小写

**坑**：API 文档写 `/invSO/getReport`，Postman 实测 `/invso` 全小写才通。

**后来的实测结论（Round 43/62）**：IIS 大小写不敏感——`/api/`、`/API/`、`GetReportStream`、`getReportStream` 四组合全通，Bearer 前缀带不带都通。但配置里仍全部 `apiPath` 精确指定：**文档大小写不可信，实测为准**；且精确指定让代码成为唯一权威，换环境（如新站点路径规则变化）时行为可预期。

### 3.4 成功判断与错误形态

```
✅ response.code === 0     （这个项目的标准）
❌ response.ok             （fetch 的 HTTP 成功标志）
❌ response.status === 200 （HTTP 状态码）
```

ERP API 不管什么错都返回 HTTP 200 + `code: 10001`。只检查 HTTP 状态码 → 把错误当成功处理。**实测见过的错误形态速查表**：

| 形态 | 含义 | 处理 |
|------|------|------|
| HTTP 200 + `code: 10001` + 异常文本 | 业务错误：字段缺键 / SQL 报错（异常文本会透出 .NET 信息，如 `列名 'DEP_ORG' 无效`） | 提示 message；SQL 错 → 服务端问题 |
| HTTP 200 + `code: 20004` | 凭据过期 / token 失效 | 认证拦截，跳登录 |
| HTTP 406 + 空 body | API4 总分类账 BOOK_NO 空（仅此端点） | 前端非空拦截，不发请求 |
| HTTP 200 + SSE `ERR` 非空 | API5 流式报表错误（如「账簿不能为空」） | 流解析后提示 ERR |
| HTTP 200 + 空 body 永久挂起 | 流式端点无 Authorization | 120s 超时兜底 |
| `code: 10002 REGCHECK_ERR` | 登录端注册检查环境性错误 | 环境问题，旧 token 仍可用 |

### 3.5 长连接（SSE）流式报表 — 三批端点踩出来的完整经验

**背景**：API4 总分类账起，全部长连接报表走 `GetReportStream` 端点，响应是 SSE 流而不是一次性 JSON。流中消息含 `PERCENT` 进度字段，最终数据随流到达。用户要求：进度条实时反映进度；后续所有【长连接】报表统一此模式。三批端点（API4 总分类账 → API5 八只 → Online 五只）踩出来的完整坑单：

1. **EventSource 只支持 GET**，报表是 POST → 必须 `fetch` + `response.body.getReader()` + `TextDecoder` 手动解析
2. **chunk 边界可能切断一行** → 行缓冲：`buffer += chunk; lines = buffer.split('\n'); buffer = lines.pop()`
3. **PERCENT 是消息 JSON 里的字段**，不是 `data: PERCENT: 90.0` 前缀行——首版按文档提取后示例推断前缀行就是错的，实测才发现（三步验证法 Step 2 的价值）。序列：0→10→30→40→50→70→90(空)→90(带数据)→100 结束；**100.0 结束消息实测存在**，流结束（done）兜底置 100%
4. **错误判别靠 res.ok + Content-Type**：`application/json` = 错误 JSON（复用认证拦截）；`text/event-stream` = 正常流。不要 JSON.parse 流首行去猜
5. **同类端点的行为也会不同**，别拿 API4 结论套 API5：BOOK_NO 空 = API4 的 HTTP 406，API5 却是 200 + SSE ERR「账簿不能为空」；API4 该端点不校验 PGM（写错照常返回），Online 端点只认 REPNO 字段名。每个端点单独实测
6. **无 Authorization → HTTP 200 + 空 body 永久挂起** → AbortController 120s 超时是必须的
7. **PAGE_COUNT 不可靠**（有数据为 0 / 无数据为 1）→ 一律客户端计算条数
8. **COLUMN_INFO 空数据时是 {} 对象**（不是数组）→ 前端 `Array.isArray` 防护
9. **SSE 可能是带空格的 pretty JSON**（`"ERR": "..."`）——不留空格的正则把 ERR 和行数全部漏报，曾因此误判整批实测结果（Round 59 教训，浪费一次参数矩阵复测才破案）
10. **Online 端点数据表键各异**：总分类账/科目余额表 = `RPT`，资产负债/利润表 = `MyTable`，现金流量表 = `RPT_CSHFROLIST`；COLUMN_INFO 是**嵌套对象** `{表键:[...]}` 且无 TITLE；数据行只出现在 PERCENT:100 消息
11. **配置驱动扩展**：REPORT_CONFIG 加 `apiMethod: 'getReportStream'` 专用分支 + `cfg.stream`（fixCondition @占位符模板）+ `buildStreamCtx`（日期/期间/公式派生）——同一套流解析代码服务 17 只报表

**经验**：流式 ≠ 普通 fetch。`res.json()` 直接失败；数据到达时机不可预知，UI 必须按事件驱动（onProgress/onData 回调）而不是等一个返回值。**文档只给提取后的 JSON 示例时，别推断原始流格式**。

### 3.6 服务端分页不可信 → 客户端分页（Round 31）

**坑**：翻页永远显示第一页数据。实测证据：`offset=[20,40]` 返回 40 条且 firstRow 和 `[0,20]` 一样——**服务端无视 offset 的 start 值**，end 直接当"返回前 N 条"。文档所有示例都是 `[0, 5000]`，没有任何非零 start 的例子（当时就该起疑）。

**方案**：查询/重置/切报表 → API 取全量 `[0, 5000]` → 缓存 `_allData` → 翻页纯客户端切片，零 API 调用即时响应；改每页条数从缓存重切。

**教训**：服务端能力不可假设。文档示例永远是 happy path，边界行为（非零 start）必须实测。

### 3.7 依赖端点链 & 异步竞态（Rounds 44/52/60）

有些报表的查询条件依赖**别的 API 提供的数据**，链式获取：
```
账簿清单 AccBook/GetList → BOOK_NO（总账 5 只必填）
账簿行 TYPE_NO → 报表样式 accRptStyle/getlist → RPT_NO（三财务报表）
制表公式 billcommon/GetAccRepNoList → REPNO（Online 资产负债/利润表）
```
（曾经以为 TYPE_NO 要查 AccType/getlist——**用户纠正后实测**：账簿行直接带 TYPE_NO，科目表只在需要 TYPE_NAME 时才查。依赖链以实测为准，不猜。）

三个经验：
1. **账簿一旦改变，样式清单必须重取**——缓存键要含 BOOK_NO 维度
2. **加载前置模式**（Round 60 定案）：先拉清单再渲染报表，拉取失败时面板从未打开、屏幕保持原状——比「先打开再报错」干净得多
3. **异步竞态两类解法**：
   - 回调里**不读全局状态**（`AppState.currentReport`），把 reportKey 当参数显式传——Round 52 样式下拉串报表的根因就是异步完成时全局已被切走
   - 序号守卫 `_openSeq`：快速切换报表时旧请求的迟到回调直接作废
4. **登出要清状态**：内存缓存的依赖数据（账簿/样式/公式）要世代计数作废（`_gen`），否则退出重登后下拉残留旧账套数据——Round 45 实测 bug

### 3.8 站点迁移与地址兼容（Round 62：SUNFUSION → ERPAPI）

服务方迁移规范落地经验：
1. **base 拆分**：登录与报表不再共用 base——`API_PATH = '/ERPAPI/api'`（报表）+ `AUTH_PATH = '/ERPAPI/auth/login'`（登录），全部收敛在一个文件，其余模块零改动
2. **43 个报表配置全是相对路径 → 站点迁移零改动**。当初坚持相对路径 + 统一拼接，这一刻全部回本
3. **normalizeHost 统一清洗**：用户 localStorage 里可能存着旧地址（`/SUNFUSION/API`）或各种粘贴形态（`/ERPAPI`、`/ERPAPI/api`、`/ERPAPI/auth/login`），一个正则全部洗成裸 host——**旧数据兼容要主动设计，别指望用户清缓存**
4. **新登录响应字段缺失**：新接口 data 只有 TOKEN/EXPIRES_IN/EXPIRES_TIMESTAMP，没有 USR/USR_NAME/COMPNO——代码用输入值/已存值兜底。**响应字段变少也是破坏性变更**
5. **迁移后全量健康扫描**：40 只报表逐一实测，发现 4 个依赖端点新站点 404（AccBook/accRptStyle/billcommon/mrppu）→ 穷举探测（大小写/GET-POST/其他站点）后判定服务端部署不全 → 用户修服务端后复测全通。**迁移 = 客户端改完 + 全量扫描 + 服务端核对，缺一不可**

### 3.9 "没数据"归因方法论（Rounds 54/63 血泪）

**rows=0 时先别下结论**，按顺序验证：
1. **先证明请求格式正确**——跨账套对照：AT04 在制成本汇总 0 行，AT02 同请求 119 行 → 格式对、是数据侧空表。单账套 0 行什么都证明不了
2. **跨站点对照**：新站点 monAA 0 行 + 旧站点同请求体也 0 行 → 是测试库没有收款流水，不是修复没生效
3. **同库同管道的其他报表出数**：invso 返回 9 行 → 证明登录/token/管道正常
4. **日期区间覆盖账套数据年份**：默认当前月查 2022 年的老账套必然 0 行——空是正确结果
5. **测试用 vm 加载真实代码构造请求体**，不要手工拼 body（手工拼的 8623 空壳误判浪费过一轮）

**建立测试库数据画像**（本项目实测）：AT01 有受订数据、无收款/付款/采购；AT02 有生产+总账全量；AT03/AT04 极简演示账套（0 账簿、缺 DB 函数）。知道每个账套有什么数据，测试预期才不会错。

### 3.10 双端问题定位（Round 63：DEP_ORG_PAY_TYPE）

服务方更新文档要求 fixCondition 必带 `DEP_ORG_PAY_TYPE: "1"`，客户端补上后报错**升级**了：

```
缺字段     → 10001 字典缺键（The given key was not present in the dictionary）
补字段后   → 10001 SQL 列名 'DEP_ORG' 无效（服务端认了键，但 SQL 引用了数据库不存在的列）
服务方修列 → code=0 ✅
```

**经验**：报错内容变化 = 排查进度的里程碑。缺键 → 客户端问题；认键但 SQL 错 → 服务端第二个问题。**服务方文档更新 → 调用方同步改动 → 实测验证闭环**，三个环节一个都不能省。附带发现：IIS 部署目录停在 15 天前 → 用户看到的"旧提示文案"根因是部署没跟上，不是源码没改（见 6.3）。

### 3.11 测试环境陷阱速查

- **token 短命**（EXPIRES_IN=60000，新旧站点都是）：登录 + 请求必须跑在同一个脚本进程内，内存里传 token；旧站点每个请求前都要新登录
- **token 单会话互踢**（同账套）：后续任何登录会立即作废先前的 token（20004）——每批测试前重新登录，别复用旧 token
- **PowerShell 5.1 给 curl.exe 传 JSON 吞引号** → 伪造「服务端拒绝字符串日期」假象：请求体一律写文件 + `--data-binary @文件`
- **vm 沙箱加载真实 js**：node vm 需要 console/localStorage/document/window/fetch/Api 桩，`var ReportEngine` 变成 `sandbox.ReportEngine`——构造出的请求体才是客户端真实请求体
- **REPORT_CONFIG key 大小写敏感**：`invSa` 不是 `invsa`（Unknown report 的另一种形态）

---

## 四、AI 多模型集成 — 最折腾的部分

### 4.1 架构演进

```
v1: SettingsStore.apiKey → DeepseekClient.call() → fetch Deepseek API
                         （单一模型，硬编码）

v2: SettingsStore.aiConfig.provider (选哪个)
     SettingsStore.aiConfig.keys.{deepseek|qwen|gemini|claude} (各自的Key)
                ↓
          AIClient.call() → 根据 provider 路由
                ↓
    ┌───────┬──────────┬──────────┬──────────┐
    │Deepseek│  QWen    │  Gemini  │  Claude  │
    │OpenAI  │ OpenAI   │ OpenAI   │ Anthropic│
    │compat  │ compat   │ compat   │ native   │
    └───────┴──────────┴──────────┴──────────┘
```

### 4.2 四个模型的 thinking mode 问题（最大的坑）

**现象**：切到多模型后，Deepseek 卡死/空回复，Gemini 输出自我检查文本，Claude 返回空响应。

**根因**：新一代模型（Deepseek V4、Gemini 3.x、Claude Sonnet 5）**默认开启 thinking mode**。

| 模型 | thinking 默认 | 表现 | 修复方式 |
|------|-------------|------|---------|
| Deepseek V4 Flash | **enabled** | 思维链消耗 `max_tokens` 预算 → 实际回复为空 → "卡死" | `thinking: { type: "disabled" }` |
| QWen Plus | disabled | 无问题 | 无需处理 |
| Gemini 3.6 Flash | **enabled（强制）** | 输出格式自检文本混入 `content`：`[format? Yes. Chart block: Valid...]` | `reasoning_effort: "none"` 被 API 拒绝(400)→ 仅靠 Prompt 抑制 |
| Claude Sonnet 5 | **enabled** | content 块 type 为 `"thinking"` 而非 `"text"` → `parseClaudeResponse` 取不到内容 | `thinking: { type: "disabled" }` + content 遍历解析 |

**核心教训**：
1. **升级模型 ID 之前，先查 thinking mode 默认行为**。新模型大概率默认开启
2. **每个模型禁用 thinking 的方式不同**：Deepseek/Claude 用 `thinking: { type: "disabled" }`，Gemini 用 `reasoning_effort: "none"`（但 OpenAI 兼容端点可能拒绝 → 400）
3. **120s 超时保护**是必须的（`AbortController`），防止请求永久挂起
4. **System Prompt 加 `严禁输出思考过程`** 作为兜底（Gemini 就是靠这个抑制的）

### 4.3 Claude 的 CORS 问题

**现象**：`Failed to fetch` — 网络请求根本没发出去。

**原因**：Anthropic API 默认阻止浏览器跨域请求。

**修复**：加一个特殊的 header：
```javascript
headers['anthropic-dangerous-direct-browser-access'] = 'true';
```
名字里的 "dangerous" 是 Anthropic 的警告 — API Key 在浏览器端暴露有风险。但 bring-your-own-key 场景可以接受。

### 4.4 统一客户端设计

```javascript
// 三个入口，屏蔽所有 provider 差异
AIClient.call(messages, onChunk)           // 对话（非流式）
AIClient.suggestQuestions(datasources)      // 推荐提问
AIClient.validateKey(provider, key)         // 验证 Key

// 内部路由
function sendRequest(provider, key, messages, opts) {
  var config = PROVIDERS[provider];
  // Deepseek/QWen/Gemini → buildOpenAIBody + parseOpenAIResponse
  // Claude → buildClaudeBody + parseClaudeResponse
}
```

### 4.5 向后兼容

```javascript
var DeepseekClient = AIClient;  // 旧代码引用不报错
```

旧版单 `apiKey` 字段首次读取时自动迁移到 `aiConfig.keys.deepseek`。

### 4.6 模型输出不可控 → 解析器当兜底网（Rounds 26/53）

提示词要求「必须使用 ```table 格式」，模型经常不遵守。与其抱怨模型，不如把解析器做成兜底网：

- **异构行表格**：`Object.keys(data[0])` 只认第一行的键 → 后续行的独有列全丢。必须取**所有行的键的并集**（Round 26：AI 输出"异常点1/2/3"列全丢事故）
- **裸 JSON 数组**：行首 `[` 开头 → 括号平衡扫描（字符串/转义感知）提取完整 JSON → 非空对象数组才渲染表格；防误伤四规则（代码围栏内不转、解析失败不动、非对象数组/单对象不动）
- **未闭合围栏兜底**：```` ```table ```` 开了不闭合 → 围栏行丢弃，下方 JSON 按裸 JSON 规则转
- **所有段落路径都要走渲染器**：chart 块**之前**的文本段落曾走错函数，导致闭合的 ```table 围栏被降级成代码块（Round 53 破案：用户真实回复是「加粗+表格围栏+chart 围栏+洞察」组合，字节级证据才找到）

**验证教训**：验证解析器必须**行为级验证**（`AIParser.parse('[{"a":1}]').html`），不能用 `typeof AIParser.bareJsonScan` 查私有函数——新旧代码都返回 undefined，引发"你确定改了吗"的误判（我给的验证命令错了，认）。

---

## 五、UI/UX — 看似简单但反复踩坑

### 5.1 侧边栏滚动 & 退出登录固定底部

**问题**：加了 40 个报表菜单项后，侧边栏高度超出屏幕，底部"退出登录"被挤出视野。

**错误做法**：整个侧边栏 `overflow-y: auto` → 底部按钮跟着滚动。

**正确做法**：4 段式 Flex 布局，只让导航区滚动：
```css
.sidebar { display: flex; flex-direction: column; overflow: hidden; }
.sidebar-brand { flex-shrink: 0; }   /* 固定 */
.sidebar-user   { flex-shrink: 0; }   /* 固定 */
.sidebar-nav    { flex: 1; overflow-y: auto; min-height: 0; }  /* 可滚动 */
.sidebar-footer { flex-shrink: 0; }   /* 固定 */
```

**关键**：`min-height: 0` — flex 子元素默认 `min-height: auto`，不会缩小到内容以下。不加这个，`overflow-y: auto` 不生效。

### 5.2 滚动条与深色侧边栏的色差

```css
.sidebar-nav::-webkit-scrollbar { width: 5px; }
.sidebar-nav::-webkit-scrollbar-track { background: transparent; }
.sidebar-nav::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.15); border-radius: 9999px; }
.sidebar-nav { scrollbar-width: thin; scrollbar-color: rgba(255,255,255,0.15) transparent; }
```

### 5.3 Toast 通知的三次迭代

**v1**：绿底大框 / 红底大框，全宽，居中 — "像系统报错"
**v2**：白卡 + 彩色左边框 + SVG 图标，右上角 — 好一些但还是太显眼
**v3（最终）**：暗色 Snackbar 风格，右下角浮起小条，3 秒自动消失

**教训**：操作反馈 ≠ 错误报警。成功提示应该低调（暗色小条），只有真正的错误才需要红色。

### 5.4 浏览器缓存 — 本项目最高频事故（漏 bump 四次）

**问题**：改了 CSS/JS，用户刷新页面还是老样子。整个项目周期至少发生 4 次（Round 25 新报表 Unknown report、Round 39 B17、Round 41 记事本回存、Round 55 mrpcf 无数据）。

**解决方案**：所有静态资源加版本号 query string：
```html
<link rel="stylesheet" href="css/dashboard.css?v=20260811h">
<script src="js/reports.js?v=20260916a"></script>
```

**教训升级版**：
- 开发阶段第一天就该加 cache buster（最初没有，导致多轮"改了但没生效"的无效沟通）
- **改任何 js/css，版本号 bump 是收尾动作的一部分**，与文档同步同级——Round 55 那次 3 个 JS 改了但版本号没动，白白排查一整轮，用户看到"代码已修、页面照旧"的假象
- 用户报 bug 时第一问：**跑的是源码还是安装包？强刷了吗（Ctrl+F5）？**——Round 55 排查一半才发现是缓存

### 5.5 其他 UI 细节

- **日期范围独占两列**：`grid-column: span 2`，不然两个 date input + 分隔符挤在一个 200px 列里会溢出重叠
- **Dashboard 锁定视口**：`height: 100vh; overflow: hidden`，内部子容器各自处理滚动，防止整页滚动
- **AI Tab 弹性布局**：`.tab-panel.active { display: flex; flex-direction: column; flex: 1; overflow: hidden; }` 让 Chat 面板自适应高度，不被 Task Bar 遮挡
- **流式进度条悬浮视口正中央**：`position: fixed` + 白卡片，不随表格滚动——第一版贴表格顶部细条被用户否决（"感觉上就像卡住了"），恢复原厚度只改位置才通过。**用户否决过的方案形态要记住，别重提**

### 5.6 三语 i18n（简/繁/英，Round 37-40 + 61）

**方案**：静态字典 + 大模型预生成（807 key × 2 语言，对账工具查缺失/僵尸）；语言值 = ERP 服务器 LangTag（zh-cn/zh-tw/en）；登录页可切换，默认按 OS 预判。

**四个关键经验**：
1. **LANG_ID 跟随 UI 语言**传登录接口（实测三语登录全 code:0），错误消息随语系返回
2. **动态渲染的内容没有 data-i18n 属性**（表头、分页、徽章是 JS 画的）→ 必须靠 `i18n:changed` 事件驱动重渲染。Round 40 的 bug：登录页切了语言，表头还是页面加载时的语言——修复 = 表头渲染收敛为唯一入口 + 事件监听
3. **服务端数据的语言随登录 LANG_ID，不随 UI 切换**——Online 报表资料格/下拉选项直出服务端简体。修复：固定词组白名单直翻（`ONLINE_VALUE_FIELDS` 逐字段白名单），**自由文字（ERP 科目名）绝不翻译**，防误翻
4. **下拉重渲染要保留用户已选值**（`cur || defaultValue`），不然语言一切用户的选择就丢了

### 5.7 localStorage 配额（Round 56）

**坑**：流式报表转入拿**全量**数据（分页参数对 SSE 无效），mrpcx 26996 行 = 10.19MB JSON → QuotaExceededError。

**三个认知**：
1. **配额按整个站点（origin）计**——单纯拆分成多卡片不解决，压缩才解决
2. **LZString compressToUTF16**：输出字符范围 0x20–0x7FFF（无控制符/代理区），直接嵌 JSON 无转义膨胀；compressToBase64 反而大 2.5 倍。10.19MB → 0.65MB（9.3x）
3. **透明压缩 + `_z` 标志**：存储层自动压缩/解压，调用方零改动、旧数据零迁移；>5000 行再拆多卡片 `_1/_2/_3`

### 5.8 配置驱动的报表菜单

报表 31 只后静态侧边栏找菜单吃力。方案：REPORT_CONFIG 加 group/icon/pinyin 字段 → 菜单动态渲染（搜索：中文模糊+拼音首字母+key 兜底；分组可折叠；收藏置顶）。**状态分层**：收藏持久化（localStorage），折叠态会话内内存态（刷新复位）——用户明确要求"登录/刷新后分组默认全折叠"。折叠态 56px 窄栏渲染收藏去重 + 组间分割线，纯视觉细节也要想清楚。

---

## 六、部署 & 运维

### 6.1 一键部署工具

**思路**：不是写一份 12 章的部署文档，而是写一个脚本。

```
install.ps1 -Mode quick     → 检测Python → 启动HTTP Server → 打开浏览器
install.ps1 -Mode iis       → 检测IIS → 复制文件 → 创建站点 → 开防火墙
install.ps1 -Mode package   → 打包为 ZIP
```

`启动.bat` 给不懂命令行的用户，双击即用。

**经验**：内网 Windows 环境，Python HTTP Server 是最可靠的零配置启动方式。IIS 作为正式部署方案。不要浪费时间写 Nginx/Apache 配置（目标用户不用）。

### 6.2 服务器地址输入简化

用户只需填 `http://192.168.1.100:8080`，系统自动追加 `/ERPAPI/api`（登录接口为 `/ERPAPI/auth/login`）。向前兼容：旧格式（`/SUNFUSION/API`）自动清洗（normalizeHost 正则）。

**经验**：能帮用户省掉的输入就省掉。固定路径系统自己拼——但**路径变了之后旧存值要主动清洗**，别指望用户自己改设置（Round 62 迁移教训）。

### 6.3 部署目录一致性（Round 63 教训）

用户看到"旧提示文案"，排查发现源码早就改对了，是 **IIS 部署目录停在 15 天前的 v1.11**。用户浏览器打开的是部署目录，不是源码。

**经验**：源码改了 ≠ 用户看到了。链路是「源码 → ?v= bump → 打包 → 部署目录/IIS」四环。用户报"代码没生效"时，按这条链逐环排查；**部署目录同步是修复动作的一部分**，不能只改源码。

### 6.4 测试环境数据画像（各账套实测地图）

每个测试账套的数据特性不同，行为逐套而异：
- AT01：文档示例账套，有受订数据、无收款/付款/采购流水；1 本账簿
- AT02：全量真实数据（生产+总账+Online 五端点全出数据）；5 本账簿
- AT03/AT04：极简演示账套，0 账簿（触发 Online 静默降级验证）、缺 DB 函数（FN_GET_ACCN_USR → 科目余额表 SQL 错，API 侧问题）

**经验**：把「哪套数据能验证什么」建成地图写进文档（总账报表调用方法.md 6.5 就有一张 AT01~AT04 × 5 端点的实测地图）。测试预期错了比没测试更浪费时间；**账套间端点行为逐套而异，别从一套推广到全部**（Round 59 两条旧结论被参数矩阵复测推翻）。

---

## 七、文件结构 & 文档纪律

### 7.1 最终文件清单

```
AsCoding/
├── index.html              ← 生产入口（零内联 CSS/JS）
├── ui-template.html        ← UI 原型模板（与 index.html 同步，唯一设计合约）
├── css/ (4 个文件)          ← auth / dashboard / ai-analysis / notepad
├── js/  (27 个文件)         ← 模块化 JS（依赖顺序加载）
├── 流程图/ (10 个 .drawio)   ← 业务流程图（Python 脚本生成）
├── scripts/build_drawio.py ← 流程图生成脚本（可复用）
├── screenshots/            ← 应用截图
├── install.ps1 / 启动.bat / deploy.ps1  ← 一键部署 + 打包
├── *.md 原始 API 文档 ×12   ← 制表API1-5、账簿/科目表/报表样式/制表公式清单、Online总账、API调整说明
├── 总账报表调用方法.md       ← 即拿即用速查（标准版+Online 版双路径整并）
└── 项目管理文档             ← 对话记录/进度追踪表/需求架构/流程图/报表明细清单/部署指南/API服务调用说明
```

### 7.2 CLAUDE.md 纪律体系（24 轮 → 63 轮演化）

```
编码前     → 查文档、做 PoC、列 checklist
模块完成后 → 对照流程图、覆盖所有路径、补齐边界
对话结束时 → 对话记录 ✅ | 计划书 ✅ | 进度表 ✅ | 流程图 ✅
```

**为什么需要这个**：24 轮对话时，如果不强制每轮结束更新文档，3 轮之后文档就和代码完全脱节。63 轮跑下来，这个纪律演化为更完整的命令体系：
- **【要开新对话】** → 赶紧同步未写对话/未更新的文档
- **【推送代码】** → ① 更新对话记录 ② 同步进度表/流程图/需求架构/README ③ 重新打包 ④ commit + push（Round 49 用户指定入档）
- **staged-work SKILL** → 把推送六步自动化（含垃圾清理、deploy.ps1 bump、zip 与 index.html 清单核对），听到关键词自动触发（Round 59 用户要求创建）
- **垃圾文件直接清理** → 0 字节杂散文件/乱码名文件当场删除，不留待办不等批准（Round 47 用户指定入档；根目录多次出现 PowerShell 编码产生的乱码空文件）

**最容易被漏的**：
- 对话记录（流水账，不是摘要）→ 每一轮都要追加
- 流程图 → 新增功能要更新 .drawio 文件（不是 .md！）+ 流程图.md Mermaid 源
- README → 报表数量、API 列表、模型列表、版本号（deploy.ps1/zip 引用散落 3-5 处）都要同步
- 进度追踪表 → 上轮任务行漏补是真实发生过的（Round 63 补 Round 62 的行）

### 7.3 ui-template.html 机制

**规则**：任何新界面/新组件 → 先在模板里做，用户浏览器确认 → 才写正式代码。Round 45 用户中途纠正再次确认：「涉及界面要先改 ui-template 我看效果」。

**价值**：
- 避免在 `index.html` 上反复修改导致代码混乱。模板是设计合约
- 用户的两次 UI 纠正（齿轮锚在登录卡片不是页面角落、公式栏位自动判断不让用户选场景）都发生在模板阶段——**在原型上改的成本是正式代码的十分之一**
- 模板的演示逻辑（demo 按钮）要用**同源镜像**实现（同函数、同 i18n key），否则模板确认的效果和生产行为脱节

---

## 八、问题清单（按严重程度排列）

| # | 问题 | 根因 | 影响 | 修复 |
|---|------|------|------|------|
| 1 | 登录连续 5 次 `code: 10001` | 字段名凭猜测（PSW/PWD、CPY/COMPNO），漏必填字段 | 阻塞项目启动 | API 三步验证法 |
| 2 | 多模型 AI 全部异常（卡死/空回复/自检文本） | 新模型默认开启 thinking mode | 4 个模型全部不可用 | 逐个禁用 thinking + Prompt 抑制 |
| 3 | Claude `Failed to fetch` | Anthropic API 阻止浏览器 CORS | Claude 完全不能用 | `anthropic-dangerous-direct-browser-access` header |
| 4 | 浏览器缓存旧代码（**发生 4 次**） | 改 js 漏 bump `?v=` 版本号 | 多轮"改了但没生效"、白排查 | cache buster + 「改 js 必 bump」收尾纪律 |
| 5 | 翻页永远显示第一页 | 服务端无视 offset 的 start 值 | 分页功能完全失效 | 客户端全量取数 + 切片分页 |
| 6 | 流式报表解析错误 | 按文档示例推断原始流格式（PERCENT 前缀行是错的）；SSE pretty JSON 空格让正则漏报 | 首版解析白写；整批实测结论误判 | 先抓真实响应再写解析；正则容忍空格 |
| 7 | 长连接端点无 Authorization 永久挂起 | 服务端不返回错误，空 body 挂着 | 查询无限转圈 | AbortController 120s 超时 |
| 8 | 报表 404 | 文档 URL 的 `api/` 段被重复拼接（Base 已含）——**同类错误犯两次** | 总分类账 404 | 所有新端点只认「相对 Base 的路径」，文档 URL 前缀与 Base 比对后再落代码 |
| 9 | 转入数据源配额溢出 | 流式报表全量 26996 行 = 10.19MB 超 localStorage 配额（按站点计） | 转入直接报错 | LZString 透明压缩 + 超 5000 行拆卡 |
| 10 | 退出重登账簿下拉残留旧账套 | 登出不清 DOM/状态 + 内存缓存跨登出存活 | 显示别的账套的账簿 | 世代计数作废 + 登出重置 + 登录走 openReport 全链路 |
| 11 | 样式下拉串报表（利润表显示资产负债表的样式） | 异步回调读全局 currentReport，完成时已被切走 | 下拉内容错乱 | 显式传参 + `_openSeq` 序号守卫 |
| 12 | 老账套查报表必空 | 快捷日期操作符（last_year/this_month）锁死当前年，服务端行为按报表而异 | 生产 9 只报表 3 只失效 | 日期条件一律 range 走用户输入 |
| 13 | 站点迁移后 4 个依赖端点 404 | 服务端新站点部署不全 | 总账/成本分析/公式下拉全断 | 全量健康扫描 + 穷举探测 + 服务方修复 |
| 14 | monAA/monBA 新站点报错 | 双端问题：客户端缺字段（字典缺键）→ 服务端 SQL 引用不存在列 | 收款/付款明细表不可用 | 补 `DEP_ORG_PAY_TYPE` + 服务方修列 + 复测闭环 |
| 15 | 用户看到旧界面 | IIS 部署目录停 15 天未同步 | 误报"没修复" | 部署目录同步纳入修复流程 |
| 16 | 侧边栏滚动条白底 / 退出被挤出屏幕 / 日期重叠 / AI Tab 遮挡 / Toast 像报错 | 见 5.1-5.5 | 视觉与可用性问题 | 各见对应小节 |

---

## 九、如果重来一次 — 核心原则清单

### 9.1 API 对接

1. **绝不凭记忆写请求字段**。每次打开 API 文档逐字段对照
2. **先 curl 通，再写代码**。Postman/curl 通过的请求体直接拷进代码做模板
3. **成功判断看业务码**（`code === 0`），不是 HTTP 状态码；错误形态要实测归类（10001/20004/406/ERR/挂起）
4. **端点大小写以实测为准**，文档可能写错；相对路径只认「相对 Base 的部分」，绝不照抄文档 URL 前缀
5. **相似≠相同**。40 只报表结构类似但细节差异多 → 配置驱动，不复制粘贴；if 超过 2 次就抽配置
6. **流式 API 用 fetch ReadableStream，别想 EventSource**。PERCENT 是消息 JSON 里的字段不是前缀行；判别靠 res.ok + Content-Type；120s 超时；同类端点行为逐个实测别套用
7. **服务端分页不可信**。offset start 被无视过——文档示例全是 happy path，边界行为实测
8. **依赖端点链**：账簿→样式→公式层层依赖；账簿变了清单必须重取；拉清单用「加载前置」模式，失败时屏幕保持原状
9. **异步回调不读全局状态**。传参 + 序号守卫；登出清缓存/状态（世代作废）
10. **站点迁移三件事**：base 拆分（登录/报表分开）、旧存值主动清洗、全量健康扫描
11. **"没数据"归因顺序**：先证明请求格式正确（跨账套/跨站点对照）→ 再归因数据侧；建立测试库数据画像
12. **服务方文档更新 → 调用方同步 → 实测闭环**；报错内容变化 = 排查进度的里程碑（缺键→客户端，认键后 SQL 错→服务端）
13. **token 短命且互踢**：登录+请求同进程，批测前重登

### 9.2 AI 模型集成

14. **换模型先查 thinking mode 默认值**。新一代模型大概率默认开启
15. **每个模型配独立的 body builder 和 response parser**，不要在通用路径里加 `if provider === 'xxx'`
16. **120s 超时 + AbortController** 是底线保护
17. **System Prompt 加 `严禁输出思考过程`** 作为兜底
18. **验证 Key 时调真实 API**，不要只检查格式
19. **模型输出不可控 → 解析器当兜底网**：异构行取键并集、裸 JSON/未闭合围栏兜底、所有段落路径都走渲染器；验证用行为级断言

### 9.3 前端工程

20. **第一天就加 cache buster**；**改 js 必 bump 版本号**（收尾动作，本项目漏了 4 次）
21. **Flex 布局 + `min-height: 0`** 是滚动区域的正确打开方式
22. **固定底部的元素加 `flex-shrink: 0`**，不然会被挤出视野
23. **纯前端项目不需要框架**（尤其是表单+表格类的），但需要严格的模块加载顺序
24. **CSS 变量统一设计 Token**；新增样式独立成文件，靠加载顺序解决优先级
25. **localStorage 配额按站点计**，拆分不能解决、压缩才能；透明压缩 + `_z` 标志 = 调用方零改动
26. **i18n 动态内容靠事件重渲染**（`i18n:changed`）；服务端数据语言≠UI 语言 → 白名单翻译防误翻；下拉重渲染保留已选值

### 9.4 流程纪律

27. **每轮对话结束 = 文档更新**。不拖，不"等一下补"
28. **先用 ui-template.html 做原型，用户确认再写代码**；原型演示逻辑与生产同源镜像
29. **新模块做完追问三个边界**：空输入怎样？网络断了怎样？有另一条路径到达这里吗？
30. **流程图用脚本生成**（Python → .drawio XML），不要手绘。后续改一个配置跑脚本重新生成
31. **部署工具 > 部署文档**。写一个 `install.ps1` 比写 12 章文档有用得多
32. **垃圾文件当场删**。0 字节/乱码名/临时文件，不留待办不等批准
33. **收尾走 staged-work 六步**：对话记录 → 文档同步 → 垃圾清理 → README/部署指南 → 打包核对 → commit+push
34. **源码改了 ≠ 用户看到了**。部署目录同步是修复的一部分；用户报 bug 先确认跑的是源码还是安装包、强刷没有

### 9.5 沟通

35. **用户说"差不多"的时候 = 需要逐项对照确认**
36. **不要说"应该是..."，说"文档写的是..."或"实测结果是..."**
37. **用户批评文档没更新 → 立刻补，不要辩解**
38. **用户实测反馈 → 先定位根因再动手**（"先诊断，经确认后动手"是多轮默认流程）；用户的判断可能比我先对（TYPE_NO 链路、账簿依赖分量都是用户纠正的）
39. **我给的验证命令错了要当场认**（typeof 私有函数那次），别让错误的验证方式浪费用户一轮

---

## 十、数据一览

| 指标 | 数值 |
|------|------|
| 对话轮次 | 63 轮（2026-08-10 ~ 09-16） |
| JS 模块 | 27 个 |
| CSS 文件 | 4 个（auth / dashboard / ai-analysis / notepad） |
| HTML 文件 | 2 个（index 生产 + template 原型） |
| 报表数量 | 40 只（8 分组：进销存/财务管理/库存/采购价格/生产制造/人力资源/固定资产/总账） |
| API 调用模式 | 4 种（getReport / getList / GetReportStream 标准版 / GetReportStream Online 降级版） |
| AI 模型 | 4 个（Deepseek / QWen / Gemini / Claude） |
| i18n | 807 key × 2 语言（zh-tw / en），0 缺失 |
| 业务流程图 | 10 张 .drawio（Python 脚本生成）+ 流程图.md Mermaid 源 |
| 筛选面板布局 | 8+ 种（配置驱动动态切换） |
| 部署方式 | 3 种（quick / IIS / package ZIP），当前部署包 v1.13（32 文件） |
| 依赖端点链 | 3 条（账簿清单 / 报表样式 / 制表公式） |
| 项目文件总数 | ~60+（含文档、截图、流程图、部署包） |

---

*2026-08-11 首版（24 轮）· 2026-09-23 更新至 63 轮 · Sunlike ERP 报表查询系统*
