# 需求架构文档

> 项目：Sunlike ERP 报表查询系统
> 创建日期：2026-08-10
> 最后更新：2026-08-20（Round 57：登录页系统设置入口 + 登录前置检查）

---

## 一、项目概述

为 Sunlike ERP 系统构建一个 Web 报表查询平台，统一查询以下 40 类报表（8 个分组）：

| # | 分组 | 报表名称 | API 端点 | PGM 标识 |
|---|------|---------|---------|---------|
| 1 | 进销存 | 采购报表 | `POST /invpo/getReport` | `REP_POLIST` |
| 2 | 进销存 | 进货报表 | `POST /invpc/getReport` | `REP_PCLIST` |
| 3 | 进销存 | 受订报表 | `POST /invSO/getReport` | `REP_SOLIST` |
| 4 | 进销存 | 销货报表 | `POST /invSa/getReport` | `REP_SALIST` |
| 5 | 财务管理 | 收款明细表 | `POST /monAA/getReport` | `REP_RTLIST` |
| 6 | 财务管理 | 付款明细表 | `POST /monBA/getReport` | `REP_PTLIST` |
| 7 | 财务管理 | 科目预算报表 | `POST /ACCABGT/GetReport` | `REP_ACCABGTLIST` |
| 8 | 财务管理 | 信用额度查询表 | `POST /Rptsarplist/GetReport` | `RPTSARPLIST` |
| 9 | 财务管理 | 报销报表 | `POST /monbx/getReport` | `REP_BXLIST` |
| 10 | 财务管理 | 员工借款报表 | `POST /monjk/getReport` | `REP_JKLIST` |
| 11 | 财务管理 | 应收票据报表 | `POST /monCA/getReport` | `REP_CALIST` |
| 12 | 财务管理 | 应付票据报表 | `POST /monCB/getReport` | `REP_CBLIST` |
| 13 | 库存管理 | 过期货品预警表 | `POST /rptinvdo/getReport` | `RPT_INVDO` |
| 14 | 库存管理 | 安全存量预警表 | `POST /rptinvdl/getReport` | `RPT_INVDL` |
| 15 | 库存管理 | 负库存预警表 | `POST /rptinvswa/getReport` | `RPT_INVSWA` |
| 16 | 库存管理 | 库存调拨报表 | `POST /invic/getReport` | `DRPIC_REP` |
| 17 | 库存管理 | 库存调整报表 | `POST /invij/getReport` | `REP_IJLIST` |
| 18 | 采购与价格 | 送货单报表 | `POST /scmdrpti/getReport` | `REPTILIST` |
| 19 | 采购与价格 | 采购交货状况表 | `POST /InvPoPcStatus/GetReport` | `INVPOPCSTATUS` |
| 20 | 采购与价格 | 委外交货状况表 | `POST /InvTwPcStatus/GetReport` | `INVTWPCSTATUS` |
| 21 | 采购与价格 | 采购政策价格表 | `POST /invhp/GetReport` | `REP_HPLIST` |
| 22 | 采购与价格 | 售价政策价格表 | `POST /invhs/GetReport` | `REP_HSLIST` |
| 23 | 生产制造 | 工单完成情况表 | `POST /MrpPK/GetReport` | `MRPPK` |
| 24 | 生产制造 | 完工入库报表 | `POST /mrpafc/GetReport` | `MRPPS` |
| 25 | 生产制造 | 产品成本分析表 | `POST /mrppu/getList` ⚠️ | `MRPPU` |
| 26 | 生产制造 | 领退补料报表 | `POST /mrpag/getReport` | `REP_MLLIST` |
| 27 | 生产制造 | 单位成本分析表 | `POST /mrpcf/getReport` | `MRPCF` |
| 28 | 人力资源 | 员工年度薪资清册 | `POST /rptWagCG3/getReport` | `REP_WAGCG3` |
| 29 | 人力资源 | 人事资料分析表 | `POST /rptwagyg/getReport` | `REP_WAGYG0` |
| 30 | 人力资源 | 员工明细表 | `POST /rptwagyg/getReport` | `REP_WAGYG` |
| 31 | 固定资产 | 财产目录 | `POST /fixaa/getReport` | `FIXCE` |
| 32 | 总账报表 | 总分类账 | `POST /accGeneralLedger/GetReportStream` | `ACCRPTGL` |
| 33 | 总账报表 | 科目余额表 | `POST /accBalanceTable/GetReportStream` | `ACCRPTABT` |
| 34 | 总账报表 | 资产负债表 | `POST /accRptPreview/GetReportStream` | `CUS_ACC_RPT__2_ZCFZB` |
| 35 | 总账报表 | 利润表 | `POST /accRptPreview/GetReportStream` | `CUS_ACC_RPT__3_LRB` |
| 36 | 总账报表 | 现金流量表 | `POST /accRptPreview/GetReportStream` | `CUS_ACC_RPT__4_XJLL` |
| 37 | 生产制造 | 物料分析明细表 | `POST /mrpcu/GetReportStream` | `MRPCU` |
| 38 | 生产制造 | 在制成本明细表 | `POST /mrpct/GetReportStream` | `MRPCT` |
| 39 | 生产制造 | 在制原料明细表 | `POST /mrpcx/GetReportStream` | `MRPCX` |
| 40 | 生产制造 | 直接原料明细表 | `POST /mrpce/GetReportStream` | `MRPCE` |

## 二、技术架构

### 技术选型 ✅

- [x] 前端框架：**纯 HTML + 原生 JS**（无框架依赖）
- [x] 是否需要后端中间层：**纯前端直连 API**
- [x] UI 样式方案：**Inter 字体 + 设计 Token 体系**（ui-ux-pro-max Skill）
- [x] 图表库：**Chart.js 4.x**（CDN）
- [x] AI 模型：**Deepseek Chat V3**（非流式调用）

### API 层

- **Base URL**：`http://localhost/SUNFUSION/API`（用户可配置服务器地址，系统自动追加 `/SUNFUSION/API`）
- **认证**：Bearer Token，通过 `/user/login` 获取
- **成功判断**：`response.code === 0`（❌ 不是 `response.ok`）
- **请求格式**：JSON，Content-Type: application/json

### 文件结构

```
index.html              ← 生产入口（完整 Dashboard + 40 报表 + AI 分析 + 记事本）
ui-template.html        ← 设计原型/模板（与 index.html 同步）
css/auth.css            ← 登录页样式
css/dashboard.css       ← Dashboard 布局、侧边栏、筛选、表格、分页
css/ai-analysis.css     ← AI 分析 Tab、Chat、设置弹窗
css/notepad.css         ← 记事本 Tab 面板、ds-footer 双按钮覆写（在 ai-analysis.css 后加载）
js/i18n.js              ← I18n 核心（boot/getLang/setLang/t/applyStatic/detectOSLang/audit，ES5 IIFE）
js/i18n-data.js         ← 三语静态字典（zh-tw/en 各 628 条，简体原文即 key，末尾同步 I18n.boot()）
js/auth.js              ← 认证模块（登录/登出/Token 管理/会话恢复，LANG_ID 动态化）
js/api.js               ← API 客户端（fetch 封装、Auth 注入、错误拦截）
js/reports.js           ← 报表引擎（40 报表配置、SEARCH_INFO 构造、动态表格渲染）
js/utils.js             ← 通用工具（Toast、HTML 转义、时间格式化、剪贴板、深拷贝）
js/dialog.js            ← 应用内 confirm/prompt 弹窗（替换原生弹窗，随语言）
js/settings-store.js    ← 设置数据管理（API Key、服务器地址本地持久化）
js/datasource-store.js  ← 数据源存储（localStorage CRUD，上限 20 个）
js/datasource-list.js   ← 数据源列表 UI（卡片渲染、筛选摘要、事件绑定）
js/book-store.js        ← 账簿清单存储（AccBook/GetList，内存缓存 + localStorage 按公司隔离）
js/notepad-store.js     ← 记事本存储（localStorage CRUD，key sunlike_notepad，上限 50 个）
js/notepad-ui.js        ← 记事本 UI（事件卡片渲染、保存/加载/删除/清空）
js/tabs.js              ← Tab 页签切换（数据查询 / AI 数据分析 / 记事本）
js/chat-ui.js           ← Chat 界面渲染（消息气泡、操作按钮、滚动控制、历史恢复）
js/chat-core.js         ← Chat 核心逻辑（发送流程编排、输入框事件）
js/ai-client.js         ← 多模型 AI 客户端（Deepseek/QWen/Gemini/Claude 四 provider）
js/ai-parser.js         ← AI 响应解析（Markdown/```table```/```chart``` 标记 + 裸 JSON/```json``` 围栏 → 表格，Round 53）
js/ai-chart.js          ← Chart.js 图表渲染（bar/line/pie/doughnut）
js/export.js            ← 导出模块（Excel CSV / HTML / PDF / PPTX）
js/settings-ui.js       ← 设置面板 UI（模态弹窗、Key/服务器验证）
js/app.js               ← 应用主入口（AppState、查询/分页、侧边栏、初始化）
```

### 登录 API ✅ 已验证

```
POST http://localhost/SUNFUSION/API/user/login
Content-Type: application/json

{
    "COMPNO": "AT01",       // 公司代码（⚠️ 不是 CPY）
    "USR": "SAN",           // 用户名  （⚠️ 不是 username）
    "PWD": "",              // 密码    （⚠️ 不是 PSW/password）
    "LANG_ID": "zh-cn",     // 语言    （⚠️ 必填！）
    "SYS_TYPE": "ERP"       // 系统类型（⚠️ 必填！）
}

// 成功响应：{ code: 0, message: "RCID=USER_LOGIN_SUCCESS", data: { TOKEN: "..." } }
// Token 存放路径：response.data.TOKEN
```

> 🔴 PoC 教训：初版请求漏了 `LANG_ID` 和 `SYS_TYPE` 两个必填字段，且字段名写错（PSW→PWD, CPY→COMPNO），导致连续 5 次 `code: 10001`。

### 通用请求结构

```
POST /{module}/getReport
Headers: Content-Type: application/json, Authorization: Bearer {TOKEN}
Body: { PGM, SEARCH_INFO: [...], DISPLAY_FIELDS: "..." }
```

### 响应结构

```
{ code: 0, message: "Success", data: { REPORT__TAB: [...], COLUMN_INFO: {...}, COLUMN_PROP: {...} } }
```

**⚠️ 特殊响应结构（产品成本分析表 MRPPU）**：

```
{ code: 0, message: "Success", data: { TRANS: [...], COLUMN_INFO: [...], PAGE_INFO: {...} } }
```

- 数据路径：`data.TRANS`（不是 REPORT__TAB）
- 列信息：`data.COLUMN_INFO` 扁平数组（不是 `COLUMN_INFO.REPORT__TAB`）
- 分页：`data.PAGE_INFO`（PAGE_SIZE / CURRENT_PAGE / TOTAL）

### 两种 API 调用模式

| 模式 | 端点 | 请求体特征 | 报表 |
|------|------|-----------|------|
| **getReport**（标准） | `/{module}/getReport` | PGM + SEARCH_INFO[0]=分页 + DISPLAY_FIELDS 顶层 | 30 个报表（全部除 MRPPU 与长连接 9 只） |
| **getList**（MRPPU 专用） | `/mrppu/getList` | PGM + OTHERINFO + PAGE_INFO + SEARCH_INFO[0]=showBody + 无 DISPLAY_FIELDS | 产品成本分析表（1个） |
| **getReportStream**（长连接 SSE） | `/{module}/GetReportStream` | PGM + SEARCH_INFO[0]=展示配置 + [1]=fixCondition + 顶层 DISPLAY_FIELDS，`fetchStreamReport` 流式解析 | 总分类账 + API5 八报表（9 个） |

> 详细说明见 `API服务调用说明文档.md` 第六节。

## 三、功能需求

参照截图分析：

1. **登录页面** - 用户名/密码登录；**登录卡片（白色窗体）右上角系统设置齿轮**（与主界面齿轮同款，Round 57）：其他机器浏览器首次访问 localStorage 为空时，先配置服务器地址；**登录前置检查**：按下登录时若系统设置未正确配置（serverUrl 未保存/格式非法）→ 不发起登录请求，loginError 提示「请先进行系统设置，配置服务器地址后再登录」并自动打开设置弹窗引导配置
2. **侧边栏导航** - 40 个报表模块切换（8 分组：总账报表 5 + 进销存 4 + 财务管理 8 + 库存管理 5 + 采购与价格 5 + 生产制造 9 + 人力资源 3 + 固定资产 1）；**配置驱动动态渲染**（搜索/折叠/收藏置顶，见 4.8）
3. **报表查询区** - 日期范围选择、筛选条件（厂商/客户、货品、部门、仓库、审核状态）
4. **数据表格** - 列头、排序、分页
5. **可能**：导出功能、汇总统计
6. **AI 数据分析** - 将查询结果转入 AI 工作区，通过 Deepseek LLM 对话式分析数据

---

## 四、AI 数据分析功能

### 4.1 功能概述

用户可将报表查询结果转入 AI 数据分析工作区，通过自然语言与 Deepseek LLM (deepseek-chat V3) 对话，实现数据梳理、趋势分析、图表绘制、表格制作。

### 4.2 页签系统

在主内容区顶部提供三个平行页签：
- **数据查询**：现有 40 类报表查询功能
- **AI数据分析**：AI 对话分析工作区
- **记事本**：将当前数据源 + 对话打包保存为事件，点击卡片一键恢复（localStorage key `sunlike_notepad`，上限 50 个）

### 4.3 数据源转入

- 数据查询 Tab 中新增"查转入数据源"按钮
- 将当前查询结果（完整 REPORT__TAB 数据 + 列元数据 + 筛选条件）保存为数据源
- 数据源存储在 localStorage，上限 20 个
- 转入后自动切换到 AI 数据分析 Tab
- **0 笔拒绝**（Round 48）：查询结果为 0 笔时不执行转入——前置快查（最近一次查询结果为空 + 报表/筛选快照未变）直接拒绝、不发请求；查询后兜底（重查仍 0 笔 → 撤销占位数据源、切回查询页）；统一 toast「查无数据，所以无法为您进行转入数据源」（三语）
- **超大拆分 + 透明压缩**（Round 56，用户选定）：行数 > 5000 拆多张卡片，名称后缀 `_1/_2/_3…`（升序 _1 在前，一次性 saveAll，20 卡上限不变）；存储层 LZString UTF16 透明压缩（data JSON > 50KB → 压缩存串 + `_z:1`，getAll 解压还原调用方无感知，旧格式兼容；LZString 缺失降级原样）；配额不足友好中文报错。实测 mrpcx 26996 行 10.19MB → 6 卡共 0.65MB。**注意：流式报表转入拿全量（无分页），getReport 转入受 pageSize 5000 封顶——拆分只触发于流式报表超大场景**；记事本快照同款全容器压缩（4.9 节）

### 4.4 AI 对话工作区

- 左侧面板（30%）：数据源列表，卡片式展示，支持删除和清空
- 右侧面板（70%）：Chat 对话界面
  - 用户消息气泡（蓝底白字，右对齐）
  - AI 消息气泡（白底边框，左对齐）
  - 支持 Markdown 渲染、数据表格、Chart.js 图表
  - 操作按钮：复制、延申问答、导出 Excel、导出 PPTX

### 4.5 Deepseek 集成

- API：`POST https://api.deepseek.com/v1/chat/completions`
- 模型：`deepseek-chat`
- System Prompt 自动注入所有数据源摘要（字段列表 + 前 20 行样本）
- 支持 `table` 和 `chart` 标记语言
- **表格识别规则（Round 53 扩展，防模型不遵守 ```table 指令）**：① ```table 围栏内 JSON；② 裸 `table\n[JSON]` 模式；③ ```json 围栏内非空对象数组；④ 行首 `[` 开头的裸 JSON 数组（含缩进）。防误伤：代码围栏内不转换、解析失败/非对象数组/单对象原样保留
- **Round 53 实测修复（浏览器验证）**：① ```chart``` 块**之前**的文本同样走表格渲染（原实现走 renderMarkdown，chart 前的 ```table 围栏会降级成代码块——真实 AI 回复常是「表格 + 图表」组合，此路径必踩）；② 未闭合的 ```table/```json 围栏兜底：不进入跳过态、围栏行丢弃，下方 JSON 按裸 JSON 规则转换

### 4.6 设置面板

- 模态弹窗形式
- Deepseek API Key 配置 + 验证测试
- ERP 服务器地址配置 + 验证测试
- 首次使用自动弹出
- 设置存储在 localStorage
- **入口（Round 57）**：主界面齿轮 + **登录卡片右上角齿轮**（同款按钮，绝对定位锚在 `.login-card`，随登录页一起隐藏）+ 登录被配置检查拦截时自动打开
- **登录前置检查（Round 57）**：`SettingsStore.isConfigured()`——设置已保存 且 serverUrl 非空、`http://`/`https://` 开头才算配置正确；登录提交时未通过 → 拦截（不发请求）+ 提醒 + 自动弹设置面板；纯本地判定，离线可用

### 4.7 技术实现

- Phase A：单体原型在 `ui-template.html`（inline CSS/JS）
- 图表库：Chart.js 4.x（CDN）
- 数据存储：localStorage
- 导出：CSV 格式 Excel，HTML 报告降级 PPTX

### 4.8 报表菜单模块（搜索 / 折叠 / 收藏置顶）

解决报表数量增长后的菜单定位问题，侧边栏从手写静态 HTML 改为 **REPORT_CONFIG 驱动的动态渲染**：

- **单一数据源**：每份报表配置含 `name`/`group`/`icon`/`pinyin`，第 41 个报表只需在 `js/reports.js` 加一条配置即自动出现在菜单（不在 MENU_ORDER 的新 key 兜底追加到其分组末尾）
- **搜索**：中文名模糊 + 拼音首字母匹配（`name/pinyin` 两路 contains，key 匹配仅作全无命中时的开发者兜底）；`Ctrl+K` 聚焦（侧边栏折叠时自动展开；登录页不响应）、`Esc` 清空
- **分组折叠**：7 个分组可折叠（点击标题 / Enter / Space）；**默认全部折叠**（登录/刷新即复位）——存"已展开名单" `_expandedGroups` 会话内内存态，简洁利落
- **收藏置顶**：条目右侧 ★/☆，收藏后置顶显示在"★ 收藏"区（分组折叠时依然可见）；localStorage key `sunlike_report_menu`（`{favorites:[...]}`）持久化，读取时按 getReportKeys 剪枝、损坏 JSON 容错
- **折叠侧边栏（56px）**：折叠态忽略分组折叠状态，全部报表 emoji 平铺 + 组间 1px 细分割线，收藏去重置顶（不重复显示），分组标题不渲染；侧边栏切换即时重渲染（initSidebarCollapse → ReportMenu.render）
- **渲染架构**：`js/report-menu.js` 一条 `render()` 路径覆盖 搜索×折叠×收藏×active 四种状态；容器级事件委托（动态节点一次绑定）；active 按 `AppState.currentReport` 全量写入（收藏区 + 分组内两份条目同亮）
- **模块分工**：`js/report-menu-store.js`（状态持久化）+ `js/report-menu.js`（渲染/交互）；`js/app.js` 的 `openReport()` 为报表切换统一入口（config 取名、更新筛选面板/表头、执行查询）

### 4.9 记事本模块（多数据源快照，Round 41 升级）

- **事件模型**：`{ title, dataSources, activeDSId, chatHistory, conversationCount }` —— `dataSources` 为**全部数据源快照数组**（含 loading/error 状态）；`activeDSId` 记录保存时选中的源，回存时恢复选中态
- **保存**（`NotepadUI.saveCurrent`）：列表空 → toast 拦下；快照保存瞬间重新读取 `DataSourceStore.getAll()` 深拷贝，避免 prompt 期间状态漂移
- **回存**（`loadEventConfirmed`）：`DataSourceStore.saveAll()` 全量写入——**不能用 `add()`**（add 会强制 `status='ready'` 且重新生成 id，破坏快照保真）；`activeDSId` 校验存在性后恢复（缺失回退第一个）
- **旧事件兼容**：Round 40 之前的事件为单数 `dataSource` 字段，`getEventSources()` 统一迁移为 `[dataSource]`，读取/回存两端透明
- **容量边界**：50 事件 × 多源快照（每个源最多 5000 行）可能触及 localStorage 5MB 上限，由 quota 异常 toast 兜底；Round 56 起快照全容器 LZString 压缩（>50KB 信封 `{_z:1,d}`，实测 26996 行快照 10.19MB → 0.55MB），容量压力大幅缓解

### 4.10 总账报表模块（长连接 SSE + 账簿下拉，Round 45 实施）

- **菜单**：侧边栏**新增独立分组【总账报表】排最上面**（`MENU_GROUPS` 首位），总分类账 config `group: '总账报表'`、`icon: '📗'`，加配置即自动入菜单
- **配置驱动**：REPORT_CONFIG `apiMethod` 多态（getReport / getList / **getReportStream**）+ `needsBook` 标志；`accgl` 配置 `apiPath: 'accGeneralLedger/GetReportStream'`（⚠️ 相对 /SUNFUSION/API，**不带 api/ 前缀**——原文档 URL 里的 /api/ 段就是 API 本身；浏览器实测带前缀拼成 /API/api/... → 404）、`pgm: 'ACCRPTGL'`
- **流式查询**（`Api.fetchStreamReport`）：POST + fetch + ReadableStream + TextDecoder 行缓冲（跨 chunk），AbortController 120s 超时；三步判别：`res.ok`（BOOK_NO 空 → HTTP 406）→ Content-Type application/json（错误信封 + Auth 拦截）→ text/event-stream 逐行 `data: {JSON}`；消息 `{CODE, PERCENT, TITLE, ERR, DATA}`：ERR 非空抛错、PERCENT 驱动进度条（`|| 0` 不吞 0.0 首消息）、`DATA.REPORT__TAB` 累积数据行
- **请求构造**（`buildRequestStream`）：会计期间 YYYY-MM 派生 ACC_IPERIOD_B/E、YEARS/IPERIOD、START_DD（dateB+T00:00:00）、DATE_B/E；fixCondition 25 字段含 BOOK_NO（**铁律必填，前端非空拦截**）、CUR_ID、TYPE_NO '01' 等；[0] 展示配置 + [1] fixCondition + [2] orderBy {SYS_DATE: asc} + DISPLAY_FIELDS
- **账簿清单**（`BookStore`，IIFE）：登录成功 + 会话恢复两处后台 `prefetch()`（不阻塞登录）；`ensureLoaded()` 内存缓存直返 / 失败重试一次；数据路径 `data.ACC_BOOK_BS`（⚠️ 不是 REPORT__TAB）
- **0 账簿拦截**：`openReport` needsBook 预检（唯一收口）+ `doQuery` `_ledgerBlocked` 兜底——拉取失败 → toast 不切换；`code===0` 且空 → `Dialog.alert`（新增方法，仿 confirm）「你的账套没有启用总账，所以你不能操作这个查询」+ **【查询】【转入】按钮禁用**，不调流、不切换；切其他报表自动恢复
- **下拉绑定**：`select#filterBookNo` 显示 `BOOK_NO · NAME`，>1 预选第一个；config 用 `filterId` 绑定（⚠️ `fieldToKey` 里 BOOK_NO 已被映射到 filterMrpNo，必须 filterId 避开）
- **进度条 UI**：`.stream-progress` 白卡片 `position:fixed` 悬浮**视口正中央**（top/left 50% + translate，z-index 1000，不随表格滚动）；TITLE 原样显示不翻译（服务器端混合 i18n key 与中文直文）
- **摘要类型映射**（Round 46）：`REM_TYPE` 值映射语义文字——`"1"`=期初余额 / `"2"`=本期合计 / `"3"`=本年合计（用户确认语义，API 文档 9.1）；表格列彩色 badge 展示（badge-info/warning/success，模板先行确认）；**转入数据源时行数据 REM_TYPE 同样替换为映射文字**（AI 读到「期初余额」而非「1」）；三语 key 入 i18n-data
- **金额格式化**：AMTN_D/C/BAL、AMT_D/C/BAL、UP_D/C/BAL 入 DECIMAL_FIELDS；金额列入 CURRENCY_FIELDS

### 4.11 API5 八报表模块（长连接 SSE 推广，Round 51 实施）

- **范围**：总账 4 只（科目余额表/资产负债表/利润表/现金流量表，`needsBook:true` 依赖账簿）+ 生产制造 4 只（物料分析明细/在制成本明细/在制原料明细/直接原料明细，无账簿），全部 `getReportStream` 长连接；`buildRequestStream` 从 accgl 硬编码改为 **`cfg.stream` 配置驱动**（fixCondition @占位符模板 + elements 数组 + topFields + statGroup + showLadder + orderBy + datePreset）
- **@占位符派生**（`buildStreamCtx`）：@BOOK_NO/@PERIOD/@YEARS/@IPERIOD/@DATE_B/@DATE_E/@START_DD/@MONTH_FIRST/@MONTH_LAST/@YEAR_FIRST/@TODAY；会计期间非法/空兜底当前月
- **动态列**（财务报表 3 只）：响应消息携带 `COLUMN_INFO [{NAME,TITLE}]` → `_currentColumnInfo` 收集（⚠️ 制造报表空数据时是 `{}` 对象，须 Array.isArray 防护）→ `renderColumns:'columnInfo'` 走前导列（ITEM_NO 项目编号 + ITEM_NAME 项目名称）+ 动态数值列；层级用行 SPACES 缩进 16px/级 + 一级行加粗（.row-level1）
- **汇总行**（MRPCE `showLadder:'T'`）：响应行 `_SKIP_STAT==='T'` → 表格 `.row-total` 加粗 + **转入数据源剔除**（initTransferBtn 过滤）
- **STAT_GROUP 分组统计**：MRPCT/MRPCX/MRPCE 按文档原样传（CHK_DEF/ROW_TO_COL/TITLE/_X_ROW_KEY），不做分组 UI
- **筛选面板**：总账 4 只复用账簿下拉 + 会计期间（`filterLayout:'accgl'`）；MRPCU 工单号+母件代号；MRPCT/MRPCE 生产货品+日期；MRPCX 材料代号+日期；生产 3 只日期**预填默认可编辑**（MRPCT 本月 1 日×2 / MRPCX 本月 / MRPCE 年初~今天）
- **五阶段列**（用户确认）：QC 期初在制/TR 本期投入/WG 本期完工/DQ 本期对冲/QM 期末在制/HY 本期耗用；标签规则 QTY_X=X数量、QTY1_X=X基本数量、CST_X=X成本、CST_MAN/MAK/PRD/OUT_X=X人工/制造费用/材料/委外成本、CST_STD_X=X标准成本、QTY_LOST_WG=本期完工损耗数量
- **API5 vs API4 差异**（实测）：BOOK_NO 空 = HTTP 200 + SSE ERR「账簿不能为空」（**不是 API4 的 406**）；PAGE_COUNT 不可靠一律客户端计算；PERCENT 序列因报表而异且可能回退，UI 只取最新值不假设单调；财务报表数据在 PERCENT 100 消息到达
- **前端就绪/服务端待办**：财务报表 3 样式 STD001/2/3 需 ERP 侧建立/授权（当前报「报表样式不存在或无访问权限」）；本服务器无制造数据，行格式以文档样例为准，待 ERP 数据后回归
- **Round 54 制造数据实测（账套 AT04 2022 生产数据，9 只逐一验证）**：
  - ✅ 无修改：mrpPK（2022 30 行）/ mrpPS（5000 行）/ mrppu（成本年月 2022-07 → 1078 行）/ mrpce（4699 行）
  - 🔧 **日期锁死修复**：流式报表元素 operator=`this_month` 时**服务端忽略传入日期、自取服务器当月**（MRPCX 实测 this_month+2022 → 0 行、range+2022 → 26996 行）——MRPCX 元素 operator 改 `range`；同理 v3 报表 dateFilter `last_year`/`this_year`（客户端 computeDateValue 写死上年/今年）对老账套必空，mrpag/mrpcf 改 `range` 走用户输入。**纪律：日期条件一律 range，不用快捷操作符**
  - 🔧 **MRPCU 母件代号铁律**：MO_NO/BOM_NO/MRP_NO 全空 → 服务端 SQL Server 8623「查询处理器用尽了内部资源」（fixCondition 全变体复现）；母件代号非空 → 正常（1.01.01.092 → 3 行）。app.js doQuery 前置拦截（沿用 BOOK_NO/RPT_NO 铁律模式），i18n 键「请输入母件代号」三语
  - 🔍 **MRPCT 数据侧空表判定**（跨账套对照证据）：AT02 同格式请求 → 119 行（MO 40 + TW 79）证明端点与请求体正确；AT04 全日期范围（2022/2023/2020-2030）均 0 行，但其在制原料明细（MRPCX）有 1223/1232 行非零成本 → 在制成本汇总表本身无记录；AT01（文档示例账套）已无任何工单数据。结论：请求代码无需改，空态即正确结果，待用户 ERP 客户端对照确认
  - 🔧 **日期默认值动态化**：重置按钮与 init 的写死 2026-01-01/2026-12-31/2025-07 → 动态当前年/月（老账套重置后不再必查空）
  - **回归方法**：vm 加载真实 js/reports.js 构造器生成请求体 + 真实 API（非手工拼请求体）；测流式报表必须走 query→buildRequestStream 路径（误用 getReport 风格构造器曾得空壳请求造成 8623 误判，已澄清）
- **Round 55 版本号 bump 纪律（2026-08-20）**：用户复报 mrpcf 无数据，根因是 Round 54 改了三只 JS 但 index.html `?v=` 版本号未 bump，浏览器缓存跑了修复前旧代码。**纪律：任何 js/css 修改，index.html 引用版本号同步 bump 是收尾动作**（与文档同步同级）；日期输入框 HTML 不再写死默认值，由 app.js init 动态填当前年/月

### 4.12 报表样式模块（三财务报表 RPT_NO 动态化，Round 52 实施）

- **动机**：Round 51 实测三财务报表写死 RPT_NO=STD001/2/3 时 ERP 报「报表样式不存在或无访问权限」；样式清单与科目表代号均不能写死
- **数据链路**（用户指定并 PoC 实测）：`AccBook/GetList（账簿查询）→ 账簿行 TYPE_NO → accRptStyle/getlist(TYPE_NO)（样式清单：RPT_NO/RPT_TYPE/NAME/TYPE_NO）→ 按 RPT_TYPE 过滤进下拉（2=资产负债表/3=利润表/4=现金流量表）→ 查询时传所选 RPT_NO + 该样式所属 TYPE_NO`；**账簿一旦改变，样式清单必须重新获取**——缓存按账簿（BOOK_NO）隔离；AccType/getlist 不在链路内（仅需要 TYPE_NAME 时才查）
- **新模块 js/rpt-style-store.js**（照抄 book-store 模式）：fetchStyles(typeNo) 单端点拉取（accRptStyle/getlist，顶层 PGM+TYPE_NO）；ensureLoaded(bookNo, typeNo) 内存 + localStorage（sunlike_rpt_styles，按 compno+BOOK_NO 两级隔离）+ 按账簿并发去重 + 失败重试一次 + 登出世代作废；getStyles(bookNo)/getStyle(bookNo, rptNo)/getCachedStyles(bookNo)
- **筛选面板**：新布局 `accglStyle`（账簿 + 报表样式 + 会计期间），不动共用的 `accgl`（总分类账/科目余额表）；样式下拉按报表类型过滤、选项 "RPT_NO · NAME"、预选第一个匹配样式；账簿 option 带 data-type-no（账簿行 TYPE_NO），账簿切换 → change 监听重取样式清单（下拉先回「加载中...」，失败回「无可用样式」）
- **打开时序（Round 52 用户实测反馈后修正）**：**先 `_openReportCore(reportKey, skipAutoQuery=true)` 渲染面板 → 再异步 `loadRptStylesForBook(reportKey, bookNo)` 拉样式填充下拉 → 样式就绪才自动 doQuery**。填充必须用**传入的 reportKey** 做类型过滤，绝不能读 `AppState.currentReport`（异步完成时用户可能已切换报表）。教训：原实现「先拉样式再开报表」，(a) 填充早于面板渲染被覆盖 → 下拉永远「加载中」；(b) populate 读 AppState.currentReport 时还是旧报表 → 类型过滤错，切到利润表时显示资产负债表的样式
- **请求动态化**：三 config 的 fixCondition 与 topFields 改 `@RPT_NO`/`@TYPE_NO` 占位符（topFields 注入同样走 ph() 替换——原实现不替换，已修）；buildStreamCtx 新增 RPT_NO/TYPE_NO。**TYPE_NO 注入下沉到 readFilters**：样式下拉选中 option 带 `data-type-no`（样式行 TYPE_NO），readFilters 直接读出 `styleTypeNo`——查询（doQuery）与**转入数据源（独立全量请求，不经过 doQuery）**两条路径都拿到；曾因转入路径拿不到 TYPE_NO，ERP 报「科目表代号不能为空」写进数据源卡片（已修）
- **防护**：未选样式 doQuery 拦截 toast；样式加载失败 → 下拉「无可用样式」+ 不自动查询（报表已打开）；账簿无 TYPE_NO → toast「账簿无科目表代号」；0 样式下拉「无可用样式」占位；重置按钮按账簿重取样式后再查询 + 会计期间按 filterLayout 回当前月
- **实测**：账簿响应行直接带 TYPE_NO "1"（请求 displayFields 本就含 TYPE_NO/TYPE_NAME，零请求变更）；写死 "3" 报样式错误 vs 账簿行 TYPE_NO "1" + STD001 + BOOK_NO 00000000 出 67 行完整数据

### 4.13 Online 降级模块（0 账簿 → 空白纸打印版端点，Round 59 实施）

- **动机**：Round 44/45 的 0 账簿拦截（弹「你的账套没有启用总账」+ 禁用查询）被推翻——账套未启用总账 ≠ 不能出总账报表；ERP 另有一套「Online 空白纸打印版」端点（无账簿概念，仅可选 ACCA/ZHANGID 账别），正是为无总账账套准备的。用户拍板 4 决策：①公式框预填文档示例值 10/20/30/40；②**完全静默切换**（不弹窗、不 toast）；③日期沿用「会计期间(月)」选择器映射月初/月末（zcfzb 用月末）；④5 只全部按响应 COLUMN_INFO 动态列渲染
- **触发**：`openReport` needsBook 预检 AccBook/GetList 返回 0 条 → `ReportEngine.ledgerOnline = true` → 照常 `_openReportCore`；切报表/登出/重置复位。`doQuery` 的 BOOK_NO/RPT_NO/styleTypeNo 拦截全部加 `!onlineMode` 守卫（Online 端点无账簿/样式概念）；`_ledgerBlocked` 死代码块（block/unblock/setLedgerButtonsDisabled）整体清除
- **端点映射**（全部 GetReportStream SSE）：总分类账→`RPTACCBlank`（dataKey `RPT`）/ 科目余额表→`RPTACCDetail`（`RPT`）/ 资产负债表→`RPTZFListA`（`MyTable`）/ 利润表→`RPTSYListA`（`MyTable`）/ 现金流量表→`RPTCshFroList`（`RPT_CSHFROLIST`）——各 REPORT_CONFIG 加 `online` 块（apiPath/responseDataKey/filterLayout/body 扁平模板/inputs 公式框声明）
- **请求构造**（`buildOnlineBody`）：扁平请求体（无 PGM/SEARCH_INFO/DISPLAY_FIELDS），复用 `buildStreamCtx` 的 @DATE_B/@DATE_E 派生 + `inputs` 注入 ctx（未填用 defaultValue）→ 占位符解析
- **公式框自动判定**（用户两次纠正后的定论）：按各端点**输入参数**差异自动推导——`inputs.length` 3 → 资产负债表 3 框（REPNO1/2/3 预填 10/20/30，字段名实测定论，文档参数表 REPO1/2/3 是笔误）、1 → 利润表 1 框（REPNO 预填 40）、0 → 仅会计期间（总分类账/科目余额表/现金流量表）——ui-template 演示与生产代码同源逻辑，不让用户挑场景；**重置按钮公式框回默认值**（不是清空）
- **渲染**：响应嵌套 `COLUMN_INFO {表键:[{NAME,TYPE,SIZE}]}`（无 TITLE，与标准版扁平数组不同）→ 在线分支解包后强制动态列、丢弃前导固定列；`ONLINE_COLUMN_LABELS` 按 reportKey 分表（只收实测高/中置信度字段，与全局冲突的 NAME/REM 在此覆盖），未收录回退英文原名；`DECIMAL/CURRENCY_FIELDS` 追加 Online 数值/金额列（AMTND/AMTNC/AMTNYE/AMTN_SUML/RATE1 系等）
- **响应差异**：三态判别同标准流式；ERR 非空即业务错误透出；SSE 消息是带空格 pretty JSON（解析须 `\s*` 容差）；公式值无效 → ERR「制表公式不存在」（10/20/30/40 在 AT02/AT03 实测有效）
- **账套实测地图**（2026-08-31，详见 总账报表调用方法.md 6.5）：AT02 全 5 端点真实数据（9/4/5/23/3 行）；AT01 凭证挂账簿模型 → Online 作用域读不到（gl/detail/xjll 0 行、lrb 23 行）；AT03 zcfzb/lrb 公式壳出表（6/23 行）、**科目余额表 SQL 错（DBO.FN_GET_ACCN_USR 缺失，仅此账套此端点）——用户 Postman 复测确认 API 侧问题，2026-08-31 拍板搁置、等新 API 再试**；AT04 形态又不同（科目余额表干净空表、lrb 报 ##CSH_FRO_TEMP 临时表冲突）。账套间端点行为逐套而异，均属服务端数据层
- **验证状态**：AT01 标准版路径回归通过（请求体与旧版一致）；AT03 浏览器实测：静默降级（无弹窗）、Online 端点命中（堆栈 RPTACCDetailController）、面板公式框自动判定正确（科目余额表仅期间+部门，无公式框）；其余 4 只面板形态/转入/重置/切报表/登出待用户浏览器验证

## 五、多语言（i18n）架构

产品界面文字支持【简体 zh-CN / 繁体 zh-TW / 英文 en】三语系，ERP 数据不翻译。

### 5.1 核心机制（gettext 风格）

- **简体原文即 key**：`I18n.t(简体字符串, args...)` 查当前语言字典 → 命中返回译文，未命中回退简体原文（漏翻必然露出简体，本身即完整性审计信号）
- **占位符**：`t('第 {0}-{1} 条，共 {2} 条', a, b, c)`，支持英文语序重排
- **静态文本**：HTML 上 `data-i18n`（textContent）/ `data-i18n-placeholder` / `data-i18n-title` / `data-i18n-aria-label` 四属性，`I18n.applyStatic(root)` 批量应用；含子元素/`<br>`/`<strong>` 的混合节点把文本片段包独立 `<span data-i18n>`
- **动态渲染点**：REPORT_CONFIG/COLUMN_LABELS/MENU_GROUPS 零重构，仅在渲染点包 t()（如 `getColumnLabel` 一行通杀全部表头）

### 5.2 语言解析链（启动时序）

```
i18n-data.js 末尾同步 I18n.boot()（body 末尾脚本求值，早于 DOMContentLoaded → 零闪变）
  → localStorage sunlike_lang（仅用户显式点击时写入；值校验非法则忽略）
  → navigator.language OS 检测（zh-TW/zh-HK/zh-Hant→zh-tw；zh*→zh-cn；en*→en；其他→zh-cn）
  → 应用静态文字 + 更新 <html lang> 与 <title> + 绑定切换器
```

- **登录页语言选择器**：登录卡三个分段按钮【简繁英】（`role="radiogroup"` + `aria-checked`），点击 → `setLang(l, {persist:true})` → 全文重扫（隐藏仪表盘一并翻译，登录后零闪变）；**仅登录页可切换**，登录后不可
- **LANG_ID 跟随所选语言**：登录/会话恢复/验证服务器三处请求传 LangTag（zh-cn/zh-tw/en），与服务器 `LANG_ID_DATA` 完全一致（curl PoC 三语 code:0 实测验证）——服务器错误消息天然按语系返回；客户端兜底错误走 t()
- **存储与显示分离**：AppState.currentReportName、持久化的 dataSource.reportName、记事本标题、收藏 key、分组折叠 key 全部保留**简体规范值**，仅在渲染点 t()——旧 localStorage 数据兼容、跨语系切换不损坏已存内容

### 5.3 字典维护

- `js/i18n-data.js`：`var I18nData = { 'zh-tw': {...}, 'en': {...} }`，601 key × 2 语言 = 各 628 条，分节注释（通用/报表名/分组/栏位标签/分页状态/Toast错误/导出/AI/记事本设置）——**字典是唯一事实源**
- **新增字符串纪律**：任何新 UI 字符串必须同时补入 zh-tw/en 两语字典（新功能开发步骤之一）
- **对账工具**：`.claude/extract-i18n-keys.js`（不入库）提取全部 key 来源（静态 t() + data-i18n 属性 + REPORT_CONFIG.name + COLUMN_LABELS 值 + MENU_GROUPS）与字典交叉比对，输出缺失/僵尸清单
- **已知提取盲区**：条件表达式中的字面量（`I18n.t(isFav ? '取消收藏' : '收藏')`）静态正则抓不到——对账出现"僵尸"时先人工核实调用点再删
- **运行时审计**：`I18n.debug = true` 缺失 key console.warn；`I18n.audit()` 列出非 zh-cn 下可见 CJK 文本节点（排除表格数据区与 AI 回复区）

### 5.4 翻译边界

- **只翻译文字**：数字/日期/金额格式不碰（`toLocaleString('zh-CN')`、YYYY-MM-DD、`¥` 维持现状）
- **AI 输出语言**：系统提示词按 `I18n.getLang()` 分发三语版（内嵌 table/chart 示例表头与末尾语言指令随语言），AI 回复语言与界面一致
- **导出**：HTML/PPTX/CSV 标题与文件名随语言（`AI数据分析报告` / `AI數據分析報告` / `AI Data Analysis Report`）

### 5.5 动态内容刷新机制（表头渲染时机，Round 40 修复）

- **表头 `<th>` 是 JS 动态渲染的 HTML**，不带 data-i18n 属性，不在 applyStatic 扫描范围内——语言变化时静态扫描覆盖不到
- **表头渲染唯一入口** `renderCurrentTableHead()`（app.js），四个时机共用：
  1. init（DOMContentLoaded，登录前，boot 语言）
  2. 登录成功（doQuery 前）
  3. checkAuth 会话恢复（doQuery 前）
  4. openReport 切换报表
- **`i18n:changed` 事件**（setLang 内 dispatch）：app.js init() 注册监听 → `refreshDynamicI18n()` 重刷表头 + 报表标题 + 当前页表体切片（审核徽章等动态文字）+ 分页控件 title——语言一换，已渲染的动态内容立即跟随
- **教训（2026-08-13 实测 bug）**：登录页切语言发生在 init 渲染表头**之后**，若登录成功/会话恢复不重渲染表头，默认报表表头会停留在页面加载时的语言，出现"切繁/英登录进来表头仍简体、登出重进才生效"的现象
- **筛选面板不重渲染**：updateFilterPanel 只切组可见性、不毁输入值；面板标签是静态 HTML 由 applyStatic 覆盖

## 六、API 关键注意事项

> ⚠️ 对照《项目经验行动指南》第二节

- [x] 成功判断：`code === 0`（非 HTTP 200）
- [x] 请求体结构：`SEARCH_INFO` 数组按索引位置解析，顺序固定
- [x] 日期字段名因报表而异：采购=OS_DD、进货/销货=PS_DD、收付款=RP_DD
- [x] 空筛选条件传 `"value": ""`，不传 null 或不传该字段
- [x] 登录字段名：`USR`/`PWD`/`COMPNO`/`LANG_ID`/`SYS_TYPE`（✅ PoC 验证通过）
- [x] Token 存放路径：`data.TOKEN`
