# 进度追踪表

> 项目：Sunlike ERP 报表查询系统
> 更新日期：2026-08-31

---

## 总体进度

| 阶段 | 状态 | 开始日 | 完成日 |
|------|------|--------|--------|
| 1. 项目启动 & 文档搭建 | ✅ 已完成 | 08-10 | 08-10 |
| 2. 技术选型 PoC | ✅ 已完成 | 08-10 | 08-10 |
| 3. 登录 & 认证模块 | ✅ 已完成 | 08-10 | 08-10 |
| 4. 采购报表 | ✅ 已完成 | 08-10 | 08-10 |
| 5. 进货报表 | ✅ 已完成 | 08-10 | 08-10 |
| 6. 受订报表 | ✅ 已完成 | 08-10 | 08-10 |
| 7. 销货报表 | ✅ 已完成 | 08-10 | 08-10 |
| 8. 收款明细表 | ✅ 已完成 | 08-10 | 08-10 |
| 9. 付款明细表 | ✅ 已完成 | 08-10 | 08-10 |
| 10. UI 优化 & 集成测试 | ✅ 已完成 | 08-10 | 08-10 |
| 11. AI 数据分析功能 | ✅ Phase B 完成 | 08-10 | 08-10 |
| 12. 工单完成情况表 (mrpPK) | ✅ 已完成 | 08-11 | 08-11 |
| 13. 完工入库报表 (mrpPS) | ✅ 已完成 | 08-11 | 08-11 |
| 14. 产品成本分析表 (mrppu) | ✅ 已完成 | 08-11 | 08-11 |
| 15. 员工年度薪资清册 (wagCG3) | ✅ 已完成 | 08-11 | 08-11 |
| 16. API3 — 21 报表接入 | ✅ 已完成 | 08-12 | 08-12 |
| 17. 记事本功能 | ✅ 代码完成，待 E2E 验证 | 08-12 | 08-13 |
| 18. 报表菜单改进（搜索/折叠/收藏） | ✅ 代码完成，待浏览器回归 | 08-13 | 08-13 |
| 19. 多语言支持（简/繁/英三语 i18n） | ✅ 代码完成，待浏览器三语走查 | 08-13 | 08-13 |
| 20. API4 总分类账（长连接 SSE + 进度条） | ✅ 代码完成（Round 45/46），待浏览器验证 | 08-15 | 08-19 |
| 21. API5 八报表（长连接 SSE 推广：总账 4 + 生产制造 4） | ✅ 代码完成（Round 51），待浏览器验证 | 08-19 | 08-19 |
| 22. 三财务报表报表样式下拉（RPT_NO/TYPE_NO 动态化） | ✅ 代码完成（Round 52，含时序修正），待浏览器验证 | 08-19 | 08-19 |
| 23. AI 聊天裸 JSON 直接渲染表格（AIParser 识别规则扩展） | ✅ 代码完成（Round 53），待浏览器验证 | 08-19 | 08-19 |
| 24. 总账 0 账簿静默降级 Online（Round 59） | 🟡 代码完成，待浏览器验证（AT03 科目余额表 API 侧问题搁置，等新 API） | 08-28 | 08-31 |
| 25. 总账 Online 公式下拉（Round 60） | 🟢 已完成（Round 61 验证收尾：浏览器 + 屏蔽 billcommon 失败路径哈啰 11 断言） | 09-01 | 09-01 |
| 26. 总账 Online 5 只简繁英补丁（Round 61） | 🟢 已完成（三语哈啰 16 断言 + 用户浏览器确认） | 09-01 | 09-01 |

## 模块详情

### 1. 项目启动 & 文档搭建 🟡

- [x] 对话记录.md
- [x] 需求架构文档.md
- [x] 进度追踪表.md
- [x] 流程图
- [ ] 技术选型 PoC（指南 1.1）
- [x] 确认登录 API 字段名（USR/PSW/CPY/SID ✅）

### 2. 技术选型 PoC ✅

- [x] 候选框架在当前 OS (Windows 11) 上 hello-world 验证（纯 HTML + 原生 JS，无需框架）
- [x] 搜索 `[方案名] Windows known issues`
- [x] API 连通性测试（curl 发一个真实请求 — Login + 受订报表已验证）

### 3. 登录模块 ✅

- [x] 登录页面 UI（ui-template.html + css/auth.css）
- [x] Token 存储方案（localStorage key: sunlike_auth）
- [x] Token 过期处理（checkAuth 重验证 + handleAuthError 拦截）
- [x] 401 自动跳转登录页（api.js 拦截 code 20001/20004，触发 auth:expired）
- [x] 模块化拆分（js/auth.js + js/api.js）
- [x] 生产入口 index.html（含完整登录流程）

### 4-9. 各报表模块 ✅

- [x] 报表引擎 `js/reports.js` — 集中管理 6 个报表的差异配置（PGM、日期字段、fixCondition、displayFields、筛选器）
- [x] 查询表单（日期 + 动态筛选条件，收付款自动切换 YW_TYPE/KB 字段）
- [x] 数据表格展示（动态列头，从 API COLUMN_INFO 渲染）
- [x] 分页处理（前端分页，offset 计算，上一页/下一页/页码按钮）
- [x] API 真实调用（`Api.getReport()` 替代 mock 数据）
- [x] 对照 API 文档逐字段验证（6 个报表的 SEARCH_INFO 结构均按文档实现）
- [x] CSS 模块化拆分（`css/dashboard.css` + `css/ai-analysis.css`）
- [x] `index.html` 生产入口开发完成（含完整 Dashboard + 6 报表 + AI 分析）

### 10. UI 优化 & 集成测试 ✅

- [x] CSS 拆分提取（auth.css / dashboard.css / ai-analysis.css）
- [x] `index.html` 完整生产版本
- [x] 边界测试（空数据 / 大数据量 / 网络异常 — 代码层面已覆盖）
- [x] 移动端响应式验证（768px / 480px 断点 + A11Y）
- [x] **Toast 通知重设计**（白卡+左边框强调+SVG图标，替代原来的绿底/红底大框）
- [x] **Toast 通知二次优化**（Round 14：移至右下角、缩小尺寸 max-width 320px、底部浮现动画、连续调用防抖处理）
- [x] **数据表格展示增强**（AI Chat 表格斑马纹/hover/深蓝表头/数值右对齐；System Prompt 强制数据→table格式）
- [ ] 与 API 服务器联调测试（需服务器在线，暂缓）

## 风险与阻塞

| 风险 | 影响 | 状态 |
|------|------|------|
| 登录 API 字段名未确认 | 阻塞阶段 3 | 🟢 已解决 |
| 技术栈未定 | 阻塞阶段 2 | 🟢 已解决 |
| AT03 科目余额表 SQL 错（FN_GET_ACCN_USR 缺失，仅此账套此端点） | 无总账账套降级验证部分受阻 | 🟡 API 侧问题，用户 2026-08-31 拍板搁置，等新 API 再试 |

---

## 11. AI 数据分析功能 🟡

### Phase A：单体原型 ✅

- [x] Tab 页签系统（数据查询 / AI数据分析）
- [x] 查询按钮拆分（查询 / 查转入数据源 / 重置）
- [x] 数据源存储（localStorage CRUD，上限20个）
- [x] AI Tab 左面板（数据源列表、空状态、删除、清空）
- [x] AI Tab 右面板（Chat 界面、消息气泡、输入框）
- [x] 设置面板（模态弹窗、API Key/服务器地址验证）
- [x] Deepseek Client（非流式调用、System Prompt 注入数据源）
- [x] 消息渲染（Markdown、```table → HTML表格、```chart → Chart.js）
- [x] 操作按钮（复制、延申问答、导出Excel/PPTX）
- [x] 所有代码整合在 ui-template.html（2292 行）

### Phase B：模块化拆分 ✅

- [x] 提取 CSS 到 css/ 目录（auth.css / dashboard.css / ai-analysis.css）
- [x] 提取 JS 到 js/ 目录（16 个模块文件：auth + api + reports + 13 AI 模块）
- [x] `index.html` 生产入口（317 行，引用 16 个 JS 模块 + 3 个 CSS 文件）
- [x] `ui-template.html` 同步更新（317 行，与 index.html 一致引用外部模块）
- [x] 全部 13 个新模块 Node.js 语法检查通过
- [x] HTML 结构验证（script/link 标签数量正确）
- [x] 模块加载顺序验证（依赖关系正确）

### Phase C：功能增强 ⬜

- [ ] 流式响应支持（Deepseek streaming）
- [ ] IndexedDB 迁移（突破 localStorage 5MB 限制）
- [ ] 正式 .pptx 文件导出（使用 pptx skill）

### 12. 工单完成情况表 (mrpPK) ✅

- [x] API 集成：`MrpPK/GetReport`，PGM `MRPPK`，8 个 SEARCH_INFO 元素
- [x] 筛选器：MO_NO（单号）、MRP_NO（成品代号）、DEP（部门）
- [x] orderBy：MO_DD + MO_NO + ZC_ITM 三字段排序
- [x] 列标签 ~20 个新字段（MO_NO、QTY_MO、QTY_FIN、QTY_LOST 等）
- [x] 侧边栏：生产制造 → 工单完成情况表
- [x] filterLayout: `mrpPK`（date + docNo + mrpNo + dep）

### 13. 完工入库报表 (mrpPS) ✅

- [x] API 集成：`mrpafc/GetReport`，PGM `MRPPS`，10 个 SEARCH_INFO
- [x] fixCondition 特殊字段：COMBOFCP/WL_CHK/COMBODATE
- [x] MM_NO 带 fieldType:"bilNo"
- [x] 筛选器：MM_NO、MRP_NO、DEP、WH、CHK_STATUS
- [x] 列标签 ~22 个新字段（MM_NO、CST_MAKE、CST_PRD、QTY_LOST 等）
- [x] 侧边栏：生产制造 → 完工入库报表
- [x] filterLayout: `mrpPS`（date + docNo + mrpNo + dep + wh + status）

### 14. 产品成本分析表 (mrppu) ✅

- [x] **API 架构完全不同**：`mrppu/getList`（不是 getReport）
- [x] 请求体额外字段：OTHERINFO（DEP_GROUP + INCLUDESON）
- [x] 分页方式：PAGE_INFO（PAGE_SIZE + CURRENT_PAGE）
- [x] SEARCH_INFO 特殊：showBody 替代 offset、4 个固定禁用筛选元素
- [x] 无 DISPLAY_FIELDS 顶层字段、无 orderBy
- [x] 响应数据路径：`data.TRANS`（不是 REPORT__TAB）
- [x] 列信息：`data.COLUMN_INFO` 扁平数组（不是 COLUMN_INFO.REPORT__TAB）
- [x] 筛选器：DEP + PRD_NO + DATE_CST（month 输入框）
- [x] 侧边栏：生产制造 → 产品成本分析表
- [x] filterLayout: `mrppu`（dateCst + dep + prd）

### 15. 员工年度薪资清册 (wagCG3) ✅

- [x] API 集成：`rptWagCG3/getReport`，PGM `REP_WAGCG3`，7 个 SEARCH_INFO
- [x] showLadder: `"T"`（显示阶梯价）
- [x] fixCondition 7 个字段（SZ_NO_TYPE、SZ_NO、CHK_TYPE、LOCK_TYPE 等）
- [x] 日期筛选：YEARS 等值筛选（非范围），单一 string value
- [x] 筛选器：YG_NO（员工代号）+ OUT_DAY_TYPE（在职状态 select）
- [x] 列标签 ~18 个新字段（YG_NO、NAME、SZ_NAME_1、AMTN_1~AMTN_12、AMTN_TOTAL）
- [x] 侧边栏：人力资源 → 员工年度薪资清册
- [x] filterLayout: `wagCG3`（dateYear + ygNo + outDayType）

### 架构改进（支撑 12-15）

- [x] `js/api.js`：新增 `post()` 通用方法，支持非标准端点
- [x] `js/reports.js` buildRequest 多态（getReport vs getList）
- [x] `js/reports.js` query 多态（apiPath + responseDataKey）
- [x] `js/reports.js` updateFilterPanel 重构（6 布局配置驱动）
- [x] `js/reports.js` resolveFilterValue defaultValue bug 修复
- [x] 预估值：~200 行新增代码、20+ 配置字段、~60 新列标签

### 16. API3 — 21 报表接入 ✅

**概述**：集成 `标准报表制表API3.md` 中全部 21 个报表，系统总数 10→31。全部使用 getReport 端点，无 getList 特殊情况。

**分组实现**：

- **财务管理**（6 新增 → 共 8）：accabgt 科目预算 / rptsarp 信用额度 / monbx 报销 / monjk 员工借款 / monCA 应收票据 / monCB 应付票据
- **库存管理**（5 新）：rptinvdo 过期货品预警 / rptinvdl 安全存量预警 / rptinvswa 负库存预警 / invic 库存调拨 / invij 库存调整
- **采购与价格**（5 新）：scmdrpti 送货单 / invpopc 采购交货状况 / invtwpc 委外交货状况 / invhp 采购价格 / invhs 售价价格
- **生产制造**（2 新增 → 共 5）：mrpag 领退补料 / mrpcf 单位成本分析
- **人力资源**（2 新增 → 共 3）：rptwagyg0 人事资料分析 / rptwagyg 员工明细
- **固定资产**（1 新）：fixaa 财产目录

**新增架构能力（参数化，无分支）**：

- [x] `showBody` — SEARCH_INFO[1] body 显示（rptwagyg0, monjk）
- [x] `hasPagination: false` — 跳过 offset（rptsarp）
- [x] `dateOperator` — 预设日期（last_year/this_year/today/last_week/this_month/equal，12 个报表）
- [x] `searchInfoExtra` — 隐藏固定条件插入（4 个报表）
- [x] `hideDateUI` — SYS_DATE 隐藏日期选择器（3 个报表）
- [x] `contain` operator — 文本包含搜索
- [x] 非 date `range` — 非日期字段范围筛选
- [x] apiPath 大写端点精确指定（8 个报表）

**改动文件**：

| 文件 | 改动 |
|------|------|
| `js/reports.js` | +21 REPORT_CONFIG、+150 COLUMN_LABELS、buildRequest/updateFilterPanel/readFilters/resolveFilterValue 扩展 |
| `index.html` | 侧边栏 4→7 分组 10→31 条目、+3 筛选组、版本号升级 |
| `js/app.js` | reset handler 扩展、版本号升级 |
| `ui-template.html` | 同步所有 UI 改动 |

**9 坑全部规避**：缓存→版本号先升 | 大小写→apiPath 精确 | 滚动→布局已修 | fieldDisabled→===true | 文档→当场同步

### 17. 记事本功能 🟡

**概述**：新增第三个平行页签【📓 记事本】，将**全部数据源快照**+聊天对话打包保存为事件，一键恢复（含保存时选中源）。

**已完成**：

- [x] UI 模板设计（ui-template.html：第三 tab、卡片网格布局、双按钮 ds-footer）
- [x] `js/notepad-store.js` — localStorage CRUD（key `sunlike_notepad`，上限 50）
- [x] `js/notepad-ui.js` — 卡片渲染 + 保存/加载/删除/清空交互
- [x] `js/utils.js` — 新增 `deepClone()`
- [x] `js/chat-ui.js` — 新增 `clearAllMessages()` + `renderHistory()`
- [x] `js/tabs.js` — `tabNotepad` 分支自动刷新
- [x] `css/notepad.css` — 独立样式文件（在 ai-analysis.css 后加载，避免缓存/优先级问题）
- [x] `index.html` + `ui-template.html` 同步（共享 CSS 文件，无 inline style 差异）
- [x] Bug 修复：双重按钮绑定、多数据源自动选择、try/catch 兜底
- [x] Round 41 多数据源快照（用户实测 bug）：事件结构 `dataSource` → `dataSources` 数组 + `activeDSId`；保存全量快照（含 loading/error）；回存 `saveAll` 全量恢复（保留原始 id/状态）+ 恢复保存时选中源；旧单源事件兼容迁移；按钮 title 改「保存全部数据源和对话到记事本」；i18n 对账 602 key 缺失 0
- [x] 文档同步：对话记录.md / 需求架构文档.md / 进度追踪表.md / 流程图.md

**待验证**：

- [ ] 浏览器 E2E：保存 → 切页签查看卡片 → 加载恢复 → 删除 → 清空
- [ ] 刷新页面后记事本数据保留（localStorage）
- [ ] Round 41 验证：2+ 数据源 → 存到记事本 → 清空 → 回存 → 列表恢复全部 + 选中保存时选中的那个；旧事件（修复前保存）回存正常；多源卡片显示"（共 N 个数据源）"

### 18. 报表菜单改进（搜索 / 折叠 / 收藏置顶）✅

**概述**：31 种报表静态侧边栏改为**配置驱动的动态菜单**（ReportMenu），解决报表数量增长后的菜单定位问题。搜索（中文模糊 + 拼音首字母 + Ctrl+K/Esc）、7 分组可折叠（默认全折叠，刷新复位）、⭐ 收藏置顶（localStorage 持久化）。

**已完成**：

- [x] `js/reports.js` — 31 条 REPORT_CONFIG 各加 `group`/`icon`/`pinyin`（单一数据源，第 32 个报表加配置即自动出现在菜单）
- [x] `js/report-menu-store.js` — 收藏持久化（key `sunlike_report_menu`，剪枝/容错）+ 折叠会话内态（默认全折叠，刷新/登录复位）
- [x] `js/report-menu.js` — 动态渲染，一条 render() 覆盖 搜索×折叠×收藏×active；容器级事件委托；Ctrl+K 登录页守卫
- [x] `js/app.js` — initSidebarNav 委托化 + openReport 提取（config 取名、双份高亮）；App.init 接入 ReportMenu.init
- [x] `css/dashboard.css` — 搜索框/星标/折叠标题/空态样式
- [x] `ui-template.html` 原型 → 用户浏览器确认通过 → `index.html` 落地（静态 31 条删除，版本号 20260813b）
- [x] 验证：node --check × 4 + 冒烟测试 27 项断言（默认折叠/刷新复位/收藏保持/剪枝/登录页守卫）
- [x] 文档同步：对话记录 / 需求架构（4.8 模块）/ 进度表 / 流程图（第十三节）
- [x] 折叠态（56px）优化：全量 emoji 平铺 + 组间分割线 + 收藏去重，消除分组标题文字挤压（用户选定方案 A）

**待验证（浏览器回归 index.html）**：

- [ ] 登录 → 点击报表切换（openReport 全链路：筛选面板/表头/查询/分页）
- [ ] Ctrl+K 搜索 / Esc 清空 / 分组默认折叠→展开→刷新复位 / 收藏→刷新保持
- [ ] 转移数据源按钮、登录页 Ctrl+K 无反应、56px 折叠侧边栏无破版

### 19. 多语言支持（简 / 繁 / 英三语 i18n）🟡

**概述**：产品界面文字支持简体 zh-CN / 繁体 zh-TW / 英文 en 三语系。登录页【简繁英】分段按钮切换，默认按操作系统预判；静态字典 + 大模型预生成；ERP 数据不翻译；仅登录页可切换。

**Phase A：模板原型（Round 37-38）** ✅

- [x] 新建 `js/i18n.js`（I18n 核心）+ `js/i18n-data.js`（静态字典）+ `js/dialog.js`（应用内 confirm/prompt）
- [x] ui-template.html：语言选择器 + DEMO 演示面板 + ~103 个 data-i18n 属性
- [x] 用户浏览器确认通过（A6 门禁）；分支 feat/i18n-phase-a 合并回 master

**Phase B：生产代码移植（Round 39）** ✅

- [x] B0 curl PoC：LANG_ID 传 LangTag 三语登录 code:0（规则二三步验证）
- [x] B3 index.html：104 个 data-i18n 属性 + #tableInfoText JS 填充 + 脚本顺序（i18n 最先）
- [x] B5 LANG_ID 动态化 4 处（auth.js×2 / api.js / settings-ui.js，typeof 守卫回退 zh-cn）
- [x] B6-B13 渲染点 t()：15 个文件（getColumnLabel 一行通杀表头；report-menu 四路搜索；6 处原生弹窗换 Dialog）
- [x] B14 ai-client.js：三语系统提示词（内嵌示例表头 + 语言指令随语言）；顺手修复 `_isApiError` 标志（原 indexOf 判断永远 false）
- [x] B15 export.js 23 处 t()：标题/文件名/toast 随语言
- [x] B16 deploy.ps1 v1.3；B17 ui-template.html 脚本列表与生产对齐
- [x] 字典对账：601 key × 2 语言（各 628 条），缺失 0，27 僵尸均可解释（工具 .claude/extract-i18n-keys.js）
- [x] 打包验证：sunlike-erp-report-v1.3.zip（29 文件 = 1 html + 4 css + 24 js，与 index.html 加载清单逐项一致；移除未被加载的 deepseek-client.js）
- [x] Round 40 表头语言渲染时机修复（用户实测 bug）：`renderCurrentTableHead()` 唯一入口四时机（init / 登录成功 / 会话恢复 / openReport）+ `i18n:changed` 监听 `refreshDynamicI18n`（表头 / 标题 / 表体切片 / 分页）——修"切繁/英登录进来表头仍简体、登出重进才生效"；node --check 通过

**待验证（浏览器三语走查）**：

- [ ] 登录页三语即时切换 / 刷新记住 / 清存储回 OS 预判；`<html lang>` / `<title>` 正确
- [ ] Round 40 验证：登录页切繁/英 → 默认报表表头**即时**变化；登录后表头/表体徽章/分页 title 随语言；切报表路径回归正常
- [ ] 三语各登录一次全量走查：菜单 31 报表名 / 7 分组 / 表头 / 筛选 / 分页 / 空状态 / toast；英文搜 "sales" 命中受订报表、拼音仍可用
- [ ] 6 处弹窗为应用内样式且随语言；Enter / Esc 正常
- [ ] AI 聊天：en 会话英文作答 + 英文表头，zh-tw 繁体作答；推荐问题随语言
- [ ] 导出：HTML / PPTX / CSV 标题文件名随语言
- [ ] zh-cn 回归：界面与改造前一致，收藏/折叠持久化完好，格式与 ¥ 未动

### 20. API4 总分类账 — 长连接（SSE）报表 ✅ 实施完成（Round 45，待浏览器验证）

**概述**（Round 43 模式确认，未写代码）：总分类账查询制表，系统第一个【长连接】报表，后续长连接报表统一此模式。SSE 流式响应 + PERCENT 进度条 + BOOK_NO 必填。

**已完成（文档层 + curl 实测）**：

- [x] API服务调用说明文档.md 第九节长连接模式（实测版：消息序列表 / 三步判别 / 实测代码骨架）
- [x] 项目经验总结.md 3.5 长连接经验（实测修正：PERCENT 是消息内字段非前缀行）
- [x] 对话记录 Round 43（含实测结果）
- [x] curl 实测：9 条消息序列（0→10→30→40→50→70→90→90(数据)→100 结束）；BOOK_NO 空 → HTTP 406；PGM 不校验；Token 无效 → 20004 JSON；无 Authorization → 空 body 挂起；大小写 4 组合全通；Bearer 前缀可带可不带
- [x] AccBook/GetList 账簿清单 API 实测（Round 44）：code===0、data.ACC_BOOK_BS、本账套 1 条 BOOK_NO=00000000、端点变体/Bearer 全通
- [x] 账簿下拉设计规则入档（Round 44，用户指定）：登录+会话恢复两处拉取 / BookStore / 下拉 BOOK_NO+NAME 预选第一个 / 0 账簿警告+禁用按钮 / 失败重试
- [x] ui-template.html 原型（Round 45，用户指定先改模板）：账簿下拉 + 流式进度条 + 演示按钮【预览：总分类账】【预览：流式进度条】；菜单分组规则补充入档（【总账报表】独立分组，Round 45 用户指定）

**实施完成（Round 45，用户「开工吧」后落地）**：

- [x] 新建 `js/book-store.js`：BookStore IIFE（LS_KEY sunlike_books，内存缓存 + localStorage 按 compno 隔离；ensureLoaded 缓存直返/失败重试一次；prefetch 登录 + 会话恢复两处后台预热）
- [x] REPORT_CONFIG 加 `accgl`（group 总账报表 / apiMethod 'getReportStream' / pgm ACCRPTGL / needsBook / filterId filterBookNo 避开 fieldToKey 冲突）；buildRequestStream（25 字段 fixCondition 由 YYYY-MM 派生）；query() 流分支（onProgress 回传）
- [x] api.js fetchStreamReport：res.ok（406）+ Content-Type 三步判别 → fetch + ReadableStream + 行缓冲（跨 chunk）+ AbortController 120s；PERCENT `|| 0` 不吞 0.0；ERR 非空抛错
- [x] 进度条 UI：.stream-progress fill/text，PERCENT 实时更新，TITLE 原样显示；done 置 100%（最终形态见 Bug 修复③：fixed 悬浮视口中央）
- [x] BOOK_NO 账簿下拉：select#filterBookNo（index/ui-template 同步）+ BOOK_NO · NAME + >1 预选第一个 + 空值前端拦截 toast「请选择账簿」
- [x] 0 账簿拦截：Dialog.alert 新增（仿 confirm 三语）→ openReport needsBook 预检（失败重试 toast「账簿清单加载失败: {0}」不切换；0 账簿弹「你的账套没有启用总账，所以你不能操作这个查询」+ **【查询】【转入】禁用**不调流不切换）+ doQuery `_ledgerBlocked` 兜底；切报表自动恢复按钮
- [x] 菜单分组：MENU_GROUPS 首位插入 `总账报表`，config `group: '总账报表'`（Round 45 用户指定排最上面）
- [x] ui-template.html 原型 → 用户浏览器确认（Round 45 用户「开工吧」）→ 生产代码；金额列 AMTN_*/AMT_*/UP_* 入 DECIMAL/CURRENCY_FIELDS
- [x] i18n 三语 +15 key；extract-i18n-keys.js 审计 0 缺失；7 个改动 JS node --check 通过
- [x] deploy.ps1 v1.4（30 文件 = 1 html + 4 css + 25 js）→ sunlike-erp-report-v1.4.zip（120.2 KB）
- [x] 文档四件套（对话记录 Round 45 / 进度表 / 需求架构 / 流程图）
- [x] 浏览器实测 Bug 修复（Round 45 同轮）：查询 404 —— apiPath 误带 `api/` 前缀拼成 `/API/api/...`，改为 `accGeneralLedger/GetReportStream` + 版本号 bump ?v=20260818b + 文档双重警告（同类错误第二次，getReport 早期踩过一次）
- [x] 浏览器实测 Bug 修复②（Round 45 同轮）：退出重登账簿下拉残留旧账套账簿 —— BookStore.reset() 世代计数作废 + 登出/过期清下拉占位解除阻断 + 登录成功改走 openReport 全链路（10.3 规则第 8 条入档）；book-store.js / app.js 版本号 ?v=20260818b
- [x] 浏览器实测 Bug 修复③（Round 45 同轮）：进度条位置太下面/看不到 —— 细条方案被用户否决（「感觉上就像卡住了」），按用户意见恢复原厚度样式（8px 条 + 阶段文字）只改位置：position:fixed 悬浮**视口正中央**（白卡片 + shadow-lg，不随表格滚动）；模板先行用户确认「可以」→ index.html 同结构（streamProgress 移出 table-loading 成兄弟）+ dashboard.css ?v=20260818c；重打 v1.4 包（121.4 KB）
- [x] Round 46：REM_TYPE 摘要类型语义映射（用户提供语义 1=期初余额/2=本期合计/3=本年合计）——API 文档 9.1 + Checklist 入档；表格彩色 badge 展示（info/warning/success）+ 转入数据源行数据映射为语义文字；模板先行用户确认「效果我很满意」；i18n 三语 +3 key；版本号 bump（css ?v=…d / i18n-data ?v=…b / reports·app ?v=…c）；重打 v1.4 包（121.9 KB）
- [x] Round 48：转入数据源 0 笔拒绝（用户指定）——initTransferBtn 双层拦截：前置快查（lastQueryData 为空 + reportKey/筛选 JSON 快照一致 → 直接拒绝不发请求）+ 查询后兜底（重查仍 0 笔 → 撤销占位源 + 切回查询页 + toast「查无数据，所以无法为您进行转入数据源」）；lastQueryData 扩充 reportKey+filters；i18n 三语 +1 key；版本号 ?v=20260819a（i18n-data·app）；文档四件套同步

**待验证（浏览器实测 index.html）**：

- [ ] 登录 → 打开总分类账：账簿下拉预选（本账套 1 条 00000000）→ 查询 → 流式进度条推进 → 表格数据渲染 + 分页
- [ ] 会计期间筛选（默认当前月）→ 重查；重置按钮：账簿回第一项、期间回当前月
- [ ] 0 账簿场景（换无总账账套登录）：警告弹窗 + 【查询】【转入】禁用 → 切其他报表按钮恢复
- [ ] 下拉列表多账簿（>1）预选第一个；切繁/英：菜单【總帳報表/General Ledger Reports】、警告文案、表头全随语言
- [ ] 大期间数据量下多页行为实测（当前 PAGE_COUNT=1 单条；大结果集是否拆多条消息未知）
- [ ] 转入 0 笔拒绝（Round 48）：查询出 0 笔 → 点【查转入数据源】→ 即时 toast 拒绝、不切 Tab、无占位数据源；未查询过直接点转入且结果 0 笔 → 占位源撤销 + 切回查询页 + toast；改筛选未重查且新筛选结果 0 笔 → 同兜底路径

### 21. API5 八报表 — 长连接（SSE）推广 ✅ 代码完成（Round 51，待浏览器验证）

**概述**：8 只新报表（总账 4 只依赖账簿：科目余额表/资产负债表/利润表/现金流量表；生产制造 4 只无账簿：物料分析明细/在制成本明细/在制原料明细/直接原料明细），全部 `GetReportStream` 长连接。用户核心要求：① 逐一分析注意事项再动手；② **总结写入 API服务调用说明文档.md**。

**已完成（分析 + PoC + 文档层）**：

- [x] API5 文档 2684 行全文逐报表提取（端点/PGM/SEARCH_INFO/fixCondition/响应行/PERCENT 落点）
- [x] 设计决策两轮对齐（STAT_GROUP 原样传 / 层级缩进加粗 / 科目余额表仅账簿+期间 / MRPCE 汇总行转入剔除 / 五阶段中文列名 / 日期可编辑预填 / MRPCU 工单号筛选）
- [x] PoC curl 实测 14 项：BOOK_NO=00000000 真实账簿出数据；BOOK_NO 空 = 200+ERR（**非 API4 的 406**）；服务器无 REM_TYPE → 扁平行；财务报表 3 样式 STD001/2/3 ERP 侧不存在 →「报表样式不存在或无访问权限」；制造 4 只端点正常但服务器无制造数据（MRPCT CODE=12 空结束）；COLUMN_INFO 空数据时是 {} 对象；PAGE_COUNT 不可靠（有数据为 0/无数据为 1）
- [x] **API服务调用说明文档.md 第十一节入库**（11.1 概述/11.2 八报表对照速查/11.3 实测 14 项/11.4 与 getReport·API4 差异/11.5 坑清单 13 条）

**已完成（代码层）**：

- [x] `buildRequestStream` 配置驱动化：`cfg.stream`（fixCondition @占位符模板/elements/topFields/statGroup/showLadder/orderBy/datePreset）+ `buildStreamCtx`（@BOOK_NO/@PERIOD/@YEARS/@IPERIOD/@DATE_B/@DATE_E/@START_DD/@MONTH_FIRST/@MONTH_LAST/@YEAR_FIRST/@TODAY）；accgl 29 键 fixCondition 与旧硬编码逐字一致
- [x] query() 流分支收集 `COLUMN_INFO` → `_currentColumnInfo`（Array.isArray 防护）
- [x] 8 报表 REPORT_CONFIG（accBalTable/accZcfzb/accLrb/accXjllb/mrpcu/mrpct/mrpcx/mrpce）；筛选面板 layoutMap + 标签分支（工单号/母件代号/生产货品/材料代号）+ 日期预填；MENU_ORDER 排序
- [x] 渲染特例：财务报表动态列（renderColumns:'columnInfo' 前导列 ITEM_NO/ITEM_NAME + COLUMN_INFO 映射）；SPACES 层级缩进 16px/级 + .row-level1 加粗；_SKIP_STAT='T' → .row-total 加粗；DECIMAL/CURRENCY_FIELDS + COLUMN_LABELS（科目余额 8 列 + 财务 7 列 + MRPCU 11 列 + 五阶段 46 列 + MRPCE 5 列）
- [x] 转入数据源：initTransferBtn 过滤 `_SKIP_STAT==='T'`；buildFilterSummary 追加 账簿/会计期间
- [x] ui-template.html 演示块【预览：财务报表层级】【预览：MRPCE 汇总行】（模板先行）
- [x] i18n 三语（8 报表名 + 筛选标签 + 动态列标题 + 五阶段 + 演示 key，zh-tw/en 各 ~90 key）；所有改动 JS node --check 通过
- [x] ?v= bump（i18n-data/reports/report-menu/datasource-list/app/dashboard.css → 20260819b）；deploy.ps1 $Version → "1.5"
- [x] 文档四件套（对话记录 Round 51 / 本表 / 需求架构文档 4.11 + 40 报表清单 / 流程图 K32~K39 + README 40 报表）

**待验证（浏览器实测 index.html）**：

- [ ] 8 只逐一打开：筛选面板正确；生产 4 只**不出现「请选择账簿」拦截**（needsBook 收窄生效）
- [ ] 科目余额表：账簿 00000000 + 当前期间 → 进度条 → 12 金额列 + 分页；REM_TYPE 列（服务器当前不返回）
- [ ] 财务报表 3 只：报「报表样式不存在或无访问权限」错误展示（样式 ERP 侧建立后：动态列表头 年初数/期末数/本期发生数/本年累计数 + 层级缩进 + 一级加粗）
- [ ] MRPCE：年初~今天预填 → 查询；汇总行 _SKIP_STAT='T' 加粗且【查转入数据源】结果不含汇总行
- [ ] 生产 3 只日期可编辑重查；MRPCU 工单号/母件代号筛选
- [ ] 8 只全部转入成功且筛选摘要含 账簿/会计期间（总账 4 只）
- [ ] 语言切换三语走查；空数据/超时；accgl + 31 只 getReport 抽查回归

**风险与待办**：

- ⬜ 财务报表 3 样式 STD001/2/3 需 **ERP 侧建立/授权**（已告知用户，样式就绪前端即可用）
- ⬜ 制造 4 只本服务器无数据，行格式以文档样例为准，**待 ERP 数据后回归**（五阶段/成本构成列语义、MRPCU 列标签一并校核）
- ⬜ 登录端点当日 code 10002 REGCHECK_ERR 环境性现象备查（旧 token 仍可用）

### 22. 三财务报表【报表样式】下拉 — RPT_NO 动态化 ✅ 代码完成（Round 52，待浏览器验证）

**概述**：Round 51 财务报表因样式不存在报错；用户 ERP 已建立 4 样式（STD001 资产负债表月结/STD002 利润表/STD003 现金流量表/STD004 资产负债表年结）。三报表查询条件增加【报表样式】下拉。数据链路（用户指定 + PoC 实测）：`AccBook/GetList 账簿行 TYPE_NO → accRptStyle/getlist → 样式清单 → 按 RPT_TYPE 过滤进下拉 → 查询传所选 RPT_NO + 该样式所属 TYPE_NO`；**账簿一旦改变样式清单必须重取**；AccType/getlist 不在链路内（需 TYPE_NAME 时才查）。

**已完成**：

- [x] PoC 实测：账簿响应行直接带 TYPE_NO "1"（displayFields 已含，零请求变更）；写死 "3" 报「报表样式不存在」vs 账簿行 TYPE_NO "1" + STD001 出 67 行完整数据
- [x] 模板先行 ui-template.html【预览：财务报表查询条件】（用户确认「看到了，可以！」；链路文案已按账簿行 TYPE_NO 修正）
- [x] js/rpt-style-store.js：按 compno+BOOK_NO 两级缓存（账簿改变重取），ensureLoaded(bookNo, typeNo)，getStyles/getStyle/getCachedStyles 均按账簿
- [x] reports.js：accglStyle 新布局 + 三 config @RPT_NO/@TYPE_NO 占位符 + topFields 走 ph() + buildStreamCtx/readFilters/allGroups/layoutMap
- [x] app.js：doQuery 拦截 + 按账簿查样式注入 TYPE_NO / populateRptStyleSelect(bookNo) / loadRptStylesForBook / 账簿 change 监听重取 / openReport 链路 / 重置流程（含会计期间重置旁路修复）/ option data-type-no
- [x] **时序修正**（用户实测反馈：下拉卡「加载中」+ 利润表串出资产负债表样式）：openReport 先渲染面板再异步拉样式填充（`_openReportCore(reportKey, skipAutoQuery)` → `loadRptStylesForBook(reportKey, bookNo)` → 就绪后 doQuery）；populate 用传入 reportKey 过滤、不读 AppState.currentReport；账簿 change/重置调用点同步改；版本 bump 20260819d
- [x] **转入数据源 TYPE_NO 修复**（用户实测反馈：卡片报「❌ 科目表代号不能为空」）：TYPE_NO 注入下沉到 readFilters——样式选项带 data-type-no + readFilters 返回 styleTypeNo，转入的独立全量请求也能拿到；版本 bump 20260819e
- [x] datasource-list.js 摘要加报表样式 + i18n 6 key 三语 + deploy.ps1 加入新模块 + 垃圾文件 cfg.needsBook、1 清理 + node --check 全过

**待验证（浏览器实测 index.html）**：

- [ ] 资产负债表：打开后下拉短暂「加载中」→ 填类型 2 样式且预选 STD001 → 自动查询出数据（无样式错误）；换 STD004（年结）查询 RPT_NO 生效
- [ ] 利润表预选 STD002 / 现金流量表预选 STD003；**三报表互切下拉重新过滤、不串样式**（回归：曾出现利润表显示资产负债表样式）
- [ ] **切换账簿**：样式下拉回「加载中...」→ 按新账簿重拉样式清单（多账簿账套验证；当前账套仅 1 账簿，此条待多账簿环境）
- [ ] 转入数据源摘要含「报表样式=STD001 · 名称」
- [ ] 重置按钮：账簿回第一项 → 样式按账簿重取后查询、会计期间回当前月
- [ ] 登出重登：下拉回占位 → 打开报表重拉成功；接口失败：toast 报表样式加载失败 → 下拉「无可用样式」+ 不自动查询
- [ ] 0 样式/该类型无样式：下拉「无可用样式」+ 查询拦截 toast；账簿无 TYPE_NO → toast 账簿无科目表代号

### 23. AI 聊天裸 JSON 直接渲染表格 ✅ 代码完成（Round 53，待浏览器验证）

**概述**：AI 对话中模型经常不遵守「必须使用 ```table 格式」指令，直接吐裸 JSON 数组（如 `[{"序号":1,"项目":"流动资产合计",...}]`）→ 旧解析规则不识别 → 聊天里原样显示成转义文本看不懂；而点「生成 HTML」导出却是好表格——不是技术/效率原因，是解析规则覆盖缺口（导出与聊天共用 `renderTableHTML`，渲染能力早已具备）。方案（用户批准「是的」）：扩展 AIParser 识别规则。

**已完成**：

- [x] ai-parser.js 新增 5 函数：`findJsonEnd`（括号平衡+字符串/转义感知扫描）、`isPlainObject`/`isObjectArray`（非空对象数组判定）、`tryTableOrCode`（```json 围栏内容：对象数组 → 表格；解析失败/非对象数组/单对象 → 代码块原样展示）、`bareJsonScan`（逐字符扫描，``` 行切换围栏态；围栏外行首 `[` 开头[含缩进] → 提取完整 JSON → 验证 → 渲染）、`renderJsonTables`（先处理 ```json 围栏，围栏外跑 bareJsonScan）
- [x] `renderWithTables()` 末尾 renderMarkdown → renderJsonTables（聊天端 chat-ui.js / chat-core.js 两条路径都走 AIParser.parse，一次改动全覆盖）
- [x] 防误伤四规则：非 json 代码围栏内不转换；解析失败不动；非对象数组（如 `["a","b"]`）/单对象不动；仅行首 `[` 识别
- [x] **Node 实测 24 项全过**（裸 JSON 出表格/数字列右对齐/缩进与字符串内括号/转义引号/```json 四形态/防误伤 5 项/```table·table\n 旧模式回归/管道表格回归/chart 块回归/空串/普通文字/中文空值混合），临时脚本当场清理
- [x] 版本号 bump：ai-parser.js `?v=20260814a` → `?v=20260819f`（index.html + ui-template.html 两处）
- [x] 导出端 export.js 未动：buildHTMLReport 已把围栏转 %%CODE%% 占位（export.js:119），围栏保护天然成立；可选后续把 bareJsonScan 接上（用户未要求）
- [x] **浏览器实测修复①**（用户反馈新回复仍裸 JSON + 字节级证据破案）：真实回复是「加粗 + 闭合 ```table 围栏 + 闭合 ```chart 围栏 + 洞察文字」组合——病灶是 parse() chart 循环里 chart **之前**的文本走 renderMarkdown（不是 renderWithTables），chart 前的 ```table 围栏被降级成代码块。修复：chart 前段落改走 renderWithTables；另加未闭合 ```table/```json 围栏兜底（不进入跳过态、围栏行丢弃、下方 JSON 按裸 JSON 规则转）。Node 实测 22 项全过（含用户真实消息结构复刻 9 项）；版本 bump ?v=20260819f → 20260819g
- [x] 教训入档：验证命令不能用模块私有函数（`typeof AIParser.bareJsonScan` 新旧代码都 undefined，引发误判）；必须行为级验证（`AIParser.parse('[{"a":1}]').html`）；"另一条路径"自检要覆盖 chart 循环段落

**待验证（浏览器实测 index.html）**：

- [ ] Ctrl+F5 后重新问带图表的问题 → 表格正常显示（不再裸 JSON/代码块）；chart 图正常出图
- [ ] 让 AI 吐裸 JSON 数组 → 聊天直接显示表格；```json 围栏对象数组 → 表格；非对象数组/单对象/解析失败 → 代码块原样
- [ ] 防误伤：代码围栏内 JSON 不误转；普通文字回复正常；```table / table\n[JSON] 旧模式回归；未闭合围栏兜底
- [ ] 导出 HTML 报告回归（export.js 未动，理论不受影响）

### 24. 生产制造 9 只报表逐一实测验证 + 4 处修复 ✅ 代码完成（Round 54，待浏览器验证）

**概述**：用户提供账套 AT04（SAN/空密码，2022 年生产数据），与 ERP 客户端核对发现生产制造类别报表"ERP 能查、本项目没动静"——该类别此前因无合适账套数据从未验证。逐一实测 9 只报表（16 轮临时脚本，跑完即删）。

**实测结论**：mrpPK ✅ / mrpPS ✅ / mrppu ✅（成本年月 2022-07 → 1078 行）/ mrpce ✅（2022 → 4699 行）无需修改；mrpag/mrpcf/mrpcx 为日期锁死配置 bug 已修；mrpcu 需母件代号已加铁律；**mrpct 判定为 AT04 数据侧空表**（跨账套对照关键证据：AT02 同请求 119 行 → 请求格式正确；AT04 在制原料成本明细有 1223/1232 行非零成本但汇总表无记录；AT01 文档示例账套已无任何工单数据）。

**已完成**：

- [x] js/reports.js：mrpag dateFilter last_year→range、mrpcf this_year→range、mrpcx BIL_DD 元素 operator this_month→range（用户日期生效；回归 2022 → 5000/5000/26996 行，2026 对照组 0 行证明日期真实生效）
- [x] js/app.js doQuery：MRPCU 母件代号铁律（空值 toast 拦截不发请求；沿用 BOOK_NO/RPT_NO 模式；实测仅母件代号 3 行、全空 SQL 8623、仅工单号 0 行）
- [x] js/app.js 重置按钮 + init：写死 2026-01-01/2026-12-31/2025-07 → 动态当前年/月
- [x] js/i18n-data.js：「请输入母件代号」简/繁/英三语
- [x] 回归验证用 vm 加载真实 reports.js 构造器（非手工拼请求体）+ 真实 API；node --check 三文件全过
- [x] 教训入档：① 快捷操作符（last_year/this_month）服务端行为按报表而异、对老账套必空，日期条件一律 range 走用户输入；② "服务端无数据"必须跨账套对照才能归因；③ 测流式报表必须走 query→buildRequestStream 路径（误用 getReport 风格构造器得空壳请求曾造成 8623 误判，已澄清）

**待验证（浏览器实测 index.html，AT04 账套）**：

- [ ] 领退补料报表 mrpag：日期选 2022 → 出数据（修复前 0 行）
- [ ] 单位成本分析表 mrpcf：日期选 2022 → 出数据（修复前 0 行）
- [ ] 在制原料明细表 mrpcx：日期选 2022 → 出数据（修复前 0 行）
- [ ] 物料分析明细表 mrpcu：母件代号留空点查询 → toast「请输入母件代号」不发请求；填 1.01.01.092 → 出 3 行
- [ ] 在制成本明细表 mrpct：AT04 出「无数据」空态（与 ERP 客户端对照确认该报表同样为空则验证闭环）
- [ ] 重置按钮 → 日期回当前年区间、成本年月回当前月

**Round 55 追加（2026-08-20）**：用户复报单位成本分析表（mrpcf）仍无数据（跑源码）。排查：源码 mrpcf 已是修复后 range（✅），v1.7 包是旧配置（排除），vm+真实 API 实测 2022 → 5000 行、默认 2026-08 → 0 行（正确）、留空 → 5000 行。**根因：Round 54 改了三只 JS 但 index.html 版本号未 bump（?v= 仍是 20260819e/c），浏览器缓存跑了修复前旧代码**。已修：三处版本号 → ?v=20260820a；日期输入框移除硬编码 2026 value（init 动态填）。教训：改 js 必同步 bump index.html 版本号（收尾动作）。浏览器验证前必须 Ctrl+F5 强刷。

### 25. 转入数据源配额溢出修复（拆分多卡片 + 透明压缩）✅ 代码完成（Round 56，待浏览器验证）

**问题**：mrpcx 转入数据源报 localStorage QuotaExceededError。实测：流式报表转入拿全量 26996 行 = 10.19MB，超 ~5MB 配额；且配额按站点计，单纯拆分不解决。

**方案（用户选定）**：>5000 行拆多卡片 _1/_2/_3… + LZString 透明压缩（26996 行 → 6 卡共 0.65MB）。

**已完成**：
- [x] utils.js：compressText/decompressText（LZString 缺失降级原样）
- [x] datasource-store.js：>50KB 透明压缩 + _z 标志 + 旧格式兼容 + 内存缓存 + storage 事件失效 + 配额友好报错
- [x] notepad-store.js：全容器压缩信封（记事本快照同一配额路径）
- [x] app.js：转入 >5000 行 → 拆 _1/_2/… 卡片一次性 saveAll（升序、id 加序号、20 卡上限）+ 拆分 toast
- [x] i18n-data.js：拆分 toast + 配额报错三语
- [x] index.html + ui-template.html：LZString CDN + 5 处版本号 bump
- [x] 回归 17/17：vm + 真实 26996 行（拆分顺序/计数/往返一致/旧格式兼容/小配额友好报错/记事本往返/Utils 往返）

**待验证（浏览器 Ctrl+F5，AT04）**：
- [ ] mrpcx 2022 转入 → 6 张卡片 _1…_6，计数 5000×5 + 1996，无配额报错
- [ ] AI 提问正常（每卡采样 20 行注入）
- [ ] 删除卡片 / 记事本保存恢复正常
- [ ] 旧数据源（修复前转入的）仍可正常读取

### 26. 登录页系统设置入口 + 登录前置检查 ✅ 代码完成（Round 57，待浏览器验证）

**问题**：别的机器通过浏览器登录时 localStorage 为空，服务器地址默认 localhost → 登录必然失败报网络错误；用户要求在登录页提供系统设置入口，且未正确配置时不强行登录而是提醒配置。

**已完成**：
- [x] index.html + ui-template.html：登录卡片（白色窗体）右上角新增【系统设置】齿轮按钮（`#loginSettingsBtn`，与主界面齿轮同款 SVG/按钮样式，随登录页一起隐藏）；两处齿轮补 `data-i18n-aria-label`（icon-only 按钮无障碍，Skill 规范）。**用户修正**：初版锚在浏览器页面右上角 → 改为锚在白色登录卡片右上角（按钮移入 `.login-card`）
- [x] css/auth.css：`.login-card` 加 `position:relative` + `.login-settings-btn` 绝对定位卡片右上角（auth.css 版本号二 bump → `?v=20260820d`）
- [x] js/settings-store.js：新增 `isConfigured()`——设置已保存 且 serverUrl 非空、`http(s)://` 开头才算配置正确
- [x] js/settings-ui.js：登录页齿轮绑定 openSettingsModal（与主界面齿轮共用同一设置面板）
- [x] js/app.js 登录提交：配置检查前置——未通过 → 不发登录请求，loginError 提示「请先进行系统设置，配置服务器地址后再登录」+ 自动打开设置弹窗
- [x] js/i18n-data.js：新提示文案三语（简/繁/英）
- [x] ui-template.html：新增演示按钮「演示：未配置系统设置时点登录」（临时移除 sunlike_settings → 触发登录 submit → 拦截演示 → 恢复设置，不发真实请求）
- [x] 版本号 bump：auth.css / i18n-data.js / settings-store.js / settings-ui.js / app.js → `?v=20260820c`（Round 55 纪律）；node --check 4 个 JS 全过
- [x] 根目录 4 个 0 字节垃圾文件当场删除（50KB、s.FIELD).join('、璇存槑锛歅ERCENT、锛圧ound——垃圾清理纪律）

**待验证（浏览器 Ctrl+F5）**：
- [ ] 登录页右上角出现齿轮；点击 → 打开系统设置弹窗；保存/取消正常
- [ ] 清空浏览器 localStorage → 填账号密码点登录 → 不发请求，提示「请先进行系统设置，配置服务器地址后再登录」+ 自动弹设置面板；配置服务器地址保存后重新登录 → 正常进入
- [ ] 已配置设置（现有机器）点登录 → 不弹拦截，照常登录（零回归）
- [ ] 主界面齿轮照常可用；登出后登录页齿轮可见、登录后不可见

### 27. 总账 0 账簿静默降级 Online（Round 59）🟡 代码完成，待浏览器验证

**概述**：AccBook/GetList 返回 0 账簿时，5 只总账报表**静默降级**（不弹窗、不 toast——用户拍板）到 Online 空白纸打印版端点（RPTACCBlank/RPTACCDetail/RPTZFListA/RPTSYListA/RPTCshFroList 的 GetReportStream，无账簿概念）。用户另拍板：①公式框预填文档示例值 10/20/30/40；②日期沿用「会计期间(月)」映射月初/月末；③5 只全部按响应 COLUMN_INFO 动态列渲染。

**已完成**：

- [x] PoC 实测（三步验证法）：5 端点 curl 逐只验证；REPNO1/2/3 字段名双对照定论（文档参数表 REPO1/2/3 为笔误）；公式值 10/20/30/40 有效；响应表键 RPT/MyTable/RPT_CSHFROLIST；嵌套 COLUMN_INFO；SSE pretty JSON 带空格陷阱
- [x] ui-template.html 原型：公式组两态（3 框/1 框）+ 演示按钮，**公式栏位自动判断**（inputs.length 3→onlinerepno3、1→onlinerepno1、0→无，用户两次纠正后的定论：按端点输入参数自动推导，不让用户挑场景）→ 用户浏览器确认通过
- [x] js/api.js：fetchStreamReport `dataKey` 参数化（累积 `msg.DATA[dataKey]`，默认 REPORT__TAB 不变）
- [x] js/reports.js：5 只 REPORT_CONFIG 加 `online` 块；`buildOnlineBody`（扁平 body + @占位符 + inputs 注入）；query() 流分支 onlineMode 判别 + 嵌套 COLUMN_INFO 解包；`_ledgerOnline` 模块状态；readFilters 携带 onlineMode + 公式值；getVisibleColumns 在线强制动态列弃 leadingColumns；ONLINE_COLUMN_LABELS 按 reportKey 分表；updateFilterPanel 清 _currentColumnInfo + onlinerepno3/1 组 + 4 个 Online 布局；CURRENCY/DECIMAL_FIELDS 追加 Online 金额列
- [x] js/app.js：openReport 0 账簿分支改静默降级；doQuery 拦截加 !onlineMode 守卫；`_ledgerBlocked` 死代码全清；resetBtn 公式框回默认值 10/20/30/40
- [x] index.html：公式输入两组 + ?v bump（api/reports/app/i18n-data → 20260831a）；i18n-data.js 三语 +40 key、删失效「未启用总账」文案；node --check 四文件全过
- [x] 总账报表调用方法.md 重写 + Online 原始文档整并（第六章 Online 版：端点/参数表/列清单/响应差异/金额语义/账套实测地图/陷阱/前端实现映射；CLAUDE.md 索引同步）
- [x] 文档四件套同步（报表明细清单 0 账簿降级端点列 / 流程图 15.1 静默降级 + 15.4 Online 请求构造 / 需求架构 4.13 / 本表）
- [x] **AT01~AT04 账套实测地图 + 参数矩阵复测**（推翻两条旧结论：AT03 zcfzb/lrb 公式有效能出表；账套间端点行为逐套而异）：AT02 全端点真实数据；AT01 凭证作用域不匹配；AT03 仅科目余额表 SQL 错（FN_GET_ACCN_USR 缺失，用户 Postman 复测确认 API 侧问题 → 2026-08-31 拍板搁置，等新 API 再试）；AT04 lrb ##CSH_FRO_TEMP 冲突

**已完成浏览器验证**：

- [x] AT01（books=1）标准版路径回归：请求体与旧版一致
- [x] AT03 科目余额表：静默打开（无弹窗）、Online 端点命中（堆栈 RPTACCDetailController）、面板「会计期间+部门」无公式框（自动判断正确）；SQL 错透出正常（数据层问题已定论搁置）

**待验证（浏览器 Ctrl+F5）**：

- [ ] AT03 其余 4 只：accZcfzb=期间+3 公式框（预填 10/20/30）出 6 行公式壳、accLrb=期间+1 公式框（40）出 23 行公式壳、accgl/accXjllb=仅期间出空表
- [ ] 转入数据源：online 模式二次查询走 Online 端点、数据源卡片正确
- [ ] 重置：公式框回 10/20/30/40、再查询仍走 Online
- [ ] 切非总账报表：面板恢复、端点恢复（flag 复位）；登出重登：状态清空
- [ ] （可选，手段 B）AT02 + DevTools 拦截 AccBook/GetList 返回空 + BookStore.reset() → 真实数据完整验证 Online 链路

### 28. 总账 Online 公式下拉（GetAccRepNoList，Round 60）🟢 已完成（Round 61 验证收尾）

**概述**：REPNO 公式代号原为文本框手输（对话记录 2344 决策），易输错。ERP 新增 `billcommon/GetAccRepNoList`（原始文档：制表公式-列表查询.md）——打开 Online 资产负债/利润表前先拉公式清单，成功后公式框换装**下拉选单**（全量清单 + 预选默认值）。用户 2026-09-01 决策：①下拉放**全量清单**（实测 14 条，文档示例只节选 4 条；AT02/AT03 一致）；②**拉取失败 = Dialog.alert「没有报表公式，无法进行查询」并中止打开**（拉不到即该账套无公式资料，手填必错；原「换回文本框」方案作废）。

**已完成**：

- [x] PoC 三步验证：A 正确路径 `{}` → code=0、data.Table 14 条；B 带 api/ 前缀 → 404（复验无前缀铁律）；C 全小写可通；D body `""`/`{}` 均可；AT02/AT03 清单一致
- [x] js/acc-repno-store.js：照抄 rpt-style-store 模式去 bookNo 维度（LS_KEY sunlike_acc_repno、内存+localStorage 按 compno、_pending 并发去重、_gen 世代、失败重试一次、REP_NO 全链 String 强制）
- [x] js/reports.js：renderOnlineRepnoSelects（input→select 换装 + 14 条「REP_NO · NAME」+ 预选 defaultValue，缺则插兜底项）/ restoreOnlineRepnoInputs（登出换回文本框，幂等）/ getOnlineRepnoDefaults（默认值唯一真源，resetBtn 收敛用）
- [x] js/app.js：openReport 0 账簿分支改**加载前置**（先拉清单再渲染——失败时 _openReportCore 从未执行，屏幕保持原状 = 退出该报表）；`_openSeq` 序号竞态守卫（加载前置时 currentReport 还是上一只，判等守卫失效）；resetBtn 默认值收敛；resetLedgerBooksState 加 AccRepNoStore.reset() + restoreOnlineRepnoInputs()
- [x] ui-template.html：公式下拉演示（mock 14 条实测清单 fillDemoRepnos + 失败演示按钮）→ **用户浏览器确认通过**
- [x] i18n-data.js：新键「没有报表公式，无法进行查询」三语；css/dashboard.css：select 宽 190px；index.html：acc-repno-store.js 脚本 + 5 处 ?v bump（20260901a）；node --check 四文件全过
- [x] 制表公式-列表查询.md 实测备注；总账报表调用方法.md 2.3 依赖清单 + 6.7 映射补 2 行；CLAUDE.md 索引；需求架构 4.13 / 流程图 15.5；deploy.ps1 v1.10（32 文件）打包无 MISSING

**已验（Round 61 收尾，2026-09-01）**：

- [x] AT03 打开 accZcfzb → 3 公式下拉（预选 10/20/30）→ 自动查询出表；accLrb → 1 下拉（预选 40）；手动改选公式 → 请求体 REPNO 值跟随（用户浏览器确认）
- [x] 快速切换 zcfzb↔lrb 面板收敛正确（_openSeq 守卫）；重置 → 回默认并重查（用户浏览器确认）
- [x] DevTools 屏蔽 billcommon 请求 → 弹「没有报表公式，无法进行查询」、报表未打开、屏幕保持原状、无查询请求（Claude 哈啰代验 test-open-fail.js：真实 app.js 驱动，11 断言全 PASS——失败重试一次、请求逐字符合文档、zh-cn/en 双语文案、currentReport 未写、ledgerOnline 撤销、竞态只弹一次警告）
- [x] 登出重登 → 公式框回文本框默认值；切英文界面 → 警告文案英文；其余 3 只（无公式框）行为不变；转入数据源正常（用户浏览器确认）
- [x] AT02（有账簿）zcfzb/lrb 走样式链路完全不变、公式组不出现（用户浏览器确认）

---

### 29. 总账 Online 5 只简繁英补丁（Round 61）🟢 已完成

**概述**：用户反馈 Online 5 只报表切繁/英后表头、资料、下拉仍显简体。排查定论：表头链路本身正确（陈旧缓存/繁简同形字视觉误判）；真实缺陷 = 资料格固定文案不翻译 + 公式下拉选项名直出服务端简体 + 语言切换不重渲染下拉。

**已完成**：

- [x] 排查：表头链路（I18n.t 列名链）逐只哈啰复核正确，EN 模式 0 CJK 表头
- [x] reports.js：ONLINE_REPNO_NAMES（14 条 REP_NO→简体名）+ _repnoOptionLabel（I18n.t，未知回退服务端 NAME）；ONLINE_VALUE_FIELDS 逐字段白名单（accgl: SHOWDC/REM/ACC_NOTE/YEARMONTH；accBalTable: SHOWDC/NAME）+ ONLINE_FIXED_VALUES + translateOnlineCell（固定词组直翻 + YEARMONTH/REM 模式翻译，月补零）；renderTableBody 接 _ledgerOnline 门（标准版零影响）；renderOnlineRepnoSelects 选项名本地化 + `cur || defaultValue` 选中保留
- [x] app.js：refreshDynamicI18n 在 ledgerOnline 时重渲染公式下拉（保留选中）
- [x] i18n-data.js：zh-tw/en 各补 22 键（14 公式目录名 + 借/贷/承上期/小计/合计(:) + 2 个模式键）；实测计数 **807 key × 2 语言、两语对称 0 缺失**
- [x] ui-template.html：fillDemoRepnos 同源镜像（I18n.t 选项名 + i18n:changed 重渲染 + 选中保留）+ 3 处 ?v；index.html 3 处 ?v → 20260901c
- [x] 验证：node --check 四文件全过；三语哈啰 16 断言全 PASS（资料格固定文案三语/自由文字不误翻/YEARMONTH+REM 模式/下拉选项三语名/未知回退/默认预选/语言切换选中保留）；用户浏览器 Ctrl+F5 确认通过
- [x] 边界三问：空值/未知文案原样返回；清单为空走兜底选项；无公式框 3 只 app.js 条件跳过；标准版路径零影响
