# CLAUDE.md

> 本文件来自《项目经验行动指南》中必须严格遵守的执行纪律。
> Claude 在每次回复前应检查是否符合以下规则。
当我说【要开新对话】，就赶紧的把没有写入对话记录.md的对话，或者还没同步进度追踪表、需求架构文档.md等文件，赶紧同步一下
---

## 一、对话节奏

```
编码前     → 查文档、做 PoC、列 checklist
模块完成后 → 对照流程图、覆盖所有路径、补齐边界
对话结束时 → 对话记录 ✅ | 计划书 ✅ | 进度表 ✅ | 流程图 ✅
```

---

## 二、API 对接铁律（三步验证法）

对接任何 API 之前，必须：

1. **Step 1**：打开原始 API 文档阅读（不是用户的口头描述）
2. **Step 2**：拿到可工作的 curl/Postman 示例
3. **Step 3**：逐字段对照文档定义与代码实现

具体验证项：
- [ ] API 完整路径（含所有路径前缀，本项目为 `/SUNFUSION/API/`）
- [ ] 请求参数的**确切字段名**（大小写、缩写），绝不凭"常识"猜测
- [ ] 请求体结构（扁平/嵌套？数组按索引解析？）
- [ ] 成功判断条件（本项目是 `response.code === 0`，❌ 不是 `response.ok`）
- [ ] 每个字段是必填还是可选
- [ ] 错误返回格式（用错误参数发请求实际验证）

---

## 三、本项目 API 基础信息（已 PoC 验证）

| 项 | 值 |
|----|-----|
| Base URL | `http://localhost/SUNFUSION/API` |
| 登录端点 | `POST /user/login` |
| 登录必填字段 | `COMPNO`, `USR`, `PWD`, `LANG_ID`, `SYS_TYPE` |
| Token 路径 | `response.data.TOKEN` |
| 认证方式 | `Authorization: Bearer {TOKEN}` |
| 成功判断 | `response.code === 0` |
| 报表请求体结构 | `{ PGM, SEARCH_INFO: [...], DISPLAY_FIELDS }` — SEARCH_INFO 按索引 [0]~[9] 固定顺序 |

---

## 四、口头禅纪律

| 禁止 | 用这个替代 |
|------|-----------|
| "应该是…" | "文档怎么写的？" |
| "我觉得…" | "实测结果是什么？" |
| "等一下一起补" | "现在就写" |
| "这个简单" | "这个已经验证过了吗？" |
| "差不多" | "逐项对照过了" |

---

## 五、每轮对话结束检查

不宣告一轮对话结束，直到以下全部完成：
- [ ] 对话记录.md 已更新（当场写，不拖）
- [ ] 需求架构文档.md 已同步
- [ ] 进度追踪表.md 已更新
- [ ] 流程图.md 需要修改吗？
- [ ] 以上全部完成 → 本轮真正结束

---

## 六、模块完成自检

每个模块完成后追问三个边界问题：
1. 输入为空/超大/乱码 → 会怎样？
2. 网络环境变了（离线/超时/401）→ 会怎样？
3. 有另一条路径能到达这里吗 → 也覆盖了吗？

---

## 七、对话记录规范

**对话记录.md 是每轮对话的流水账，不是摘要。**
- 每一轮对话都要追加，记录做了什么、决定了什么、遇到了什么问题
- 当场写，不等"稍后补"
- 格式：日期 + Round N + 主题 + 完成事项/问题/决策

---

## 八、UI 设计规范

1. **设计界面必须使用 UI/UX Skill**（`ui-ux-pro-max`），先跑设计系统再动手
2. **UI 模板（`ui-template.html`）是所有界面的唯一原型**：
   - 任何新界面/新组件 → 先在模板里做，用户浏览器确认通过 → 才能写正式代码
   - 后续 UI 调整 → 同步更新模板
   - 模板是设计合约，代码必须匹配模板

---

## 九、编码纪律

1. **没弄清楚、没和用户确认之前，不要开始写代码**
2. **所有 API 调用参考 `API服务调用说明文档.md`**，不要凭记忆或口头描述写请求
3. 先问清楚三个问题再动手：
   - 这个功能给谁用？
   - 核心痛点是什么？
   - 期望的体验是什么样？

---

## 十、项目文件索引

| 文件 | 用途 |
|------|------|
| 标准报表制表API.md | 6 个报表 API 原始文档（采购/进货/受订/销货/收款/付款 — getReport 端点） |
| 标准报表制表API2.md | 4 个报表 API 原始文档（工单完成/完工入库/产品成本分析/员工薪资 — MRPPU 用 getList，其余 getReport） |
| 标准报表制表API3.md | 21 个报表 API 原始文档（财务/库存/采购价格/生产/人事/固定资产 — 全部 getReport） |
| API服务调用说明文档.md | Login + 报表 API 实测文档（curl + 字段 + 错误码） |
| 需求架构文档.md | 项目需求和技术架构 |
| 对话记录.md | 每轮对话的流水账记录 |
| 进度追踪表.md | 任务进度和风险追踪 |
| 流程图.md | 业务逻辑 Mermaid 流程图 |
| ui-template.html | UI 模板（所有界面设计的原型） |
| 项目经验行动指南.md | 通用开发纪律速查卡（来源项目） |
