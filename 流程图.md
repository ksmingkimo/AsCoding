# 流程图

> 项目：Sunlike ERP 报表查询系统
> 创建日期：2026-08-10

---

## 一、系统整体流程

```mermaid
flowchart TD
    A[打开应用] --> B{已登录?}
    B -->|否| C[登录页面]
    C --> D[输入 USR / PSW / CPY / SID]
    D --> E[POST /user/login]
    E --> F{code === 0?}
    F -->|是| G[存储 TOKEN]
    F -->|否| H[显示错误信息]
    H --> C
    G --> I[进入报表 Dashboard]

    B -->|是| I

    I --> J[侧边栏选择报表]
    J --> K1[采购报表]
    J --> K2[进货报表]
    J --> K3[受订报表]
    J --> K4[销货报表]
    J --> K5[收款明细表]
    J --> K6[付款明细表]

    K1 --> L[设置筛选条件]
    K2 --> L
    K3 --> L
    K4 --> L
    K5 --> L
    K6 --> L
```

## 二、单报表查询流程（以采购报表为例）

```mermaid
flowchart TD
    A[进入采购报表] --> B[构建查询条件]
    B --> C[设置日期范围: OS_DD]
    B --> D[可选筛选: CUS_NO / PRD_NO / PO_DEP / WH / CHK_STATUS]
    B --> E[选择展示字段]
    B --> F[设置排序规则]

    C & D & E & F --> G[组装 SEARCH_INFO 数组]

    G --> H[POST /api/invpo/getReport]
    H --> I{code === 0?}

    I -->|是| J[解析 REPORT__TAB 数据]
    I -->|否| K[显示错误: message]
    K --> A

    J --> L[渲染数据表格]
    L --> M[用户操作: 翻页 / 改筛选 / 导出?]
    M -->|改筛选| B
    M -->|翻页| H
```

## 三、API 请求构造流程（所有报表通用）

```mermaid
flowchart TD
    subgraph SEARCH_INFO构造
        S0["[0] 分页: offset + temp"]
        S1["[1] 展示: displayFields + sumFields"]
        S2["[2] 固定条件: fixCondition"]
        S3["[3] 日期范围: field + operator + value"]
        S4["[4] 客户/厂商: CUS_NO"]
        S5["[5] 货品: PRD_NO"]
        S6["[6] 部门: DEP/PO_DEP"]
        S7["[7] 仓库: WH"]
        S8["[8] 审核状态: CHK_STATUS"]
        S9["[9] 排序: orderBy"]
    end

    REQ[组装完整请求体] --> BODY
    BODY["{ PGM, SEARCH_INFO: [...], DISPLAY_FIELDS }"]

    S0 --> REQ
    S1 --> REQ
    S2 --> REQ
    S3 --> REQ
    S4 --> REQ
    S5 --> REQ
    S6 --> REQ
    S7 --> REQ
    S8 --> REQ
    S9 --> REQ
```

## 四、认证流程（Token 生命周期）

```mermaid
flowchart TD
    A[应用启动] --> B{localStorage 有 TOKEN?}
    B -->|有| C{TOKEN 过期?}
    C -->|否| D[直接进入 Dashboard]
    C -->|是| E[清除过期 TOKEN]
    E --> F[跳转登录页]
    B -->|无| F

    F --> G[用户填写登录表单]
    G --> H["POST /user/login<br/>{USR, PSW, CPY, SID}"]
    H --> I{code === 0?}
    I -->|是| J["存 TOKEN 到 localStorage"]
    J --> K[设置 Authorization Header]
    K --> D
    I -->|否| L[显示错误]
    L --> G

    D --> M[发起报表请求]
    M --> N{HTTP 401?}
    N -->|是| E
    N -->|否| O[正常处理响应]
```

## 五、各报表 API 差异对照

| 要素 | 采购 | 进货 | 受订 | 销货 | 收款 | 付款 |
|------|------|------|------|------|------|------|
| 端点 | `/invpo` | `/invpc` | `/invSO` | `/invSa` | `/monAA` | `/monBA` |
| PGM | `REP_POLIST` | `REP_PCLIST` | `REP_SOLIST` | `REP_SALIST` | `REP_RTLIST` | `REP_PTLIST` |
| 日期字段 | `OS_DD` | `PS_DD` | `OS_DD` | `PS_DD` | `RP_DD` | `RP_DD` |
| 日期含义 | 采购日期 | 进货日期 | 受订日期 | 销货日期 | 单据日期 | 单据日期 |
| fixCondition 特殊字段 | 无 | `SA_BILLS` | `SUB_CUS` | `SA_BILLS` + `SEND_GROUP_FIELD` + `SUB_CUS` | `LINE` + `SHOW_LSIT` x3 + `INCLUDESON` | `LINE` + `SHOW_LSIT` x3 + `INCLUDESON` |
| 部门字段 | `PO_DEP` | `DEP` | `DEP` | `DEP` | `DEP` | `DEP` |
| 额外筛选 | - | - | - | - | `YW_TYPE` + `KB` | `YW_TYPE` + `KB` |

> ⚠️ 指南第二节提醒：每个报表的 SEARCH_INFO 结构看似相同，实则细节差异多（日期字段名、fixCondition 结构、额外筛选字段），**必须逐报表对照 API 文档**，不可复制粘贴后直接改 PGM。

---

## 六、UI 页面流

```mermaid
flowchart LR
    Login[登录页] --> Dashboard[Dashboard 主界面]

    Dashboard --> Sidebar[侧边栏导航]
    Sidebar --> R1[采购报表]
    Sidebar --> R2[进货报表]
    Sidebar --> R3[受订报表]
    Sidebar --> R4[销货报表]
    Sidebar --> R5[收款明细]
    Sidebar --> R6[付款明细]

    R1 & R2 & R3 & R4 & R5 & R6 --> FilterPanel[筛选面板]
    FilterPanel --> DatePick[日期范围选择]
    FilterPanel --> CustPick[客户/厂商筛选]
    FilterPanel --> PrdPick[货品筛选]
    FilterPanel --> DepPick[部门筛选]
    FilterPanel --> WhPick[仓库筛选]
    FilterPanel --> StatusPick[审核状态筛选]

    FilterPanel --> Table[数据表格]
    Table --> Pagination[分页组件]
```

---

## 七、AI 数据分析流程

```mermaid
flowchart TD
    subgraph 数据查询Tab
        A[选择报表] --> B[设置筛选条件]
        B --> C[点击查询]
        C --> D[展示数据表格]
        D --> E{用户操作}
        E -->|调整条件| B
        E -->|查转入数据源| F[保存数据源到 localStorage]
    end

    F --> G[自动切换到 AI数据分析 Tab]

    subgraph AI数据分析Tab
        H[显示数据源列表] --> I[用户输入问题]
        I --> J[构建 System Prompt]
        J --> K[注入所有数据源摘要]
        K --> L[调用 Deepseek API]
        L --> M{响应成功?}
        M -->|是| N[渲染 AI 回复]
        M -->|否| O[显示错误 + 重试按钮]
        N --> P[解析 Markdown / Table / Chart]
        P --> Q[渲染消息气泡 + 操作按钮]
        Q --> R{用户操作}
        R -->|复制| S[复制到剪贴板]
        R -->|延申问答| I
        R -->|导出 Excel| T[下载 CSV 文件]
        R -->|导出 PPTX| U[下载 HTML 报告]
        R -->|继续提问| I
    end

    G --> H
```

---

## 八、Tab 页签切换流程

```mermaid
flowchart TD
    A[Dashboard 主界面] --> B{当前 Tab}
    B -->|数据查询| C[显示筛选面板 + 数据表格]
    B -->|AI数据分析| D[显示数据源列表 + Chat 界面]
    C --> E[用户点击查转入数据源]
    E --> F[保存数据源]
    F --> D
    D --> G[用户点击数据查询 Tab]
    G --> C
```

---

## 九、设置面板流程

```mermaid
flowchart TD
    A[首次使用 / 点击齿轮图标] --> B[打开设置模态弹窗]
    B --> C[填写 Deepseek API Key]
    B --> D[填写 ERP 服务器地址]
    C --> E[点击验证]
    E --> F{API 响应}
    F -->|200 OK| G[✅ 验证成功]
    F -->|401/错误| H[❌ 验证失败]
    D --> I[点击验证]
    I --> J{服务器响应}
    J -->|code=0| K[✅ 连接成功]
    J -->|错误| L[❌ 连接失败]
    G & H & K & L --> M[用户点击保存]
    M --> N[写入 localStorage]
    N --> O[关闭弹窗]
```
