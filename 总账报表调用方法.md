# 总账报表调用方法

> 适用范围：总账报表分组全部 **5 只**报表（总分类账、科目余额表、资产负债表、利润表、现金流量表）。
>
> **同一报表两套 API 并存**，按账簿清单条数分流（Round 59 起）：
> - **标准版**（账簿 ≥1 条）：PGM/SEARCH_INFO 骨架 + 账簿/报表样式链路 —— 本文第三~五章
> - **Online 空白纸打印版**（账簿 0 条，静默降级）：扁平请求体 + 报表公式代号，无账簿/样式概念 —— 本文第六章（内容已整并自 Online总账报表-查询制表.md，原始文档保留作溯源与完整数据示例）
>
> 本文逐项对照原始文档（[标准报表制表API4.md](标准报表制表API4.md)、[标准报表制表API5.md](标准报表制表API5.md)、[Online总账报表-查询制表.md](Online总账报表-查询制表.md)、[账簿列表查询.md](账簿列表查询.md)、[报表样式 列表查询.md](报表样式 列表查询.md)）与实测代码（`js/reports.js`、`js/api.js`、`js/app.js`），请求 JSON 可直接照抄使用。
> 生成日期：2026-08-28；更新：2026-08-31（整并 Online 版 + Round 59 降级实施）

---

## 〇、总账报表一览（5 只 × 2 套 API）

| # | 报表 | key | 标准版端点 | Online 版端点 | 标准版前置依赖 | Online 版公式参数 |
|---|------|-----|-----------|--------------|--------------|------------------|
| 1 | 总分类账 | `accgl` | `accGeneralLedger/GetReportStream` | `RPTACCBlank/GetReportStream` | 账簿 | 无 |
| 2 | 科目余额表 | `accBalTable` | `accBalanceTable/GetReportStream` | `RPTACCDetail/GetReportStream` | 账簿 | 无 |
| 3 | 资产负债表 | `accZcfzb` | `accRptPreview/GetReportStream` | `RPTZFListA/GetReportStream` | 账簿 + 报表样式 | `REPNO1/2/3`（默认 10/20/30） |
| 4 | 利润表 | `accLrb` | `accRptPreview/GetReportStream` | `RPTSYListA/GetReportStream` | 账簿 + 报表样式 | `REPNO`（默认 40） |
| 5 | 现金流量表 | `accXjllb` | `accRptPreview/GetReportStream` | `RPTCshFroList/GetReportStream` | 账簿 + 报表样式 | 无 |

> 两套 API 全部 **POST + SSE 长连接**。分流规则：登录后 `AccBook/GetList` 返回 **0 条账簿** → 5 只全部走 Online 版（前端完全静默切换，不弹窗不 toast，用户 2026-08-31 拍板）。

---

## 一、公共约定（5 只通用，两套 API 同）

### 1.1 基本信息

| 项 | 值 |
|----|-----|
| Base URL | `http://localhost/SUNFUSION/API` |
| 端点拼接 | 端点**直接**相对 Base URL。⚠️ 原始文档 URL 里的 `/api/` 段就是 `/SUNFUSION/API` 本身，**不要再带 `api/` 前缀**，否则拼成 `/SUNFUSION/API/api/...` → 404 |
| 请求头 | `Content-Type: application/json`；`Authorization: Bearer {TOKEN}` |
| 成功判断 | 清单接口 `response.code === 0`；报表流按 SSE 消息解析（见 1.4） |

### 1.2 登录拿 Token（第一步）

```
POST /user/login
Content-Type: application/json
```

| 字段 | 类型 | 必填 | 示例值 |
|------|------|------|--------|
| `COMPNO` | string | 是 | `"AT01"` |
| `USR` | string | 是 | `"SAN"` |
| `PWD` | string | 是（可为空串） | `""` |
| `LANG_ID` | string | 是 | `"zh-cn"` |
| `SYS_TYPE` | string | 是 | `"ERP"` |

```bash
curl -s -X POST http://localhost/SUNFUSION/API/user/login \
  -H "Content-Type: application/json" \
  -d '{"COMPNO":"AT01","USR":"SAN","PWD":"","LANG_ID":"zh-cn","SYS_TYPE":"ERP"}'
```

Token 路径：`response.data.TOKEN`。后续所有请求带 `Authorization: Bearer {TOKEN}`。

### 1.3 会计期间参数的派生规则（关键！）

调用方只需提供会计期间 `PERIOD = "YYYY-MM"`，其余字段一律由它派生（与 `js/reports.js` 的 `buildStreamCtx` 实测一致）：

| 派生字段 | 规则 | 示例（PERIOD=2026-01） |
|---------|------|------------------------|
| `ACC_IPERIOD_B` / `ACC_IPERIOD_E` | = PERIOD | `"2026-01"` |
| `YEARS` | 年份，Int32 | `2026` |
| `IPERIOD` | 月份，Int32 | `1` |
| `DATE_B` | 当月 1 日 | `"2026-01-01"` |
| `DATE_E` | 当月最后一天 | `"2026-01-31"` |
| `START_DD` | 当月 1 日零点 | `"2026-01-01T00:00:00"` |

> Online 版同样沿用「会计期间(月)」选择器：`dateB/dtB/dtDateB` = 月初（`DATE_B`）、`dateE/dtE/dtDateE` = 月末（`DATE_E`）；资产负债表只传 `dtE`。

### 1.4 SSE 响应解析规范（5 只通用，两套 API 同）

响应 `Content-Type: text/event-stream`，**逐行**解析，只处理以 `data: ` 开头的行，每行是一个完整 JSON 消息：

```
消息结构：{ CODE, PERCENT, TITLE, ERR, DATA }
```

| 字段 | 说明 |
|------|------|
| `ERR` | 非空即业务错误，直接抛错（如 BOOK_NO 空时实测返回 HTTP 200 + `ERR`） |
| `PERCENT` / `TITLE` | 进度回调（`100.0` 结束消息实测存在；数据行只出现在 PERCENT:100 消息） |
| `DATA.REPORT__TAB` | 标准版数据行数组，**跨消息累积拼接** |
| `DATA.COLUMN_INFO` | 标准版财务报表 3 只的动态列 `[{NAME,TITLE}]`；⚠️ 空数据时是 **`{}` 对象**不是数组。Online 版为嵌套对象 `{表键:[...]}`，见 6.3 |

错误三形态（`js/api.js` 三步判别实测）：

1. HTTP 非 ok → HTTP 错误；
2. Content-Type 含 `application/json` → 错误信封 JSON `{code, message}`（`20001`/`20004` 认证失效，需重新登录）；
3. SSE 流消息 `ERR` 非空 → 抛 `ERR` 内容。

⚠️ `PAGE_COUNT` 不可靠（实测有数据为 0 / 无数据为 1），不要依赖它。

---

## 二、依赖清单接口（仅标准版需要；Online 版无账簿/样式概念）

### 2.1 账簿清单 — 5 只都要（BOOK_NO 来源 + 分流判定）

```
POST AccBook/GetList
Authorization: Bearer {TOKEN}
```

请求体照抄（实测验证）：

```json
{
    "DEP": "00000000",
    "SEARCH_INFO": [
        {
            "showBody": "F",
            "showLadder": null,
            "showSumField": null,
            "displayFields": ["BOOK_NO", "NAME", "BOOK_XZ", "ACC_MD", "CUR_ID", "CUR_NAME", "TYPE_NO", "TYPE_NAME", "DEP", "DEP_NAME", "START_DD", "STOP_DD", "YEARS", "IPERIOD", "USR_NAME", "SYS_DATE", "MODIFY_MAN_NAME", "MODIFY_DD"],
            "sumFields": []
        },
        { "fixCondition": {} },
        { "field": "BOOK_NO", "operator": "in", "fieldType": "string", "fieldDisabled": false, "value": "" },
        { "field": "NAME", "operator": "contain", "fieldType": "string", "fieldDisabled": false, "value": "" }
    ],
    "PAGE_INFO": { "PAGE_SIZE": 200, "CURRENT_PAGE": 1 }
}
```

响应要点（`code === 0` 时取 `data.ACC_BOOK_BS` 数组）：

| 字段 | 用途 |
|------|------|
| `BOOK_NO` | 账簿代号 → 传给报表请求的 `fixCondition.BOOK_NO`（实测真实账簿 `"00000000"` 出数据，示例库 `"001"`） |
| `NAME` | 账簿名称（下拉显示） |
| `TYPE_NO` | **科目表代号** → 三财务报表报表样式链路要用（实测 `"01"`） |

> ⚠️ **`ACC_BOOK_BS` 为 0 条 = 账套未启用总账** → 5 只报表走 Online 版（第六章），不再弹警告禁用（Round 59 起）。

### 2.2 报表样式清单 — 仅三财务报表（RPT_NO 来源）

```
POST accRptStyle/getlist
Authorization: Bearer {TOKEN}
```

数据链路（用户指定，Round 52 修正）：**账簿行 `TYPE_NO` → accRptStyle/getlist 顶层 `TYPE_NO` → 样式清单**（AccType/getlist 不在链路内）。

请求体照抄（实测验证）：

```json
{
    "PGM": "ACCRPTSTYLE",
    "SEARCH_INFO": [
        {
            "showBody": "F",
            "showLadder": "F",
            "showSumField": "F",
            "displayFields": ["RPT_NO", "NAME", "RPT_TYPE", "REM", "STOP_DD", "USR_NAME", "SYS_DATE"],
            "sumFields": []
        },
        { "fixCondition": {} },
        { "field": "RPT_NO", "operator": "in", "fieldType": "string", "need": true, "fieldDisabled": false, "value": "" },
        { "field": "NAME", "operator": "contain", "fieldType": "string", "fieldDisabled": false, "value": "" },
        { "orderBy": { "RPT_NO": "asc" } }
    ],
    "PAGE_INFO": { "PAGE_SIZE": 200, "CURRENT_PAGE": 1 },
    "TYPE_NO": "01"
}
```

> 顶层 `TYPE_NO` = 账簿清单所选账簿行的 `TYPE_NO`（**不是写死**，实测为 `"01"`）。

响应要点（`data.MF_RPTSTYLE_BS` 数组），**按 `RPT_TYPE` 过滤**：

| RPT_TYPE | 报表 | 默认样式 |
|----------|------|---------|
| `"2"` | 资产负债表 | `STD001`（示例：资产负债表-月结；`STD004` 为年结） |
| `"3"` | 利润表 | `STD002` |
| `"4"` | 现金流量表 | `STD003` |

⚠️ 实测：部分服务器不存在 `STD001/2/3` → 查询时报「报表样式不存在或无访问权限」。样式清单为空时前端应提示先建立样式。

### 2.3 制表公式清单 — 仅 Online 资产负债/利润表（REPNO 下拉数据源，Round 60）

`POST billcommon/GetAccRepNoList`，请求体空对象 `{}`（原始文档：制表公式-列表查询.md）。

响应要点（`data.Table` 数组，**实测 14 条**，文档示例只节选前 4 条；AT02/AT03 清单一致）：

| REP_NO | NAME | REP_NO | NAME |
|--------|------|--------|------|
| 10 | 资产(一般企业) | 40 | 损益表(一般企业) |
| 11 | 资产 | 41 | 损益表 |
| 20 | 负债(一般企业) | 42 | 金融业损益表 |
| 21 | 负债 | 43 | 保险业损益表 |
| 30 | 股东权益(一般企业) | 44 | 证券业损益表 |
| 31 | 股东权益 | 50/51/61 | 费用明细表/营业成本表/管销费用明细表 |

> 前端行为（用户 2026-09-01 决策）：打开 Online 资产负债/利润表前拉取，成功后公式框换装下拉（全量清单、预选默认值 10/20/30/40）；**拉取失败 = Dialog.alert「没有报表公式，无法进行查询」并中止打开**（不渲染、不查询）。

---

## 三、标准版五只报表逐个

### 3.1 总分类账 — `accGeneralLedger/GetReportStream`

**请求体骨架**：`{ PGM, SEARCH_INFO: [展示配置, fixCondition, orderBy], DISPLAY_FIELDS }`

完整请求 JSON（期间 2026-01、账簿 001，可直接照抄）：

```json
{
    "PGM": "ACCRPTGL",
    "SEARCH_INFO": [
        {
            "showBody": "T",
            "showLadder": "F",
            "displayFields": ["ACC_NO", "ACC_NAME", "ACC_IPERIOD", "DC", "REM_TYPE", "FZHS_TITLE", "CUR_NAME", "AMTN_D", "AMTN_C", "AMTN_BAL"],
            "sumFields": []
        },
        {
            "fixCondition": {
                "BOOK_NO": "001",
                "CUR_ID": "",
                "ACC_IPERIOD_B": "2026-01",
                "ACC_IPERIOD_E": "2026-01",
                "CHK_ACCN_TYPE": "2",
                "ACC_NO": "",
                "ACC_NO_B": "",
                "ACC_NO_E": "",
                "REL_CLS_B": 1,
                "REL_CLS_E": 10,
                "AMTN_BAL_TYPE": "1",
                "CHK_POSTACC": "F",
                "CHK_STOP": "F",
                "CHK_NOVOH": "F",
                "CHK_NOVOH_BAL_ZERO": "F",
                "CHK_FZHS_DETAIL": "F",
                "CHK_ARP_DAT": "F",
                "CHK_DETAIL_ACCN": "F",
                "CHK_NO_POA_VOH": "F",
                "CHK_NO_SYTZ_VOH": "F",
                "SHOW_BN_TYPE": "1",
                "ORDERBY_TYPE": "1",
                "START_DD": "2026-01-01T00:00:00",
                "YEARS": 2026,
                "IPERIOD": 1,
                "TYPE_NO": "01",
                "CHK_GROUP_CUR_LOC": "F",
                "DATE_B": "2026-01-01",
                "DATE_E": "2026-01-31"
            }
        },
        { "orderBy": { "SYS_DATE": "asc" } }
    ],
    "DISPLAY_FIELDS": "ACC_NO,ACC_NAME,ACC_IPERIOD,DC,REM_TYPE,FZHS_TITLE,CUR_NAME,AMTN_D,AMTN_C,AMTN_BAL"
}
```

**fixCondition 字段表**（动态值见 1.3 派生规则）：

| 字段 | 类型 | 值 | 说明 |
|------|------|-----|------|
| `BOOK_NO` | string | 动态 | 账簿代号（账簿下拉所选） |
| `CUR_ID` | string | `""` | 币别，空 = 综合本位币 |
| `ACC_IPERIOD_B` / `ACC_IPERIOD_E` | string | 动态 | 会计期间起/止，`YYYY-MM` |
| `CHK_ACCN_TYPE` | string | `"2"` | 检查科目类型，固定 |
| `ACC_NO` / `ACC_NO_B` / `ACC_NO_E` | string | `""` | 科目编号（单值/起/止），空 = 全部 |
| `REL_CLS_B` / `REL_CLS_E` | Int32 | `1` / `10` | 关联类别起/止，固定 |
| `AMTN_BAL_TYPE` | string | `"1"` | 余额类型，固定 |
| `CHK_POSTACC` / `CHK_STOP` / `CHK_NOVOH` / `CHK_NOVOH_BAL_ZERO` / `CHK_FZHS_DETAIL` / `CHK_ARP_DAT` / `CHK_DETAIL_ACCN` / `CHK_NO_POA_VOH` / `CHK_NO_SYTZ_VOH` | string | `"F"` | 过账/停用/无凭证/辅助明细/应收应付日期/明细科目/POA/SYTZ 等开关，全部否 |
| `SHOW_BN_TYPE` | string | `"1"` | 显示期初余额类型，固定 |
| `ORDERBY_TYPE` | string | `"1"` | 排序类型，固定 |
| `START_DD` | string | 动态 | `YYYY-MM-01T00:00:00` |
| `YEARS` / `IPERIOD` | Int32 | 动态 | 年度 / 期间 |
| `TYPE_NO` | string | `"01"` | 类型编号（科目表代号），固定 |
| `CHK_GROUP_CUR_LOC` | string | `"F"` | 按币别分组，否 |
| `DATE_B` / `DATE_E` | string | 动态 | 日期起/止 |

**响应行字段**（`DATA.REPORT__TAB[]`）：

| 字段 | 说明 |
|------|------|
| `ACC_NO` / `ACC_NAME` | 科目编号 / 科目名称 |
| `ACC_IPERIOD` | 会计期间（`"2026-1"` 格式） |
| `REM_TYPE` | 摘要类型：`"1"`=期初余额 / `"2"`=本期合计 / `"3"`=本年合计（用户确认语义）；⚠️ 本服务器可能不返回此字段 → 扁平行，有值才显示 |
| `DC` | 借贷方向（`D`/`C`，可能为空） |
| `FZHS_TITLE` / `FZHS_KEY` | 辅助核算标题 / 键 |
| `CUR_ID` / `CUR_NAME` | 币别 / 币别名 |
| `AMT_D` / `AMT_C` / `AMT_BAL` | 原币借方 / 贷方 / 余额 |
| `AMTN_D` / `AMTN_C` / `AMTN_BAL` | 本位币借方 / 贷方 / 余额 |
| `QTY_D` / `QTY_C` / `QTY_BAL` | 数量借/贷/余 |
| `YEARS` / `IPERIOD` | 年度 / 期间 |

> ⚠️ `displayFields` 数组与顶层 `DISPLAY_FIELDS` 字符串必须保持一致。

### 3.2 科目余额表 — `accBalanceTable/GetReportStream`

完整请求 JSON（期间 2026-01、账簿 001，可直接照抄）：

```json
{
    "PGM": "ACCRPTABT",
    "SEARCH_INFO": [
        {
            "showBody": "T",
            "showLadder": "F",
            "displayFields": ["ACC_NO", "ACC_NAME", "AMTN_NC_D", "AMTN_NC_C", "AMTN_QC_D", "AMTN_QC_C", "AMTN_D", "AMTN_C", "AMTN_Y_D", "AMTN_Y_C", "AMTN_QM_D", "AMTN_QM_C"],
            "sumFields": []
        },
        {
            "fixCondition": {
                "BOOK_NO": "001",
                "CUR_ID": "",
                "ACC_IPERIOD_B": "2026-01",
                "ACC_IPERIOD_E": "2026-01",
                "CHK_ACCN_TYPE": "2",
                "ACC_NO": "",
                "DATE_TYPE": "1",
                "ACC_NO_B": "",
                "ACC_NO_E": "",
                "DATE_B": "2026-01-01",
                "DATE_E": "2026-01-31",
                "REL_CLS_B": 1,
                "REL_CLS_E": 10,
                "AMTN_BAL_TYPE": "1",
                "CHK_POSTACC": "F",
                "CHK_STOP": "F",
                "CHK_NOVOH_BAL_ZERO": "F",
                "CHK_FZHS_DETAIL": "F",
                "CHK_DETAIL_ACCN": "F",
                "CHK_NO_POA_VOH": "F",
                "CHK_NO_SYTZ_VOH": "F",
                "CHK_GROUP_CUR_LOC": "F",
                "START_DD": "2026-01-01T00:00:00",
                "YEARS": 2026,
                "IPERIOD": 1,
                "TYPE_NO": "01"
            }
        },
        { "orderBy": { "SYS_DATE": "asc" } }
    ],
    "DISPLAY_FIELDS": "ACC_NO,ACC_NAME,AMTN_NC_D,AMTN_NC_C,AMTN_QC_D,AMTN_QC_C,AMTN_D,AMTN_C,AMTN_Y_D,AMTN_Y_C,AMTN_QM_D,AMTN_QM_C"
}
```

**与总分类账 fixCondition 的差异**：多了 `DATE_TYPE: "1"`（日期类型固定值）；少了 `CHK_NOVOH`、`CHK_ARP_DAT`、`SHOW_BN_TYPE`、`ORDERBY_TYPE`。其余字段同 3.1。

**10 个金额列成对出现（`_D` 借方 / `_C` 贷方）**，列标题（`js/reports.js` COLUMN_LABELS 实测）：

| 字段对 | 标题 |
|--------|------|
| `AMTN_NC_D` / `AMTN_NC_C` | 年初借方 / 年初贷方 |
| `AMTN_QC_D` / `AMTN_QC_C` | 期初借方 / 期初贷方 |
| `AMTN_D` / `AMTN_C` | 本期借方 / 本期贷方 |
| `AMTN_Y_D` / `AMTN_Y_C` | 本年借方 / 本年贷方 |
| `AMTN_QM_D` / `AMTN_QM_C` | 期末借方 / 期末贷方 |

> `AMTN_*` 为本位币金额；响应行同样带 `ACC_NO`/`ACC_NAME`/`DC` 等公共字段与 `REM_TYPE`（本服务器可能不返回）。

### 3.3 资产负债表 — `accRptPreview/GetReportStream`（RPT_TYPE=2）

**与 3.1/3.2 的关键差异**：
1. 顶层多 **`RPT_NO`** 和 **`TYPE_NO`** 两个必填字段：`RPT_NO` = 样式下拉所选（如 `STD001`），`TYPE_NO` = 该样式所属科目表代号（来自账簿行 `TYPE_NO`）；
2. `displayFields` 为空数组、`DISPLAY_FIELDS` 为空串 → 返回全部字段；
3. 列全走响应 `COLUMN_INFO` 动态生成。

完整请求 JSON（期间 2026-01、账簿 001、样式 STD001，可直接照抄）：

```json
{
    "PGM": "CUS_ACC_RPT__2_ZCFZB",
    "SEARCH_INFO": [
        { "showBody": "T", "showLadder": "F", "displayFields": [], "sumFields": [] },
        {
            "fixCondition": {
                "RPT_NO": "STD001",
                "BOOK_NO": "001",
                "CYCLE_TYPE": "1",
                "IPERIOD_TYPE": "1",
                "ACC_IPERIOD_B": "2026-01",
                "ACC_IPERIOD_E": "2026-01",
                "UP_AMTN": "1",
                "CHK_POSTACC": "F",
                "CHK_NO_POA_VOH": "F",
                "DOUBLE_CLASS": "F",
                "SHOW_RPT_ITEM_AMTN_ZERO": "T",
                "CHK_NO_SYTZ_VOH": "F",
                "START_DD": "2026-01-01T00:00:00",
                "YEARS": 2026,
                "IPERIOD": 1,
                "TYPE_NO": "01",
                "DATE_TYPE": "1",
                "DATE_B": "2026-01-01",
                "DATE_E": "2026-01-31",
                "CHK_DATE": "1"
            }
        },
        { "orderBy": { "SYS_DATE": "asc" } }
    ],
    "DISPLAY_FIELDS": "",
    "RPT_NO": "STD001",
    "TYPE_NO": "01"
}
```

**fixCondition 字段表**（三财务报表共用骨架，差异见下表标注）：

| 字段 | 类型 | 值 | 说明 |
|------|------|-----|------|
| `RPT_NO` | string | 动态 | 报表样式代号（**与顶层 RPT_NO 一致**） |
| `BOOK_NO` | string | 动态 | 账簿代号 |
| `CYCLE_TYPE` | string | `"1"` | 周期类型，固定 |
| `IPERIOD_TYPE` | string | `"1"` | 期间维度，固定 |
| `ACC_IPERIOD_B` / `ACC_IPERIOD_E` | string | 动态 | 会计期间起/止 |
| `UP_AMTN` | string | `"1"` | 更新金额，固定 |
| `CHK_POSTACC` | string | `"F"` | 检查过账科目，否 |
| `CHK_NO_POA_VOH` | string | `"F"` | 排除POA凭证，否 |
| `DOUBLE_CLASS` | string | `"F"` | 双类别，否 |
| `SHOW_RPT_ITEM_AMTN_ZERO` | string | `"T"` | 显示金额为零的项目，是 |
| `CHK_NO_SYTZ_VOH` | string | 资产负债/现金流量=`"F"`；**利润表=`"T"`** | 排除SYTZ凭证（唯一一只为 `"T"`） |
| `START_DD` | string | 动态 | `YYYY-MM-01T00:00:00` |
| `YEARS` / `IPERIOD` | Int32 | 动态 | 年度 / 期间 |
| `TYPE_NO` | string | 动态 | 类型编号（**与顶层 TYPE_NO 一致**，来自样式所属科目表） |
| `DATE_TYPE` | string | `"1"` | 日期类型，固定 |
| `DATE_B` / `DATE_E` | string | 动态 | 日期起/止 |
| `CHK_DATE` | string | `"1"` | 检查日期，固定 |

**响应要点**：

| 字段 | 说明 |
|------|------|
| `DATA.REPORT__TAB[]` | 报表项目行：`ITEM_NO`（项目编号）、`ITEM_NAME`（项目名称）、`ITEM_NO_UP`（上级项目）、`REL_CLS`（层级）、`SPACES`（缩进层级，一级加粗、逐级缩进展示）、`IS_SUM`（小计标识）、`FORMULA`/`FORMULA_TEXT`（取数公式） |
| `DATA.COLUMN_INFO[]` | 动态数值列 `[{NAME:"STD002",TITLE:"年初数"},{NAME:"STD001",TITLE:"期末数"}]`；空数据时是 **`{}` 对象** |
| `DATA.OTHER_DATA` | `RPT_TYPE` 等元信息 |

### 3.4 利润表 — `accRptPreview/GetReportStream`（RPT_TYPE=3）

**与资产负债表的差异（两个实测陷阱）**：
1. `fixCondition.CHK_NO_SYTZ_VOH = "T"`（资产负债/现金流量表为 `"F"`）；
2. **请求侧字段带 `_1` 后缀，`DISPLAY_FIELDS` 有前导逗号**（逐字保留，响应无后缀）：
   - `displayFields`: `["ITEM_NO_1", "ITEM_NAME_1", "ROW_ID_1", "STD003_1", "STD004_1"]`
   - `DISPLAY_FIELDS`: `",ITEM_NO_1,ITEM_NAME_1,ROW_ID_1,STD003_1,STD004_1"`

完整请求 JSON（期间 2026-01、账簿 001、样式 STD002，可直接照抄）：

```json
{
    "PGM": "CUS_ACC_RPT__3_LRB",
    "SEARCH_INFO": [
        {
            "showBody": "T",
            "showLadder": "F",
            "displayFields": ["ITEM_NO_1", "ITEM_NAME_1", "ROW_ID_1", "STD003_1", "STD004_1"],
            "sumFields": []
        },
        {
            "fixCondition": {
                "RPT_NO": "STD002",
                "BOOK_NO": "001",
                "CYCLE_TYPE": "1",
                "IPERIOD_TYPE": "1",
                "ACC_IPERIOD_B": "2026-01",
                "ACC_IPERIOD_E": "2026-01",
                "UP_AMTN": "1",
                "CHK_POSTACC": "F",
                "CHK_NO_POA_VOH": "F",
                "DOUBLE_CLASS": "F",
                "SHOW_RPT_ITEM_AMTN_ZERO": "T",
                "CHK_NO_SYTZ_VOH": "T",
                "START_DD": "2026-01-01T00:00:00",
                "YEARS": 2026,
                "IPERIOD": 1,
                "TYPE_NO": "01",
                "DATE_TYPE": "1",
                "DATE_B": "2026-01-01",
                "DATE_E": "2026-01-31",
                "CHK_DATE": "1"
            }
        },
        { "orderBy": { "SYS_DATE": "asc" } }
    ],
    "DISPLAY_FIELDS": ",ITEM_NO_1,ITEM_NAME_1,ROW_ID_1,STD003_1,STD004_1",
    "RPT_NO": "STD002",
    "TYPE_NO": "01"
}
```

**响应要点**：行字段无 `_1` 后缀（`ITEM_NO`/`ITEM_NAME`/`ROW_ID`/`STD003`/`STD004`）；`COLUMN_INFO` 为 `[{NAME:"STD003",TITLE:"本期发生数"},{NAME:"STD004",TITLE:"本年累计数"}]`。

### 3.5 现金流量表 — `accRptPreview/GetReportStream`（RPT_TYPE=4）

完整请求 JSON（期间 2026-01、账簿 001、样式 STD003，可直接照抄）：

```json
{
    "PGM": "CUS_ACC_RPT__4_XJLL",
    "SEARCH_INFO": [
        { "showBody": "T", "showLadder": "F", "displayFields": [], "sumFields": [] },
        {
            "fixCondition": {
                "RPT_NO": "STD003",
                "BOOK_NO": "001",
                "CYCLE_TYPE": "1",
                "IPERIOD_TYPE": "1",
                "ACC_IPERIOD_B": "2026-01",
                "ACC_IPERIOD_E": "2026-01",
                "UP_AMTN": "1",
                "CHK_POSTACC": "F",
                "CHK_NO_POA_VOH": "F",
                "DOUBLE_CLASS": "F",
                "SHOW_RPT_ITEM_AMTN_ZERO": "T",
                "CHK_NO_SYTZ_VOH": "F",
                "START_DD": "2026-01-01T00:00:00",
                "YEARS": 2026,
                "IPERIOD": 1,
                "TYPE_NO": "01",
                "DATE_TYPE": "1",
                "DATE_B": "2026-01-01",
                "DATE_E": "2026-01-31",
                "CHK_DATE": "1"
            }
        },
        { "orderBy": { "SYS_DATE": "asc" } }
    ],
    "DISPLAY_FIELDS": "",
    "RPT_NO": "STD003",
    "TYPE_NO": "01"
}
```

**与资产负债表的差异**：仅 `PGM`（`CUS_ACC_RPT__4_XJLL`）、`RPT_NO`（`STD003`）不同；`fixCondition` 完全一致。响应 `COLUMN_INFO` 同利润表（`STD003` 本期发生数 / `STD004` 本年累计数）。

---

## 四、完整调用顺序（推荐）

```
1. POST /user/login                              → TOKEN
2. POST AccBook/GetList                          → ACC_BOOK_BS 条数判定分流
   ├─ 0 条 → 账套未启用总账：跳过 3/4，直接按第六章构造 Online 扁平请求体（公式代号用面板输入值）
   └─ ≥1 条 → 选账簿，取 BOOK_NO（记住该行 TYPE_NO），继续 3/4
3. 仅三财务报表：POST accRptStyle/getlist
        （顶层 TYPE_NO = 账簿行 TYPE_NO）
   → 按 RPT_TYPE 过滤（2/3/4）→ 选样式 RPT_NO
4. 构造标准版请求体：
       期间 PERIOD "YYYY-MM" → 按 1.3 派生 YEARS/IPERIOD/DATE_B/DATE_E/START_DD
       BOOK_NO / RPT_NO / TYPE_NO 填入 fixCondition（三财务报表还要填顶层 RPT_NO/TYPE_NO）
5. POST {端点} → 逐行解析 SSE data: 消息 → 累积数据行
   → 标准版累积 DATA.REPORT__TAB，Online 版累积 DATA[表键]（RPT/MyTable/RPT_CSHFROLIST）
   → 财务报表 3 只再取 DATA.COLUMN_INFO 动态生成列
```

---

## 五、常见错误与陷阱（实测）

| # | 陷阱 | 实测结果 |
|---|------|---------|
| 1 | 端点带 `api/` 前缀 | `/SUNFUSION/API/api/...` → 404（文档 URL 的 `/api/` 段就是 `/SUNFUSION/API`） |
| 2 | `BOOK_NO` 传空 | HTTP 200 + SSE 消息 `ERR`（**不是** API4 文档写的 406） |
| 3 | 三财务报表样式不存在 | 「报表样式不存在或无访问权限」（STD001/2/3 部分服务器未建立） |
| 4 | 认证失效 | 错误信封 code `20001`/`20004` → 重新登录 |
| 5 | `PAGE_COUNT` | 不可靠（有数据为 0 / 无数据为 1），不要用于判断 |
| 6 | `COLUMN_INFO` | 空数据时是 `{}` 对象不是数组，遍历前判类型 |
| 7 | `REM_TYPE` | 本服务器可能不返回（显式请求也不返回）→ 扁平行 |
| 8 | 利润表 `_1` 后缀 | 请求字段带 `_1`、`DISPLAY_FIELDS` 前导逗号逐字保留；响应无后缀 |
| 9 | 利润表 `CHK_NO_SYTZ_VOH` | 唯一为 `"T"`（其余两表 `"F"`） |
| 10 | 清单接口错误码 | code `10001` 是通用错误码，message 不一定准确，不要按 message 做逻辑判断 |
| 11 | `displayFields` 与 `DISPLAY_FIELDS` | 两者必须保持一致（总分类账/科目余额表） |

---

## 六、Online 空白纸打印版（账簿 0 条时的降级路径，Round 59 实测 + 实施）

> 原始文档：[Online总账报表-查询制表.md](Online总账报表-查询制表.md)（保留作溯源；本章已整并全部参数表，并修正原始文档两处笔误、补充 AT02 实测结论）。
> 触发条件：`AccBook/GetList` 返回 **0 条账簿**（账套未启用总账）。此时总账分组 5 只报表**静默降级**调用本套端点——不弹窗、不 toast（用户 2026-08-31 拍板）。无账簿概念、无报表样式链路；公式代号由条件面板输入框提供。

### 6.1 端点一览（全部 POST + GetReportStream + SSE，扁平请求体）

| # | 报表 | 端点（相对 Base URL） | 数据表键 | 公式参数（面板输入框） |
|---|------|----------------------|---------|----------------------|
| 1 | 总分类账 | `RPTACCBlank/GetReportStream` | `RPT` | 无（ACCLEVEL 固定 `"0"`） |
| 2 | 科目余额表 | `RPTACCDetail/GetReportStream` | `RPT` | 无 |
| 3 | 资产负债表 | `RPTZFListA/GetReportStream` | `MyTable` | `REPNO1`/`REPNO2`/`REPNO3` = 10/20/30（资产/负债/业主公式） |
| 4 | 利润表 | `RPTSYListA/GetReportStream` | `MyTable` | `REPNO` = 40 |
| 5 | 现金流量表 | `RPTCshFroList/GetReportStream` | `RPT_CSHFROLIST` | 无 |

> 前端公式栏位**自动判断**：`online.inputs.length` 3 → 显示 3 公式框；1 → 1 公式框；0 → 仅「会计期间」（与 ui-template.html 演示同源）。日期沿用「会计期间(月)」选择器，映射 `DATE_B`/`DATE_E`（月初/月末）。

### 6.2 五端点逐个（参数表整并自原始文档，请求体照抄实测验证版）

#### 6.2.1 总分类账 — `RPTACCBlank/GetReportStream`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `dateB` | string | 是 | 起止日期起 `YYYY-MM-DD` → 填 `@DATE_B`（月初） |
| `dateE` | string | 是 | 起止日期止 `YYYY-MM-DD` → 填 `@DATE_E`（月末） |
| `AccNoB` / `AccNoE` | string | 否 | 起止科目起/止，空 = 全部 |
| `DEPT` | string | 否 | 部门选择 |
| `DEPTSHOW` | string | 否 | 部门列示 |
| `ACCLEVEL` | string | 是 | 科目级别（0,1,2,3），固定 `"0"` |
| `YE` | string | 否 | 科目余额方向 |
| `POSSTYLE` | string | 否 | 过账方式 |
| `ACCA` | string | 否 | 账别 |
| `CUR_SEL` | string | 否 | 币别 |
| `CHKCUR_ID` | string | 否 | 依币别汇总 |

```json
{ "dateB": "2026-01-01", "dateE": "2026-01-31", "AccNoB": "", "AccNoE": "", "DEPT": "", "DEPTSHOW": "", "ACCLEVEL": "0", "YE": "", "POSSTYLE": "", "ACCA": "", "CUR_SEL": "", "CHKCUR_ID": "" }
```

响应行 19 列（`COLUMN_INFO.RPT` 全列）：`NAME, ENG_NAME, ACC_NO, ACC_NOTE, YEARMONTH, YEARS, MONTHS, REM, AMTND, AMTNC, AMTNYE, SHOWDC, ACCNDC, AMTD, AMTC, AMTYE, ENG_NOTE, CUR_ID, CUR_NAME`。行形态：科目行（承上期）/ 月度汇总行 / 小计行。

#### 6.2.2 科目余额表 — `RPTACCDetail/GetReportStream`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `dateB` | string | 是 | 起止日期起 `YYYY-MM-DD` |
| `dateE` | string | 是 | 起止日期止 `YYYY-MM-DD` |
| `AccNo` | string | 否 | 主科目代号，空 = 全部 |
| `DEPT` | string | 否 | 部门选择 |
| `DEPTSHOW` | string | 否 | 部门列示 |
| `ACCLEVEL` | string | 否 | 科目级别方式显示（`"T"`） |
| `YE` | string | 否 | 科目余额方向 |
| `POSSTYLE` | string | 否 | 过账方式 |
| `ACCA` | string | 否 | 账别 |
| `CUR_SEL` | string | 否 | 币别 |
| `CHKCUR_ID` | string | 否 | 依币别汇总 |
| `ACCNOBJLEVEL` | string | 否 | 依对象别显示 |

```json
{ "dateB": "2026-01-01", "dateE": "2026-01-31", "AccNo": "", "DEPT": "", "DEPTSHOW": "", "ACCLEVEL": "", "YE": "", "POSSTYLE": "", "ACCA": "", "CUR_SEL": "", "CHKCUR_ID": "", "ACCNOBJLEVEL": "" }
```

响应行 34 列（`COLUMN_INFO.RPT` 全列）：`ACC_NO, NAME, ENG_NAME, AMTNQC, AMTND, AMTNC, SHOWDC, AMTNYE, AMTQC, AMTD, AMTC, AMTCUR, AMTYE, AMTDCCE, AMTNSHOWQC, AMTSHOWQC, AMTNQC_D, AMTNQC_C, AMTQC_D, AMTQC_C, AMTNYE_D, AMTNYE_C, AMTYE_D, AMTYE_C, DC, CLS, TOP, D_NUM, C_NUM, CUR_ID, OBJ, OBJ_NAME, OBJ_NAME_F, SW_OBJ`。末行为「合计:」行。

#### 6.2.3 资产负债表 — `RPTZFListA/GetReportStream`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `txtDep` | string | 否 | 部门 |
| `dtE` | string | 是 | 日期 `YYYY-MM-DD` → 填 `@DATE_E`（月末；**本端点只传 dtE，无 dtB**） |
| `REPNO1` | string | 是 | **资产公式**（如 `"10"`）。⚠️ 原始文档参数表写 `REPO1` 是**笔误**：实测 REPO 变体报「请先设置报表公式」，以请求示例的 `REPNO1` 为准 |
| `REPNO2` | string | 是 | **负债公式**（如 `"20"`） |
| `REPNO3` | string | 是 | **业主公式**（如 `"30"`） |
| `ZHANGID` | string | 否 | 账别 |
| `SHOW_LST` | string | 否 | 列示方式（`"1"`=默认） |
| `ZERO_SHOW` | string | 否 | 科目为零显示否 |

```json
{ "txtDep": "", "dtE": "2026-01-31", "REPNO1": "10", "REPNO2": "20", "REPNO3": "30", "ZHANGID": "", "SHOW_LST": "1", "ZERO_SHOW": "" }
```

响应行 26 列（`COLUMN_INFO.MyTable` 全列）：`ID, ACC_NOL, ACC_NAMEL, LL, AMTNL, AMTN_SUBL, AMTN_SUML, RATE1L, ACC_NOR, ACC_NAMER, RR, AMTNR, AMTN_SUBR, AMTN_SUMR, RATE1R, LA, AMTNA, AMTN_SUBA, AMTN_SUMA, RATE1A, RB, AMTNB, AMTN_SUBB, AMTN_SUMB, RATE1B, ITM`。行数由公式决定（公式壳），与凭证数无关。

#### 6.2.4 利润表 — `RPTSYListA/GetReportStream`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `dtB` | string | 是 | 起止日期起 `YYYY-MM-DD` → 填 `@DATE_B`（月初） |
| `dtE` | string | 是 | 起止日期止 `YYYY-MM-DD` → 填 `@DATE_E`（月末） |
| `REPNO` | string | 是 | **报表公式**（如 `"40"`） |
| `DEPNO` | string | 否 | 部门选择 |
| `DEP_LST` | string | 否 | 部门列示（`"1"`=不含下属） |
| `OBJ_ID` | string | 否 | 对象别类型（`"0"`=全部） |
| `OBJ` | string | 否 | 统计对象别 |
| `ADDUP` | string | 否 | 计算年度累计否 |
| `RATEMODE` | string | 否 | 比率计算方式（`"1"`=依销货收入总额） |
| `ZHANGID` | string | 否 | 账别 |
| `ZERO_SHOW` | string | 否 | 科目为零显示否 |

```json
{ "dtB": "2026-01-01", "dtE": "2026-01-31", "REPNO": "40", "DEPNO": "", "DEP_LST": "1", "OBJ_ID": "0", "OBJ": "", "ADDUP": "", "RATEMODE": "1", "ZHANGID": "", "ZERO_SHOW": "" }
```

响应行 21 列（`COLUMN_INFO.MyTable` 全列）：`ITM, KEY_ITM, CLS, REL_CLS, ACC_NO, ACC_NAME, AMTN, AMTN_SUB, AMTN_SUM, RATE1, AMTN_BUDGET, AMTN_DIFF, RATE2, CAS_NO, CAS_NAME, CAS_ITM, UP, SEG3, SEG2, AMT, FUN`。

#### 6.2.5 现金流量表 — `RPTCshFroList/GetReportStream`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `dtDateB` | string | 是 | 凭证日期起 `YYYY-MM-DD` → 填 `@DATE_B` |
| `dtDateE` | string | 是 | 凭证日期止 `YYYY-MM-DD` → 填 `@DATE_E` |
| `txtCshFroB` / `txtCshFroE` | string | 否 | 原因代号起/止 |
| `ACCN_SEL` | string | 否 | 会计科目 |
| `DEPT_SEL` | string | 否 | 部门选择 |
| `DEPT_LST` | string | 否 | 部门列示（`"1"`=不含下属） |
| `DATE_TYPE` | string | 否 | 制表日期（0,1） |
| `BILL_TYPE` | string | 否 | 单据显示（0,1,2） |
| `CHK_CASH` | string | 否 | 依现金流量科目制表 |

```json
{ "dtDateB": "2026-01-01", "dtDateE": "2026-01-31", "txtCshFroB": "", "txtCshFroE": "", "ACCN_SEL": "", "DEPT_SEL": "", "DEPT_LST": "1", "DATE_TYPE": "", "BILL_TYPE": "", "CHK_CASH": "" }
```

响应行 28 列（`COLUMN_INFO.RPT_CSHFROLIST` 全列）：`CSH_FRO, CSH_NAME, CSH_ENG_NAME, MAK_DAT, MAK_NO, VOH_NO, ACC_NO, ACC_NAME, ACC_ENG_NAME, CUR_ID, EXC_RTO, BIL_ID, BIL_NO, DEP, DEP_NAME, DC, ACC_REM, REM, OBJ, ARP_DAT, CLS_ID1, CLS_ID2, CLS_ID3, CLS_ID4, VOH_ID, CANCEL_ID, AMT, AMTN`。凭证明细行。

### 6.3 响应与标准版的差异（实测）

| 项 | 标准版 | Online 版 |
|----|--------|----------|
| 数据表键 | `DATA.REPORT__TAB` | `DATA.RPT` / `DATA.MyTable` / `DATA.RPT_CSHFROLIST` |
| COLUMN_INFO | 扁平数组 `[{NAME,TITLE}]`（空为 `{}`） | **嵌套对象** `{表键:[{NAME,TYPE,SIZE}]}`（**无 TITLE 字段**，表头靠前端列名映射） |
| 公式报表 | 由样式 RPT_NO 驱动 | 由公式代号 REPNO* 驱动，行数是公式壳（与凭证数无关） |
| 请求体 | `{PGM, SEARCH_INFO, DISPLAY_FIELDS}` | 扁平键值对 |

SSE 消息结构与错误三形态同标准版（`{CODE:1,PERCENT,TITLE,ERR,DATA}`，ERR 非空即业务错误）。

### 6.4 金额列语义（AT02 实测行值对照锁定）

- 总分类账：`AMTND`=本期借方 / `AMTNC`=本期贷方 / `AMTNYE`=期末余额；`REM`=摘要（承上期/凭证总金额/小计）；`SHOWDC`=借/贷
- 科目余额表：`AMTNQC`=期初；`AMTND`/`AMTNC`=本期借/贷；`AMTNYE_D`/`AMTNYE_C`=期末借/贷余额；`AMTDCCE`=期末余额（贷方为负）
- 现金流量表：`AMT`=原币 / `AMTN`=本位币（凭证明细行，MAK_NO/VOH_NO/BIL_NO/DEP 等）
- 资产负债表：`ACC_NAMEL/R`=左/右科目名称；`AMTN_SUML/R`=左/右金额；`RATE1L/R`=比率
- 利润表：`AMTN`=本期金额 / `AMTN_SUM`=合计 / `RATE1`=比率 / `AMTN_BUDGET`=预算

### 6.5 账套实测地图（2026-08-31 参数矩阵复测：2024 全年 + 公式 10/20/30/40）

| 账套 | AccBook 账簿 | 总分类账 | 科目余额表 | 资产负债表 | 利润表 | 现金流量表 |
|------|-------------|---------|-----------|-----------|--------|-----------|
| AT01 | 1 本（First Department） | 0 行 | 0 行（无错误） | 未复测 | 23 行（金额非空） | 0 行 |
| AT02 | 5 本 | 9 行 | 4 行 | 5 行 | 23 行 | 3 行（**唯一全端点真实数据的账套，列语义锁定来源**） |
| AT03 | 0 本 | 0 行（干净空表） | **SQL 错 FN_GET_ACCN_USR（参数无关）** | 6 行公式壳（金额空） | 23 行公式壳（金额空） | 0 行（干净空表） |
| AT04 | 0 本 | 0 行（干净空表） | 0 行（干净空表） | 0 行（无错误） | **SQL 错 ##CSH_FRO_TEMP 临时表冲突** | 0 行（干净空表） |

> 要点：①公式 10/20/30/40 在 AT03 有效（返回公式壳行，金额空因无凭证数据）；②`FN_GET_ACCN_USR` 缺失**仅 AT03 的科目余额表**（5 组参数矩阵：AccNo 有/无 × 2024/2026-08 全复现，参数无关、客户端不可修）；③AT04 各端点形态又不同（lrb 报临时表冲突）→ 降级后个别报表的报错**逐账套逐端点而异，均属服务端数据层**；④浏览器验证预期：AT03 上 zcfzb/lrb 能出表（空金额），accgl/accXjllb 空表，仅科目余额表报错。

### 6.6 实测陷阱

| # | 陷阱 | 实测结果 |
|---|------|---------|
| 1 | REPO1/2/3（文档参数表笔误） | 报「请先设置报表公式」；正确字段名是 **REPNO1/2/3** |
| 2 | 公式值无效 | 报「制表公式不存在」；10/20/30/40 已在 AT02/AT03 实测有效 |
| 3 | 数据作用域 | 凭证挂在账簿模型下的账套（AT01），Online 总分类账/科目余额表/现金流量表返回 0 行（非参数错误）；公式引擎报表仍可出数 |
| 4 | Token 会话互踢 | 同账套重复登录互踢（20004）；跨账套登录不互踢（实测：AT01 token 在 AT03/AT04 多次登录后仍有效）。稳妥做法不变：测试每账套批次前重新登录 |
| 5 | PowerShell 传 JSON | 直传 curl.exe 无空格参数会剥双引号 → 一律 `--data-binary @文件` |
| 6 | SSE 消息带空格（解析陷阱） | 消息是带空格的 pretty JSON（`"ERR": "..."`、`"ACC_NO": "1001"`）→ 正则/解析必须允许冒号后空白，否则 ERR 与数据行全部漏判 |
| 7 | AT03 科目余额表 SQL 错 | `FN_GET_ACCN_USR` 无效（Error 208），5 组参数矩阵全复现 → 仅此账套此端点缺函数，非端点通病（AT04 同端点正常空表） |

### 6.7 前端实现映射（Round 59，代码即本文）

| 行为 | 实现位置 |
|------|---------|
| 分流判定（0 账簿 → 静默置位） | `js/app.js` openReport：`ReportEngine.ledgerOnline = true; _openReportCore(reportKey);`（无弹窗无 toast） |
| 公式框自动判断 | `js/reports.js` updateFilterPanel：`online.inputs.length` 3→`onlinerepno3` 组、1→`onlinerepno1` 组、0→仅会计期间 |
| 请求体构造 | `js/reports.js` buildOnlineBody：`cfg.online.body` 模板 + buildStreamCtx 派生值 + 公式框（未填回退默认值 10/20/30/40） |
| 表键/动态列 | `js/api.js` fetchStreamReport `dataKey` 参数化；`js/reports.js` query 分支解包嵌套 `COLUMN_INFO[表键]`；getVisibleColumns 在线模式强制动态列 |
| 列名映射 | `js/reports.js` `ONLINE_COLUMN_LABELS`（按 reportKey 分表，只收实测高/中置信度字段，未收录回退英文原名） |
| 公式框/样式链拦截 | `js/app.js` doQuery：BOOK_NO/RPT_NO/TYPE_NO 拦截全部加 `!isOnline` 守卫；resetBtn 公式框回默认值（Round 60 起默认值取自 `ReportEngine.getOnlineRepnoDefaults()`，cfg.online.inputs 唯一真源） |
| 面板 UI | `index.html` `filter-group-onlinerepno3`（3 框预填 10/20/30）、`filter-group-onlinerepno1`（1 框预填 40）；原型见 ui-template.html |
| 公式下拉换装（Round 60） | `js/acc-repno-store.js`：GetAccRepNoList 拉取+缓存（内存+localStorage 按 compno）；`js/reports.js` renderOnlineRepnoSelects（input→select、全量 14 条、预选默认值）/ restoreOnlineRepnoInputs（登出换回文本框）/ getOnlineRepnoDefaults |
| 打开时序（Round 60） | `js/app.js` openReport 0 账簿分支：先 `loadAccRepNos`（`_openSeq` 序号竞态守卫）→ 成功 `_openReportCore(skip)` + 换装 + doQuery；失败 `ledgerOnline=false` + `Dialog.alert('没有报表公式，无法进行查询')` 中止打开 |

---

## 七、原始文档索引

| 原始文档 | 覆盖内容 |
|---------|---------|
| 标准报表制表API4.md | 总分类账（accGeneralLedger/GetReportStream）原始文档 |
| 标准报表制表API5.md | 科目余额表 + 三财务报表（#1~#4 节）原始文档 |
| Online总账报表-查询制表.md | Online 空白纸打印版 5 端点原始文档（参数表已整并至第六章；原始文档保留完整行数据示例） |
| 账簿列表查询.md | AccBook/GetList 账簿清单原始文档 |
| 报表样式 列表查询.md | accRptStyle/getlist 报表样式清单原始文档 |
| API服务调用说明文档.md | 登录 API + SSE 三步判别 + 错误码实测 |
